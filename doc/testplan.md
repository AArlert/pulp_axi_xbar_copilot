# Testplan

> **注（0.5.4 重置）**：文中出现的 `doc/evidence|review|design-prompt|archive`、`workflow/`、`doc/guards.md`、`doc/fw-feedback.md` 等路径为重置前产物，工作树已移除；查阅用 `git show v0.5.3-pre-reset:<path>`。

Scenario truth table — contract: workflow/records.md's "Testplan entry" section. Register rows BEFORE coding; ✅/evidence/repro are script-owned.

| id | milestone | description | config | status | evidence | repro |
| --- | --- | --- | --- | --- | --- | --- |
| M0-01 | M0 | 上游 tb_axi_xbar sanity：6 主 × 8 从随机读写各 200 笔并发（ATOP 开），tb 自带 FIFO 参考网络校验路由/保序/ID，print_result 零 mismatch，仿真自然结束（当前被 BUG-0001 阻塞：VCS-2018 编译 NCE，见 doc/bugs/BUG-0001.md） | 上游 tb 默认（6×8，SlvIdW 5/用 3，DW 64，AW 32，Pipeline 1） | ✅ | doc/evidence/v0.0.1/M0-01.log | `make run TEST=upstream_sanity SEED=1` |
| M1-01 | M1 | 自研 UVM env happy-path 路由 smoke：6 slave 端口各发命中地址表的写/读 burst，目标覆盖多个 master 端口；scoreboard 参考模型校验路由目标（SPEC-3.1/SPEC-3.2）、master 侧 ID 前缀（SPEC-5.1）、数据完整性（SPEC-1），响应码 OKAY，零 mismatch，env 无 UVM_ERROR、仿真自然结束。**Enrichment（REV-026 批准清单 A-1/A-2 合并卡）**：在原有激励之上（非替换）叠加一轮地址镜像 all-0/all-1 饱和读——每个 slave 端口对每个 master 端口各发一笔单拍读，地址取该 master 端口地址表区间的低饱和值（区间内地址位全 0）与高饱和值（区间内地址位全 1，含 beat 对齐边界），闭合 `axi_mux mst_resp_i.r.data[63:0]` 与 `axi_demux_simple slv_resp_o.r.data[63:0]`（本环境读数据=地址镜像，`xbar_types_pkg::predict_beat_data`）的逐位 toggle 覆盖缺口；判决门不变（仍是 SPEC-1/SPEC-5.1 既有数据完整性判据取值域加宽，不引入新判决维度）。**Enrichment（REV-026 批准清单 B-1）**：在上述读向饱和序列之上再叠加同构的**写向**地址饱和序列——每个 slave 端口对每个 master 端口各发一笔单拍写，地址同取该 master 端口地址表区间的低饱和值/高饱和值（构造与读向饱和序列相同），补齐 A-1/A-2 只做读向未做写向的缺口，闭合 `axi_mux`/`axi_demux_simple` `aw.addr[31:0]`（连带 `ar.addr[31:0]` 的写向对偶收敛）逐位 toggle 覆盖缺口；判决门不变（仍是 SPEC-3.1/SPEC-3.2 路由判据 + SPEC-1 数据完整性判据取值域加宽，不引入新判决维度，仍为命中地址、`aw.atop≡'0`）。**Enrichment（REV-026 批准清单 C-1）**：在上述读/写向饱和序列之上再叠加一轮 AXI4 sideband 多样化序列（`slvport_sideband_div_seq`），逐条覆盖 REV-026 C-1 点名的五个维度：(1) **WRAP 突发**（`aw/ar` `burst[1]`=BURST_WRAP，`len`∈{1,3,7}，起址置于回卷边界前最后一槽使下一拍真实回卷；每拍地址由既有共享预测器 `xbar_types_pkg::predict_beat_data`→`axi_pkg::beat_addr` 按 AXI4 A3-51 通用回卷语义推算，responder 与 scoreboard 共用该函数，故 WRAP 读回卷比较路径由本序列真实吃到）；(2) **长突发**（`AxLEN`=15 / 16 拍，超过既有 ≤7 拍档；读预测 16 拍逐拍 INCR 地址、写为长突发+稀疏 strb）；(3) **稀疏 wstrb**（非全 1 逐拍 strobe 轮换 {0x55/0xAA/0x0F/0xF0/0x81/0x01/0x80/0x00}，骑既有全字 data+strb 透传比较，SPEC-1 无差别透传对任意 strb 取值成立，无新 checker 代码）；(4) **属性轮换**（`cache/prot/qos/region/lock` 高饱和(全 1)/低饱和(全 0) 对，两向 toggle 在本序列程序序内自足）；(5) **user=1**（`aw/ar/w` user 非零）。全部命中地址、`aw.atop≡'0`，判决门不变（xbar 对 sideband 不解释，路由只看 addr、保序只看 ID，SPEC-1/SPEC-3.1/SPEC-3.2/SPEC-5.1 判据取值域加宽，不引入新判决维度）；WRAP 读回卷比较（SB_RDATA）与稀疏 strb 写完整性比较（SB_WDATA）两条判决路径经注伤自证 KILL-0005 证伪（红→复原→绿，见 doc/bugs.md）。**Enrichment（REV-026 批准清单 D-1，"简单 ready 翻转"，merge-first；DV 独立复核 merged urg 报告确认真实残余、并核实本"单笔"构造的可达边界后落地——非 toggle-theater）**：在上述四轮激励之上再叠加第五轮 ready-delay 序列（`slvport_readydelay_seq`，读方向）+ `m1_01_smoke_test` 对一个 master 端口的 responder 施加 `bp_enable_w`：每 slave 端口对每 master 端口各发一笔单拍读，发起方在接受自身 R 前短暂拉低自己的 `r_ready`（合法 AXI4 主机延迟不敏感背压，SPEC-7.4 items 1/3/5），同时该 master 端口的 responder 对 `w_ready` 施加同构的周期性背压（复用 M4-AW01/M4-BP03 的 `bp_enable`/`bp_enable_ar` 背压机制形状）；闭合 `axi_demux_simple slv_req_i.r_ready`（demux 自身外部 slave 端口边界，无中间 `axi_multicut` 缓冲）与 `axi_mux mst_resp_i.w_ready`/`gen_mux.mst_w_ready`（axi_mux 外部 master 端口边界，同样无中间缓冲）——命中地址流上这两个 ready 信号此前从未在任何已有场景中翻转过。写方向 `slv_req_i.b_ready`（同构边界）不重复加腿——已被 M4-EB01 的 `b_backpressure` 闭合。**DV 独立核实、有意不追**：`axi_mux` 内部 `mst_req_o.b_ready`/`r_ready` 与 `gen_mux.slv_b_readies[5:0]`/`slv_r_readies[5:0]`——demux→mux 间 `axi_multicut`（`PipelineStages=1`，`SpillB`/`SpillR` 未被 baseline `CUT_ALL_AX` 覆盖，故为真实 2 级流水缓冲、非 bypass）在 M1-01 单笔（非并发）语义下永不被打满，这些信号对"单笔 ready 翻转"结构性不可达，需持续压力（多笔并发在飞+消费端背压）构造，超出 D-1 原始范围，另行登记后续卡；判决门不变（仍是既有 SPEC-1/SPEC-3.1/SPEC-3.2/SPEC-5.1 数据完整性/路由判据，延迟不敏感 SPEC-7.4，不引入新判决维度）；红线：不得断言 ready 拉低的具体拍数或释放时点（SPEC-7.4 item 3，周期数未定义） | baseline（6×8，Cfg 全 13 字段钉定：MaxMst 10/MaxSlv 6/FallThr 0/CUT_ALL_AX/Pipe 1/SlvIdW 5/Used 3/UniqueIds 0/AW 32/DW 64/Rules 8；ATOPs 1/Conn '1） | ✅ | doc/evidence/v0.4.26/M1-01.log | `make run TEST=m1_01_smoke_test SEED=1` |
| M1-02 | M1 | ID 前缀响应路由 smoke：多 slave 端口发低位相同 slave ID、目标不同 master 端口的事务（构造为不制造同 ID 同向跨端口未决对，避开 SPEC-5.2 stall）；scoreboard 校验每笔 B/R 按 master 侧 ID 高 clog2(NoSlvPorts) 位回送到正确源 slave 端口（SPEC-5.1.2/SPEC-5.1.3），无跨端口错送 | baseline（同 M1-01） | ✅ | doc/evidence/v0.1.2/M1-02.log | `make run TEST=m1_02_id_prefix_test SEED=1` |
| M2-CFG01 | M2 | 地址表/default port 运行时重配置：env 仅在全部 6 个 slave 端口 AW/AR 通道均空闲的一拍窗口内更改一次 `addr_map_i`（新增/挪动 rule）与 `en_default_mst_port_i`/`default_mst_port_i`；scoreboard 参考模型按每笔事务 AW/AR 握手完成时刻生效的表版本译码，验证变更前/变更后两批事务均按各自生效表正确路由（SPEC-3.1/SPEC-3.2/SPEC-3.3），且变更本身不落在任一端口 AW/AR valid 期间（SPEC-3.4，由独立 SVA C3.1 监视）。**Enrichment（REV-026 批准清单 B-3，rule 边界重配多样性；DV 卡独立 urg 核实合并基线 `addr_decode_dync` 现状后落地，非静默替换既有激励）**：在既有单次 V0→V1 重配（batch1/reconfig/batch2）之上，再叠加两次**只改 default-master-port 索引、不改地址表**的运行时重配（`addr_map_i`/`en_default_mst_port_i` 全程维持 V1，未新增/挪动任何 rule）——V1→V2 把每个 slave 端口的默认索引取成其 V1 值（`DEFAULT_MST_V1[i]=i`）的按位取反 `DEFAULT_MST_V2`（`MST_PORT_IDX_W`=3 位在基线 `NoMstPorts`=8 下恰好铺满，取反结果仍落在 `[0,NoMstPorts)`、仍是合法 master 端口索引）、V2→V3 复原为与 V1 相同的取值（round-trip）；两次重配均沿用同一条全端口 AW/AR 空闲窗口纪律（SPEC-3.4，C3.1 SVA 同样监视），各自跟一小轮译码未命中地址写/读事务，按各自事务自身 AW/AR 握手时刻生效的表版本判决（判据不变，仍是既有 SPEC-3.3/SPEC-4 未命中路由判据的取值域加宽，不引入新判决维度，`aw.atop` 恒 `'0`，BUG-0032 宽读约束原样保留）。**技术依据与残余**：DV 卡独立 urg 核实合并基线（`make regress COV=1` 后）`addr_decode_dync` 现状 LINE 100/COND 100/Toggle 89.00%/Branch 83.33%，逐位核对 12 个实例的 Port Details 后确认——Toggle 残余的主体是 `default_idx_i[2:0]` 缺失 1→0 方向（`DEFAULT_MST_V1` 只把每端口索引从复位 0 单向抬升一次，port0 甚至双向皆缺），本 enrichment 的取反+复原两步据此补齐；Toggle 残余的另一部分（`addr_i[2:0]`，AXI4 beat 字节对齐位）与 Branch 83.33% 的唯一残余分支（`proc_check_addr_map` 的 `~config_ongoing_i` 分支，该端口在 `addr_decode.sv` 例化处硬 tie `1'b0`，非 DUT 顶层可控端口）经核实为结构性不可达/另案范围（分别属 REV-026 C-1 字节级非对齐范围、与 Kind-A 结构常量候选），本 enrichment 不覆盖、不声称覆盖，如实登记为残余 | baseline（同 M1-01；新增一次运行时地址表/default port 变更窗口 + 两次纯 default-port 索引变更窗口） | ✅ | doc/evidence/v0.4.29/M2-CFG01.log | `make run TEST=m2_cfg01_reconfig_test SEED=1` |
| M2-OR01 | M2 | 同 ID 保序 stall 触发：单 slave 端口连续发出两笔低 `AxiIdUsedSlvPorts=3` 位相同、同方向（同写或同读）、地址译码命中不同 master 端口的事务；验证完成后第二笔正确路由到其目标 master 端口、且两笔的 B/rlast 完成序与接受序一致（不发生跨 master 端口响应乱序，SPEC-5.2.1/SPEC-5.2.2/SPEC-5.2.3）——判据锚点为完成序而非"接受边界即时性"，因基线 `CUT_ALL_AX` 下后者与该字面读法有出入，见 BUG-0013（OPEN，非阻塞，待 rev 仲裁；"接受边界"字面前提已用非判决性 cover 留痕） | baseline（同 M1-01） | ✅ | doc/evidence/v0.3.12/M2-OR01.log | `make run TEST=m2_or01_stall_test SEED=1` |
| M2-OR02 | M2 | 同 ID 非 stall 对照：(a) 低位 ID 相同、同方向、地址译码命中同一 master 端口；(b) 低位 ID 相同、方向相异（一读一写）；两类事务对均正常连续完成、不出现非预期停顿（SPEC-5.2.4，及 5.2.1"同方向"范围边界）——判据口径同 M2-OR01（见 BUG-0013） | baseline（同 M1-01） | ✅ | doc/evidence/v0.2.0/M2-OR02.log | `make run TEST=m2_or02_nonstall_test SEED=1` |
| M2-TL01 | M2 | MaxMstTrans 事务数上限（单一低位 ID 桶×方向×目标 master 端口构造，跨该桶 4 个兄弟全 ID 铺开、每 ID ≤3 远低于 MaxSlvTrans，故与 BUG-0010 粒度分歧无关）：单 slave 端口背靠背连发 12 笔（>10）低位 ID 同桶、方向相同、目标同一 master 端口的事务，responder 有界拖延 B/R 压满在飞数。判决锚点须延迟不敏感（SPEC-7.4.5）——由 `tb/sva/axi_xbar_txlimit_sva.sv` 在 master 端口侧计数（`MuxAw` spill 在 mux 计数之后、`DemuxAw` spill 在 demux 核心之前，故 master 端口所见 ≤ demux 核心计数，是唯一可锁定的外部映像）。**新发现 BUG-0016（SPEC_CHANGED，REV-007 终判 SPEC_ISSUE、改判 DUT_BUG）**：master 端口延迟不敏感在飞计数**越 10**（送 12 全放行；RTL demux 计数器按 idx_width(MaxTrans) 定宽、full 判定为全一 ⇒ 结构封顶 15 非 10），故"上限最终被守（计数不越 10）"读法**降级为非判决见证**（`SVA_TXLIMIT_OVER` cover/uvm_info；REV-007 裁决后为终态，不升格），**不**升格为 assert（同 BUG-0013 先例）。落地判决门 = scoreboard 在持续同桶压力下路由/数据/响应路由/完成正确（SPEC-1/SPEC-3.1/SPEC-5.1，零 mismatch）＋达标 cover（某桶计数达 MaxMstTrans=10 至少一次，非空转）；**不**断言第 11 笔在外部边界某具体时点被拒收（SPEC-5.4.1/SPEC-7.4.5） | baseline（同 M1-01） | ✅ | doc/evidence/v0.2.0/M2-TL01.log | `make run TEST=m2_tl01_txlimit_test SEED=1` |
| M2-TL02 | M2 | MaxSlvTrans 事务数上限——弱化可观测上界监视（BUG-0011 SPEC_CHANGED / REV-005 §3 条件 C2：执行机制许可来源未定义、列上游确认项，仅解锁一条弱化监视器）：单 slave 端口向同一 master 端口背靠背连发同（前缀后）ID、同方向事务（SPEC-5.2.4 同目标可合法堆积）压满 `MaxSlvTrans=6`；判据锚延迟不敏感的可观测上界——每 master 端口 × 每可观测（前缀后）ID × 每方向在飞事务数（方向分开计），由 axi_xbar_txlimit_sva 在 master 端口侧计数。**新发现 BUG-0016（SPEC_CHANGED，REV-007 裁决）**：REV-005 解锁的"≤ `MaxSlvTrans=6` 保守下侧、绝不假红"监视器被**证伪**——mux 侧无 per-ID 计数器（BUG-0011），观测在飞受上游 demux 每桶上限（结构封顶 15）主导，送 8 全放行、计数越 6，故"≤6"读法**降级为非判决见证**（`SVA_TXLIMIT_OVER` cover/uvm_info），**不**升格为 assert。落地判决门 = scoreboard 在持续同 ID 压力下路由/数据/响应/完成正确（SPEC-1/SPEC-5.1，零 mismatch）＋达标 cover（某（ID,方向）计数达 MaxSlvTrans=6 至少一次）；**不**断言任何机制级"第几笔在某具体通道/时点被拒收"（SPEC-5.4.2/SPEC-5.4.3：mux 侧 per-ID 在飞机制不存在为已定结论，硬性红线） | baseline（同 M1-01） | ✅ | doc/evidence/v0.2.0/M2-TL02.log | `make run TEST=m2_tl02_slvtrans_test SEED=1` |
| M2-WO01 | M2 | W 通道次序：≥2 个 slave 端口并发向同一 master 端口发起写 burst（AW 交错到达/接受，构造比 M1-02 更紧密的汇聚），验证该 master 端口侧观测到的 W burst 完成序**按各自源 slave 端口自身的 AW 接受先后**排列（同源内部，SPEC-5.5.1）、不同来源 burst 互不交织，每个 burst 的数据/wstrb/wlast 与其源 slave 端口一致（复用 SPEC-1 数据完整性判据）。**硬性红线：不断言任何跨源服务序 == 跨源 AW 接受序**（SPEC-5.5.4/REV-006 §4.3，round-robin 具体发生序非可锁定外部行为）；仅加一条非空转 cover 证明"某 W burst 起始时该 master 端口有 ≥2 个不同源 AW 未决"的竞争真被激励到（SPEC-5.5.2） | baseline（同 M1-01） | ✅ | doc/evidence/v0.3.12/M2-WO01.log | `make run TEST=m2_wo01_worder_test SEED=1` |
| M2-AT01 | M2 | ATOP 原子读：至少一个 slave 端口发起 `aw.atop` 编码要求读响应的原子事务，验证 B 与 R 两通道均返回响应（SPEC-6.3）；env 保证该事务 ID 与其发起端口当前所有（读+写方向）在飞事务 ID 不同（SPEC-6.4）；旁证观察（非判决，见 BUG-0012）：若同低位 ID 的普通读与该 ATOP 写在飞重叠，读通道可能因 ATOP 的"影子 AR"注入产生额外假冲突 stall。**Enrichment（REV-026 批准清单 C-2，ATOP[3:0] 编码多样性；技术依据 doc/evidence/v0.4.15/M4-toggle-bit-decomposition.md §1 行"axi_mux aw.atop[5:0]([4:0] 恒定，[5]仅0→1)"）**：既有全部 atomic-load draw（Phase A 每端口两笔独立原子读 + Phase B 一笔与在飞普通读重叠的原子读）由固定单一编码 `{ATOMICLOAD, LITTLE_END, ADD}` 改为在 `ATOP[5:4]=ATOP_ATOMICLOAD` 子集内按端口索引确定性遍历 `ATOP[3:0]`（endianness × opcode）全部 16 种取值（跨 6 端口的 18 笔 draw 覆盖 0..15 全集，无重复缺档），闭合该子集内此前恒定的 4 个残余位；判决门不变（仍是 SPEC-6.3 既有 B+R 应答判据，opcode/endianness 不改变该义务，取值域加宽、不引入新判决维度）。**本卡只在 atomic-load 编码子集内闭合**——atomicstore/atomicswap/atomiccompare 三个 ATOP 子类型（`ATOP[5:4]` 落在 `ATOP_ATOMICSTORE`/全 6 位 `ATOP_ATOMICSWAP`/`ATOP_ATOMICCMP`）**未覆盖**，spec §6 全节无这三者的应答义务条款，scoreboard 无 spec 可推导期望值，残余归属既有登记 **BUG-0044（ACCEPTED@M5）**，本卡不重复登记、不现场发明期望值 | baseline（同 M1-01；ATOPs=1 已是基线默认） | ✅ | doc/evidence/v0.4.30/M2-AT01.log | `make run TEST=m2_at01_atop_test SEED=1` |
| M2-OR03 | M2 | BUG-0023/BUG-0024 联合回归守卫（stall_sva 跟踪表"同沿写冲突"与"单 bit 容量不足"两处 TB_BUG，前者已修 FIXING、后者待修 OPEN）：单 slave 端口、单一完整 ID `X`，先连发两笔同方向（写或读，各镜像构造一次）、同目标 master 端口 `A` 的事务并使其合法堆积在飞（SPEC-5.2.4，≥2 笔同时在飞、同一目标）；在该堆积尚未全部完成期间，再发一笔同完整 ID `X`、目标 master 端口 `B`（`B≠A`）的事务——按 SPEC-5.2.1，该笔须待 `A` 上**全部**同 ID 事务完成后才被转发；对 `A` 上堆积事务的应答不额外背压，使核心判决逻辑解除 stall（转发 `X→B`）与 `A` 上最后一笔完成握手在自然运行下有机会落在同一拍（不强行断言具体拍数，SPEC-7.4.5）；跨多个完整 ID 迭代若干次以提升上述时序巧合被自然触达的概率。判据：(1) 判决锚点（延迟不敏感，SPEC-5.2.3）——`A` 上两笔与 `B` 上一笔的完成序（B/rlast）须与接受序一致，零失配；(2) 达标覆盖（非判决，仅证 stimulus 真正触达两处角落、非空转）——至少一次观测到"同一完整 ID 有 ≥2 笔同目标事务同时在飞、且另一笔异目标同 ID 事务处于待转发状态"（BUG-0024 的容量角落）与至少一次观测到"某完整 ID 一笔事务的完成握手（B/rlast）与另一笔同完整 ID 事务的接受握手（AW/AR）落在同一时钟沿"（BUG-0023 的同沿角落）——既有 9 个场景对这两个角落命中数均为 0（DV 实测，见 BUG-0023/BUG-0024 regression_guard），本场景专为构造它们注册 | baseline（同 M1-01） | ✅ | doc/evidence/v0.2.0/M2-OR03.log | `make run TEST=m2_or03_guard_test SEED=1` |
| M3-DE01 | M3 | decode error slave 基本应答（`en_default_mst_port_i='0`）：每个 slave 端口各发写 burst 与读 burst 若干，地址落在地址表 8 条 rule 全部区间之外（SPEC-3.2 不匹配），`AxLEN` 覆盖 0 与 >0 两档以使 beat 数判据非空转。判据（scoreboard 参考模型，延迟不敏感 SPEC-7.4）：(1) 两类事务均由**本端口自己的**内部 decode error slave 应答，响应码 `RESP_DECERR`（SPEC-4.1/SPEC-4.2/SPEC-4.3）；(2) 读事务出齐 `AxLEN+1` 个 R beat、末拍 `RLAST=1`，每 beat 数据 = err_slv 默认 `RespData=64'hCA11AB1EBADCAB1E` 按 `AxiDataWidth=64` 取值（基线 = `64'hCA11AB1EBADCAB1E`，SPEC-4.4 经 REV-014/BUG-0033 校正后）（SPEC-4.3/SPEC-4.4）；(3) 写事务在整个 W burst 收齐后返回**单拍** B(DECERR)（SPEC-4.3）；(4) 响应 ID 与握手遵循 AXI4 一般规则、响应回到发起端口无跨端口错送（SPEC-4.5/SPEC-5.1）；零 mismatch、无 assert 失败、仿真自然结束。**Enrichment（REV-026 批准清单 B-2，err_slv 侧地址多样性）**：在上述窄带未命中地址（`M3_UNMAPPED_BASE` 邻域）之上再叠加一轮地址多样化序列（`slvport_de01_addrdiv_seq`，非替换）：每个 slave 端口再发写/读各两笔，地址取 (a) 刚越过地址表最后一条 rule `end_addr` 的边界值（`32'h8000_0000` + 端口偏移，SPEC-3.2"含起址、不含终址"边界，此前未被本场景覆盖）、(b) 32 位地址空间顶端饱和值、(c)(d) 两个 bit31 恒 1 的交替位模式值——四者均满足 bit31=1，故不论哪张地址表当下生效（`ADDR_MAP`/`ADDR_MAP_V1`/`ADDR_MAP_OV1` 均只在 `[0, 32'h8000_0000)` 内定义 rule，`xbar_types_pkg.sv` 三个地址表生成函数逐一确认未越界）都必然落在未命中区间；ID 与 beat 长度沿用既有 idx/idx+4/idx+8/idx+12、len 0/>0 映射，不新增 ID 取值（ID 多样性为 REV-026 E-1 另卡范围，本行不做）；地址均保持 8 字节对齐（`BEAT_SIZE`，字节级非对齐属 REV-026 C-1 范围，本行不做）。判决门不变（仍是既有 SPEC-4.1/SPEC-4.2/SPEC-4.3/SPEC-4.4/SPEC-4.5 DECERR 应答判据取值域加宽，不引入新判决维度）。**Enrichment（REV-026 批准清单 E-1，err_slv 侧 ID 多样性）**：在既有窄 ID 映射（`slvport_de01_seq`/`slvport_de01_addrdiv_seq` 均只用 idx/idx+4/idx+8/idx+12，6 个基线 slave 端口下写方向 ID 最大仅至 9、从未触达 `id_slv_t[4]`，读方向仅端口 4/5 触达且只有 0→1 单向）之上再叠加一轮 ID 饱和序列（`slvport_de01_iddiv_seq`，非替换）：每个 slave 端口另发写/读各三笔，ID 取全 0（`id_slv_t'(0)`）→全 1（`id_slv_t'(31)`）→全 0 的饱和往返（写方向一轮、读方向另一轮，各自独立闭合 0→1 与 1→0 两个方向），地址复用本行既有未命中窗口内、与上述两轮互不重叠的偏移，`AxLEN` 固定为 0（beat 数判据已由基础序列非空转，ID 维度与之正交）。判决门不变（仍是既有 SPEC-4.1/SPEC-4.2/SPEC-4.3/SPEC-4.4/SPEC-4.5 DECERR 应答判据在 ID 取值域上的加宽，不引入新判决维度）。**env 约束**：本场景（及其余全部 M3 与 M4 场景，BUG-0039 裁决延展，REV-017 §Item 3；范围取宽读，含未命中但经 default master port 路由的地址，REV-018 裁决）不向译码未命中地址发起任何 ATOP（所有 AW 恒 `aw.atop ≡ '0`）——err_slv 对要求读响应的原子操作如何应答在许可来源中未定义，见 BUG-0032（SPEC_ISSUE，构造性不可达、不阻塞 M3/M4） | baseline（同 M1-01；`en_default_mst_port_i='0`） | ✅ | doc/evidence/v0.4.28/M3-DE01.log | `make run TEST=m3_de01_decerr_test SEED=1` |
| M3-DE02 | M3 | default master port 与 decode error slave 分流（逐 slave 端口独立使能，SPEC-3.3）：`en_default_mst_port_i` 取**混合**值——半数 slave 端口使能且 `default_mst_port_i[i]` 各指向不同 master 端口，半数不使能；每个端口各发未命中地址的读/写，并夹带命中 rule 的对照事务。判据：(1) 使能端口的未命中事务被路由到其 `default_mst_port_i[i]` 所指 master 端口并由该端口的 slave agent 正常应答（非 DECERR），未使能端口的同类事务由 err_slv 以 DECERR 应答（SPEC-3.3/SPEC-4.2）；(2) 命中 rule 的事务在两类端口上均照常按地址表路由，不受 default port 使能影响（SPEC-3.2/SPEC-3.3）；(3) 走 default port 的事务其 master 侧 ID 前缀与响应回送正确（SPEC-5.1）；零 mismatch。(4) **BUG-0025 第 1 层守卫（SPEC-5.2.6 第 1 条：default master port 是真实 master 端口，SPEC-5.2 第 1-4 条原样适用）**：一条非判决 cover 见证"经 default master port 路由的事务**已进入** `tb/sva/axi_xbar_stall_sva.sv` 的在飞跟踪表"——今日该数结构性恒 0（`stall_sva.sv:99-100` 把 `en_default` 硬编码为 `1'b0`），守卫兑现后须 >0 | baseline（同 M1-01；`en_default_mst_port_i` 逐端口混合使能） | ✅ | doc/evidence/v0.3.9/M3-DE02.log | `make run TEST=m3_de02_default_test SEED=1` |
| M3-OR04 | M3 | 译码未命中事务的保序地位（SPEC-5.2.6，BUG-0025 第 2/3 层守卫）：单 slave 端口、该端口 `en_default_mst_port_i[i]=0`，构造同一**完整 ID** `X`、同方向的两笔事务同时在飞——一笔命中 rule（目标 master 端口 A）、一笔未命中（走 err_slv），两种先后各构造一次，读/写各镜像一次。判据：(1) **完整 ID 维度（可断言，SPEC-5.2.6 第 2.a 条，依据 SPEC-4.5 + SPEC-5.2.3）**——两笔的 B/rlast 完成序须与其 AW/AR 接受序一致，零失配，**无论**该笔被路由到 master 端口还是内部 err_slv；该判据须能证伪（人为把完成序调乱时见红），否则视为空转。(2) **桶级维度的显式排除（SPEC-5.2.6 第 2.b/第 3 条）**——另构造低 `AxiIdUsedSlvPorts=3` 位相同、**完整 ID 不同**且其中一笔走 err_slv 的事务对：checker/SVA 对该组合**不得**下任何完成序判决（许可来源未定义，断言会假红），排除须在代码中以**引 SPEC-5.2.6 的注释显式写出**并配一条**非判决 cover** 记录该角落被触达（≥1 命中）；**红线**：该排除不得以"未登记 ⇒ 目标/序号读到默认或陈旧值 ⇒ 比较恰好为假"实现（SPEC-5.2.6 第 3 条），否则"有意排除"与"忘了写"在报告上同形 | baseline（同 M1-01；`en_default_mst_port_i='0`） | ✅ | doc/evidence/v0.3.9/M3-OR04.log | `make run TEST=m3_or04_order_test SEED=1` |
| M3-CFG02 | M3 | 运行时地址表活值在判决路径上的可见性守卫（BUG-0031）：沿用 M2-CFG01 的重配窗口纪律（仅在全部 slave 端口 AW/AR 均空闲的一拍内改表一次，SPEC-3.4），把地址表由 V0 换到 V1（某条 rule 的 `idx` 指向另一个 master 端口）并使能 default port；**重配之后**在单一 slave 端口上令三要素并存——同低位 ID 桶的**异**完整 ID 兄弟同时在飞 + 其目标在 V1 下跨到不同 master 端口。判据：(1) scoreboard 按每笔事务 AW/AR 握手时刻生效的表版本译码，路由/ID 前缀/数据/响应回送全部正确（SPEC-3.1/SPEC-3.4/SPEC-5.1/SPEC-1），零失配；(2) 完成序与接受序一致（SPEC-5.2.1/SPEC-5.2.3），且**不得**出现无来源的乱序告警——本缺陷误差双向，故守卫双向：守卫兑现前该激励应能观察到差别（假红或漏检），兑现后二者归零且既有 assert 无新增失败；(3) 活值表正判据（非判决 cover）——一条 cover 其命中**当且仅当** SVA 侧对**重配后**命中被改动 rule 的事务算出的目标等于 **V1 的 idx**（而非 V0 的），今日结构性恒 0；(4) 非空转对照——`stall_sva` 的兄弟类 cover 在重配之后命中数须 >0（今日基线 `make run TEST=m2_cfg01_reconfig_test SEED=1` 为该文件 84 条 cover 行全部 0 match） | baseline（同 M1-01；运行中一次 V0→V1 地址表/default port 变更） | ✅ | doc/evidence/v0.3.20/M3-CFG02.log | `make run TEST=m3_cfg02_reconfig_test SEED=1` |
| M3-OR05 | M3 | stall SVA 判决范围外解除武装的定向证伪（BUG-0024，REV-011 §2.3 路线 (b)）：单 slave 端口、同一低位 ID 桶内取两个完整 ID `X`/`Y`，按四步构造——`X→目标 A`、`Y→目标 B`（`B≠A`）、再一笔 `X→目标 A`（SPEC-5.2.4 允许的同 ID 同向同目标合法堆积，使同一完整 ID 有 ≥2 笔在飞）、随后令 `A` 上最老的那笔 `X` 先完成；读/写各镜像一次，跨多个桶迭代。判据：(1) 判决锚点（SPEC-5.2.3）——全部事务完成序与接受序一致、scoreboard 零失配（完整 ID 有 N≥2 笔在飞时的保序判决由 scoreboard 的每事务队列判据承担）；(2) 该四步构造落在 `axi_xbar_stall_sva` 声明的判决范围（"每完整 ID 至多一笔在飞"）**之外**，故运行中**不得**出现 `SVA_OR_W_REORDER`/`SVA_OR_R_REORDER`——范围外须真正解除武装，仅在文档里声明范围不算兑现；(3) 范围边界见证——该次运行的 `w_lost_now`/`r_lost_now` 命中数须随证据一并报出，非 0 即表示该次运行的 SVA 保序判决是范围受限的、结论只由 scoreboard 承担 | baseline（同 M1-01） | ✅ | doc/evidence/v0.3.11/M3-OR05.log | `make run TEST=m3_or05_range_test SEED=1` |
| M3-AT02 | M3 | ATOP 原子读引发的跨方向读 stall 定向守卫（BUG-0012 裁决落地 SPEC-6.5 后的条款守卫，其 `regression_guard` 点名要求 planner 在条款落地后注册本场景）：单 slave 端口发起一笔要求读响应的原子操作（其 ID 按 SPEC-6 与该端口所有读+写在飞事务 ID 不同），并在其在飞期间于同一端口发起一笔**低 `AxiIdUsedSlvPorts=3` 位与之相同**、目标 master 端口与该原子操作**不同**的普通读，使 SPEC-6.5 所述"原子读 ID 被同时注入 AR 方向计数/比较机制"引发的跨方向假冲突 stall 真实发生。判据：(1) 功能正确性不受影响（SPEC-6.5 明述该交互只影响是否被 stall、不影响功能正确性）——原子操作的 B 与 R 两通道均返回（SPEC-6.3）、普通读最终正常完成，路由/数据/响应回送零失配（SPEC-1/SPEC-3.1/SPEC-5.1）；(2) **不得**把该跨方向 stall 判为 SPEC-5.2.1 违反、更不得判 DUT_BUG——它超出 SPEC-5.2 第 1-4 条"仅同方向配对"的字面框架但属**正常设计行为**（SPEC-5.2.5/SPEC-6.5），保序 SVA 对它不报违反是**有意的范围边界**（sva_bind C3.2 范围边界说明）；(3) 达标 cover（非判决）：`cg_atop_read_interaction` 的 `colliding_read_present` bin 须在**本场景内**命中 ≥1——M2-AT01 只作旁证观察、未专门构造该重叠 | baseline（同 M1-01；`ATOPs=1` 已是基线默认） | ✅ | doc/evidence/v0.3.15/M3-AT02.log | `make run TEST=m3_at02_atop_read_test SEED=1` |
| M3-CF01 | M3 | 配置点 A 回归（拓扑 1×N + `LatencyMode=NO_LATENCY`，SPEC-0 行 3 / SPEC-7.2）：`NoSlvPorts=1`、`NoMstPorts=8`，其余 `Cfg` 字段与地址表区间布局同基线。唯一 slave 端口向全部 8 个 master 端口发命中读/写 burst，另发一批未命中地址（`en_default_mst_port_i='0` ⇒ err_slv）。判据：路由（SPEC-3.1/SPEC-3.2）、数据完整性（SPEC-1）、响应回送（SPEC-5.1）、decode error 应答（SPEC-4.1/SPEC-4.3）全部正确，零失配、无 assert 失败；全部判决延迟不敏感（SPEC-7.4）——本配置点的意义即证明 `LatencyMode` 只改通路延迟、不改功能响应，其期望值与基线 `CUT_ALL_AX` 下**逐条相同**。**ID 前缀退化说明**：`NoSlvPorts=1 ⇒ $clog2(1)=0`，故 master 端口 ID 宽 = `AxiIdWidthSlvPorts=5`、前缀为 0 位宽（SPEC-5.1 公式的字面取值）；参考模型须按该字面取值推导期望，不得沿用基线的 3 位前缀假设 | cfgA（1×8；`LatencyMode=NO_LATENCY`；其余同基线） | ✅ | doc/evidence/v0.3.14/M3-CF01.log | `make run TEST=m3_cf01_cfga_test SEED=1` |
| M3-CF02 | M3 | 配置点 B 回归（拓扑 N×1 + `LatencyMode=CUT_ALL_PORTS`，SPEC-0 行 3 / SPEC-7.2）：`NoSlvPorts=6`、`NoMstPorts=1`，8 条 rule 全部 `idx=0`（SPEC-3.1：多条 rule 可映射到同一 master 端口）。6 个 slave 端口并发向唯一 master 端口发写/读 burst（mux 侧汇聚最大化），另发未命中地址触发 err_slv。判据：路由/master 侧 ID 前缀（高 3 位 = 源 slave 端口索引，SPEC-5.1）/数据完整性（SPEC-1）/响应回送正确，零失配；W burst 随其**各自源**的 AW 保持同序、burst 内不与他源交织（SPEC-5.5.1）；decode error 应答正确（SPEC-4.1/SPEC-4.3）；仿真自然结束（无饿死，SPEC-5.5.3 只准取"每个持续 valid 的请求终将被授予"这一性质）。**红线**：不得断言任何跨源 round-robin 发生序或某一拍的具体被授权端口（SPEC-5.5.4） | cfgB（6×1；`LatencyMode=CUT_ALL_PORTS`；8 条 rule 全 idx=0；其余同基线） | ✅ | doc/evidence/v0.3.15/M3-CF02.log | `make run TEST=m3_cf02_cfgb_test SEED=1` |
| M3-CF03 | M3 | 配置点 C 回归（拓扑 4×4 + `UniqueIds=1'b1`，SPEC-0 行 3 / SPEC-5.3）：4 slave × 4 master，8 条 rule 的 `idx` 取 `rule_index mod 4`（每 master 端口 2 条 rule，SPEC-3.1）。**env 前置条件为构造性硬约束**（SPEC-5.3.1：每笔事务 ID 在同方向所有在飞事务中唯一，或同 ID 同向在飞事务全部目标同一 master 端口；SPEC-5.3.3：前置条件不满足时 DUT 行为**未定义**，届时任何判决都无意义）。判据：路由/ID 前缀/数据完整性/响应回送正确、零失配（SPEC-3.1/SPEC-1/SPEC-5.1）；另配一条 **env 侧兜底监视**（沿用 sva_bind C3.5 属性 2 先例）：若 SPEC-5.3.1 前置条件在运行中被违反则报 TB_BUG（env 违反自身约束），**不得**先入为主判 DUT_BUG——无此兜底则本场景的 ✅ 可能建立在未定义行为上，与"忘了检查"同形 | cfgC（4×4；`UniqueIds=1'b1`；rule idx = index mod 4；其余同基线） | ✅ | doc/evidence/v0.3.15/M3-CF03.log | `make run TEST=m3_cf03_cfgc_test SEED=1` |
| M3-CF04 | M3 | 配置点 D 回归（拓扑 4×4 + 稀疏 `Connectivity` + `ATOPs=1'b0`，SPEC-0 行 3 / SPEC-8 / SPEC-6）：4 slave × 4 master；地址表 8 条 rule 只指向 master 端口 0/1（SPEC-3.1：无任何 rule 指向的 master 端口为合法配置），master 端口 2/3 仅经 default master port 可达——slave 端口 0/1 的 default = mst2、slave 端口 2/3 的 default = mst3（SPEC-3.3）；`Connectivity` 相应取行 0/1 = 连通 {mst0,mst1,mst2}、行 2/3 = 连通 {mst0,mst1,mst3}。该构造使 **SPEC-8.3 的环境约束构造性成立**（地址表不把任一 slave 端口的任何地址译码到其非连通 master 端口，各 default port 亦指向本行连通端口），故 SPEC-8.4 的未定义情形不可触发。**env 约束（SPEC-6/SPEC-6.2，ATOPs=0 行）**：所有 AW 恒 `aw.atop ≡ '0`。判据：命中事务按表路由到 mst0/mst1、未命中事务按各端口 default 路由到 mst2/mst3、数据完整性/ID 前缀/响应回送正确，零失配（SPEC-3.1/SPEC-3.3/SPEC-1/SPEC-5.1）；一条非判决 cover 证明 mst2/mst3 上确有经 default port 到达的流量——否则稀疏维度与 default port 维度均为空转 | cfgD（4×4；稀疏 `Connectivity`；`ATOPs=1'b0`；rule 仅指向 mst0/mst1；其余同基线） | ✅ | doc/evidence/v0.3.15/M3-CF04.log | `make run TEST=m3_cf04_cfgd_test SEED=1` |
| M3-TL01 | M3 | BUG-0010 跨桶定向回归守卫（demux.md 分桶口径由"文档信任"升级为"波形经验确认"）：单 slave 端口，构造 ≥2 个不同低位 ID 桶（低 `AxiIdUsedSlvPorts=3` 位互不相同的完整 ID 组），同方向背靠背分别压至各自桶接近/越过 `MaxMstTrans=10`（可至结构有效上限 15，SPEC-5.4.1/BUG-0016），保持多桶同时在飞，使该 slave 端口**合计**在飞事务数明显超过扁平读法上限（例如 ≥2 桶各达 8~10 笔 ⇒ 合计 16~20 > 10）。判据：(1) 判决锚点——scoreboard 路由/数据/响应/完成正确（SPEC-1/SPEC-3.1/SPEC-5.1），零失配，证明 DUT 在该合计规模下合法全部接受、不因合计规模产生非预期停顿或拒收；(2) 达标覆盖（非判决、延迟不敏感，同 M2-TL01/TL02 先例，SPEC-7.4.5）——至少一次观测到"该 slave 端口合计在飞事务数 > `MaxMstTrans`（10）且同时 ≥2 个不同低位 ID 桶均非空"。**若 DUT 实际表现为扁平**（合计一触顶 10 即拒收，纵有桶内余量），须立即按 BUG-0010 `regression_guard` 触发对 demux.md 的 DUT_BUG/文档-实现分歧复核，不得静默降级本场景判据 | baseline（同 M1-01；`AxiIdUsedSlvPorts=3` 已提供 8 个低位 ID 桶，无需 M3 稀疏 Connectivity/多拓扑维度） | ✅ | doc/evidence/v0.3.19/M3-TL01.log | `make run TEST=m3_tl01_xbucket_test SEED=1` |
| M4-RC01 | M4 | M4 spec-gap sweep 候选（REV-018 注册）：运行时把一个**已使能**的 default master port **关闭**（`en_default_mst_port_i` 位 1→0）并/或把 `default_mst_port_i` 索引改回更低值，改动限于全端口 AW/AR 空闲窗口内（复用 M2-CFG01 重配纪律，SPEC-3.4.1）。动作合法性锚 SPEC-3.4.2（default 使能位/索引同样运行时可变）。判据（scoreboard 按各笔 AW/AR 握手时刻生效配置重算期望）：(1) 关闭后原经该 default port 路由的未命中事务改由本端口 err_slv 应答，响应码 `RESP_DECERR`（SPEC-4.2/4.3），读出齐 `AxLEN+1` 个 R beat（末拍 `RLAST=1`）/写单拍 B，读数据 = err_slv 默认 `RespData`（SPEC-4.4，`64'hCA11AB1EBADCAB1E` 按 `AxiDataWidth=64`，**不得引 RTL 行号**，BUG-0033 纪律）；(2) 关闭前后两批事务各自按其生效时刻的配置正确路由，无跨批错送。**env 约束**：承载 SPEC-4 clause 7（不向译码未命中地址发起 ATOP，范围取宽读，见 M3-DE01 行同款约束句，REV-018）。**DV 核对项（非阻塞）**：本场景真实激励 default-port 写路径，落地时核对 `axi_xbar_unmuxed` AW 侧 default assert 是否随之 real-succeed（BUG-0025/BUG-0031/M3-DE02 既有债务同根，不重复登记新 bug）。零 mismatch、无 assert 失败 | baseline（同 M1-01；运行中把一个已使能 default port 关闭/降位） | ✅ | doc/evidence/v0.4.6/M4-RC01.log | `make run TEST=m4_rc01_reclose_test SEED=1` |
| M4-AW01 | M4 | M4 spec-gap sweep 候选（REV-018 注册）：某 master 端口的 slave-agent 在 mux 仲裁**决定转发某笔 AW 的那一拍**保持 `aw_ready` 为低（≥2 个源 slave 端口对该 master 端口的 AW 同时竞争时施加背压），激励仲裁"已选中但下游未就绪→锁定选择、下拍重试"路径（结构覆盖动机，非判决对象）。**判决门（spec 性质）**：背压下路由/数据完整性（SPEC-1）/响应回送（SPEC-5.1）正确；W burst 随其各自源 AW 保持同序、不与他源交织（SPEC-5.5.1）；无饿死（SPEC-5.5.3："每个持续 valid 的请求终将被授予"）。**红线（同 M2-WO01 先例）**：不得断言任何跨源 round-robin 具体发生序或某一拍的具体被授权端口（SPEC-5.5.4/REV-006 §4.3）；仲裁重试路径被进入仅作**非判决 cover**，证明背压激励非空转，不得升格为"重试路径已发生"的判决期望值（否则期望值将落到被测 RTL 上）。**env 约束**：承载 SPEC-4 clause 7 | baseline（同 M1-01；slave-agent 在仲裁决策拍对 `aw_ready` 施加背压） | ✅ | doc/evidence/v0.4.6/M4-AW01.log | `make run TEST=m4_aw01_awbp_test SEED=1` |
| M4-OV01 | M4 | M4 spec-gap sweep 候选（REV-018 注册）：地址表配置两条**区间重叠**的 rule（指向不同 master 端口），令某地址同时落入二者（SPEC-3.2.1：含起址不含终址）。判据：参考模型按 SPEC-3.1.3（重叠时位于地址表**更高（更显著）位置**的 rule 胜出）推导期望目标 master 端口 idx，验证 DUT 路由到该 rule 目标、非低位置 rule 目标；数据完整性/ID 前缀/响应回送正确，零失配（SPEC-1/SPEC-5.1）。**取向消歧纪律（REV-018）**：若 SPEC-3.1.3"更高（更显著）位置"字面不足以唯一确定地址表数组的高/低下标取向，登记 `SPEC_ISSUE` 走仲裁补 spec，**不得**转读 `addr_decode_dync` 实现体消歧（spec-from-RTL 红线）。**env 约束**：承载 SPEC-4 clause 7 | baseline（同 M1-01；地址表新增一对重叠区间 rule，指向不同 master 端口） | ✅ | doc/evidence/v0.4.6/M4-OV01.log | `make run TEST=m4_ov01_overlap_test SEED=1` |
| M4-FT01 | M4 | M4 spec-gap sweep 候选（REV-018 注册，前提＝提案 1 已裁 (a)，SPEC-0 行 3 现含 `FallThrough` 维）：`FallThrough=1'b1` 配置点（SPEC-2.1 字段定义、SPEC-7.3.1 语义与推荐值）：验证 W beat 可与对应 AW 同拍被接受（AW 路由决策直通到 W）。**判决门 = 与 `FallThrough=1'b0` 基线功能等价**（延迟不敏感，SPEC-7.4）：路由/数据完整性/wstrb/wlast/响应回送逐条与基线判据相同，零失配（SPEC-1/SPEC-3.1/SPEC-5.1）。**红线**："W 与 AW 同拍被接受"是 SPEC-7.4.3 禁止断言的固定时序性质，只作**非判决 cover**证明该分支被触达（`axi_demux_simple` 内 fall-through 分支，结构覆盖动机），**不得**升格为"必须同拍"的时序断言。**env 约束**：承载 SPEC-4 clause 7 | cfgE（同基线；`FallThrough=1'b1`，其余同基线） | ✅ | doc/evidence/v0.4.6/M4-FT01.log | `make run TEST=m4_ft01_cfge_test SEED=1` |
| M4-EB01 | M4 | M4 覆盖率收敛清单 B 候选（REV-026 注册，arch 分诊 §D-2，非 merge-gated）：单/多 slave 端口持续向译码未命中地址发写 burst（地址不匹配任何 rule 且该端口未使能 default master port，SPEC-3.2/SPEC-4.2），由本端口 err_slv 以 DECERR 应答；发起端口的 master(DUT)侧 agent 对 err_slv 的 **B 响应通道持续保持 `b_ready` 低**，令 err_slv 内部 B 响应缓冲在多笔 decode-error 写收齐后堆积至结构容量上界，进而使 err_slv 反压其入侧 `aw_ready`（结构覆盖动机：err_slv B-fifo 满/入侧背压路径，**非判决对象**）。**判决门（scoreboard，延迟不敏感 SPEC-7.4）**：背压解除后每笔未命中写最终返回**单拍** B(DECERR)（SPEC-4.3），响应码 `RESP_DECERR`、响应 ID/握手回到发起端口无跨端口错送（SPEC-4.5/SPEC-5.1），背压期间无 B 丢失/重复（SPEC-1 响应侧）；零 mismatch。**红线**：不得断言 err_slv 内部缓冲深度、`b_fifo` 满的具体拍或 `aw_ready` 拉低的具体时点（延迟不敏感 SPEC-7.4.5，接受/拒收边界即时性非判决锚点）；内部背压路径被进入仅作**非判决 cover**（证背压非空转），不得升格为判决期望值。**env 约束**：承载 SPEC-4 clause 7（不向未命中地址发 ATOP，宽读） | baseline（同 M1-01；`en_default_mst_port_i='0`；master(DUT)侧 agent 对 err_slv 响应通道持续背压） | ✅ | doc/evidence/v0.4.17/M4-EB01.log | `make run TEST=m4_eb01_errbp_test SEED=1` |
| M4-BP02 | M4 | M4 覆盖率收敛清单 B 候选（REV-026 注册，arch 分诊 §E-2；orch 复核确认 merge-first 前置已满足——`doc/evidence/v0.4.15/M4-toggle-bit-decomposition.md` 引用的 `urgReport_baseline` 本就是 `make regress COV=1` 后对 M1-M4 全部 20 个基线拓扑场景（含 M2-TL01/TL02/M3-TL01/M4-AW01/M3-DE02/M2-CFG01）合并覆盖率的度量，非单跑 m1_01，故 `lock_aw/ar` FSM 与 `w_open[3:2]` 残留缺口已是合并后真实数字，无需另跑合并即可注册本行）：单 slave 端口向同一 master 端口目标并发多笔写 burst 使 demux 侧同时打开 ≥3 个 W-burst 通道（`w_open` 高位），同时下游 mux 对该 master 端口 AW 施加持续背压（`aw_ready` 低，复用 M4-AW01 机制）令 demux 已选中 AW 进入锁定-重试路径（`lock_aw` FSM），并令同桶（低 `AxiIdUsedSlvPorts` 位）在飞事务压至 §5.4.1 有效上限（基线 15）——三者叠加（结构覆盖动机，**非判决对象**）。**判决门（scoreboard，延迟不敏感 SPEC-7.4）**：叠加压力下路由/数据完整性/wstrb/wlast/响应回送/完成序全部正确（SPEC-1/SPEC-3.1/SPEC-5.1/SPEC-5.2.3），零 mismatch；无饿死（SPEC-5.5.3）。**红线**：不得断言 lock FSM 状态序、`w_open` 具体计数、`id_cnt` 达上限的具体拍或第 N+1 笔被拒的具体时点（延迟不敏感 SPEC-7.4.5；有效上限非字面 `MaxMstTrans`，BUG-0016）；内部 FSM/计数路径进入仅作**非判决 cover**。**env 约束**：承载 SPEC-4 clause 7 | baseline（同 M1-01） | ✅ | doc/evidence/v0.4.21/M4-BP02.log | `make run TEST=m4_bp02_demuxlock_test SEED=1` |
| M4-BP03 | M4 | M4 覆盖率收敛清单 B 加固卡 B（REV-027 §5「加固卡 B」注册，读方向对偶——doc/review/REV-027.md §2.5/§5 独立核实 M4-BP02 只闭合了 AW 侧 lock_aw/aw_id_cnt_full，AR 侧 lock_ar/ar_id_cnt_full 及其伴随的 3 条 Assert 在既有全部 28 个场景里从未被触达，doc/evidence/v0.4.20/M4-P0-remeasure.md §3 精确定位）：单 slave 端口向同一 master 端口目标并发多笔读 burst（同桶，低 `AxiIdUsedSlvPorts` 位相同，跨其 4 个兄弟全 ID 铺开，spread_bucket=1，同 M2-TL01/M4-BP02 先例），同时下游 mux 对该 master 端口 AR 施加持续背压（`ar_ready` 低，`bp_enable_ar`——M4-AW01/M4-BP02 `bp_enable` 机制的 AR 镜像）令 demux 已选中的 AR 进入锁定-重试路径（`lock_ar` FSM），responder 对 R 通道有界拖延（`resp_hold`，M2-TL01/M3-TL01/M4-BP02 同一机制，此处作用于 R 而非 B）使同桶在飞读事务（AR 计数器 pop 于 R 末拍，非 AR 接受时刻）压至 §5.4.1 有效上限（基线 15）——两者叠加（结构覆盖动机，**非判决对象**）。**判决门（scoreboard，延迟不敏感 SPEC-7.4）**：叠加压力下路由/数据完整性/响应回送/完成序全部正确（SPEC-1/SPEC-3.1/SPEC-5.1/SPEC-5.2.3），零 mismatch；无饿死（SPEC-5.5.3）。**红线（同 M4-BP02）**：不得断言 lock FSM 状态序、`ar_id_cnt`/计数具体值、或某一拍的具体行为（延迟不敏感 SPEC-7.4.5；有效上限非字面 `MaxMstTrans`，BUG-0016）；内部 FSM/计数路径进入仅作**非判决 cover**（`cg_tx_limit` 的 `at_effective_ceiling`、新增 `cg_ar_retry`，均为外部 valid/ready 观测，无 DUT 内部信号读取）。**env 约束**：承载 SPEC-4 clause 7（本场景纯读、无 AW，天然满足） | baseline（同 M1-01） | ✅ | doc/evidence/v0.4.22/M4-BP03.log | `make run TEST=m4_bp03_demuxlock_ar_test SEED=1` |
| M4-EB02 | M4 | M4 覆盖率残余分诊 DV-D 卡（REV-030 §3「DV-D」注册，doc/review/REV-030.md——M4-EB01 的读方向镜像，rev 亲验 `err_resp.ar_ready = ~r_fifo_full`、`i_r_fifo` DEPTH=`MaxTrans`=4）：单/多 slave 端口持续向译码未命中地址发**读** burst（地址不匹配任何 rule 且该端口未使能 default master port，SPEC-3.2/SPEC-4.2），由本端口 err_slv 以 DECERR 应答；发起端口的 master(DUT)侧 agent 对 err_slv 的 **R 响应通道持续保持 `r_ready` 低**（`axi_burst_item.r_backpressure`，读向镜像 `b_backpressure`），令 err_slv 内部 R 响应缓冲在多笔未命中读收齐后堆积至结构容量上界，进而使 err_slv 反压其入侧 `ar_ready`（结构覆盖动机，**非判决对象**）。**判决门（scoreboard，延迟不敏感 SPEC-7.4，复用 M3-DE01/M4-EB01 既有 SB_DECERR_* 判据族，不新发明期望值）**：背压解除后每笔未命中读最终返回 `AxLEN+1` 拍 R(DECERR)（SPEC-4.3），末拍 `RLAST=1`，每拍数据 = err_slv 默认 `RespData`（SPEC-4.4，`64'hCA11AB1EBADCAB1E`，按 `AxiDataWidth=64`，**不得引 RTL 行号**，BUG-0033 纪律），响应码全程 `RESP_DECERR`、响应 ID/握手回到发起端口无跨端口错送（SPEC-4.5/SPEC-5.1），背压期间无 R 丢失/重复（SPEC-1 响应侧）；零 mismatch。**红线**：不得断言 err_slv 内部 `r_fifo` 深度、满的具体拍或 `ar_ready` 拉低的具体时点（延迟不敏感 SPEC-7.4.5，接受/拒收边界即时性非判决锚点）；内部背压路径被进入仅作**非判决 cover**（`cg_errbp` 新增 `EB_AR_HELD` bin，证背压非空转），不得升格为判决期望值。**env 约束**：承载 SPEC-4 clause 7（不向未命中地址发 ATOP，宽读） | baseline（同 M1-01；`en_default_mst_port_i='0`；master(DUT)侧 agent 对 err_slv 响应通道（R）持续背压） | ✅ | doc/evidence/v0.4.34/M4-EB02.log | `make run TEST=m4_eb02_errbp_test SEED=1` |
