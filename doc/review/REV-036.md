# REV-036 — BUG-0050 / BUG-0051 关闭复验（closer 卡，L2）

- **卡性质**：closer 卡。订正由 arch fixer 落 `69cf5a6`，处置面裁决由
  `doc/review/REV-035.md` 给出。**二者都是本卡的被审对象，不是已证事实。**
- **隔离**：本实例既非该 fixer、亦非 REV-035 的作者；工作目录
  `…/scratchpad/closer0050/`（BUG-0059 隔离要求），未读同级其它实例产物，
  只取 orch 转达的接口件 `closer-commands.txt` 两行命令。
- **事实源纪律**：132 格三态**从 `sim/out/urgText6/modlist.txt` 自行枚举**
  （fixer 的复验命令走 `modinfo.txt`——两条独立通路，交叉验证）；
  任何 markdown 汇总（§2.3 / §6.3 / REV-035 §0 台账）**只作被审对象、不作输入**。

---

## Scope list

| # | 对象 | 取证方式 |
|---|---|---|
| S1 | `doc/bugs.md` BUG-0050 / BUG-0051 两行全部列（含追加 (v)/(iv)） | 逐字读 |
| S2 | `git show 69cf5a6` 全 diff（5 文件 / 198+ 19−） | 逐 hunk 读 + 程序化行级核对 |
| S3 | `doc/coverage-waivers.md`（现行 14 行） | 逐行读 + 登记面手工转录 |
| S4 | `doc/design-prompt/milestone_restructure.md` §6.1/§6.2/§6.3 | 程序化解析 + 手工读 |
| S5 | `doc/evidence/v0.4.35/M4-coverage-final-sweep.md` §2.3 + 8 条脚注 + §10 | 全文读 + 与 modlist 逐格对账 |
| S6 | `doc/bugs/BUG-0044.md` `## regression_guard` | 读 + `make guards` 实跑 |
| S7 | `doc/review/REV-035.md` §Q1/§Q2/§Q4/§e | 逐条与一手事实对质 |
| S8 | 原始机器事实 `sim/out/urgText6/{modlist,modinfo,hierarchy}.txt` | 唯一事实源 |
| S9 | vendor RTL（`lzc/fifo_v3/stream_register/spill_register_flushable/rr_arb_tree/counter/delta_counter/addr_decode/axi_id_prepend/axi_xbar/axi_mux/axi_err_slv` + `registers.svh`） | 亲读 |

判据来源：`doc/milestone.md` M4 出口第二/三条（L72-84）· `doc/spec.md` §0#4（已 pin）·
`doc/fw-feedback.md` FB-23 · `workflow/bugs.md` 生命周期 · `workflow/review.md` 七问。

---

## 逐项 verdict

### V1 全量集合差：132 格三态自行枚举 —— **pass**

方法（`closer0050/tri_enum.py`）：从 `modlist.txt` 取 35 个**模块定义行**
（缩进 ≤2、名字不含 `( parameter`），按 22 DUT / 13 TB 划分（与
`doc/milestone.md` L73「22+13=35」一致），六列 `--`→N/A、`≥90`→PASS、
`<90`→LT90。

```
CELLS=132 NA=59 GE90=43 LT90=30      （fixer 报同一组数 —— 两条独立通路一致）
```

30 个 LT90 格逐格对四个归属面（CW 登记列 / `ACCEPTED@M<n>` 债务行 / §6.1
backlog / 已修）做差集：

```
UNOWNED (<90% 格无归属 token) = 0        ← ∅
反向：带归属 token 却非 <90% 的格 = 0     （无凭空认领）
```

`sed -n '405,452p'` 的 §6.3 对照表**未作为枚举来源**，仅作被审对象：把它的
132 格逐格与我的 modlist 三态对质，**三态/数值/PASS 标记零失配**（`closer0050/xref.py`）。

### V2 越权引用是否清零 —— **pass（无第六处）**

登记面由我**手工逐行转录** `doc/coverage-waivers.md` 第 2 列（14 行 → 34 个
（模块,类型）对，去重后的授权集），不复用 fixer 的正则解析，以免继承其解析口径：

```
§6.3 引用面 = 31 个 (模块,类型,token)
引用 ∖ 登记 = 0
BUG-0044 → bugs.md 状态 ACCEPTED@M5 ✓（债务面合法）
```

**§6.2 不在 fixer 复验命令的解析面内**（D2 只解析 §6.3），故我**逐行手工**核了
§6.2 全部 13 行：CW-001(六类) / CW-002(三模块 Toggle) / CW-003-005(err_slv) /
CW-006(四模块) / CW-007(mux·demux·err_slv) / CW-008 / CW-009 / CW-010(rr_arb_tree
Line+Toggle、spill_flushable Assert+Cond+Toggle、fifo_v3 **仅** Branch) /
CW-011 / CW-012 / CW-013 / CW-014 / BUG-0044 —— **无一处越出登记列**。

**第六处：无。** 全仓 `grep CW-0` 扫过 waivers / design-prompt / 冻结证据 /
milestone / bugs 详情页，唯一仍带旧 token 的是
`doc/evidence/v0.4.37/BUG-0049-closure.md:151/152/156`——见 O4，**不构成第六处**。

**(v) 的裁决面（fifo_v3 Toggle 扩不扩 CW-010）**：现状不需要裁。该格由
§6.1 重测-1 明写"目标范围为**全格**（含 `flush_i` 位这一结构残余）"+ DV-A/B
承接，`flush_i` 端口位实存（modinfo `fifo_v3` Toggle 明细 `flush_i No No No
INPUT`，L181680 等 6 处）且落在承接范围内 ⇒ **不扩也无孤儿位**。fixer 取缩窄
一侧符合「不得扩大豁免 scope」。

### V3 FB-23 边界：冻结证据文件有无任何行被覆写/删除 —— **pass**

`git diff --numstat 69cf5a6^ 69cf5a6 -- doc/evidence/v0.4.35/M4-coverage-final-sweep.md`
= `158  4`，即行级看有 4 行"被改"。**逐字节核对结论：零删除、零覆写。**
把旧版 395 行逐行拿到新版 549 行里找：

- 391 行**逐字原样存在**；
- 3 行（旧 `:162/:176/:180` 脚注 3/5/6 首行）原文完整保留，只是列表序号
  `3. `/`5. `/`6. ` 之后**插入**了勘误标记，原句移到下一行（新 `:162/:177/:184`
  仍以 `3. `/`5. `/`6. ` 起）；
- 1 行（旧 `:128` counter 表行）为**行内插入**：
  `新行.replace(" **[勘误 E1 → §10]**","") == 旧行` → **True**（字符级相等）。

⇒ 符合 REV-035 §Q2(b) 自立的边界「"不回改" = 不覆写、不删除；加标记是加法」。
**FB-23 未被违反，关闭不被阻断。**

### V4 两条新写 N/A 成因是否"已核实" —— **pass（自行回源，非采信）**

**(A) `(lzc, Line)`** —— 成因"无被精化的过程块 ⇒ 无 Line bin"：

| 断言 | 我的核对 | 结果 |
|---|---|---|
| `lzc.sv` 唯一过程块 = `:56 always_comb : flip_vector`，在 `:54 if (MODE)` 内 | `grep -n always vendor/common_cells/src/lzc.sv` → 仅 `:56`；`sed -n '35,70p'` 读上下文 | ✓ |
| 本闭包两实例均 `.MODE(1'b0)` | `rr_arb_tree.sv:202-205`/`:211-214` 亲读，两处 `.MODE ( 1'b0 )` | ✓ |
| urg 只精化 MODE=0 两变体 | `modlist.txt:53/54` = `WIDTH=6,MODE=0` / `WIDTH=9,MODE=0` | ✓ |
| 闭包内 lzc 实例只来自 rr_arb_tree | `hierarchy.txt` 计数 `i_lzc_upper`=28、`i_lzc_lower`=28（合 56 = §6.3 例列） | ✓ |
| `lzc` 模块页**无 Line 节** | modinfo L146860 起节种类 = `{Assert,Branch,Cond,Toggle}` | ✓ |
| 判别式全闭包自洽 | 精确计数 `^\s*always(_comb\|_ff\|_latch)?\b`：9 个无 Line 节模块全部 **0**；`lzc` **1**（被 MODE 排除）；反例 `stream_register` 字面 0 却**有** Line 节（宏展开） | ✓ |

（注：`grep -c always addr_decode.sv` 得 1，但那一处在文档注释
`dec_error_o is always 1'b0` 内——用精确正则复核后为 0，**fixer 的表述成立**，
是我的第一次粗查不精。记此以免下一位重复。）

**(B) `(stream_register, Cond)`** —— 成因"运算符种类判别式"：

| 断言 | 我的核对 | 结果 |
|---|---|---|
| `:34/:35` 是**按位** `\|`/`&` | 亲读 `stream_register.sv:34/35` | ✓ |
| 条件语句仅来自 `FFLARNC` 宏、条件逐条单信号 | `registers.svh:211-225` 展开体：`if(!__arst_n)` / `if(__clear)` / `else if(__load)` | ✓ |
| 该模块页无 Cond 节 | modinfo L2611 起节种类 = `{Branch,Line,Toggle}` | ✓ |
| 模块内对照：`spill_register_flushable:90`(`\|\|`) 生成、`:93`(`\|`) 不生成 | 程序化提取该模块 Cond 节源码行号集 = `{53,72,78,79,84,85,90,96}`，**93 不在其中** | ✓ |
| 模块内对照：`lzc:76/:95`(`?:`) 生成、`:75/:93-94`(`\|`) 不生成 | lzc Cond 行号集 = `{76,95}` | ✓ |
| 全闭包自洽：有 Cond 节的 11 模块，其 Cond bin 源码行**无一例外**含 `&&`/`\|\|`/`?:` | 程序化提取 11 模块全部 Cond 行号 → 逐行回 RTL 验运算符：**57/57 成立**（56 行本行含；`axi_atop_filter:128` 为跨行条件，续行含 `\|\|`/`&&`） | ✓ **结论成立** |

⇒ 两条成因均**被原始报告正向支持**（不是"没被推翻"），满足 REV-035 §Q2(3)
与 M4 出口第二条"附**已核实**成因"。**但该段有一处数字不可复现，见 I1。**

**8 条脚注抽样反证：我独立重做 3 条**（选择理由随附）：

| 脚注 | 我选它的理由 | 我的独立核对 | 结论 |
|---|---|---|---|
| 1 | 复用面最广（8 个模块行、约 30 格），且 §10.3 自陈其末句"措辞过宽"——错的爆炸半径最大 | `addr_decode` 页节种类 = `{Toggle}`；9 个"纯壳"模块精确 `always*` 计数全 0 | 成立（末句问题见 I2） |
| 4 | 正是 REV-035 §Q2(d) **硬警告禁止照抄**去补 stream_register 的那条规则——它对 `delta_counter` 是否仍立，决定重写(B)是否自相矛盾 | `delta_counter` 页节 = `{Branch,Line,Toggle}`（无 Cond）；其仅有的 `\|\|`/`&&`（`:43/:45`）在 `:29 if(STICKY_OVERFLOW)` 内，而 `modlist:43/44` 两变体均 `STICKY_OVERFLOW=0` ⇒ 未精化 | 成立 |
| 7 | 机理与新写的 lzc 成因**同形**（generate/精化期排除）——若判别式有误，最可能在此复发 | `axi_id_prepend` 页节 = `{Assert,Line,Toggle}`（无 Cond/Branch）；`:82 if (PreIdWidth == 0)` 在 `for (genvar …)` generate 内、参数常量；`:86-92 always_comb` 体内无运行时条件 | 成立 |

顺带亲验：脚注 2（全库 `^FSM Coverage for Module` 计数 = **1**，属
`axi_atop_filter`）· 脚注 3/5/6/8 的模块页节种类（`counter={Toggle}`、
`stream_register={Branch,Line,Toggle}`、`axi_pkg={Assert}`）。

### V5 fixer 报的四条发现是否属实 —— **四条全部属实（与 fixer 一致）**

| id | 声称 | 我的独立核对 | 判定 |
|---|---|---|---|
| **F2** | REV-035 §0 行 F / §Q2(a).2 称 `lzc.sv` "L39-103 全 assign + generate、**无 `always` 块**（亲读）" 失实 | `grep -n always vendor/common_cells/src/lzc.sv` → **`56:      always_comb begin : flip_vector`** | **属实**：REV-035 该句为假；其结论（原脚注 3 的"过程块"预设不成立）仍对，但理由必须换成"MODE=0 未精化"——fixer 拒绝照抄是正确的，照抄将制造第三个未核实成因 |
| **F1** | REV-035 §Q1(i) 称 CW-010"论证列现把 `fifo_v3.sv:122` 并列在 Cond 项里"失实 | 订正前 CW-010 第 3 列逐字：`…另 \`fifo_v3.sv:122\` flush 复位分支（**Branch IF-117 \`0 1\`**）、\`spill_register_flushable.sv:78/79/84/85\` … 的 **Cond 项**、\`rr_arb_tree\` Toggle 端口 …` —— `:122` 自带 Branch 限定括注，与其后独立的 Cond 项并列而非归入 | **属实**：该列原本自洽；REV-035 另把列名写作"论证列"（实际在"具体 bin/信号范围"列）。fixer 拒绝"订正一处不存在的错误"正确，无损害 |
| **T2** | §6.3 `fifo_v3` **Toggle** 格同引 CW-010 而登记列未含——同形第五处 | 订正前 §6.3 `:431` = `**CW-010+DV-A/B+重测-1**(82.09)`；订正前 CW-010 第 2 列 = `…（+ \`fifo_v3\` Branch/Cond、\`spill_register_flushable\` Cond/Toggle、\`rr_arb_tree\` Toggle …）`，**不含 fifo_v3 Toggle** | **属实**：第五处越权真实存在，且**不在 BUG-0050 原四项内**，fixer 的完备性扫描抓到了裁决者与四路复算都漏掉的一处 |
| **T3** | 脚注 5 括注"`stream_register.sv` 全文无 `if`"与脚注 6 同根同被证伪 | 订正前 `:176-179` 逐字含"`stream_register.sv` 全文无 `if`"；`FFLARNC` 展开实有三处 `if`，urg Branch 表列 `IF 37`/`IF 38` | **属实**：判定（Assert N/A）与操作性成因（RTL `assert` 计数 0，页无 Assert 节）不受影响，仅括注失真 |

### V6 有无"放宽判据让账目好看" —— **pass，但有一处须显式承认的扩权**

14 处订正逐处过判：**无删格、无三态改判、无数值改动**（V1 已机器复核 132 格
零失配）。方向上 12 处是**缩窄或订正**，唯一的**扩权**是 CW-002 登记列
`axi_xbar` → `axi_xbar`/`axi_err_slv`/`axi_mux` Toggle。该扩权我**不采信裁决、
自行回源**：

- `axi_xbar.sv:74` 顶层 `test_i` 输入 → `:114 .test_i,`（隐式连接 `i_xbar_unmuxed`，
  转 `axi_xbar_unmuxed.sv:60` → `:186/:207/:248`）与 `:151 .test_i,`（`i_axi_mux`，
  转 `axi_mux.sv:59`）——**同一根顶层网**，非各自独立端口；
- `tb/tb_top.sv:137 .test_i (1'b0)` 恒 0；
- urg 亲证三模块页 Toggle 明细均 `test_i  No  No  No  INPUT`
  （modinfo L324328 / L296100 等 6 处 / L324587 等 6 处）。

⇒ 论证在三个模块页上**平行成立**，扩的是模块页而非 bin。**BUG-0047 guard 边界
遵守核实**：第 3 列写死"`test_i` scan-enable **单一位**…**不含任何载荷位**"，
Kind 仍为 A，未新增 Kind-B。`make guards FILES="doc/coverage-waivers.md"` 实测
BUG-0047 guard 命中且其禁令（不得把 err_slv 等改记 Kind-B、不得凭"是 Toggle"
笼统豁免）未被触碰。

**替代方案为何不可取**（我独立复核 REV-035 的理由而非照抄）：删引用会使
`(axi_err_slv,Toggle)`/`(axi_mux,Toggle)` 中的 `test_i` 位失去归属主体——它不属
CW-006(rst_ni)/CW-007(size[2])/BUG-0044(atop)，也不可能落 DV（定向卡不测 scan，
那正是被判范围外之物）。故扩登记是**唯一不制造孤儿位**的一侧。

### V7 机器背书命令的强度与执行 —— **pass（我先证伪，后采用）**

两条命令我**先在 scratch 镜像上做了 8 次证伪**（`closer0050/mirror/`，只读符号链接
`sim`/`vendor`，md 为副本，仓内文件零改动）：

| kill | 注入的缺陷 | 输出 | 退出码 |
|---|---|---|---|
| K1 | §6.3 `fifo_v3` Cond 重新挂回 `CW-010` | `REFS=32 OVERCLAIM=1` + `VIOL ('fifo_v3','Cond','CW-010')` | 1 |
| K2 | CW-002 登记列退回只写 `axi_xbar` | `OVERCLAIM=2` + 两条 VIOL | 1 |
| K3 | 删掉 CW-013 整行 | `WAIVERS=13 OVERCLAIM=2` | 1 |
| K4 | §6.3 counter 例列改回 108 | `INSTMISMATCH=1 MISMATCH counter` | 1 |
| K5 | 覆写证据文件那行原文（108→12） | `MARKERS=3/4 PRESERVED=2/3` | 1 |
| K6 | 删掉一个就地勘误标记（E3） | `MARKERS=3/4` | 1 |
| K7 | 篡改 `ERRATA-FACTS` 的 E1 值 12→13 | `ERRATA=3/4` | 1 |
| K8 | §6.3 把 `lzc` Line 由 N/A 改 PASS | `NAMISMATCH=1 MISMATCH ('lzc','Line')` | 1 |

**为何不可被假绿伪造**：D2 的 `WAIVERS=14/GRIDROWS=22/REFS=31` 三个基数被钉死
⇒ 靠删 token 或删行让 `OVERCLAIM=0` 会同时打断基数（K1/K3 实证）；D3 的
`CELLS=132/INSTMISMATCH/NAMISMATCH/VALMISMATCH` 全部现场从 `modinfo.txt` 重算，
签名里**没有一个数字取自被检文档**，要伪造须篡改只读 urg 产物。

**两条命令的已知盲区（我用人工/自写脚本补上，记此以便下次收紧）**：
1. D2 只解析 §6.3，**不覆盖 §6.2** → 我手工核了 §6.2 全 13 行（V2）。
2. D2 对"**扩登记**让越权合法化"不敏感（K2 只在缩窄方向变红）→ 属人核，见 V6。
3. D3 的 `ERRATA-FACTS` 四行由作者自选 → 成因散文不受机器约束 → 见 V4 我的独立回源。
4. `PRESERVED=3/3` 只钉 3 个字符串，是"未覆写"的代理 → 我改用全量 395 行逐行核对（V3）。
5. 两条签名**都不钉 `UNOWNED=∅`**（`milestone_restructure.md` §8.2 自陈其为
   "当前人核、机核待建"）→ 我自写脚本算，得 0（V1）。

执行结果：

```
doc/evidence/v0.4.38/BUG-0050.log   首行 CMD:，exit_code: 0
  D2 WAIVERS=14 GRIDROWS=22 REFS=31 OVERCLAIM=0 DEBT_NOT_ACCEPTED=0 PARSEFAIL=0
doc/evidence/v0.4.38/BUG-0051.log   首行 CMD:，exit_code: 0
  D3 ERRATA=4/4 MARKERS=4/4 PRESERVED=3/3 CELLS=132 INSTMISMATCH=0 NAMISMATCH=0 VALMISMATCH=0
```

`doc/bugs.md` 两行由 `scripts/evidence.py` **自行翻列**（`git diff` 仅 2 行、
仅 status 与 verify_evidence 两列变化，无人工编辑）。

### V8 `doc/bugs/BUG-0044.md` guard —— **pass（有一条 guidance）**

`paths:` 由中文顿号改逗号后实测生效：
`make guards FILES="doc/coverage-waivers.md doc/design-prompt/milestone_restructure.md"`
→ BUG-0044 guard 命中（订正前对 `verification_maturity.md` 零命中，属 BUG-0061）。
消费路径 2 的 note 说清了"债务行改状态 ⇒ 两格 Toggle 归属失效须同卡重裁"这一
既有耦合，且**未**给 `ACCEPTED@M<n>` 行发明 spec 里没有的自述字段——与
`doc/spec.md` §0#4"义务只挂在格这一侧"一致，我复核 pin 值确认该条读法正确。

---

## 发现（issue，均**不阻断**本次关闭，须登记/跟进）

### I1 §10.2(B)"共 31 个行号"不可复现 —— 与 BUG-0051(i) **同类**，出现在订正内部

原文：「有 Cond 节的 11 个模块……其全部 Cond bin 的源码行（**程序化逐行核对，
共 31 个行号**）无一例外都含 `&&`/`||`/`?:`」。

我的实测（`closer0050/cond_lines.py`，两种独立提取口径互证）：11 个 DUT 模块
的 Cond bin 源码行 = **57 个（模块,行号）对 / 53 个不同行号**——
`axi_atop_filter` 19、`rr_arb_tree` 8、`spill_register_flushable` 8、
`axi_demux_simple` 4、`fifo_v3` 4、`axi_demux_id_counters` 3、`axi_mux` 3、
`lzc` 2、`axi_err_slv` 2、`addr_decode_dync` 2、`axi_xbar_unmuxed` 2。
**没有任何一种自然口径得出 31。**

- **结论本身独立成立**（我逐行回 RTL 验过 57/57），故成因仍被正向支持
  ⇒ **不阻断 BUG-0051 关闭**；
- 但它是"声明性数字与其自称的机器来源对不上"——**正是 BUG-0051 (i) 的形状**，
  这次载体是**订正段本身**。属 `workflow/bugs.md` TB_BUG 类，登记无条件。

### I2 脚注 1 的证伪陈述未获与 E4 同等处置 —— 处置面不对称

`§10.3` 行 1 自陈：脚注 1 末句"以上均只有 Toggle 有意义…Assert 无 bin"对
`axi_xbar`/`axi_xbar_unmuxed`/`axi_multicut` **不成立**（三者 Assert=100.00，
我的枚举复核：三格均为 GE90、§2.3 亦未标 `N/A¹` ⇒ **三态判定不受影响**）。

问题在处置**不对称**：结构完全相同的 E4（脚注 5 括注失真、同样不影响判定）
拿到了 **就地 `[勘误 E4 → §10]` 标记 + §10.1 表格行 + bugs.md 枚举追加 (iv)**
三件；本条只在 §10.3 抽样表里留了一句括注，**既无就地标记、也未进 bug 行枚举**。
未来读者读脚注 1 时不会被任何标记提示。→ 报 orch 判：补标记 or 明写"仅记录不标记"
的判据。

### I3 `docs.py` 指路到一个不存在的 target —— 本次关闭实测撞上（TOOL_ENV）

`make evidence BUG=BUG-0051 …` 写盘并翻列成功后，尾部 docs-check 判红：

```
[FAIL] bugs.md has 5 terminal rows > 4 — run: make docs-archive
```

而本仓 Makefile 的目标名是 **`make archive`**（`Makefile:85`，`make help` 亦印
`archive`）——**`make docs-archive` 不存在**。`scripts/docs.py:412/419/477/544`
四处都印这个不存在的名字。属"机器给出的补救指令本身不可执行"，
`workflow/bugs.md` TOOL_ENV 类；`scripts/docs.py` 与根 `Makefile` 均为上游文件，
订正须登 `doc/fw-feedback.md` FB 行。

**本次 `make check` 因此为红**，且**红点与本卡的技术结论无关**：它是两条
CLOSED 使终态行 3→5 触发的**归档节流**。归档会整行搬动 bugs.md，
超出 closer 的编辑面（我只准动 ruling/status 列）⇒ **留给 orch 跑 `make archive`**。

---

## 观察（不构成 issue）

- **O4 `doc/evidence/v0.4.37/BUG-0049-closure.md:151/152/156` 仍带旧 token**
  （`CW-010+重测-1` on fifo_v3 Cond、`CW-010+…` on fifo_v3 Toggle、
  `CW-007/010+…` on rr_arb_tree Toggle）。**不要求勘误**：该处是对当时 §6.3 的
  **逐字转录**，写作时为真——正是 FB-23 的保护对象；且其 ⑤ 段的原话是
  "`CW-001~014` 均在 `doc/coverage-waivers.md` 表内**实有行**"，做的是**行存在性**
  核对而非（模块,类型）成员核对，**并未声称做过它没做的事**。与 §2.3 的情形
  （写作时即为假、被自己援引的数据源证伪）不同类，不适用 BUG-0051 的处置。
- **O5 CW-010 的 rev 记录列未引 REV-035 §Q1(i)**（缩窄的裁决出处只写在第 3 列
  正文里），而 CW-002 做了双引。签核核对若只读 rev 记录列，看不出该行被缩窄过。
- **O6 BUG-0044 guard 仍是 `type: checklist`**。`workflow/bugs.md` 说 checklist
  是"机械化 TODO：写明它应变成什么脚本/SVA，或为何不能"——现 note 未答这一问，
  而本次落地的 D2 恰好就是消费路径 2 失效形态（越权引用）的机械化形。

---

## Guidance（原则与方向，不写实现）

1. **"程序化核对"这四个字是一种承诺。** 写下它就等于声明"这个数字是机器产出的"。
   I1 说明：一旦承诺与产出脱钩，读者失去的不只是那个数字，而是对整段回源工作的
   信任——而这段回源工作**本身是扎实的**（我复核了 57/57）。方向：凡在散文里出现
   的计数，要么让它进入可复跑的签名（像 `CELLS=132`），要么不写数字只写结论。
   本卡的 D2/D3 已经证明"把数字钉进签名"成本很低。
2. **同形缺陷要拿同一把尺。** I2 的两处（脚注 5 括注 vs 脚注 1 末句）在事实层面
   完全同构：陈述为假、判定不受影响。处置却一处三件、一处一句。判据应当写死在
   处置规则里（例如"任何被证伪的陈述都就地标记，是否进 bug 行枚举取决于是否
   影响判定"），而不是逐条临场裁——临场裁的结果就是遗漏，而遗漏正是这一族
   （BUG-0049/0050/0051）的共同形状。
3. **登记面与引用面的机器核对已经有了 D2，但它只长了一只眼。** 三处盲区
   （§6.2 未解析、扩登记方向不敏感、UNOWNED 未钉）都在 V7 列出。方向：若 orch
   把 D2/D3 沉淀成常驻检查（REV-035 §Q5 的族级 guard 提议），优先补的是
   **UNOWNED=∅** 那一条——它是 M4 出口第三条的正面判据，现在**完全靠人**。
4. **机器给出的补救指令必须可执行。** I3 的危害不在这一次（我知道该跑 `archive`），
   而在于它训练读者忽略 docs-check 的指路。属"机制建好了但判据只覆盖一条通路"
   （与 BUG-0054/0058/0060/0061 同谱）。
5. **本卡最值得留存的一条**：fixer 拒绝照抄裁决（F1/F2）→ 拦下一个会变成"第三个
   未核实成因"的错误 → 登记 BUG-0062；而我这一轮又在 fixer 的订正里找到 I1/I2。
   这不是谁不称职，是**"每一层都自己回源"这条纪律在连续两层上都产生了实效**。
   隔离链的价值在此有了两次连号的实证——值得写进下一次 M4 签核书的残余风险段。

---

## Overall verdict

**PASS —— BUG-0050 / BUG-0051 两条均判 `CLOSE_OK`，机器背书已落地。**

- **BUG-0050 → CLOSE_OK**：五处越权引用（原四项 + fixer 扫出的第五项）全部消解，
  `引用 ∖ 登记 = ∅`（我手工转录登记面独立复算），§6.2 手工核清，**无第六处**；
  UNOWNED 仍为 ∅；唯一的扩权（CW-002）经我回源 RTL + urg 确认论证平行成立且
  bin 范围未放宽。证据 `doc/evidence/v0.4.38/BUG-0050.log`。
- **BUG-0051 → CLOSE_OK**：三处（+ 追加第四处）事实错误全部就地标记并追加勘误；
  FB-23 边界经全量 395 行逐行核对确认**零覆写零删除**；两格重写成因经我**独立**
  回源 `modinfo.txt`/`modlist.txt`/vendor RTL **正向成立**；8 条脚注我独立重做 3 条
  全部成立。证据 `doc/evidence/v0.4.38/BUG-0051.log`。
- **附带条件（不阻断关闭，须 orch 处理）**：I1 / I2 登记为新行；I3 登记并留 FB 行；
  `make check` 现红于 `bugs.md 终态行 5 > 4`，remedy = **`make archive`**（orch 的
  记忆系统维护面，非 closer 编辑面）。

**M4 出口条件判定（下一张签核卡的直接输入，以我的复验为准）：**

- **第二条（全闭包三态扫描，逐格 N/A 附已核实成因）—— 现成立。**
  REV-035 §e 判其不成立的两处缺口 `(lzc, Line)` / `(stream_register, Cond)`
  已各补一条被原始 urg 报告**正向支持**的成因（V4 逐条我方回源）；132 格三态
  与 `modlist.txt` 逐格对账零失配（含冻结的 §2.3 表与活的 §6.3 底板两份）；
  22+13=35 模块划分复核一致。
- **第三条 (b)（rev 签核的 Kind-A 书面豁免）—— 现成立。**
  CW-002 的 rev 记录列由占位"待 rev 卡填"改为 `REV-024 §4` + `REV-035 §Q1(ii)`
  双引；全表 14 行 **零占位**、全为 Kind-A、所引 11 份 REV/signoff 文件**全部实存**
  （逐个 `test -f` 核过），签核核对可机械确认。
- **第三条整体（UNOWNED = ∅）—— 仍成立**（V1 独立复算 = 0），BUG-0049 的关闭
  不受本次影响。
- **边界**：以上只判 REV-035 点名阻塞的两条；M4 其余出口条件（KILL 覆盖、
  Kind-B 规则、cg 抽查）与全 rubric 属签核卡，本卡不判。

---

## 强制字段

### Taxonomy-class anomaly（`workflow/bugs.md` 五类自查）

**yes —— 三处新异常，均未立行。我不代立行、不代分配 BUG-ID，报 orch。**

| # | 现象（可复现命令） | 建议类 | 与既有行的关系 |
|---|---|---|---|
| I1 | §10.2(B)"共 31 个行号"与实测 57 对 / 53 个不同行号不符（复现：程序化提取 11 模块 Cond 节 `LINE` 头） | `TB_BUG` | 与 **BUG-0051 (i)** 同形（声明性数字 ⊥ 机器事实），载体是订正段本身 |
| I2 | 脚注 1 末句被证伪但未获就地标记 / 未进 bug 行枚举，与 E4 处置不对称 | `TB_BUG` | **BUG-0051** 同族；先例是该行的枚举追加 (iv) |
| I3 | `make check` 印 `run: make docs-archive`，而实有 target 为 `make archive`（`Makefile:85`；`docs.py:412/419/477/544` 四处） | `TOOL_ENV` | 与 BUG-0054/0058/0060/0061 同谱；上游文件 ⇒ 须登 `doc/fw-feedback.md` FB 行 |

已存在、本卡**再次确认现象成立**故不重复登记的：**BUG-0062**（REV-035 的 F1/F2
事实错误，V5 逐条亲验属实；其处置面依卡内约束**不由本卡裁**）。

### 七问（`workflow/review.md`）

1. **Origin**：本卡判据链 = `doc/spec.md` §0#4（pin）→ `doc/milestone.md`
   M4 出口第二/三条 → REV-035 的处置面裁决。期望值无一来自被测 RTL；vendor RTL
   只用于**结构事实**（tie-off、参数、宏展开），不作期望来源。
2. **Falsifiability**：两条签名我亲手证伪 8 次（V7 表），每次红点与注入缺陷一一
   对应且可区分；未证伪的盲区逐条列出并用人工/自写脚本补齐。
3. **Replayability**：两份 evidence 首行即 `CMD:`，命令自包含（只读仓内文件 +
   `sim/out/urgText6/`），任何人可在 clone 上复跑；本记录与它证明的 commit 同仓。
4. **Attribution**：BUG-0050/0051 无详情页，全记录在行内；taxonomy=TB、rca 在
   root_cause 列、fix=`69cf5a6`、rerun=本卡两份 evidence、guard 面见 BUG-0044/0047。
   **closer ≠ fixer 成立**：订正者是 arch fixer 实例，裁决者是 REV-035 实例，
   复验者是本实例，三者互不相同。
5. **Judgment**：证据支持 CLOSED——见 V1-V8；不支持的部分（I1/I2/I3）已单列，
   且逐条说明为何不构成关闭阻断。
6. **Retention**：教训落点 = 本记录 V7 的"签名盲区五条" + Guidance 3（UNOWNED
   机核待建）+ I1/I2/I3 的登记建议。**未只留在对话里。**
7. **Kill coverage**：本卡属非仿真类关闭，不引入新 checker 类；两条判据命令各自
   完成了"注伤→红→复原→绿"（K1-K8 + 复原后基线绿），等价于对这两条**新增机器
   判据**的击杀自证。M4 的 KILL 集完备性属签核卡的 rubric #5，本卡不判。

### 定级 vs 实际

声明 **L2 / opus**。实际相符：工作量集中在"从原始 urg 与 RTL 独立重建 132 格三态、
登记/引用两面集合差、两条成因的正向回源、8 次签名证伪"——任何一处采信上游结论
都会使本卡失去价值（BUG-0062 已经证明上游裁决会错）。不降级的实质理由：本卡的
输出是**下一张 M4 签核卡的直接输入**（M4 出口第二/三条的成立与否），判错的代价是
一次错误签核。
