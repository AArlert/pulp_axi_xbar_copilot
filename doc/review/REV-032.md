# REV-032 — 里程碑重构提案门禁（卡C，arch delivery gate，L3）

审查对象：`doc/design-prompt/milestone_restructure.md`（M4 出口重定义 + 新增 M6；
谱系 BUG-0047 终判选项 (ii)）。方向已由用户裁定，不在审查范围；本记录审提案的
**正确性 / 完备性 / 可机械应用性 / 合规性**。

- 复核实例：全新 rev；未与提案作者 arch 共用；未与 REV-024/030/031、卡A（final-sweep
  测量卡）作者共用。
- 判据来源：`doc/spec.md` §0#4（六类三态/例化闭包/≥90% 门/"判定单位=（模块,类型）
  二元组"/"不得以父模块 N/A 代表子模块达标"）、`CLAUDE.md` 五条不变量 + §3 门禁、
  `workflow/review.md`（七问 + rubric）、`workflow/discipline.md` rule 2/3/4、
  BUG-0038/0047 guard、`doc/evidence/v0.4.35/M4-coverage-final-sweep.md`（权威扫描）、
  REV-030 §3、REV-031 §5/§7、`scripts/docs.py`（机核条件实现）。

---

## Scope list

审查材料全部亲取：提案全文 9 段；OLD 侧四文件（spec §0#4 / milestone.md / coverage-
waivers.md / verification_maturity.md）；backlog 数据源三份（final-sweep §2.3/§3/§5/§8、
REV-030 §3、REV-031 §5/§7）；谱系（BUG-0047.md + archive 行 + bugs.md 三债务行）；
`scripts/docs.py` 机核实现。八轴逐条给 verdict。

---

## 逐轴 verdict

### 轴1 — old 文本保真度 · PASS

四处 OLD 逐字核对 + 唯一可定位（`grep` 亲验）：

- **spec §0#4 句**（`doc/spec.md` L25）："有 bin 时须 ≥90%，否则按 `doc/milestone.md`
  M4 出口条件逐条修补或出具 rev 签核的书面豁免。" —— 全文件唯一出现（grep 命中仅
  L25），提案 §2 OLD 逐字一致。NEW 只替换此单句，周边六类口径/例化闭包/三态 N/A
  规则/"空白不得记 0%/100%/省略"全部留在句外、一字不动。**外科手术式，pass。**
- **coverage-waivers 抬头 Kind-B**（L7–L8）：提案 §4 OLD 与现行逐字一致，唯一定位。
- **verification_maturity §0 范围**（L36–L37）：提案 §5 修订 A OLD 逐字一致。
- **verification_maturity §5 标题**（L293）：逐字一致，唯一。
- **milestone.md**：§3.1（L1–L18）/§3.2（M4，L56–L66）/§3.3（M5，L68–L101）行范围与
  现行文件逐字对应；§3.1 只改 Abstract 三行（M4/M5 内容+场景列、新增 M6），L1–L8
  与现状相符。**pass。**
  - **应用提示（非 gap）**：§3.1 宜按"Abstract 三行定点编辑 + M4/M5/M6 整节替换"应用，
    而非整块重打 L1–L8，以免 L5 KILL 批注行尾空格等空白漂移。

### 轴2 — 锚点完整性 · PASS

三条 `ACCEPTED@M5` 债务锚点零移动：`doc/bugs.md` L8（BUG-0046）/L9（BUG-0045）/L10
（BUG-0044）现存 `ACCEPTED@M5`（grep 计数 3 行）。提案改动面 = spec §0#4 一句 +
milestone.md + coverage-waivers 抬头 + verification_maturity §0/§5——**均不含 bugs.md
三行的 `@M5` 字符串**，也不含 §3.2/§6/C2.5（三债务守卫 spec 面）。锚定的是里程碑号
M5 而非版本串，版本 `v1.1.*→v0.5.*` 变更不移锚。提案 §7.1 清单逐行复核成立。**pass。**

### 轴3 — guard 合规 · PASS（就"改动面不触守卫面"而言）

- **BUG-0047 guard（`doc/coverage-waivers.md`）**：提案 §4 只改抬头 Kind-B 解锁口径
  （M5→"M5 落地 + M6 重测"），**保留"须附逐位 toggle 分解"**、不新增任何 Kind-B 行
  （集为空集，REV-025/REV-031）、不把 REV-024"需补场景"条目改记 Kind-B。守卫面未
  松动。**pass。**
- **BUG-0038 guard（`doc/spec.md` §0#4）**：§0#4 编辑保留例化闭包定义（`addr_decode_dync`/
  `axi_demux_simple` 仍在 ≥90% scope）与 N/A 三态成因规则，只迁百分比门至 M6（同 scope）。
  就编辑本身合规。**但见轴4**：guard 要的"dashboard 可区分 M4-done vs unowned"由提案
  以"UNOWNED=∅"作答，而该前提在事实上不成立（stream_register），故 guard 的实质目的
  未被满足——归因记轴4，非本轴对编辑面的判定。
- **背景守卫（提案自证不触碰，rev 核实真不触碰）**：BUG-0039（§4 clause 7 decode-miss
  `atop≡'0`）、BUG-0044（§6 + C2.5 atop 有界子集）、BUG-0045/0046（§3.2 `end_addr=='0`
  哨兵 / `<=` 不悄改 `<`）——提案编辑面（§0#4/milestone/waivers 抬头/vm §0+§5）**不含**
  §3.2/§4/§6/C2.5 任一，四条背景守卫面一字未动。**pass。**

### 轴4 — backlog 完备性 · REJECT（本卡最重的一轴，致命）

以 final-sweep §2.3 全表为准，对 22 模块 × 6 类逐格核对每个 <90% 格是否在提案 §6.1
（待派/重测）或 §6.2（排除说明）二者有且仅有其一。**21 个模块全部有归属**（DV-A~G /
重测-1/2 / 薄壳 note / CW-001~013 / BUG-0044，逐格对上，见附核对）。**唯一例外——
`stream_register` 三格无任何归属，归属丢失**：

- final-sweep §2.3 L132：`stream_register`（`axi_atop_filter` 内 `r_resp_cmd`，6 例）
  **Line 75.00 / Toggle 22.00 / Branch 50.00**，三格均为真实 <90% 数字（非 N/A）。
- 提案 §6.1、§6.2 **全文均无 `stream_register`**（grep 亲验：0 命中）；`doc/coverage-
  waivers.md` 无 stream_register 条目（0 命中）；`doc/bugs.md` 无。三格**既不在 6.1
  也不在 6.2**。
- 提案 §6.2 CW-001 行只承接"`axi_atop_filter` 六类"。按 spec §0#4"判定单位=（模块,
  类型）二元组"且"**不得以父模块…代表子模块达标**（BUG-0038 裁决）"，stream_register
  是闭包内独立模块页（final-sweep §2.1 列为 22 成员之一），**不得**折入父模块
  axi_atop_filter 的 CW-001。
- 该丢失早于提案：final-sweep §8（L369–376）只枚举"9 个 (模块,类型) 组合、跨 8 个
  模块"的 UNOWNED，stream_register 不在其中；log [0.4.36] 与 REV-031 亦据此宣告
  "UNOWNED = 空集"。提案 §6 忠实继承了这一漏账。

**后果**：提案的核心交付物——新 M4 出口条件与 Abstract 明写"每格具名归属，UNOWNED =
空集"、并断言"现状：0.4.36 闭环已达成 UNOWNED = 空集"（§3.2）——被其自身权威数据源
§2.3 直接证伪。这同时使 §2/§7.2 关于"UNOWNED=∅ 正是 BUG-0038 guard 要的 dashboard
可区分"的合规论证不成立（存在一个真正 unowned 的格）。按本卡轴4 判据（"任何格既不在
6.1 也不在 6.2 = 归属丢失，直接 REJECT 轴"），**本轴 REJECT。**

DV-A~G 内容对 REV-030 §3 / REV-031 §7 的忠实度：逐条比对，估级（DV-A err_slv L1/mux
L2、DV-B L1/L2、DV-C L2、DV-D L1、DV-E L2、DV-F L2、DV-G L2）、宿主、判决门约束
（红线/延迟不敏感/env clause 7）均与源记录一致，未歪曲；random-first 列是 M6 规划新
增维度、非对源记录的改写。**DV 忠实度 pass；本轴 REJECT 单因 stream_register 漏账。**

### 轴5 — 不变量与门禁顺序 · PASS

- **双门保留**：新 M4/M5/M6 出口均写 `make check MILESTONE=<n>` + `signoff-M<n>.md`
  二者缺一不可（§3.2 末条 / §3.3 末条 / §3.4 末条 + §9.2）。**pass。**
- **KILL 覆盖三里程碑连续**：M4（§3.2 条4）+ M5（§3.3 三类 checker）+ M6（§3.4 定向卡
  新路径）均保留。
- **机核条件4 可满足性（M4 标签 KILL 行）**：亲查——`doc/bugs.md` **零 KILL 行**，全部
  KILL 在 `doc/archive/bugs-archive.md`。`scripts/docs.py:928–951 check_kill_coverage`
  读 **bugs.md + archive** 并按 `\bM<n>\b` 裸 token 匹配 summary。archive KILL-0004
  （summary"M4 重叠 rule tie-break…"）与 KILL-0005（"…M4 覆盖收敛加固卡…"）均含裸
  `M4` token → **条件4 PASS 可满足**。提案 §8.1 条4 表述正确、可机核。**pass（附事实：
  M4 KILL 行确实存在，位于 archive；签核时引 KILL-0004/0005 即可）。**

### 轴6 — 机核/人核划分诚实性 · PASS

§8.2"UNOWNED=∅ 当前为人核、机核可选待建"声明成立：`scripts/` 内确无解析四表算集合差
的校验（`docs.py` cmd_check/signoff 只做 4 条机核条件，无 UNOWNED 交叉校验）。提案
**不**把 UNOWNED=∅ 挂进 `make check`，明确列为 rev 签核人核项（§8.3 rubric #5），并把
tooling 卡标注为"建议但不强制、未建成前维持人核、绝不挂空转伪门"。符合 BUG-0038 guard
"不得把无法机核的条件伪装成机器门"。**pass。**（注：诚实划分本身合格；但被划为人核的
UNOWNED=∅ 其事实值 ≠ ∅，问题在轴4 的完备性，非本轴的划分诚实性。）

### 轴7 — 版本方案自洽 · ISSUE（verification_maturity 残留 v1.1，提案遗漏该处修订）

- **milestone.md 侧自洽**：M5 签核路径改 `v0.5.*`、删除"签核后转 v1.0.0"；M6 挂
  `v0.6.*` + "签核后转 v1.0.0"；Abstract/抬头一致（§3.1/§3.3/§3.4/§7.3）。**这部分 pass。**
- **遗漏**：`doc/design-prompt/verification_maturity.md` 仍存 **三处 `v1.1.*` 引用**——
  L70"M4 签核转 v1.0.0；M5 建议走 v1.1.0"、L71"版本号占位 `v1.1.*`"、L242 证据路径
  例 `doc/evidence/v1.1.*/regress-multiseed.log`。提案 §5 对 verification_maturity 只列
  两处修订（§0 范围行 + §5 标题批注），**未触这三处**；而提案 §7.3 明写版本方案已被
  修正（M4 不转 v1.0.0、M5=v0.5.*、v1.0.0 挂 M6）。L70 与 §7.3 **直接矛盾**。
  verification_maturity 是提案正在编辑的活体 design-prompt（非冻结记录），"历史不回改"
  不适用——应用后本文件将自带版本自相矛盾。**本轴 ISSUE（提案 §5 修订清单在版本维度
  不完备）。**

### 轴8 — 副作用扫描 · 见轴7 残留清单

全仓扫"M4 六类 ≥90%"旧口径与"M5 cov_loop"旧归属：
- `doc/milestone.md`：由提案 §3 整节替换覆盖，无残留。
- `doc/design-prompt/verification_maturity.md`：Decision 5 归属由 §5 修订 B 批注一次性
  重定向（C5.x 功能规格不逐行改，合理）；**但版本串 v1.1.*（L70/L71/L242）残留**——见
  轴7，须随应用一并修订或显式批注 superseded，**不可保留为矛盾态**。
- `workflow/`、`scripts/` 注释：未见"M4 六类≥90%"硬编口径引用（提案不触上游文件，合规）。
- 判定：轴8 唯一须处置的残留 = verification_maturity 三处 v1.1（其余无残留）。

---

## Guidance（原则 + 方向，不写实现）

1. **完备性是这份提案的产品本身，不能有静默排除。** 新 M4 出口的全部价值在"UNOWNED=∅
   可被人核证伪"。只要有一个 <90% 模块页无归属，出口门就退回 BUG-0038 描述的失败态
   （"M4-done 与 M4-done-except-unowned 无法区分"）。stream_register 必须先获得归属，
   提案方可承载"UNOWNED=∅"。原则：闭包内**每一个** urg 独立模块页（含 common_cells
   子模块）都是 spec §0#4 的判定单位，父模块的豁免不覆盖子模块。
2. **stream_register 的方向（供 arch 定，rev 不代判 Kind）**：其低覆盖根因与 CW-001 的
   INJECT 路径同源——`r_resp_cmd`（= stream_register 实例）仅在真正的 ATOP R 响应注入
   （非-load ATOP）时才充分翻转，而 env 从无非-load ATOP（REV-031 scope L41、CW-001）。
   因此它是**强 Kind-A 候选**（可给可证伪不可达论证），但它是独立模块页，须走自己的
   CW 条目 + rev 复核，**或**若判为定向可达则路由 DV——二选一由 arch 分诊，不得默认折入
   CW-001。
3. **完备性验证方法本身要留痕。** 本次 stream_register 被两份独立制品（0.4.36 closeout +
   REV-031）同时漏掉，说明"22 模块×6 类逐格对账"这一步在当时没有可复查的完整清单。
   建议 backlog §6 附一份"22×6 全格 → 归属"对照（即把 final-sweep §2.3 全表的每个非-N/A
   格显式连到 6.1/6.2 某一处），使 UNOWNED=∅ 可被机械复核。这也直接支撑 §8.2 未来的
   tooling 卡。
4. **版本维度要在同一次改动内自洽。** 既然提案在编辑 verification_maturity，就应把该文件
   里被 §7.3 取代的 v1.1.* 口径（L70/L71/L242）一并对齐或显式标注 superseded；留矛盾态
   违反 discipline rule 3"清理自己这次改动带出的不一致"。
5. **保持已通过项不倒退。** 轴1/2/3/5/6 与 milestone.md 版本方案已合格，重提时无须重做；
   本次返工范围应严格限于 stream_register 归属（轴4）+ verification_maturity v1.1 残留
   （轴7/8），避免扩大改动面（discipline rule 3）。

---

## Overall verdict — REJECTED

致命轴：**轴4 backlog 完备性**——`stream_register`（Line 75.00 / Toggle 22.00 /
Branch 50.00，final-sweep §2.3 L132）三格既不在提案 §6.1 也不在 §6.2，亦无 CW / bugs
条目，构成 spec §0#4 判定单位下的**归属丢失**，直接证伪提案核心交付物"M4 出口 UNOWNED
= 空集"（§3.2/Abstract）及其 BUG-0038 guard 合规论证（§2/§7.2）。该提案将被 orch 逐字
应用并重 pin，不得把一个事实为假的"UNOWNED=∅"写进里程碑出口门。

并列须修（重提时一并闭合，范围严格限于以下两项）：
- **G-1（轴4，致命）**：为 `stream_register` Line/Toggle/Branch 三格补具名归属——强
  Kind-A 候选（同 CW-001 的 `r_resp_cmd`/INJECT 根因），须独立 CW 条目 + rev 复核，**或**
  路由 DV；由 arch 分诊，不得折入 CW-001。补后 backlog §6 须给出 22×6 全格→归属的可复核
  对照，使 UNOWNED=∅ 成立。
- **G-2（轴7/8）**：提案 §5 补一处 verification_maturity 修订，将 L70/L71/L242 的
  `v1.1.*`/"M4 转 v1.0.0"口径对齐 §7.3（v0.5.*/v0.6.*、v1.0.0 挂 M6），或显式批注
  superseded；不可保留与 §7.3 矛盾的版本串。

其余轴（1/2/3/5/6，及 milestone.md 版本方案）已合格，返工不涉。orch 应将本提案退回
arch 修订 G-1/G-2 后重走卡C 门禁；**不得**由 orch 内联补 stream_register 归属（Kind
判定属技术制品，越 orch 边界）。

---

## 强制字段

### Taxonomy-class anomaly（五类自查）

**yes——surface 一处 `failure_taxonomy` 类，当前非 `doc/bugs.md` 行：** `stream_register`
（Line/Toggle/Branch <90%）是一个无豁免/无 bug/无 backlog 归属的覆盖率缺口，命中
`workflow/bugs.md` 的 "coverage hole / silent-exclude 失效" 类（coverage-waivers.md 抬头
引 `workflow/bugs.md` L229"silent exclude 失效"）。此前 final-sweep §8 与 REV-031 均漏
登记。**登记是无条件的**：建议 orch 就此 (模块,类型) 缺口新建一行——可归 BUG-0047
planning-gap 伞下（同其余 DV-routed 缺口），但**必须**有显式归属（CW 或 DV 路由），不得
沿用当前的静默状态。rev 边界只 surface + flag，不代建行、不代判 Kind。BUG-ID 由 orch
分配。

TOOL_ENV / TB_BUG / CONSTRAINT_BUG / DUT_BUG / SPEC_ISSUE：本次门禁未 surface 其它新
实例（提案不触 RTL/TB/仿真；§0#4 里程碑绑定改动属既有 BUG-0047 SPEC_ISSUE 的选项 (ii)
落地，非新 SPEC_ISSUE）。

### 定级 vs 实际

定级 **L3**（spec/豁免/签核面，rev 必到，全 rubric）。实际：本卡审 pinned-spec §0#4
变更 + 里程碑治理三节 + coverage-waivers 抬头 + arch 面 verification_maturity——纯治理/
判据口径面，L3 相符。**无失配。**

---

# 复审段（卡C'，2026-08-02）— REJECTED 条件返工核验

原文（上）不改。本段为 arch 返工链（BUG-0049 登记 + REV-033 归属裁决 + CW-014 登记 +
提案原地更新 G-1/G-2）走完后的独立复审。复核实例同 REV-032（连续门禁），亲读返工材料
不采信提案转述：`doc/review/REV-033.md`、`doc/coverage-waivers.md` CW-014 行、`doc/bugs.md`
BUG-0049 行、修订后 `doc/design-prompt/milestone_restructure.md` 全文、
`doc/design-prompt/verification_maturity.md`（G-2 OLD 面）。

## 复审范围逐项核验

### 1. G-1 落实（stream_register 归属）— 闭合

- **22×6 全格集合差重做**（以 final-sweep §2.3 为准）：亲读提案新增 §6.3 全格→归属对照，
  逐格与 §2.3 数字/N/A 成因比对——**132 格逐格一致**（数字、N/A 上标 1–8 成因、PASS 判定
  全部对上）。每个非-PASS/非-N/A 格均落 CW / BUG-0044 / §6.1 backlog 之一，**无空白格、
  无未归属 <90% 格**。此前 REV-032 轴4 唯一漏账 `stream_register`（Line 75.00/Toggle 22.00/
  Branch 50.00）现三格均具名：Line→CW-014、Branch→CW-014、Toggle→CW-014（31-bit Kind-A）
  + DV-A(D1)（8-bit 定向）。**UNOWNED=∅ 现为可复核事实。**
- **CW-014 忠实 REV-033**（亲读两处，非采信提案）：REV-033 逐格裁决表（Line 全 Kind-A/
  Branch 全 Kind-A/Toggle 拆 31 Kind-A + 8 D1）与 `doc/coverage-waivers.md` CW-014 行逐条
  对齐——P1（clr_i/testmode_i tie-off，`axi_atop_filter.sv:353/354`，同 CW-010/013 根因）
  + P2（push-gate=`atop[ATOP_R_RESP]` 抵 err_slv 仅经译码未命中路径，被 §4 clause 7 所禁，
  同 CW-001 INJECT_R 根因）+ P3（rst_ni 1→0，同 CW-006）；bit 计数自洽（39=4+1+26+8，
  无残余未归因位）；三前提各挂可证伪解锁。**D1（`data_i.len[7:4]` 8 bit）判为定向可达
  合理**——`data_i` 为 `slv_req_i.aw.len` 组合馈通（`:362`）、与 push 门无关、送一笔
  len≥16 译码未命中写即翻、env clause 7 只禁 atop 不禁 len，**非 Kind-A、非 Kind-B**
  （单笔长写即闭，不满足"纯定向不经济"），路由 §6.1 DV-A(D1) L1。裁决不折入父模块 CW-001
  （spec §0#4 判定单位=（模块,类型）+ BUG-0038），独立成条。**pass。**
- **REV-033 独立性 + 对 REV-032 的订正**：REV-033 声明未与 REV-031/032/arch 共用，从
  `stream_register.sv` 全文 + `axi_atop_filter.sv` 驱动逻辑 + urg 原文独立重走；并订正
  REV-032 §Guidance 2 的 "(非-load ATOP)" 表述为读返回原子 `atop[5]`——该精度差**不改**
  Kind-A 结论（任何 `atop≠'0` 抵 err_slv 均被 clause 7 挡），BUG-0044 非-load debt 对本
  模块无关。订正诚实、不影响完备性判定。**pass。**

### 2. G-2 落实（verification_maturity 版本残留）— 闭合

- **四处 OLD 保真 + 唯一定位**（逐处 `grep -cF`）：修订 C（L43–44 建议句）/ D（L70–71
  版本方案段）/ E（L242 证据路径例）OLD 各 **唯一出现（=1）**，与现行 `verification_maturity.md`
  逐字一致。**全库 v1.1/转 v1.0.0 残留恰为这 4 行（L44/L70/L71/L242），无遗漏**（`grep -nE`
  验证：正是修订 C/D/E 覆盖面）。
- **L44 补入合理**：orch 复核比 REV-032 清单（L70/L71/L242）多出的 L44（"M5 排在 M4
  （六类 ≥90%）签核转 v1.0.0 之后"+ 旧标题含"覆盖率闭环"）确属过时口径——M4 无 ≥90% 门/
  不转 v1.0.0、cov_loop 已移交 M6。修订 C 一并修，**比 REV-032 清单更完备**，采纳。
- **NEW 与 §7.3 零矛盾**：修订 C/D/E NEW 全部对齐 §7.3（M5=v0.5.*、M6=v0.6.*、v1.0.0 挂
  M6、M4 不转 v1.0.0、cov_loop 归 M6）。应用后 `verification_maturity.md` 无版本自相矛盾态。
  **pass。**

### 3. 回归扫描（返工是否扰动 REV-032 判合格的七轴）— 零扰动

- **spec §0#4 old/new（§2）未变**：OLD 子串仍全文件唯一（`grep -cF`=1），NEW 一字未动——
  **重 pin 基础稳定**。
- **锚点清单（§7.1）未变**：BUG-0044/45/46 `@M5` 三行不触，`ACCEPTED@M5` 锚零移动。
- **guard 合规声明（§7.2）未变**：§3.2/§6/§4-clause-7/C2.5 仍不触碰；Kind-B 登记规则
  （"须附逐位 toggle 分解"）未松动，零 Kind-B。
- 其余 delta 均为**加法/如实化**、不倒退：§6.1 DV-A 增 D1 分量；§6.2 增 CW-014 行；
  新增 §6.3 全格底板；§3.2 UNOWNED 现状由"已达成"如实化为"曾宣告→REV-032 证伪→
  REV-033 裁决后达成"（诚实修正，正是 REV-032 要的）；CW 计数 CW-013→CW-014 全库一致
  更新（§3.2/§3.4/§4/§6.2 + coverage-waivers 注行）；§8.2/§8.3 引 §6.3 为未来 tooling
  底板 + rubric #5 底板（采纳 REV-032 Guidance 3）；§9.4 补 BUG-0049 谱系。**七轴零扰动。**

### 4. 新引用一致性（BUG-0049/REV-032/REV-033/CW-014）— 一致

- **BUG-0049**（`doc/bugs.md`，OPEN|TB）：summary 忠实 REV-032 轴4 发现（spec §0#4 判定
  单位 + BUG-0038 + final-sweep §3/§8 漏账 + REV-031 承漏 + log [0.4.36] 失实）。
- **CW-014**（`doc/coverage-waivers.md`）：rev 记录列引 REV-033（2 处），行内容与 REV-033
  裁决表逐条对齐；抬头"注"计数已更新 CW-001~CW-014。
- 提案对 [R33]/CW-014/BUG-0049/REV-032 的引用（§3.2/§5 前言/§6.1/§6.2/§6.3/§8.3/§9.4）与
  台账/记录原文一致。**pass。**

## 残余风险（非本提案门禁项，M4 签核须处置）

- **BUG-0049 现 OPEN**：`make check MILESTONE=4` 机核条件3（bugs 全终态或未到期 ACCEPTED）
  要求其达 CLOSED/ACCEPTED 后方过。REV-033 明确其 CLOSED 由 closer（≠fixer/≠rev）另派，
  须待 CW-014 已 pin + D1 backlog 已录（若 M6 收 D1 则其 evidence 齐备）后。本提案不声称
  BUG-0049 已闭，非本门禁缺陷；M4 签核卡须核。
- **应用提示（承 REV-032）**：§3.1 宜按 Abstract 三行定点编辑 + 整节替换应用，免 L1–L8
  空白漂移；§5 修订 A–E 逐处 OLD 唯一可定位，可机械应用；应用 §2 并 `docs.py --pin-spec`
  重 pin 后，按 §9.4 对齐 BUG-0047 archive 行终态与本次 §0#4 变更/重 pin 证据。

## 复审 Verdict — APPROVED

G-1（stream_register 三格具名归属，CW-014 忠实 REV-033 + D1 路由 DV-A）与 G-2
（verification_maturity 四处版本残留经修订 C/D/E 对齐 §7.3）**均已闭合**；返工为 delta-only，
**未扰动** REV-032 判合格的七轴；新引用与台账/记录一致。REV-032 唯一致命轴（轴4 backlog
完备性）现闭合，`UNOWNED=∅` 由 §6.3 全格底板成为**可复核事实**。提案可由 orch 逐字机械
应用并重 pin（残余风险为 M4 签核项，非应用阻塞）。

## 复审强制字段

### Taxonomy-class anomaly（五类自查）

**yes（已有台账行，非新增未登记类）**：REV-032 surface 的 stream_register 覆盖率漏账
（silent-exclude 失效类）已登记 `doc/bugs.md` BUG-0049（属 BUG-0047 planning-gap 同伞），
本轮返工据 REV-033/CW-014 补齐归属，漏账闭合。REV-033 新析出的 D1（`data_i.len[7:4]`
定向可达）为 BUG-0049 缺口子项，已入 §6.1 DV-A backlog（BUG-0047 伞下），**不另开新
bugs.md 行**（同缺口，非新缺陷类）。本次复审未 surface 其它新 `failure_taxonomy` 类
（TOOL_ENV/TB_BUG/CONSTRAINT_BUG/DUT_BUG/SPEC_ISSUE 均无新实例）。

### 定级 vs 实际

定级 **L3**（spec/豁免/签核面）。实际：复审 pinned-spec §0#4 稳定性 + Kind-A 归属裁决
（CW-014/REV-033）+ 里程碑治理 + arch 面 verification_maturity 版本对齐——纯治理/判据
口径面，L3 相符。**无失配。**
