# REV-008 — 设计输入增量门（M2 design-prompt 四文件同步 REV-007/BUG-0016 spec 重 pin）

> 卡类型：rev gate（task type 1 的增量形态）。Fresh instance，与产出本次改动
> 的 arch 实例、与产出 REV-005/006/007 的各 rev 实例均不共享。判据全部建立在
> 本卡独立读取 `doc/spec.md`（重 pin 后现文，sha `bfe8542b`……）原文与
> `git diff 0e62eb8 -- doc/design-prompt/` 实际内容之上；未采信 orch 转述、
> 未采信 bug 详情页/前序 REV 记录对本次改动的任何"建议裁决方向"。

## Scope 列表

**改动本体**（`git diff 0e62eb8 -- doc/design-prompt/`，工作树 vs HEAD，共
254 行 diff，仅涉及以下四个文件，无其他 design-prompt 文件被触碰）：
- `doc/design-prompt/sva_bind.md`（C3.4 段整段改写 + M2 判据表更新）
- `doc/design-prompt/scoreboard_refmodel.md`（C5.3 段整段改写 + §5/§7 判据激活
  集措辞同步）
- `doc/design-prompt/uvm_env.md`（C5.3 压测目标段 + M2 判据表更新）
- `doc/design-prompt/functional_coverage.md`（`cg_tx_limit` bin 定义 + §3
  assert 类覆盖配对约定更新）

**独立核验的许可来源**：
- `doc/spec.md` §0（item 2）、§2.1（`MaxMstTrans`/`MaxSlvTrans` 字段行）、
  §5.4（全，1/2/3 条）、§7.4（全，尤第 5 条）、文末 Change record #3/#4/#5
- `doc/spec.sha256`（`bfe8542bd6d86e03c7d32ad5d29d7f7ee7e13ed5f56fe8aac6bb05c390b5996f`，
  与 `sha256sum doc/spec.md` 实测一致）
- `doc/review/REV-005.md` §4（C1/C2/C3 条件清单）、`doc/review/REV-006.md`、
  `doc/review/REV-007.md` §5/§6/§8
- `doc/testplan.md` 行 M2-TL01/M2-TL02、`doc/feature-matrix.md` 行 F-M2-07
- `doc/bugs.md`（当前活跃行：BUG-0014/0015/0016）+ `doc/archive/bugs-archive.md`
  （BUG-0010/0011/0012/0013，均已归档为 SPEC_CHANGED）+ 四份详情页
  `doc/bugs/BUG-0010~0013.md`、`doc/bugs/BUG-0016.md`
- `tb/sva/axi_xbar_txlimit_sva.sv`（全文）、
  `doc/evidence/v0.2.0/M2-TL01.log` / `M2-TL02.log`（尾部核对，非重放）
- `.claude/agents/arch.md` Behavior-leak exclusion zone、
  `doc/design-prompt/README.md`
- `doc/design-prompt/` 全目录 grep 扫描（`MaxMstTrans|MaxSlvTrans|上游确认项|
  BUG-0010|0011|0013|0016|REV-005|006|007|弱化.*checker|绝不假红`），核对无
  其他文件残留失效条款

## 1. 独立核验记录（rev 亲读，逐条锚定）

| 核验目标 | 核验方法与结论 |
| --- | --- |
| spec.md 现文是否已把 BUG-0016/REV-007 裁决落地齐全 | 亲读 §2.1 `MaxMstTrans`/`MaxSlvTrans` 字段行、§5.4.1/5.4.2/5.4.3、§7.4.5：均含"有效上限 `2^⌈log₂MaxMstTrans⌉−1`（基线 15）"、"撤销每 ID ≤ MaxSlvTrans"、"mux 侧机制不存在"（已定结论，非上游确认项）三处实质，与 Change record #5 摘要逐字对应。**结论：spec.md 已完整落地，非部分应用** |
| 四份 design-prompt 改动是否与上述 spec 现文口径一致 | 逐段比对：sva_bind.md C3.4「不构成 assert property 判据」「REV-005 曾解锁…已被 spec §5.4.2 正式收回」；scoreboard_refmodel.md C5.3「判决门降级」「不升格 assert——这不是临时状态而是已定结论」；uvm_env.md「压到并越过 MaxSlvTrans」「不再为其构造激励」；functional_coverage.md `cg_tx_limit` bin 含"有效上限"档。**结论：四文件口径与 spec 现文逐条对应，无残留矛盾** |
| `doc/design-prompt/` 全目录是否还有本次未改但因重 pin 失效的条款 | grep 全目录扫描 `MaxMstTrans/MaxSlvTrans/上游确认项/BUG-0010~0016/REV-005~007/弱化.*checker/绝不假红`：命中仅落在本次四改动文件内；`tb_top.md` 唯一命中处（§0 基线值 `MaxMstTrans=10`/`MaxSlvTrans=6`）是字面配置数值引用（`Cfg` 字段值本身未变，仅其"上限"语义变了），非失效条款；`README.md`、地址译码/ATOP/W 次序相关段落无命中。**结论：闭合，无遗漏** |
| 四文件改动与已落地实现（`tb/sva/axi_xbar_txlimit_sva.sv`）、testplan M2-TL01/TL02 判决门口径 | SVA 文件当前实现：C3.4/C5.3 描述的"assert 降级为 cover/uvm_info、判决门锚 scoreboard 正确性"与 `axi_xbar_txlimit_sva.sv` L183-270（无 assert、仅 `uvm_info`+`cover property`）**完全对应**；testplan M2-TL01/TL02 行判决门描述（"落地判决门 = scoreboard…＋达标 cover"）与设计输入口径一致。**唯一发现**：testplan 两行与 SVA 文件注释头（L44、L222-248）仍逐字使用"BUG-0016（OPEN，非阻塞/待 rev 签核）""pending rev arbitration"措辞——REV-007 已终判 SPEC_CHANGED（`doc/bugs.md` 行 8 现文状态列即为 `SPEC_CHANGED`），这两处的**状态标签**滞后于已完成的裁决，但其**判决逻辑本身**（cover-only、不升格 assert、判决门锚 scoreboard）与裁决后设计输入完全一致，不构成实质冲突——见 §2.4 |
| REV-005 §4 条件 C1（直引 vendor 文档需再锚定到 spec 章节）当前状态 | 本次 diff 与 diff 前状态（`git diff 0e62eb8` 的"-"侧）均已是"依据 spec §5.4.1（分桶口径的规范来源；spec 该条自身出处标注 axi_demux.md L70-74）"——即design-prompt 自身的锚点是 spec 章节，vendor 文档仅作为"spec 该条自身出处"的旁注，非 design-prompt 直接采信 vendor 文档为权威。核对 `functional_coverage.md`/`sva_bind.md`/`scoreboard_refmodel.md` 三处全部如此。**结论：C1 已满足**（在本卡 diff 之前即已满足，本次改动未回退） |
| REV-005 §4 条件 C2（M2-TL02 只准落地弱化可观测上界 checker，机制级断言不派发）当前状态 | REV-007 §5(2)/§8 已"正式收回"该弱化 checker 本身（其前提——mux 侧存在 per-ID 在飞计数机制——被证伪），spec §5.4.2 现文明载"此条同时正式收回 REV-005 为 M2-TL02 解锁的…可观测上界监视器"。C2 原本限定的对象（那条弱 checker）已不存在，其限定范围随之作废，替换为 REV-007 §6 的新判据（(a) MaxMstTrans 侧可选公式化 assert、(b) MaxSlvTrans 侧永不可断言、cover-only）。本次四文件改动均已按 REV-007 §6 的新判据落笔（非按已废止的 C2 落笔）。**结论：C2 已被 REV-007 取代**，非"仍然有效"、非"待处理" |
| REV-005 §4 条件 C3（F-M2-07 标题"C3.1-C3.5"与正文四项不一致，minor）当前状态 | `doc/feature-matrix.md` F-M2-07 行现文：标题与正文并列"配置稳定性(C3.1) / 保序 stall(C3.2) / W 次序(C3.3，cover-only) / 事务上限(C3.4) / ATOP 成对+ID 唯一(C3.5)"五项俱全，scene 列含 `M2-WO01`。**结论：C3 已满足**（在本卡 diff 之前已被订正，与本次改动无关，属既有事实核验） |

## 2. 评审范围逐项 verdict

### 2.1 behavior-leak 门禁（增量）—— pass

- 四处改动的方向统一是**降级/收窄可证伪范围**（assert → cover/uvm_info、判决
  门改锚已有的 scoreboard 正确性判据），无一处新增外部可见判决语义。
- `cg_tx_limit` 新增的"有效上限"bin 明文声明"不隐含 `≤ MaxSlvTrans` 为上界"
  "仅作非判决观察"——延续 REV-005 §2.1 对该 covergroup 的原有 non-decisional
  定性，未升级为判决依据。
- 引入的"有效上限 `2^⌈log₂MaxMstTrans⌉−1`"公式，四处改动均**引用 spec §5.4.1
  的公式**（该公式已由 orch 落入 spec 正文，来源链 rev 裁决→spec 编辑→
  design-prompt 引用），而非直接从 RTL/vendor 文档现读现抄——符合
  `discipline.md` rule 1 与 CLAUDE.md §4 的"单一事实源"要求。REV-007 §6 checker
  指导明文"该有效上限值由 spec 公式从 pinned Cfg 推导，DV 不得从 RTL 直接取
  15"，四文件改动无一处违反。
- 无泄漏。

### 2.2 spec 一致性 —— pass

见 §1 表第 1/2 行独立核验：spec.md 现文（§2.1/§5.4.1/5.4.2/5.4.3/§7.4.5）与
四份改动文档逐条对应，未发现方向性矛盾、未发现改动文档引入 spec 未载的
新读法。

### 2.3 范围闭合性 —— pass（独立扫描完成，非以"改了四个文件"自证）

见 §1 表第 3 行：对 `doc/design-prompt/` 全目录做了独立 grep 扫描（覆盖
`MaxMstTrans`/`MaxSlvTrans`/`上游确认项`/四条 BUG 号/三份 REV 号/"弱化…
checker"/"绝不假红"等全部相关关键词），命中范围与本次 diff 触碰的四个文件
完全重合，`tb_top.md`/`README.md` 及其余未改动内容无残留失效条款。

### 2.4 与已落地实现的自洽性 —— pass（附一条非阻塞文档新鲜度缺口）

- 判决逻辑层面**完全自洽**：四份设计输入描述的"C3.4/C5.3 从 assert 降级为
  cover/uvm_info、判决门改锚 scoreboard 正确性"与 `tb/sva/axi_xbar_txlimit_sva.sv`
  的实际实现（L183-270：全部为 `uvm_info` + `cover property`，无一条
  `assert property`）、与 testplan M2-TL01/M2-TL02 行描述的判决门（"scoreboard
  路由/数据/响应路由/完成正确 ＋ 达标 cover"）三方口径一致，无一方需要改判为准。
- **发现（非阻塞、非五类失败之一，minor documentation staleness）**：
  `doc/testplan.md` 行 M2-TL01/M2-TL02 与 `tb/sva/axi_xbar_txlimit_sva.sv`
  的注释头（L44、L222-248）仍逐字标注"BUG-0016（OPEN，非阻塞/DUT_BUG 待 rev
  签核）""pending rev arbitration"——但 `doc/bugs.md` 现文该行状态列已是
  `SPEC_CHANGED`（REV-007 终判已落地、spec 已重 pin）。这是**状态标签滞后**，
  不是判决逻辑冲突：两处的实际判决行为（cover-only、不升格 assert、判决门锚
  scoreboard）本就与终判后的设计输入完全一致，只是文字仍停留在"待裁决"措辞。
  **不阻塞 F-M2-08 派发**（这两处文件均不在本卡 scope、也不在 F-M2-08 自身
  交付物 `tb/functional_coverage.sv` 范围内）。同 REV-005 §4 C1/M-1、REV-006
  §4.3 对 C5.5(b) 措辞歧义的先例处置一致：作为本卡审阅条件记录（见 §4），
  不另开 `doc/bugs.md` 行（不落 taxonomy 五类任何一类）。

### 2.5 REV-005 §4 遗留条件的现状裁定 —— 明确结论

| 条件 | 现状 | 依据 |
| --- | --- | --- |
| C1（直引 vendor 文档需再锚定 spec 章节） | **已满足** | §1 表第 5 行：三处均已锚 `spec §5.4.1`，vendor 文档仅作 spec 出处旁注，非 design-prompt 自身权威来源 |
| C2（M2-TL02 只准落地弱化可观测上界 checker，机制级断言不派发） | **已被 REV-007 取代** | §1 表第 6 行：C2 限定的对象（弱化 checker）已被 REV-007 §5(2) 正式收回（其前提被证伪），C2 本身随对象消失而失效；现行判据是 REV-007 §6 的新判据（(a)(b)(c) 三条），本次四文件改动均按新判据落笔 |
| C3（F-M2-07 标题/正文标签不一致，minor） | **已满足** | §1 表第 7 行：feature-matrix.md 现文标题/正文/scene 列三者一致，五项俱全 |

### 2.6 外科手术性 —— pass

- 254 行 diff 全部落在与 BUG-0016/REV-007 裁决直接相关的段落（C3.4/C5.3 判据
  本体、M2 判据激活表、`cg_tx_limit` bin 定义、§3 assert-cover 配对约定）；
  未触碰同文件内其余条目（C3.1/C3.2/C3.3/C3.5、C1.5/C5.1/C5.2/C5.4/C5.5、
  uvm_env 其余 C5.1/C5.2/C5.4/C5.5 段落、functional_coverage 其余 covergroup）。
- 每一改动行均可追溯到 REV-007 §5 的三处 spec 编辑范围或 §6 的 checker 指导，
  无顺手改进、无无关重排。
- 无违反。

## 3. F-M2-08 派发结论（卡明文要求的强制回答）

**F-M2-08（功能覆盖采集基建，交付物 `tb/functional_coverage.sv`）现在可以
派发。**

理由：
1. 其唯一设计输入 `doc/design-prompt/functional_coverage.md` 已通过本卡
   behavior-leak（§2.1）与 spec 一致性（§2.2）核验，全文（含本次改动的
   `cg_tx_limit` 段与 §3 assert-cover 配对约定段）均已同步至重 pin 后的 spec
   现文，无待决/矛盾条款。
2. 依赖的仲裁链条（BUG-0010→0011→0013→0016）已全部终局（均为 SPEC_CHANGED，
   见 `doc/archive/bugs-archive.md` + `doc/bugs.md` 行 8），无 OPEN 状态的
   SPEC_ISSUE 悬而未决地卡在 `cg_tx_limit` 的判据来源上。
3. REV-005 §4 三条遗留条件：C1/C3 已满足，C2 已被 REV-007 取代（不再是限制，
   而是被更宽松/更明确的 REV-007 §6 判据取代）——三条均不构成对 F-M2-08 的
   阻塞。
4. §2.4 发现的 testplan.md/SVA 注释头状态标签滞后，与 F-M2-08 的交付物
   （`tb/functional_coverage.sv`，一个新文件、独立于 `axi_xbar_txlimit_sva.sv`）
   无直接读写关系，不构成阻塞；建议 orch 在下次触碰这两处文件时顺带刷新状态
   标签（非阻塞、非独立卡）。

## 4. 指导（原则与方向，不写实现）

- **testplan.md M2-TL01/TL02 + `tb/sva/axi_xbar_txlimit_sva.sv` 注释头的状态
  标签刷新**：建议 orch 在下一次 DV 触碰这两处文件时（例如 F-M2-08 落地、或
  下一次 M2-TL01/TL02 相关改动）顺带把"BUG-0016（OPEN…）""pending rev
  arbitration"措辞更新为"BUG-0016（SPEC_CHANGED，REV-007 终判）"，使文字状态
  与 `doc/bugs.md` 行 8 一致——纯文档新鲜度问题，不改变任何判决逻辑，不需要
  单独派卡，顺路处理即可（同 REV-005 §4 C1/M-1 先例的非阻塞记录方式）。
- 无其他指导事项；四份设计输入的实质内容不需要改动。

## 5. 六问收敛

1. **Origin（Q1，适用）**：本卡为设计输入增量门，非仿真证据评审；四份改动文件
   的每条判据变更均标注 `spec §5.4.1/§5.4.2/§5.4.3/§7.4.5` 章节号 + `BUG-0016/
   REV-007` 裁决来源，可逐条溯源到 spec 现文（§1 表第 2 行独立核验）。达标。
2. **Falsifiability（Q2，适用）**：改动后的 `cover property`/`uvm_info` 判据
   （"reached ceiling"/"exceeded ceiling"）本身即为可复现见证，`tb/sva/
   axi_xbar_txlimit_sva.sv` 现文与 `doc/evidence/v0.2.0/M2-TL01.log`/`M2-TL02.log`
   尾部 cover 命中记录印证非空转（§1 表第 4 行）；判决门（scoreboard 全绿）
   若 DUT 路由/数据/完成有误则会翻红，仍可证伪。达标。
3. **Replayability（Q3，本卡不适用）**：本卡交付物是 `doc/design-prompt/` 文本
   增量核验，无新仿真产出；引用的 `M2-TL01.log`/`M2-TL02.log` 系既有已提交
   证据（本卡仅尾部核对非重放）。**why not**：与 REV-005/REV-007 先例一致，
   设计输入门本身不产 sim log。
4. **Attribution（Q4，部分适用）**：涉及的 BUG-0010/0011/0012/0013/0016 五条
   详情页均已完整（symptom/first_anomaly/taxonomy/rca/fix/regression_guard
   齐备），本卡未发现新失败、无需新增归因记录。closer≠fixer 于本卡不适用
   （本卡为纯文档核验，非修复关闭）。达标。
5. **Judgment（Q5，适用）**：四份设计输入声称"已同步 REV-007 裁决" → 本卡核
   实为真（§1 表全部核验通过）；唯一缺口（testplan/SVA 注释头状态标签滞后）
   已具名（§2.4），且已裁定不阻塞 F-M2-08。达标。
6. **Retention（Q6，适用）**：REV-005 §4 三条遗留条件的现状已在本卡 §2.5 落
   定案（C1/C3 已满足、C2 已被 REV-007 取代），使下游派卡不必重新翻查三份历史
   REV 记录；§2.4 的文档新鲜度缺口已具名记录、给出非阻塞处置建议，避免下一
   个触碰这两处文件的人重新纠结其状态字面矛盾。达标。

## 6. 总体裁决

**Conditional pass.** 四份设计输入改动本身对 behavior-leak / spec 一致性 /
范围闭合性 / 外科手术性四项**全部 pass**；REV-005 §4 三条遗留条件已全部有
明确现状（C1/C3 满足、C2 被 REV-007 取代）。

**条件**（均非阻塞、非本卡评审对象本身的缺陷）：
1. `doc/testplan.md` 行 M2-TL01/M2-TL02 与 `tb/sva/axi_xbar_txlimit_sva.sv`
   注释头的"BUG-0016（OPEN…）"状态标签建议在下次触碰这两处文件时顺带刷新为
   "SPEC_CHANGED"（§2.4/§4）——不阻塞任何派发。

**F-M2-08（`tb/functional_coverage.sv`）现在可以派发**（§3），不受上述条件
限制。

**无 rejected 条目。**

## 7. Taxonomy-class anomaly（强制字段）

**No.** 本次评审未 surface 任何 `failure_taxonomy.md` 五类中尚无 `doc/bugs.md`
行者。§2.4 发现的 testplan.md/SVA 注释头状态标签滞后于已完成裁决，属**设计
输入/测试计划文档措辞新鲜度**层面，不落五类任何一类（非 TOOL_ENV/TB_BUG/
CONSTRAINT_BUG/SPEC_ISSUE/DUT_BUG）——同 REV-005 §8 对 C1/M-1、REV-006 §7 对
C5.5(b) 措辞歧义的先例处置一致，作为本卡审阅条件记录于 §6，不另开
`doc/bugs.md` 行。本次涉及的全部 SPEC_ISSUE（BUG-0010/0011/0012/0013/0016）
均已登记且已终局，无"就地解释、未登记"的新发现。
