# REV-031 — 覆盖率 9 个 UNOWNED 格子分类裁决 + CW-001 R_HOLD 措辞订正 + REV-024 行 9 数值勘误（定级 L3）

取数/复核请重放（本机 VCS/urg O-2018 可用；覆盖率取自 M4-coverage-final-sweep 现场重生的 text 报告，未重跑 sim）：

```
# 全闭包 text 报告（本卡全部数字来源；测量卡命令②产出，本卡只读）
sim/out/urgText6/{dashboard,modlist,modinfo,hierarchy,asserts}.txt
# 结构事实亲验（例化点 tie-off / inject / 常量）：
grep -rn 'flush_i' vendor/axi/src/axi_mux.sv vendor/axi/src/axi_demux_simple.sv \
    vendor/common_cells/src/spill_register.sv
#  → axi_mux.sv:272/324/417 · axi_demux_simple.sv:273/391 · spill_register.sv:38 全 .flush_i(1'b0)
sed -n '1720,1835p' sim/out/urgText6/modinfo.txt   # counter Toggle WIDTH=4/WIDTH=8 Port Details
sed -n '147540,147578p' sim/out/urgText6/modinfo.txt  # lzc Toggle Signal Details（index_lut/padding）
sed -n '221463,221556p' sim/out/urgText6/modinfo.txt  # rr_arb_tree Line 159/182/233 全 flush
sed -n '16982,16997p' sim/out/urgText6/modinfo.txt    # spill_register_flushable flush_valid 2912000/0
```

- 日期：2026-08-02 · 卡类型：rev 覆盖率 UNOWNED 分类裁决 + 豁免登记 + 归因订正（task type 5 / 豁免登记面）· 定级 **L3**
- 复核实例：全新 rev；未与本轮任何 M4 相关卡（REV-016/017/018/020–030、M4 签核卡、M4-coverage-final-sweep 测量卡）作者共用实例；未与 arch 共用。本裁决建立在 rev **独立亲读**只读 vendor RTL、测量卡现场重生的 urg text 报告、pinned spec §0、`doc/coverage-waivers.md` 全文、REV-024/028/029/030 之上；**测量卡 §3/§5/§8 的"UNOWNED/候选"标注仅作路由线索、不作结论依据**——下文全部分类从 RTL 例化事实与 urg 逐信号/逐 bin 原文自行重走。
- 快照：pulp-platform/axi v0.39.9，SHA `a256a3b8…`（`vendor/VENDOR.md` pin）；common_cells 快照 SHA 见 `vendor/VENDOR.md`。
- 判据来源：`workflow/bugs.md`（coverage-hole dispatch L217–234，五分类 L29–48）、`workflow/discipline.md` rule 1/2/3、`workflow/review.md`（七问 + rubric #5/#8）、`doc/spec.md` §0 item 4（六类三态/例化闭包/≥90% 门）、`doc/coverage-waivers.md` 抬头（Kind-A/B 定义 + 可证伪解锁 + silent-exclude 失效）、BUG-0047 guard（Kind-B 须先逐位分解；REV-024"需补场景"条目不得改记 Kind-B）、REV-028（已过门不补行先例）/REV-029（Kind-A 登记 + REV-024 追加批注格式）/REV-030（批量分诊 + DV 构造指引 + CW-009 拆分先例）。
- **输入边界声明（CLAUDE.md §4 红线）**：本卡读 vendor RTL **仅用于认定信号驱动源/例化连接/常量 tie-off/计数器结构**（判断某覆盖率残余是"结构不可达"还是"定向可达未测"），**未从 RTL 推导任何 checker 期望值**；产出不含任何 DUT 行为条款、不写任何 tb 代码，只动"UNOWNED 分类 + Kind-A 豁免登记 + CW-001 措辞订正 + REV-024 数值勘误 + 后续 DV 构造指引"这一 §0 验证适配面。

---

## Scope 列表（rev 亲读原件，含行号锚点）

**裁决主体**：9 个 UNOWNED `<90%` (模块,类型) 格子（跨 8 模块）+ CW-001 R_HOLD 措辞疑点 + REV-024 §2.2 行 9 数值疑点。

**RTL（只读 vendor，认定驱动源/例化性质/常量）**
- `vendor/common_cells/src/rr_arb_tree.sv`：L158-159/181-182/232-233 `if(flush_i)` 复位赋值；L67 `index_lut`（经 lzc）；L197-224 `gen_fair_arb`（lzc 例化 L202-218）
- `vendor/common_cells/src/lzc.sv`：L67 `index_lut[j]=(NumLevels)'(j)`（常量 LUT）、L86-89 `g_out_of_range`（超范围节点 tie `1'b0`/`'0`）
- `vendor/common_cells/src/spill_register.sv`：L38 `.flush_i(1'b0)`（唯一 `spill_register_flushable` 实例源）
- `vendor/common_cells/src/spill_register_flushable.sv`：L78/79/84/85 含 flush 的 Cond 项、L99 `flush_valid: flush_i \|-> ~valid_i`
- `vendor/common_cells/src/fifo_v3.sv`：L122 flush 复位分支、L105 `FALL_THROUGH && …`、L80/92 幂-2 wrap dead-code
- `vendor/common_cells/src/counter.sv`（纯壳）/`delta_counter.sv`（逻辑体）
- `vendor/axi/src/axi_id_prepend.sv`：L35 `pre_id_i`、L82-91 prepend（`PreIdWidth=3≠0` 走 gen_prepend）
- `vendor/axi/src/axi_mux.sv`：L227 `.pre_id_i(switch_id_t'(i))`、L272/273/324/417/418（rr_arb_tree/fifo flush + rr_i tie）
- `vendor/axi/src/axi_demux_simple.sv`：L210-228 `i_aw_id_counter`（`.inject_i(1'b0)`）、L355-374 `i_ar_id_counter`（`.inject_i(atop_inject)` **活信号**）、L236-249 `i_counter_open_w`（`.clear_i(1'b0)`/`.load_i(1'b0)`/`.d_i('0)`）、L513-631 `axi_demux_id_counters`
- `vendor/axi/src/axi_err_slv.sv`：i_r_counter 例化（`.down_i(1'b1)`/`.d_i(r_fifo_data.len)`）
- `vendor/axi/src/axi_atop_filter.sv`：L273-306 `r_state_q`（R_FEEDTHROUGH→R_HOLD `:274`、R_HOLD 回迁 `:303`、INJECT_R `:285` 需 `r_resp_cmd_pop_valid`）
- `vendor/axi/src/axi_multicut.sv`（唯一 generate-if `NoCuts=='0`）/`axi_cut.sv`（无条件表达式）

**覆盖率事实（本机 text 报告，观测"实际是什么"，不抄成期望值）**
- `modinfo.txt`：counter Toggle（L1720-1835）、lzc Toggle（L147378-147578）、rr_arb_tree Line/Toggle（L221463-222596）、spill_register_flushable Cond/Branch/Assert（L12282-16997）、fifo_v3 Branch/Toggle（L181626-182162）、axi_id_prepend Toggle（L275606-275642）、axi_demux_id_counters（L165460-166601）

**判据/先例**：见抬头"判据来源"。

---

## 1. flush 根因 —— `flush_i≡1'b0` 全例化点，结构不可达（登记 CW-010）

rev 亲验：`flush_i` 在闭包**全部**例化点硬接字面 `1'b0`——`spill_register.sv:38`（唯一 `spill_register_flushable` 实例源）、`axi_demux_simple.sv:273/391`（rr_arb_tree flush）、`axi_mux.sv:272/417`（rr_arb_tree）/`:324`（i_w_fifo）。本 xbar 集成**从不对外暴露 flush 能力**，flush 前提永假。三处覆盖足迹：

- **rr_arb_tree Line 80.00%**：urg Line 表唯一 3 条未覆盖 = `:159`/`:182`/`:233`，**全部**是 `if(flush_i)` 的复位赋值体（`lock_q/req_q/rr_q <= '0`）——本格 100% 为 flush 结构死，**整格 Kind-A**。
- **spill_register_flushable Assert 0.00%**：唯一断言 `flush_valid: flush_i \|-> ~valid_i`，Attempts 2,912,000 / Real Successes 0 / Failures 0——前提 `flush_i` 永假 → **vacuous（从未真正触发），非 failing**。整格 Kind-A。
- **bin-scoped 分量**：`fifo_v3` Branch IF-117 `0 1`（flush 复位）、`spill_register_flushable` Cond 的 `!flush_i`/`\|\|flush_i` 项、`rr_arb_tree` Toggle 端口 `flush_i` 位——同根 flush 结构死；**这些格的非-flush 余量另路由（§5）**。

**per-item verdict**：flush 根因**成立**（RTL 亲验 6 例化点 tie-off + urg Line/Assert 亲证），登记 **CW-010（Kind-A）**。解锁 = 任一例化点驱 `flush_i≠0`。

## 2. lzc Toggle 42.59% —— 常量 LUT + 非-2-幂树 padding，结构性永 <90%（登记 CW-011）

urg lzc Toggle Signal Details 亲取：端口（in_i/cnt_o/empty_o）**100%**；未覆盖信号主体 = `gen_lzc.index_lut[5:0][2:0]`（No/No/No）、`gen_lzc.sel_nodes[7:6]`（No）、超范围 `gen_lzc.index_nodes[6:7]`（No）。RTL 核实：
- `index_lut[j]=(NumLevels)'(unsigned'(j))`（`lzc.sv:67`）是**编译期常量查找表**，恒定不可翻。
- `lzc.sv:86-89` `g_out_of_range` 把超范围树节点 `sel_nodes`/`index_nodes` 恒赋 `1'b0`/`'0`，仅因 `WIDTH`(=NumIn=6/9) **非 2 的幂**而存在。

算术核验：模块 Total Bits 132（端口 20 + 信号 112），covered 64；index_lut(36 bit-dir)+padding(≈16 bit-dir) 结构死。**即便在范围内部节点全闭（≤16 bit-dir 定向），covered ≤ ~80/132 ≈ 60% < 90%**——结构性永不达标。少量在范围节点位属仲裁模式定向（DV-F），但闭之无功能收益（门仍不过），**不单列 DV**。

**per-item verdict**：lzc Toggle 结构性永 <90% **成立**，登记 **CW-011（Kind-A）**。

## 3. axi_id_prepend Toggle 78.26% —— pre_id_i generate 常量（登记 CW-012）+ b_readies（DV-C）

urg Port Details：46 位 3 信号未覆盖——`pre_id_i[2:0]`（6 bit-dir）、`slv_b_readies_i`（2）、`mst_b_readies_o`（2）。
- **pre_id_i**：`axi_mux.sv:227` `.pre_id_i(switch_id_t'(i))` = generate-loop 编译期常量（每实例固定=i），单实例内永不翻转——结构死（性质同 CW-002/CW-007）。**即便 DV-C 关闭 b_readies（→ 42/46=86.96%）本格仍恒 <90%** → pre_id_i 须书面承接（同 REV-029"门真失败须登记"，区别于 REV-028"已过门不补行"）。**登记 CW-012（bin-scoped）**。
- **slv/mst_b_readies**：B 通道 ready 直通，外部主机 `b_ready` 恒高（`slvport_agent.sv:44`）——**REV-030 DV-C（#17）同根**，路由 DV-C，不入豁免。

**per-item verdict**：**拆分**——pre_id_i → CW-012（Kind-A）；b_readies → DV-C（§5）。

## 4. CW-001 对 `axi_atop_filter r_state_q.R_HOLD` 的措辞订正

rev 亲验 `axi_atop_filter.sv:273-306`：
- `R_FEEDTHROUGH: if (mst_resp_i.r_valid && !slv_req_i.r_ready) r_state_d = R_HOLD;`（`:274`）——**普通读背压**，与 ATOP **完全无关**（任意一笔读的 R valid 但下游未 ready）。
- `R_HOLD: if (mst_resp_i.r_valid && slv_req_i.r_ready) r_state_d = R_FEEDTHROUGH;`（`:303`）——回迁，同样 ATOP 无关。
- `INJECT_R`（`:285`）须 `r_resp_cmd_pop_valid`（`:276`）——**真正的 ATOP R 响应注入**，确需 ATOP。

结论：CW-001 原文"进入其（非-FEEDTHROUGH）状态须 atop!=0 抵达 miss 地址被环境约束禁止"对 `r_state_q.R_HOLD` **不成立**——R_HOLD 是普通读背压路径，已被现有非-ATOP 背压场景 Covered（urg 亲证 r_state_q States 3/4）。对 `w_state_q` 全部非-FEEDTHROUGH 状态与 `r_state_q.INJECT_R` 仍成立。

**处置（同 REV-028/029"只订正归因、不改判"先例）**：CW-001 **不撤销、不改 Kind**（`axi_atop_filter` 六类仍全 <90%，仍需 CW-001 承接真正 env-不可达的大宗），仅**订正措辞**——已在 `doc/coverage-waivers.md` CW-001 行"具体 bin/信号范围"列加 R_HOLD 除外标注、"可证伪论证"列追加 REV-031 §4 订正、"rev 记录"列追加本记录。

**per-item verdict**：CW-001 措辞**须订正**（R_HOLD 非 env-不可达），已落地；豁免本身**维持有效**。

## 5. 五格定向可达 / 混合残余 —— 驳回整格 Kind-A，路由 DV（构造指引见 §7）

以下经 rev 亲验为**定向可达**或**混合（结构少数 + 定向多数，结构少数可被 ≥90% 余量吸收）**，**驳回整格 Kind-A**：

- **axi_demux_id_counters Line 73.91%/Toggle 74.06%/Branch 79.49%**：未覆盖行集中在 `axi_demux_simple.sv:582-589`（`push_en && inject_en` 同拍的 unique-case 分支）。AW 侧 `i_aw_id_counter.inject_i(1'b0)` 结构死，但 **AR 侧 `i_ar_id_counter.inject_i(atop_inject)` 是活信号**（`sv:367`，rev 亲验）——模块页合并下该分支经 AR 侧**定向可达**（同拍接受 AR + 注入 ATOP 写 R 响应 + ID 低位相同）。**非结构不可达 → 驳回 Kind-A，路由 DV-G。**
- **rr_arb_tree Toggle 77.45%**：结构少数（flush=CW-010、`rr_i[2:0]`=`.rr_i('0)` tie/ExtPrio=0、`size[2]`=CW-007）≈5 位 / 184 端口位；定向多数 = `len[7:4]`/`addr[2:0]`（DV-A 载荷值域）+ 仲裁内部节点（index_nodes 其余位/req_nodes[6]/sel/masks，DV-F 仲裁竞争）。定向全闭后结构少数被 ≥90% 余量吸收（REV-028 先例）→ **不登记，路由 DV-A + DV-F。**
- **fifo_v3 Cond 80.19%/Toggle 82.09%/Branch 78.43%**：混合——flush（CW-010）+ `FALL_THROUGH=0` dead-code（`sv:105`，param 常量）+ 幂-2 wrap dead-code（`sv:80/92`）+ `testmode_i` scan tie + 背压（定向）+ mem 值域（err_slv 恒定应答，DV-B 同根）。结构分量已由 CW-010/既有 Kind-A 家族承接（bin-scoped），**非-flush 定向余量路由 DV**；**本格是否需为结构残余补 Kind-A 待定向闭合后重测**（M5 解锁式：若定向闭后仍 <90% 再登记结构残余，不预登记——BUG-0047 guard 逐位分解前置 + REV-028 不过度登记）。
- **spill_register_flushable Cond 82.49%/Toggle 79.76%**：flush（CW-010，bin-scoped）+ 背压（定向，`a_full_q/b_full_q/ready_i` 组合）+ 载荷值域（DV-A/DV-B）。同上，非-flush 余量路由 DV，结构残余待重测。
- **axi_multicut/axi_cut/spill_register 薄壳 Toggle 89.22/89.22/88.51%**：三者为透传薄壳，Toggle 残余 = AXI 通道载荷位（data/addr[2:0]/len[7:4]/size[2]/resp/user）——**DV-A/DV-B 载荷值域同根，仅内层粒度阴影**；数值已逼近 90%，DV-A/DV-B 落地后越门。**不登记豁免、不新开 DV 卡，随 DV-A/DV-B 关闭。**
- **counter/delta_counter Toggle 43.48/41.20%**（§6 单列拆分）。

**per-item verdict**：上述整格 Kind-A **驳回**；结构分量归 CW-010/CW-007 家族（bin-scoped），定向分量路由 DV-A/DV-B/DV-C/DV-F/DV-G（§7）。

## 6. counter/delta_counter Toggle —— tie-off 结构位登记 CW-013 + 值域定向路由

rev 亲验例化：`i_counter_open_w`（WIDTH=4×6）`.clear_i(1'b0)`/`.load_i(1'b0)`/`.d_i('0)`（`axi_demux_simple.sv:242/244/245`）；`i_r_counter`（WIDTH=8×6）`.down_i(1'b1)`（`axi_err_slv.sv`）。urg Port Details 亲证 clear_i/load_i/d_i/down_i 对应位 No/No/No。
- **结构 tie-off 位**（clear/load/d/down + overflow_o——`w_open` 受 CW-009 封顶从不溢出）：使本格**即便定向关闭值域仍恒 <90%** → 登记 **CW-013（Kind-A，bin-scoped）**。
- **定向可达位**：`i_r_counter` `d_i[7:2]`/`q_o[7:2]`（=未命中读 `len` 高位）= **DV-A 同根**；`delta_counter` id-bucket 96 例在飞高位 = **DV-G 同根（深同-ID 占用）**——路由 DV，不入豁免。

**注**：counter 模块页仅 12 例（WIDTH=4×6 + WIDTH=8×6）；测量卡 §5-6"108 例"实为 `delta_counter`（12 壳内 + 96 id-bucket 直接例化）之数，本卡订正此归属（不改测量卡，仅记于此）。

**per-item verdict**：**拆分**——tie-off 结构位 → CW-013（Kind-A）；r_counter len 高位 → DV-A；id-bucket 高位 → DV-G。

---

## 7. 需 DV 卡清单（rev 只写方向性技术要求，不写 tb 代码；供 orch 据此派发）

> 通则：期望值只准从 pinned spec 推导，**绝不从被测 RTL 抄**；触及既有 ✅ 场景的加固须重跑 `make evidence`；每卡若新增/扩展 checker 期望路径须做一次注伤自证 KILL（不变量 5）。以下两卡为**本卡新增**方向；DV-A/DV-B/DV-C 为 REV-030 既有卡的内层粒度阴影（不新开、随其关闭）。

### DV-F（新）— 仲裁竞争多样性（rr_arb_tree 内部节点）
- **目标 bin**：`rr_arb_tree` Toggle 内部节点 `gen_arbiter.index_nodes[…]` 其余位、`req_nodes[6]`、`gen_levels[*].sel`、`upper_mask[0]`/`lower_empty`——需穷举**仲裁请求向量模式**（哪些端口位置在上/下半区搜索路径单独命中）。
- **构造**：宿主 = 新压力场景或扩 M2-WO01——多主并发汇聚同一 master 端口 + 请求端口分布多样化（轮转/稀疏/满载），使 rr 指针 `rr_q` 遍历全部起点。
- **判决门**：SPEC-5.5.3 无饿死 + 轮转公平；红线：不得断言具体授权序/拍（SPEC-7.4）。**估级 L2**。

### DV-G（新，DV-E 家族）— axi_demux_id_counters push+inject 同拍同 index + 深同-ID 占用
- **目标 bin**：`axi_demux_id_counters` Line/Toggle/Branch（`axi_demux_simple.sv:582-589` `push_en&&inject_en` unique-case 分支，经 AR 侧 `inject_i(atop_inject)`）+ `delta_counter` id-bucket 在飞高位。
- **可达性核实**：**可达**（AR 侧 inject 是活信号，rev 亲验 `sv:367`）——需同拍"接受一笔 AR（`ar_push`）+ 注入一笔 ATOP 写的 R 响应（`atop_inject`）+ 二者 ID 低位相同"。与 REV-030 DV-E（#16，针对 `sv:168` 的 `ar_id_cnt_full && atop[R_RESP]`）**相邻但不同 bin**：DV-E 判"计数器是否已满"，DV-G 判"计数器如何变化（push+inject 同拍）"。
- **构造**：宿主 = 扩 M3-AT02 或新行——fork：同 index 群 AR + 窗口内呈现 atop-load 写（命中地址，避 SPEC-4 clause 7）；深占用用 `resp_hold` 撑同-ID 在飞。
- **判决门**：SPEC-3 路由 + SPEC-6.3 atop 双通道应答；红线：不得断言计数值/拍（SPEC-7.4.5）。**估级 L2**。

### 既有卡阴影（不新开，随其关闭；orch 派 DV-A/B/C/D 时纳入内层粒度目标）
- **DV-A（REV-030）**：`rr_arb_tree`/`axi_multicut`/`axi_cut`/`spill_register`/`spill_register_flushable` Toggle 的 `addr[2:0]`/`len[7:4]`/`size[2]` 载荷位、`counter` `i_r_counter` `len` 高位。
- **DV-B（REV-030）**：上列薄壳 Toggle 的 data/resp/user 载荷位、fifo_v3 mem 值域（err_slv 恒定应答子集或 DV-B）。
- **DV-C（REV-030）**：`axi_id_prepend` `slv_b_readies_i`/`mst_b_readies_o`。
- **DV-D（REV-030）**：`counter` `i_r_counter` `q_o[7:2]`（r_fifo 填满，读向背压）——已由 `m4_eb02` 部分改善，DV-D 完成后随之。

---

## 8. Overall verdict

**Conditional pass —— 登记 4 条 Kind-A 豁免（CW-010 flush / CW-011 lzc / CW-012 axi_id_prepend / CW-013 counter tie-off），订正 CW-001 R_HOLD 措辞，勘误 REV-024 行 9 数值；驳回 5 格整格 Kind-A、路由 DV-F/DV-G（新）+ DV-A/B/C/D（阴影）。**

**9 格逐格裁决表**（分类 + 独立核实依据 + RTL 锚点）：

| # | (模块,类型) | 数值 | 分类 | 独立核实依据（RTL 锚点 + urg） |
|---|---|---|---|---|
| 1 | `axi_demux_id_counters` Line/Toggle/Branch | 73.91/74.06/79.49 | **驳回 Kind-A → DV-G** | AR 侧 `i_ar_id_counter.inject_i(atop_inject)` 活信号（`axi_demux_simple.sv:367`）——push+inject 同拍定向可达，非结构死 |
| 2 | `axi_id_prepend` Toggle | 78.26 | **拆分：CW-012 + DV-C** | pre_id_i generate 常量（`axi_mux.sv:227`）恒死 → CW-012；b_readies B 背压 → DV-C（urg Port Details） |
| 3a | `rr_arb_tree` Line | 80.00 | **CW-010（整格 flush）** | urg Line 表 3 行全 `if(flush_i)`（`:159/182/233`）；flush 全例化点 tie 1'b0 |
| 3b | `rr_arb_tree` Toggle | 77.45 | **驳回 Kind-A → DV-A + DV-F** | 结构少数（flush/rr_i tie/size[2]）被余量吸收；主体=载荷(DV-A)+仲裁节点(DV-F) |
| 4 | `lzc` Toggle | 42.59 | **CW-011（结构永<90）** | 常量 `index_lut`（`lzc.sv:67`）+ 非-2-幂 padding（`:86-89`）；端口 100%，算术核验闭后仍 <90% |
| 5 | `fifo_v3` Cond/Toggle/Branch | 80.19/82.09/78.43 | **驳回整格 → CW-010 分量 + DV（余量），结构残余待重测** | flush(CW-010)+FALL_THROUGH/幂-2 dead-code(`:105/80/92`)+背压/mem 值域(定向) |
| 6 | `counter`/`delta_counter` Toggle | 43.48/41.20 | **拆分：CW-013 + DV-A/DV-G** | tie-off clear/load/d/down（`axi_demux_simple.sv:242/244/245`+`axi_err_slv` down_i）→ CW-013；值域 → DV |
| 7 | `axi_multicut`/`axi_cut`/`spill_register` 薄壳 Toggle | 89.22/89.22/88.51 | **驳回 Kind-A → DV-A/DV-B 阴影** | 透传薄壳载荷位；逼近 90%，随 DV-A/B 越门；无新卡 |
| 8 | `spill_register_flushable` Cond/Toggle | 82.49/79.76 | **驳回整格 → CW-010 分量 + DV（余量），结构残余待重测** | flush(`:78/79/84/85/99`,CW-010)+背压+载荷值域 |
| 9 | `spill_register_flushable` Assert | 0.00 | **CW-010（整格 vacuous）** | `flush_valid`（`:99`）2,912,000/0/0，flush 前提永假 → vacuous 非 failing |

**改动清单**：
1. `doc/coverage-waivers.md`：新增 CW-010/011/012/013（Kind-A）；订正 CW-001 R_HOLD 措辞（3 列）；表后"注"更新 CW-001~CW-013；"待建档项" spill_register bypass 撤项（评估非缺口）。
2. `doc/review/REV-024.md` §2.2：表后追加 REV-031 行 9 数值勘误批注（原表格行未改）。
3. `doc/review/REV-031.md`：本记录。
4. （out-of-scope，flag 给 orch）派 DV-F（仲裁竞争多样性，L2）+ DV-G（id_counters push+inject 同拍，L2）两新卡；DV-A/B/C/D 派发时纳入 §7 内层粒度目标；fifo_v3/spill_register_flushable Cond/Toggle 的结构残余待定向闭合后重测（若仍 <90% 再登记，不预登记）。

---

## 9. 强制字段 — taxonomy-class anomaly（`workflow/bugs.md` 五类自查）

**否（no new bugs.md row）+ 两项归因订正（既有伞下，不新开行）。**

- 本裁决全程属**覆盖率洞处置**（coverage-hole dispatch），非 checker 失败——未 surface 任何 TOOL_ENV/TB_BUG/CONSTRAINT_BUG/DUT_BUG/SPEC_ISSUE 新实例。CW-010~013 是结构不可达 Kind-A 登记；DV-F/DV-G/DV-A~D 是定向可达 planning-gap（既有 BUG-0047 SPEC_ISSUE/planning-gap 伞下），路由 DV 卡，不落新五分类。
- **CW-001 R_HOLD 措辞订正**：既有 CW-001（Kind-A）一处论证保真度订正——R_HOLD 实为读背压可达、非 env-不可达；不改判、不撤豁免，**非新失败类实例**（同 REV-024 §7/REV-028/029 归因订正先例）。
- **REV-024 行 9 数值勘误**：既有 BUG-0047 planning-gap 伞下一处数值归属订正（axi_multicut/axi_cut/spill_register 结构无 Cond bin，55-65% 应属 spill_register_flushable），**非新失败类实例**，只订正数据、不改原记录。
- 未 surface 任何已被 `doc/bugs.md` 行覆盖之外、且被本卡或被评审方（测量卡）就地绕过的 taxonomy 类异常——测量卡严守"只标注不处置"边界，未就地登记豁免或绕过。

## 10.「定级 vs 实际」自评

- **定级 L3（spec/豁免/签核面），实际 L3 相符**。本卡动豁免登记面（新增 4 条 Kind-A + 订正 1 条）+ 既有 rev 记录归因订正（REV-024）+ 后续 DV 路由——全程无 tb/rtl 改动、无期望值推导、无仿真，属 rev 独立复核 + 登记面裁决，与 L3"spec/豁免/签核、rev 必到、全 rubric"一致。无失配。
- 关键纪律遵守：Kind-A 登记均附**可证伪论证 + 具体解锁**（抬头契约）；**未**将任何"需补场景"条目改记 Kind-B（BUG-0047 guard 合规——本卡零 Kind-B）；混合格**拆分**（CW-009/DV-E 先例）而非笼统豁免；对可被 ≥90% 余量吸收或定向可闭的格**不过度登记**（REV-028 先例），仅对结构性永 <90% 的格登记（REV-029 先例）。
