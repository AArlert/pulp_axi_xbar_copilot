# Coverage waivers（rev 签核；REV-024 建档，REV-016 §9.5 触发）

覆盖率缺口（有 bin、<90%）的书面豁免登记本。凡 silent exclude（无本表条目的
排除）失效（`workflow/bugs.md` L229）。每条豁免须声明 **Kind** 与**可证伪解锁**。

- **Kind-A 结构/环境不可达（永久）**：解锁 = 某具体事实被推翻。
- **Kind-B 方法论受限、延后至 M5 随机层 + M6 cov_loop（临时）**：解锁 = M5 约束随机
  激励层落地 + M6 cov_loop 对该 bin 重测后仍 <90% 才讨论转永久；被推翻即作废 =
  任一 M5/M6 随机跑使该 bin ≥90%。**须附逐位 toggle 分解**。

| waiver-id | (模块,类型) | 具体 bin/信号范围 | Kind | 可证伪论证（不可达性 / 方法论受限） | 解锁条件 | rev 记录 |
| --- | --- | --- | --- | --- | --- | --- |
| CW-001 | `axi_atop_filter` 六类 | 非-FEEDTHROUGH 状态/迁移及其从属 line/cond/tgl/branch（逐弧见 `doc/evidence/v0.4.13/signoff-M4.md` §REV-017 兑现段）——**`r_state_q.R_HOLD` 及 R_FEEDTHROUGH↔R_HOLD 两弧除外**（读背压可达、现已 Covered，见论证列 + REV-031 §4） | A | §4 clause 7 环境约束：全部 6 例在 `axi_err_slv` 内，进入其状态须 `atop!=0` 抵达译码未命中地址，被环境约束构造性禁止。**订正（REV-031 §4，rev 亲验 `axi_atop_filter.sv:273-306`）**：原文"进入其状态须 atop!=0"对 `r_state_q.R_HOLD` **不成立**——R_HOLD 由普通读背压 `mst_resp_i.r_valid && !slv_req_i.r_ready`（`:274`）触达、`:303` 回迁，**与 ATOP 无关**，已被读背压场景 Covered，**不属本豁免不可达范围**；本豁免不可达论证仅对 `w_state_q` 全部非-FEEDTHROUGH 状态 + `r_state_q.INJECT_R`（`:285`，需 `r_resp_cmd` 注入）成立 | 补 err_slv×ATOP 应答许可来源 + 重开 `BUG-0032` | `doc/evidence/v0.4.13/signoff-M4.md` §REV-017 兑现段 / `doc/review/REV-016.md` §6.2 / `doc/review/REV-017.md` / `doc/review/REV-031.md` §4 |
| CW-002 | `axi_xbar` Toggle | `test_i` scan-enable 位 | A | scan 模式出验证范围（spec §0 未纳入 scan 验证） | 验证范围纳入 scan 测试（若项目决定收窄/取消本豁免） | 待 rev 卡填 |
| CW-003 | `axi_err_slv` Toggle | `slv_resp_o.r.data[63:0]`（全部 64 位恒定） | A | `err_resp.r.data = RespData`（`axi_err_slv.sv:196`），`RespData` 为编译期参数常量 `64'hCA11AB1EBADCAB1E`（`:25`），`axi_xbar_unmuxed.sv` 两处例化点均未 override；为 1 的位永 1、为 0 的位永 0，任何激励不可翻 | xbar 例化改为可变 `RespData`，或该参数被证实运行时可变 | `doc/review/REV-025.md` V1（rev 亲验 RTL + 例化点未 override） |
| CW-004 | `axi_err_slv` Toggle | `r.resp[1:0]`/`b.resp[1:0]`（恒定 `2'b11`） | A | `err_resp.r.resp`/`err_resp.b.resp = Resp`（`:197`/`:145`），`Resp` 默认 `RESP_DECERR`（`:23`）未被 override，两字段全程恒定 `2'b11` | xbar 例化改为可变 `Resp`，或该参数被证实运行时可变 | `doc/review/REV-025.md` V1 |
| CW-005 | `axi_err_slv` Toggle | 出侧 `r.user`/`b.user`（tie-off 恒 0） | A | `err_resp.r`/`err_resp.b` 先赋 `'0` 再只赋 `id`/`resp`/`data`/`valid`（`:191`/`:143`），`user` 字段从未被赋非零值 | 若 `err_resp` 结构改为显式驱动 `user`（当前设计无此路径） | `doc/review/REV-025.md` V1 |
| CW-006 | `axi_xbar`/`axi_mux`/`axi_demux_simple`/`axi_err_slv` Toggle | `rst_ni` 1→0（运行中复位）四模块一致未覆盖方向 | A | spec §2.3（`doc/spec.md` L90）仅定义 `rst_ni` 为"复位，异步、低有效"，未定义运行中（事务在飞时）复位的恢复形态——复位断言时在飞事务去向、复位释放后内部计数器/FSM 恢复语义，五份许可来源皆无。M4 若引入带在飞事务的热复位，checker 期望只能来自 RTL，属 spec-from-RTL 越界（同 BUG-0002/0003/0032 未定义→范围约束先例）；构造"零在飞热复位"场景虽可翻此 toggle 位但不测试任何有意义的复位语义（toggle-theater，REV-026 明确驳回），故按范围外处置 | 验证范围纳入运行中复位测试，且先补齐 spec §2.3 复位恢复语义条款（P-REV026-1，独立 spec-review 门禁） | `doc/review/REV-026.md`（rst_ni 独立裁决段：采纳范围豁免、驳回"空闲窗口热复位脉冲"新场景方案） |
| CW-007 | `axi_mux`/`axi_demux_simple`/`axi_err_slv` Toggle | `aw/ar.size[2]` 总线宽度上限位 | A | 64-bit 数据总线下合法 `AxSIZE≤3'b011`，`size[2]` 结构恒 0，任何合法激励不可达；`tb/xbar_types_pkg.sv:132` `DATA_W` 为单一 `localparam=64`，cfgA-E 配置块（L35-123）均未 override，故全部受验配置数据总线宽度一致为 64 位——REV-025 V4 暂缓转正的前置条件（"须先确认全部配置 ≤64"）已由 REV-026 独立复核确认解除 | 若未来配置矩阵新增更宽数据总线配置点，须重新核实 `size[2]` 可达性，本条即时作废 | `doc/review/REV-025.md` V4 + `doc/review/REV-026.md`（size[2] 独立验证段） |
| CW-008 | `addr_decode_dync` Branch | `proc_check_addr_map`（`addr_decode_dync.sv:146`）的 IF else 支（`MISSING_ELSE`，urg mod20.html Branch 表 IF-146=2/1；模块 Branch 83.33% 的**唯一**未覆盖 bin） | A | IF 条件 `!$isunknown(addr_map_i) && ~config_ongoing_i`：`config_ongoing_i≡1'b0`（`addr_decode.sv:106` 硬 tie-off，xbar 走普通 `addr_decode` 变体、`config_ongoing_i` 非其对外端口，交叉核对 `addr_decode_napot.sv:89` 亦 tie-off）使 `~config_ongoing_i≡1` 恒真 → **else 唯一触达路径 = `addr_map_i` 取 X**。该块为 `ifndef SYNTHESIS` 仿真专用地址表 sanity 断言守卫（`ASSUME_I` check_start/check_overlap）的跳过分支，else 体为空（无 DUT 功能语义）；env 只以具体合法 localparam 驱动地址表、从不注 X（`tb_top.sv:59`、`seq_lib.sv:1323/1779/2478` 仅赋 `ADDR_MAP`/`_V1`/`_OV1`，无 force/isunknown/X 路径），唯一 X 暴露仅 time-0 上电前瞬态、非功能激励且经 urg 证实未触达 else。spec §3.1/§3.2/§3.4 均假定地址表为具体合法值（≥1 rule、`start≤end`、运行时仅在全端口 AW/AR-idle 窗切换），**无未知/X 地址表语义**——构造 X 注入覆盖它属无 spec 基础的 X-theater，其 checker 期望只能来自 RTL（spec-from-RTL 越界，同 CW-006 rst_ni toggle-theater 先例的范围外处置） | 任一具体事实被推翻即作废重裁：(i) `addr_decode.sv:106` 的 `config_ongoing_i` tie-off 改为运行时可驱动非 0（config_ongoing 路径转活）；或 (ii) 验证范围纳入"地址表 X/未配置窗口"测试**且先补齐 spec 对未知地址表语义的条款**（当前 spec §3.1/§3.2/§3.4 无 X 语义，须先补 spec 方有可写 checker 的期望来源——同 CW-006 解锁须先补 spec §2.3 的先例） | `doc/review/REV-029.md`（rev 亲验 RTL tie-off + TB 驱动源 + urg Branch 表；承 REV-028 §4 分流建议独立复核并落地登记） |
| CW-009 | `axi_demux_simple` Cond | `axi_demux_simple.sv:168` 主表达式 term2 `(w_open != {IdCounterWidth{1'b1}})` 取 0 之 bin（即 `w_open`==全一=15；urg mod12.html Cond LINE-168 主表达式行 `1 0 1` Not Covered——模块 COND 82.76% 五个未覆盖 bin 中**唯一**结构不可达者；其余四 bin = `ar_id_cnt_full && aw.atop[ATOP_R_RESP]` 双 1 交叉，属**定向可达需补场景**（REV-030 §DV-E / 后续任务 #16），**不入本豁免**） | A | `IdCounterWidth = cf_math_pkg::idx_width(MaxMstTrans=10) = 4`（`axi_demux_simple.sv:69`）⇒ 全一=15；但 `w_open`（`i_counter_open_w` 输出，记"已路由而 W 未完成"的 AW 数）可达上界结构封顶远低于 15：下游 `axi_mux i_w_fifo` 深度 = `MaxWTrans = Cfg.MaxSlvTrans = 6`（`axi_mux.sv:319/46` + `axi_xbar.sv:141`），W 不排空则 mux w_fifo 满 6 笔后其 `aw_ready` 拉低、demux 锁定于第 7 笔（`w_cnt_up` 已 +1）即无法再路由、`w_open` 冻结；加 demux→mux `PipelineStages=1` 缓冲 ≈ 上界 9 < 15，任何合法激励不可使 `w_open` 达 15（经验佐证 `slvport_agent.sv:448-466` `drive_burst_wopen`：LEAD=10 全 SEED 净完成、LEAD=11 自死锁触 watchdog，即 AW→W 链在 ~10 处即无空间）。故 term2=0 结构不可达 | 任一事实被推翻即作废重裁：(i) 受验配置把 AW-ahead-of-W 缓冲深度（`MaxSlvTrans`+`PipelineStages`）提至 ≥ `2^idx_width(MaxMstTrans)−1`（当前 6 << 15）；或 (ii) `MaxMstTrans` 减小使 `idx_width(MaxMstTrans)` 全一 ≤ 该缓冲深度 | `doc/review/REV-030.md`（rev 亲验计数器位宽 `axi_demux_simple.sv:69` + mux w_fifo 深度 `axi_mux.sv:319`/`axi_xbar.sv:141` + urg mod12.html Cond LINE-168 表 + `drive_burst_wopen` 经验边界） |
| CW-010 | `rr_arb_tree` Line + `spill_register_flushable` Assert（+ `fifo_v3` Branch/Cond、`spill_register_flushable` Cond/Toggle、`rr_arb_tree` Toggle 各自的 flush 位分量，bin-scoped） | `rr_arb_tree.sv:159/182/233` 三条 `if(flush_i)` 复位赋值（Line 80.00% 的**全部**未覆盖行）；`spill_register_flushable.sv:99` `flush_valid: flush_i \|-> ~valid_i`（Assert 0.00%，Attempts 2,912,000/Real Successes 0——**vacuous 非 failing**）；另 `fifo_v3.sv:122` flush 复位分支（Branch IF-117 `0 1`）、`spill_register_flushable.sv:78/79/84/85` 含 `flush_i`/`!flush_i` 的 Cond 项、`rr_arb_tree` Toggle 端口 `flush_i` 位——均 flush 相关结构分量（bin-scoped；这些格的**非-flush 余量**另路由，见 REV-031 §5） | A | `flush_i` 在本 xbar 例化闭包**全部**例化点硬接字面 `1'b0`（rev 亲验 `grep`）：`spill_register.sv:38`（唯一 `spill_register_flushable` 实例源）、`axi_demux_simple.sv:273/391`、`axi_mux.sv:272/324/417`。本集成从不对外暴露 flush 能力，flush 前提永假 → 上列 line/assert/bin 结构不可达/vacuous，任何合法激励不可达 | 任一例化点把 `flush_i` 改为运行时可驱动非 0（本集成引入 flush 能力）——即时作废重裁 | `doc/review/REV-031.md` §1（rev 亲验 flush_i 全例化点 tie-off + rr_arb_tree Line 表 159/182/233 + spill_register_flushable Assert 明细 2,912,000/0 + fifo_v3 Branch IF-117） |
| CW-011 | `lzc` Toggle | 内部信号 `gen_lzc.index_lut[5:0][2:0]`（编译期常量 LUT，`lzc.sv:67` `index_lut[j]=(NumLevels)'(j)`，18 位恒定）+ `gen_lzc.sel_nodes[7:6]` 与超范围 `gen_lzc.index_nodes[6:7]`（`lzc.sv:86-89` `g_out_of_range` tie `1'b0`/`'0`）——占 Toggle 42.59% 未覆盖信号位主体；端口 in_i/cnt_o/empty_o 已 100%。少量在范围内部节点位属仲裁模式定向（DV-F），但即便全闭仍 <90%，故不单列 | A | (i) `index_lut` 是编译期常量查找表，任何激励恒定不可翻；(ii) `g_out_of_range` 分支把超范围树节点恒赋常量，仅因 `WIDTH`(=NumIn=6/9) **非 2 的幂**而存在。二者合计使本格 Toggle 上界结构性远低于 90%（urg 亲证：即便在范围节点全闭，covered ≤ ~60/132 ≈ 45%<90%） | 任一被推翻即作废：(i) LZC 实现改 `index_lut` 为非常量；或 (ii) `NumIn` 取 2 的幂消除 padding（注：index_lut 常量位为设计固有，(ii) 亦不解锁之） | `doc/review/REV-031.md` §2（rev 亲验 `lzc.sv:67/86-89` + urg lzc Toggle Signal Details：index_lut/sel_nodes[7:6]/超范围 index_nodes = No，端口 100%） |
| CW-012 | `axi_id_prepend` Toggle | `pre_id_i[2:0]`（No/No/No，6 个 bit-方向）——`axi_mux.sv:227` `.pre_id_i(switch_id_t'(i))` generate-loop 编译期常量，每实例固定=i、单实例内永不翻转。**仅此结构位**；同格 `slv_b_readies_i`/`mst_b_readies_o`（4 个 bit-方向）属 B 背压定向可达（REV-030 DV-C 同根），**不入本豁免**、路由 DV-C（REV-031 §5） | A | `pre_id_i` = generate-loop 常量（性质同 CW-002 `test_i`/CW-007 `size[2]`），结构上单实例内不可翻。本格 46 位仅此 6 bit-方向常量死——**即便 DV-C 关闭 b_readies（→ 42/46=86.96%）本格仍恒 <90%**，pre_id_i 常量使本格永不达标 | `axi_mux` 例化改为运行时可变 `pre_id_i`（当前 generate-loop 常量），或 ID 前缀宽度配置使该位可驱动——即时作废 | `doc/review/REV-031.md` §3（rev 亲验 `axi_id_prepend.sv:35/89` + `axi_mux.sv:227` + urg axi_id_prepend Toggle Port Details） |
| CW-013 | `counter` / `delta_counter` Toggle | **结构 tie-off 位**：`i_counter_open_w`（`axi_demux_simple.sv:236-249`，WIDTH=4×6 例）的 `clear_i`/`load_i`/`d_i[3:0]`（硬接 `1'b0`/`'0`）+ `overflow_o`（`STICKY_OVERFLOW=0` 且 `w_open` 受 CW-009 封顶从不溢出）；`i_r_counter`（`axi_err_slv.sv` i_r_counter，WIDTH=8×6 例）的 `down_i`（硬接 `1'b1`，down-only）。**其余定向可达位不入本豁免**：`i_r_counter` 的 `d_i[7:2]`/`q_o[7:2]`（=未命中读 `len` 高位，DV-A 同根）、`delta_counter` id-bucket 96 例在飞高位（DV-G 同根）——路由 DV，见 REV-031 §5 | A | rev 亲验例化点常量 tie-off：`i_counter_open_w` `.clear_i(1'b0)`/`.load_i(1'b0)`/`.d_i('0)`（`axi_demux_simple.sv:242/244/245`）、`i_r_counter` `.down_i(1'b1)`（`axi_err_slv.sv`）对应端口位结构不可翻；`overflow_o` 因 `w_open` 受 mux w_fifo 深度封顶（CW-009）从不置位。这些 tie-off 位占本格未覆盖主体，使 `counter`/`delta_counter` Toggle **即便定向关闭 r_counter/id-bucket 值域仍恒 <90%** | 任一被推翻即作废：例化点把 `clear_i`/`load_i`/`d_i`/`down_i` 改为运行时可驱动（当前常量），或 CW-009 的 w_open 封顶被解除使 overflow 可达 | `doc/review/REV-031.md` §6（rev 亲验 `axi_demux_simple.sv:236-249` + `axi_err_slv.sv` i_r_counter 例化 tie-off + urg counter Toggle WIDTH=4/WIDTH=8 Port Details） |
| CW-014 | `stream_register` Line + Branch + Toggle（`axi_atop_filter` 内 `r_resp_cmd`，6 例：`gen_slv_port_demux[0..5].i_axi_err_slv.genblk1.i_atop_filter.r_resp_cmd`） | **Line 75.00%（9/12）3 行**：`stream_register.sv:37` 37.9 `valid_o<=1'b0`@`if(clr_i)`、`:38` 38.9 `data_o<=T'('0)`@`if(clr_i)`、`:38` 38.11 `data_o<=data_i`@`else if(reg_ena)`。**Branch 50.00%（4/8）4 支**：L37 `0 1 -`（clr_i）、L37 `0 0 0`（MISSING_ELSE：ready_o=0 保持）、L38 `0 1 -`（clr_i）、L38 `0 0 1`（reg_ena 载入）。**Toggle bin-scoped 22.00%（11/50）——未覆盖 39 bit 中的 31 bit Kind-A**：`clr_i`(2)/`testmode_i`(2)、`rst_ni` 1→0(1)、`valid_i`(2)/`ready_o`(2)/`valid_o`(2)/`ready_i`(2)/`data_o.len[7:0]`(16)/`reg_ena`(2)。**`data_i.len[7:4]`（8 bit，两向）不入本豁免**——`assign r_resp_cmd_push.len = slv_req_i.aw.len`（`:362`）组合馈通、与 push 无关，**定向可达**（长译码未命中写 burst，len≥16），路由 DV（REV-033 §DV 构造指引，DV-A err_slv 宿主族） | A | 三前提逐 bin 归因（rev 亲验 RTL + urg modinfo L2611-2762）：**(P1) clr_i/testmode_i 结构 tie-off**——`axi_atop_filter.sv:353/354` 例化点硬接 `.clr_i(1'b0)`/`.testmode_i(1'b0)`，clr_i 恒假使 `if(clr_i)` 赋值支永不取 + clr_i/testmode_i toggle 位不可翻（同 CW-010/CW-013 tie-off）；**(P2) push-gate 环境约束（同 CW-001 INJECT_R 根因）**——push `r_resp_cmd_push_valid`（`:147`）仅在 `slv_req_i.aw.atop[ATOP_R_RESP]`（=`atop[5]`；`axi_pkg.sv:447` 读返回原子 AtomicLoad/Swap/Compare）AW 抵达 atop_filter slv 口时置位，全 6 例 atop_filter 在 err_slv 内（`axi_err_slv.sv:45-58`），该 AW 仅经译码未命中路径抵达——恰为 spec §4 clause 7 环境约束（送未命中地址 AW 恒 `atop≡'0`，BUG-0032/0039）所禁；链式：`valid_i≡0`→`valid_o≡0`→`ready_o=ready_i\|~valid_o≡1`→`reg_ena≡0`→data_o 不载入、pop 侧 valid_o/ready_i 不置位，使 Line 38.11、Branch L37 `0 0 0`/L38 `0 0 1`、Toggle 26 bit 不可达。**M2-AT01 原子激励不解锁**：其原子送命中地址、从不入 err_slv；触达本模块须原子送未命中地址=约束所禁。BUG-0044 非-load 子类型 oracle debt 对本条无关（任何原子到 err_slv 皆被挡，不分子类型）；**(P3) rst_ni 1→0 运行中复位**——0→1 已覆盖，1→0 须运行中重断言复位，spec §2.3 无热复位恢复语义（同 CW-006 rst_ni） | 任一具体事实被推翻即作废重裁：(i) `axi_atop_filter.sv:353/354` 把 clr_i/testmode_i 改为运行时可驱动非 0；(ii) 补 err_slv×ATOP 应答许可来源 + 重开 BUG-0032，使读返回原子可合法送未命中地址（同 CW-001 解锁）；(iii) 验证范围纳入运行中复位测试且先补 spec §2.3 热复位语义（同 CW-006 解锁）。**data_i.len[7:4] 非本豁免解锁项**——其定向可达，DV 补一笔 len≥16 译码未命中写 burst 即闭合该 8 bit，不改本豁免 31-bit scope | `doc/review/REV-033.md`（rev 亲验 `stream_register.sv` 全文 + `axi_atop_filter.sv:143/147/353-354/362` + `axi_err_slv.sv:45-58` + `axi_pkg.sv:447` + spec §4 clause 7 + urg modinfo.txt L2611-2762 三类明细；承 REV-032 轴4 发现独立重走，订正其 "(非-load ATOP)" 表述为读返回原子 `atop[5]`） |

**注**：CW-001~CW-014 均为 Kind-A，论证已就绪（CW-010~013 由 REV-031 登记：flush 能力未暴露 / lzc 常量 LUT / axi_id_prepend generate 常量 / counter tie-off，四类结构不可达根因）。**CW-014 由 REV-033 登记**：`stream_register`（`r_resp_cmd`）三前提——clr_i/testmode_i tie-off（P1）+ push-gate 环境约束（P2，同 CW-001 INJECT_R 根因）+ rst_ni 运行中复位（P3，同 CW-006）；其中 **Line/Branch 全 Kind-A、Toggle 为 bin-scoped**——`data_i.len[7:4]`（8 bit）定向可达另路由 DV（DV-A err_slv 宿主族），不入本豁免。**Kind-B 模板行经逐位分解
（`doc/evidence/v0.4.15/M4-toggle-bit-decomposition.md`）+ 独立 rev 复核
（`doc/review/REV-025.md`）后，结论是当前 M4 阶段的合法 Kind-B 豁免集为
**空集**——唯一有结构依据的候选（axi_mux R.data）被证实实为地址派生位、
定向饱和读（all-0/all-1 两向量）即可闭合，不满足"纯定向不经济"这一 Kind-B
判据。故模板行继续保留但不实例化任何具体信号，供未来若真的出现合规 Kind-B
候选时使用。

## 待建档项（Kind-A 候选，尚未走 rev 卡确认，非本表正式条目）

- ~~`spill_register` 参数 tie-off 的 bypass 路径~~ —— **REV-031 §5 评估为非缺口，撤项**：
  urg 亲证闭包内 `spill_register`/`spill_register_flushable` **同时**存在 `Bypass=0`
  与 `Bypass=1` 两种参数实例（modinfo 两参数块均有 self-instances），故 `gen_bypass`/
  `gen_spill_reg` 两分支在模块页合并粒度下**均被例化**、bypass 路径非结构死，不构成
  `<90%` 归因，无需登记。
