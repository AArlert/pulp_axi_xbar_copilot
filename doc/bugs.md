# Bugs

States: OPEN/FIXING/FIX_READY/VERIFYING/CLOSED/TB_BUG/SPEC_CHANGED/WONTFIX. Debug stories longer than one line get a detail page doc/bugs/<ID>.md — contract: workflow/fail/failure_record.md.

| id | status | suspect | summary | min_repro | root_cause | fix_commit | verify_evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-0062 | OPEN | TB | **`doc/review/REV-035.md` 的裁决论证里有两处经不起回源核对的事实陈述，其一被用作"必须重写而非删句"的理由**（BUG-0050/0051 的 arch fixer 在按卡内"每个数字自己回源、不得照抄"要求执行时发现并拒绝照抄）：**(F2，实质)** REV-035 §0 行 F 与 §Q2(a).2 称"`lzc.sv` L39-103 全 assign + generate、**无一个 `always` 块**（亲读）"——实测 `grep -n always vendor/common_cells/src/lzc.sv` 得 **`:56 always_comb begin : flip_vector`**，陈述失实。其**结论仍正确**（原脚注 3 的"过程块"预设确实不成立），但**理由必须换**：真正原因是该 `always_comb` 在 `:54 if (MODE)` 内，而本闭包两个 `lzc` 实例（`rr_arb_tree.sv:202-205`/`:211-214`）**均 `.MODE(1'b0)`**（urg 只精化 `MODE=0` 两变体）⇒ `g_flip` 未精化。**若照 REV-035 原措辞写进成因，等于制造第三个未核实成因**——即 BUG-0051 那条缺陷的第三次复发，只不过换了个载体。**(F1，无损)** §Q1(i) 称 CW-010"论证列现把 `fifo_v3.sv:122` 并列在 Cond 项里"——原 bin 列逐字为"`fifo_v3.sv:122` flush 复位分支（**Branch IF-117 `0 1`**）…、`spill_register_flushable.sv:78/79/84/85` … 的 Cond 项"，`:122` 自带 Branch 限定括注，该列**原本即自洽**；fixer 据此拒绝"订正一处并不存在的错误"，未造成损害。**本条的价值在于它是隔离链起作用的直接证据**：rev 的事实错误被下游独立回源拦下，而拦下它的正是"不得照抄裁决"这条卡内纪律 | n/a（非仿真；复现 = `grep -n always vendor/common_cells/src/lzc.sv` 得 `:56`，对照 REV-035 §Q2(a).2；`sed -n '22p' doc/coverage-waivers.md` 读 CW-010 bin 列原文，对照 §Q1(i)） | rev 亲读 RTL 时按"通读印象"下了全称否定判断（"无一个 `always` 块"），未用 `grep -c` 之类确定性查询兜底——与 BUG-0051 (ii)(iii) 的成因同形（声明未与其所依据的机器事实核对），只是这次载体是**裁决记录**而非证据文件。REV-035 已随 `7f73451` 入库、属冻结记录，处置面须依 REV-035 自己为 BUG-0053 立的判据裁（"删掉它，未来读者会失去可读的信息吗？"——此处是**有信息但信息错**，与工具标记不同类） | - | n/a — OPEN，待 rev 裁决（须新实例，不得由 REV-035 那个实例自裁；closer ≠ fixer） |
| BUG-0061 | OPEN | TB | **4 条 `regression_guard` 的 `paths:` 字段被中文标点污染，导致它们从未真正匹配过任何文件——guard 的声明作用域与机器作用域不符**（BUG-0050/0051 的 arch fixer 在订正 BUG-0044 guard 时发现，实测非推断）：`scripts/docs.py:1156` 用 `re.split(r"[,\s]+", …)` 切 glob 列表，而中文顿号 `、`、全角括号 `（）` 与中文限定词**都不是分隔符**，整段被并成一个永不匹配的废 token。实测 `make guards FILES=<声明的 path>` 命中数：**BUG-0043 `scripts/regress.py` → 0** · **BUG-0044 `doc/design-prompt/verification_maturity.md` → 0**（本次已随 BUG-0050 订正修好，现为 1）· **BUG-0045 `tb/xbar_types_pkg.sv` → 0** · **BUG-0047 `doc/milestone.md` → 0**。**最刺眼的是 BUG-0047**：其 guard 第三款白纸黑字写着"任何后续重开 M4 签核卡时，须核对本条 Kind-B 豁免是否已附带到期解锁核对"，而 `make guards FILES="doc/milestone.md"` 实测输出 **`0 guard(s) matched`**——**M4 签核卡编辑 `doc/milestone.md` 时，这条专为它写的 guard 根本不会打印**。它此前之所以在本会话的组卡中出现过，是靠同行另一个干净 token `doc/coverage-waivers.md` 命中的，与 `doc/milestone.md` 无关。危害面：guard 是本框架"教训不丢失"的唯一机械载体（七问 Q6 Retention），一条不匹配的 guard 与不存在无异，但它**看起来是存在的**——比缺失更坏 | n/a（非仿真；复现 = `make guards FILES="doc/milestone.md"` 得 `0 guard(s) matched`，而 `doc/bugs/BUG-0047.md` 的 `regression_guard.paths:` 第一项写着 `doc/milestone.md（M4/M5 节）`） | `paths:` 字段的书写惯例（中文顿号分隔 + 括注限定范围）与 `docs.py` 的机器解析口径（仅按逗号/空白切分）互不知情，且**没有任何检查覆盖"guard 的 paths 是否真的能匹配到东西"**——一条 paths 全是废 token 的 guard 与一条正常 guard 在 `make check` 下完全同形。与 BUG-0054/0058/0060 同谱：机制建好了，但判据只覆盖了一条通路 | - | n/a — OPEN，待派 fixer（订正三条剩余 paths + 评估给 `docs.py` 加"paths 零匹配即警告"；BUG-0044 那条已修；closer ≠ fixer） |
| BUG-0060 | OPEN | TB | **`scripts/evidence.py` 先写证据文件、后校验 bug 行是否存在——一次 id 笔误会在 `doc/evidence/` 留下永久孤儿工件，且 `make check` 检不出来**（BUG-0048 的独立 closer 在阶段 B 实测撞上，非静态推断）：实跑 `make evidence BUG=0048 …`（正确形式为 `BUG=BUG-0048`）得到 `evidence written: doc/evidence/v0.4.38/0048.log` → `no row with id 0048 in bugs.md` → exit 2。机理：`scripts/evidence.py:203-213` 以 `rid = args.bug` 直接命名并**写盘**，`:214` 才调 `update_row` 去查行——**写在校验之前**。结果：退出码是红的，但磁盘上多了一份内容完整（`exit_code: 0`、签名命中）却不被任何 bug 行引用的证据文件。**危害静默性经 orch 受控实验证实**：往 `doc/evidence/v0.4.38/` 放一份构造的孤儿 `.log` 后 `make check` 仍打印 `docs-check passed`——`docs.py:527-531` 的孤儿检查只覆盖 `doc/bugs/*.md` 详情页，**对 `doc/evidence/` 无任何对应检查**。故一份"看起来完全合格"的证据可以长期游离在账目之外，签核清点时既可能被当成有效证据、也可能被当成漏账线索。**与 BUG-0049/0050/0058 同族**：声明面与事实面不一致且无机器核对。次要面：id 形式不统一——行 id 是 `BUG-0048`，`.claude/agents/dv.md` 写简写 `BUG=<id>`，根 `Makefile:48` 注释示例用完整形式 `BUG=BUG-003`。本次孤儿文件已由制造它的 closer 自行删除（discipline §3「只清自己的烂摊子」），留证于 session scratchpad | n/a（非仿真；复现 = `make evidence BUG=<不存在的id> CMD='echo x' EXPECT='x'` → 脚本退非零但 `doc/evidence/v<ver>/<不存在的id>.log` 已落盘；再跑 `make check` 仍绿） | 脚本的动作顺序是"跑命令 → 写盘 → 查行 → 翻列 → docs-check"，把**唯一的输入校验**放在了写盘之后。相邻风险同源：若失败点再晚一步（落在 docs-check 阶段）台账就会半改——两种失败模式的分界线很薄，宜一并收口成"先校验、后写盘、最后翻列"。`scripts/evidence.py` 属上游文件，改动须登 `doc/fw-feedback.md` FB 行 | - | n/a — OPEN，待派 fixer（先校验后写盘 + id 形式兼容 + evidence 孤儿检查；须附注伤自证；closer ≠ fixer） |
| BUG-0059 | OPEN | TB | **子代理 scratchpad 未按实例隔离——同一目录下混放多个角色实例的中间产物，closer 可直接读到 fixer 的推理过程，构成共模隔离的实质漏洞**（BUG-0048 的独立 closer 实例在执行中发现并报出；该实例声明自己**未读**这些文件、全部工作放在自建子目录 `closer0048/` 下）：本会话分配的 scratchpad `/tmp/claude-1000/<repo>/<session-uuid>/scratchpad/` 被**所有**子代理共用，实测其中同时存在 BUG-0048 fixer 的 `gone_sites.txt`/`new_sites.txt`/`bl_mut.md`/`lint-baseline.new.md`、BUG-0050 arch fixer 的 `arch0050_modinfo.py`、orch 的 `orch-findings.md`/`guards-*.txt`、以及 closer 自己的 `closer0048/`。CLAUDE.md §0 的隔离要求是"**DV 绝不读 DE 的推理过程（卡中只传文件路径、章节号、行 id）**"、"共模误差是大敌"——卡内传参做到了防火墙，**文件系统侧没有**。历史对照：`doc/evidence/v0.4.6/BUG-0040.log` 的 CMD 里嵌着另一个 session uuid（`292cdefc-…`），可见 uuid 本应逐 session 变、但**同一 session 内的所有实例共用一个**。危害不是理论：一个实例只要 `ls` 一下就能看到另一个实例正在算什么、算到哪一步 | n/a（非仿真；复现 = 在任一子代理内 `ls -la $SCRATCHPAD`，可见其他实例的产物） | 隔离纪律只在**卡片传参**这一层实现（dispatch 契约的共模防火墙），未覆盖**文件系统**这一层；orch 派卡时也未为每个实例分配独立子目录（本会话 orch 自己就往共享根目录写了 `orch-findings.md` 与四份 guards 快照）。属"机制建好了但只覆盖了一条通路"，与 BUG-0054 同谱 | - | n/a — OPEN，待裁决（orch 侧派卡约定 vs dispatch 契约补一条；closer ≠ fixer） |
| BUG-0058 | OPEN | TB | **`sim/lintdiff.py` 对"幽灵站点"方向不判红——基线若认领了实测根本不存在的站点，门禁只打印不阻塞**（BUG-0048 独立 closer 报出）：`sim/lintdiff.py:65-67` 只在 `new = 实测 − 基线` 非空时 `sys.exit(...)`；`gone = 基线 − 实测` 仅在 `:59-60` 打印 `[LINT-DIFF] 已消失（不阻塞，可在分诊后并入基线）`，**不进退出码**。后果：基线里的凭空条目不会被门禁发现，而它会让未来某个**真**新站点被误认为"已知"（若其三元组恰好撞上一条幽灵行）。**与 BUG-0050 完全同形**——那条是覆盖率豁免认领了物理不存在的 bin（CW-010 的 `fifo_v3` Cond），这条是 lint 基线可以认领不存在的站点，两者都是"声明面 ⊋ 事实面"且都无机器核对。当前实测幽灵站点为 0（orch 与 closer 各自独立算过），故**无现行账目损坏**，是机制缺口而非数据缺口。closer 本次靠签名设计（`exit 0` 蕴含 `实测 ⊆ 基线` + 签名钉死两侧基数相等 ⇒ 集合相等）用算术补住了这一次关闭，但那不覆盖下一次 | n/a（非仿真；复现 = 读 `sim/lintdiff.py:59-60` 与 `:65-67`，`gone` 不进 `sys.exit`） | 差分器写作时的判据是"不得引入新告警"，单向即可；"基线不得含凭空条目"这条判据当时没被想到，直到 BUG-0050 在覆盖率账目上暴露同形问题才显形。`scripts/regress.py` 自 0.8.0 起归本项目所有，`sim/lintdiff.py` 亦为本项目文件，订正不涉上游 | - | n/a — OPEN，待派 fixer（`gone` 非空时退非零，或输出可机器核对的等式；须附注伤自证；closer ≠ fixer） |
| BUG-0057 | OPEN | TB | **`doc/evidence/v0.4.6/BUG-0040.log`——一条已 CLOSED 缺陷的关闭证据首行不可重放，且其签名是自印口令**（BUG-0048 独立 closer 报出，BUG-0048 fixer 独立确认同一现象）：该文件首行为 `CMD: bash /tmp/claude-1000/<repo>/292cdefc-1c37-4e09-b45b-4098f085a2b2/scratchpad/bug0040_verify.sh` —— 指向一个**早已结束的 session 的 scratchpad 脚本**（当前 session uuid 为 `ecca52b6-…`，`292cdefc-…` 目录不存在），**任何人无法从仓库重放它**，直接违反 CLAUDE.md 不变量 2（"记录首行即重放命令"）与 `workflow/review.md` 七问 Q3（"Could a stranger reproduce this run from the repo alone?"）。第二行 `expect: BUG0040_VERIFIED_CLEAN` 是**该脚本自己 echo 的口令**，不携带任何门禁信息——**任何以 `echo BUG0040_VERIFIED_CLEAN; exit 0` 结尾的脚本都能生成它**，签名可伪造 = 签名设计失败。BUG-0040 因此是一条"看起来已闭环、实则证据链断裂"的 CLOSED 记录。**本条的发现路径本身值得记**：BUG-0048 的 fixer 正是照着这份先例的签名体例设计自己的复验命令（自印口令 + 两个 `grep -q` 前置），被独立 closer 判为弱于把数字钉进签名而改掉——**坏先例在被发现前已经繁殖了一代** | n/a（非仿真；复现 = `head -2 doc/evidence/v0.4.6/BUG-0040.log`，再 `test -e` 其 CMD 指向的路径 → MISSING） | `make evidence BUG=… CMD=… EXPECT=…` 对 CMD 的内容与 EXPECT 的来源**均无约束**：`scripts/evidence.py:186-217` 只要求命令退出 0 且输出含 EXPECT 正则，不检查该命令是否在仓库内可重放、也不检查签名是否来自被测门禁本身而非命令自印。机制的判据是"跑通了"，意图是"别人能重跑并看到同样的判定" | - | n/a — OPEN，待裁决（BUG-0040 已 CLOSED 且冻结，重开 vs 追加勘误 vs 补一份可重放证据，须 rev 裁；同时评估是否给 `evidence.py` 的 CMD 型证据加判据；closer ≠ fixer） |
| BUG-0056 | OPEN | TB | **`doc/bugs/BUG-0040.md` 与 `doc/bugs/BUG-0048.md` 的 `regression_guard.ref` 字段写死了一条会摧毁 M4 取证基础的破坏性命令**（BUG-0048 的 DV fixer 卡在执行中亲验发现，本卡因卡内明令禁止 `make clean` 而绕开——**绕开不构成豁免**，CLAUDE.md §2）：两条 guard 的 `ref:` 均为 `cd sim && make clean && make lint-diff TEST=<any baseline TEST>`，而 `sim/Makefile:187` 的 `clean:` 是 `rm -rf $(OUT)`（`$(OUT)`=`sim/out`），会连带删除 `sim/out/{,m0/,cfgA..cfgE/}cov.vdb` 共 **7 个覆盖库**；`sim/out/` 在 `.gitignore` 第 3 行，**不在版本控制内、删除不可恢复**。这 7 个库是 M4 覆盖率取证的全部基础。**修复面追加（BUG-0048 closer 阶段 B 报出）**：同一条破坏性命令还写在 `doc/bugs.md` **BUG-0048 行的 `min_repro` 单元格**里，而该行现已 CLOSED——一条已关闭记录的"最小复现"规定了一条 closer 根本没用过的破坏性命令，下一个照 `min_repro` 重放的人仍会踩。三处（两份详情页 `regression_guard.ref` + 该 `min_repro` 单元格）须一并订正。**关键点：那一步对 guard 的目的零贡献**——`lint-run:` 目标自身第一条动作即 `rm -f $(OUT)/simv_lint.daidir/.vcs.timestamp` 强制全量重编，并逐文件校验 `Parsing '../tb/<f>'` 作执行证明（BUG-0022 修复内容，`sim/Makefile:141-165` 注释明写）。即一条**写进版本控制、要求后人照跑的复验程序，本身会摧毁它所要复验的证据基础**，且是纯代价无收益。下一个照抄它的实例恰为 BUG-0048 的 closer | n/a（非仿真；复现 = 读 `sim/Makefile:187` 的 `rm -rf $(OUT)` + `.gitignore:3` 的 `sim/out/` + 两份详情页的 `regression_guard.ref` 字段。**破坏性动作未实际执行**，判断基于静态阅读） | guard 的 `ref:` 字段写作时（BUG-0021/0040 时代）`sim/out/` 下尚无需保护的长寿命产物，`make clean` 只是"确保干净重跑"的保守习惯；BUG-0037 修复分出 7 个隔离覆盖库、M4 又把它们变成签核取证基础之后，同一条命令的代价发生了性质变化，而 guard 文本从未随之复核。无任何机制覆盖"guard 自身的 ref 命令是否仍安全" | - | n/a — OPEN，待派 fixer 订正两份详情页的 ref 字段 + note（closer ≠ fixer） |
| BUG-0055 | OPEN | TB | **本项目与上游框架共用同一 tag 命名空间，M4 关门要打的 `v0.5.0` 已被上游 tag 占用——`git tag v0.5.0` 会直接失败**（orch 组卡期机核发现，尚未触发因为还没走到打 tag 那一步）：`for t in $(git tag); do git branch --contains $t; done` 实测 24 个本地 tag 中**只有 5 个在 master 上**（`v0.1.0` M0 签核 / `v0.2.0` / `v0.3.0` M2 签核 / `v0.4.0` M3→M4 转段 / `v0.4.2`），其余 19 个（`v0.2.1`、`v0.3.1~3.3`、`v0.4.1`、`v0.4.3~4.6`、**`v0.5.0`~`v0.5.4`**、`v0.6.0`、`v0.6.1`、`v0.7.0`、`v0.7.1`、`v0.8.0`）是经 `git fetch upstream`（CLAUDE.md §5 的跟进流程）进入本地 tag 命名空间的**上游框架 tag**。`v0.5.0` 现指向上游 commit `fb2c193`。而 `doc/milestone.md` / `doc/log.md` [0.4.37] / `.claude/skills/closeout/SKILL.md` 步骤 2 三处均写明 M4 关门须 `make bump minor=1` + `git tag v0.5.0`。**碰撞是必然而非偶然**：两条版本序列都从 v0.1.0 起步、共用一个命名空间、上游跑得更快，此前只是恰好没撞同名（上游 v0.4.1/v0.4.3~4.6 已穿插在本项目 v0.4.0/v0.4.2 之间） | n/a（非仿真；复现 = `git rev-parse v0.5.0` 得 `fb2c193…`；`git branch --contains v0.5.0` 输出为空即"不在 master 上"） | CLAUDE.md §5 的上游跟进流程 `git fetch upstream` **默认连 tag 一起拉**（git 的 `--tags` 隐式行为：fetch 会带回指向已获取对象的 tag），而两个项目的版本号序列互不知情地共用一个扁平命名空间。处置面需裁决：本项目 tag 加前缀 / `git fetch upstream --no-tags` + 清理已污染的 19 个 / 上游 tag 移入独立 refs 命名空间 / 其他 | - | n/a — OPEN，待 rev 裁决处置面（涉及已入库 tag 的去留，非纯机械订正；closer ≠ fixer） |
| BUG-0054 | OPEN | TB | **`make next` 把"存在 `signoff-M4*.md` 文件"读成"M4 三条硬条件已满足"，而仓内唯一那份签核书的裁决是 REJECTED**（REV-035 的 rev 实例在执行 BUG-0050/0051/0053 裁决卡时亲验发现，与该卡本体无关）：`scripts/docs.py:755` 的 `if not signoff_file_exists(mnum)` 只查**文件存在性**、**不读一个字**（`docs.py:312` + `iverif_config.py:137` `signoff_glob="signoff-M{m}*.md"`）。实测三条互相矛盾的输出同时成立：`make next` 打印"6. M4 three hard conditions met → make bump-minor + git tag v0.5.0"；`make check MILESTONE=4` 打印 `[FAIL] 3. ... active: BUG-0053, BUG-0052, BUG-0051, BUG-0050, BUG-0048`；而 `doc/evidence/v0.4.13/signoff-M4.md`（**全仓唯一** `signoff-M4*`）第 9 行与第 257 行均写 **"REJECTED —— M4 尚不具备签核条件"**。**危害面**：`make next` 坐在每次会话开头被信任的位置（CLAUDE.md §2 工作循环第二步），它把一份 REJECTED 的签核书读成"可以进下一个里程碑"。当前未造成误签只因 orch 走了 `make check MILESTONE=<n>` 双门（CLAUDE.md §3 明写二者缺一不可） | n/a（非仿真；复现 = `make next \| tail -1` 得"M4 three hard conditions met"，同时 `sed -n '9p' doc/evidence/v0.4.13/signoff-M4.md` 得 REJECTED） | 签核判定的机器侧只实现了"载体存在"，未实现"载体的裁决为何"——与 FB-25「`required_files` 只查存在性」同族、FB-11/FB-21 同谱（机制建好了但没接上消费者）。判据与意图不符：rubric 的签核条件是"rev 判过且判过了"，脚本落成了"rev 写过文件" | - | n/a — OPEN，待裁决（本项目侧订正 vs 上游 `scripts/docs.py` 改动须登 FB 行；closer ≠ fixer） |
| BUG-0053 | OPEN | TB | **`doc/review/REV-033.md` 文件尾混入工具标记 `</content>` / `</invoke>` 并随 `861c7f8` 入库**（REV-034 收卡时发现；`grep -rn` 全仓仅此一处）。**补充实证**：REV-034 的 rev 实例在写自己的记录时**当场复现了同一泄漏**（落盘后自检发现并删除）——说明这是系统性写作陷阱而非一次性疏忽，值得一条 guard 而不只是一次清理。处置须 rev 裁决：`doc/fw-feedback.md` FB-23「冻结记录不回改」与「记录保真度」两条原则在此相撞（REV-033 已 commit） | n/a（非仿真；复现 = `tail -3 doc/review/REV-033.md`） | 评审记录由 subagent 直接落盘，无任何落盘后的标记残留自检；FB-23 使已入库记录默认不回改，于是一次泄漏即永久留存。**裁决回填（`doc/review/REV-035.md` §Q3；现象经 rev 亲验成立，全仓仅 REV-033.md:187-188 两行、均为独立整行）**：建议终态 **CLOSED**。**(a) 该删。** 理由：它**不是记录**——不是判断、不是论证、不是观测数据、也不是"当时为真的指针"，删除它移除零信息，FB-23 在此**没有保护对象**。与 0.4.37 对 BUG-0047 详情页"追加不回改"的做法**不冲突，因为对象不同类**：那次被保护的旧陈述（"无 spec 正文改动"）在 REV-024 只走选项 (i) 时确为事实、保留有信息价值；工具标记没有对应价值，且留着会向每位未来审计者公开宣告"这份一手件落盘后没人看过尾巴"，损的是**可信度**而非保真度。**边界须写死**（防本裁决被当成"清理"通行证）：允许删除的**仅限非记录内容**——由写作工具/管道产生、不承载任何作者陈述的字节；判据 = "删掉它，未来读者会失去可读的信息吗？"存疑一律走追加。最小实现：**不要**在 REV-033 里加"本文件曾有工具标记"的自述，证据链在 git，commit message 记明即可。**(b) guard 可机械化，须立 `type: script` 而非 checklist**（checklist 是机械化 TODO，把今天能关的门写成 TODO 等于留开它）：`paths:` = `doc/**/*.md`（消费面是全部落盘记录，泄漏源是写作工具、与目录无关，本次落在 review 是偶然——此点须写进 note）；`ref:` 方向是**挂进 `docs-check`**（`scripts/docs.py`，`make check` 每张卡必跑）而非新建独立脚本（新入口 = 没人跑的机制，FB-21 同族）；`note:` 须写明判据是"**行首起、整行仅由该标记构成**"而非子串匹配——子串会在引用这些标记的场合自撞（REV-035 正文即引用了它们），整行匹配对两次实例 100% 命中且零自撞。**closer 的不可省动作**：若 guard 由 fixer 写，closer 必须**证伪它一次**（throwaway 处放一行该标记、确认 `make check` 变红后恢复），否则它是"从未见过红的假设"（rubric #6）。closer ≠ fixer；非仿真类 ⇒ BUG-0029 guard 适用 | - | n/a — OPEN，待 rev 裁决处置面（订正 vs WONTFIX + guard） |
| BUG-0052 | OPEN | TB | **四处框架文档指向不存在的 `workflow/` 路径（上游 0.8.0 目录合并后的漂移）**：`.claude/agents/rev.md:18-20` 引 `workflow/review/six_questions.md`、`workflow/review/rubric.md`、`workflow/fail/`；`.claude/agents/dv.md:63` 引 `workflow/fail/*.md`；`.claude/skills/dispatch/SKILL.md:45` 引 `workflow/review/`；`doc/bugs.md:3` 表头引 `workflow/fail/failure_record.md`。本仓库 `workflow/` 实有四文件（`bugs.md`/`discipline.md`/`records.md`/`review.md`），四个被引路径**全部不存在**（`test -e` 逐条实测）。REV-034 的 rev 实例是第一个撞上并靠卡内订正绕过的（若无订正，rev 将读不到七问与 rubric 而降级执行）。`doc/fw-feedback.md` FB-25/FB-27 记有现状、FB-27 明写"建议加路径保险"但未落地，`doc/bugs.md` 无行——按 CLAUDE.md §2 无条件登记规则补。注：`doc/bugs.md:3` 是本仓库自有文件，订正不涉上游 | n/a（非仿真；复现 = `for p in workflow/review/six_questions.md workflow/review/rubric.md workflow/fail/failure_record.md workflow/fail; do test -e $p ; done` 四条全 MISSING） | 上游 0.8.0 把 `workflow/review/*` 与 `workflow/fail/*` 合并进单文件 `workflow/review.md`/`workflow/bugs.md`，但引用这些路径的 agent/skill/表头文本未同步；无任何机器检查覆盖"文档内引用的仓内路径是否存在" | - | n/a — OPEN，待派 fixer（订正四处引用 + 登 FB 行；closer ≠ fixer） |
| BUG-0051 | OPEN | TB | **M4 覆盖证据 `doc/evidence/v0.4.35/M4-coverage-final-sweep.md` §2.3 三处事实性错误——实例数错抄 + 两处 N/A 成因与自身数据矛盾**（M4 签核前 orch 四路独立复算 + REV-034 收卡各自发现）：(i) `:128` `counter` 实例数列写 **108**，而该表表头自称"实例数据源：`hierarchy.txt` 精确计数"，实测 `i_counter_open_w` 6 + `i_r_counter` 6 = **12**；108 是下一行 `delta_counter` 的数（`i_counter` 12 + `i_in_flight_cnt` 96），系整行误抄，且被 `doc/design-prompt/milestone_restructure.md:422` §6.3 原样继承；其余 21 行实例数经逐行复算全部正确（DUT 实例合计 1094）。(ii) `:162-169` 脚注 3 标题写"`counter.sv`/`lzc.sv` 的 Line/**Cond**/**Branch** N/A"、正文写"亲验 `modinfo.txt` `lzc` 小节 Line/**Branch** 均为空白"，而同表 `:136` 的 lzc 实测 Cond=**97.73**、Branch=**97.73**（`modlist.txt:48` 复核无误，modinfo 中 lzc 实例的 Branch Coverage 段真实存在）——**lzc 只有 Line 一类是真 N/A**，该脚注对 lzc 的成因陈述整体不成立。(iii) 脚注 6 对 `stream_register` Cond N/A 的成因（"全文无 `if`/`case`/`?:`"）被同一份 `modinfo.txt` 的 Line/Branch 表证伪（其中列有 IF-37/IF-38 宏展开；Cond 确为 N/A，但成因写错）。`doc/milestone.md` M4 出口条件第二条明文要求"逐格 N/A（附**已核实**成因）"——**成因写错等同该格未满足该条**；实例数虽非判据，但它是签核底板的一部分且已被继承一次。**枚举追加 (iv)（arch fixer 发现）**：脚注 5 的括注"`stream_register.sv` 全文无 `if`"与脚注 6 同根、同样被 `modinfo` 证伪；该格（Assert）的判定与操作性成因不受影响（RTL `assert` 计数 = 0），已按同一形态就地标记并入勘误段 | n/a（非仿真；复现 = `grep -c i_counter_open_w sim/out/urgText6/hierarchy.txt` 得 6、`grep -c i_r_counter` 得 6，对照 final-sweep `:128` 的 108；`grep -n lzc sim/out/urgText6/modlist.txt` 得 Cond/Branch=97.73，对照脚注 3 正文） | §2.3 的实例数列与 N/A 成因脚注均为人工撰写，与其自称的数据源（`hierarchy.txt`/`modinfo.txt`）之间无机器核对；三处错误各自独立存活过 REV-031/REV-032/REV-033 三轮复核 + 本次四路独立复算中的三路，唯一抓到 (i) 的路径也曾把它合理化后放过。**裁决回填（`doc/review/REV-035.md` §Q2；三处现象全部经 rev 亲验成立）**：建议终态 **CLOSED**。**(1) 冻结的证据文件裁决 = 追加勘误段 + 就地加"只增不删"的勘误标记，一个字节都不覆写。** 理由：FB-23 的保护对象是"**写作时为真、因外部改名而失效**"的陈述（其原文自陈"不是谁做错了"）；本条三处是**写作时即为假**、且被它自己援引的同一份 `hierarchy.txt`/`modinfo.txt` 当场证伪的陈述，FB-23 不为错误提供庇护。但落盘公理仍管用 ⇒ 不得覆写，读者须能看见"当时相信什么"以及三轮复核为何放过——那才是本条的教训。**与 0.4.37 处理 BUG-0047 详情页（追加不回改）方向一致，一处更严**：那次被追加的是散文，读者必然读到文末；本条错误在**表格单元格与脚注正文**里，纯追加会留活陷阱，而该陷阱已现实咬过一次（§6.3 原样继承 108）。可引用边界：**"不回改" = 不覆写、不删除；不等于不许批注**（加标记是加法，覆写是减法）。**(2) `milestone_restructure.md` §6.3 是非冻结的设计输入 ⇒ 直接就地订正，不追加勘误**；且**订正优于删"例"列**——该列是 §2.3 与原始 hierarchy/modinfo 之间唯一的交叉核对锚点（本卡正靠重数它确认了"单格笔误、非整列失效"：其余 14 个模块实例数逐个吻合）。**(3) (ii)/(iii) 使 `(lzc, Line)` 与 `(stream_register, Cond)` 两格在 M4 出口第二条下判为未满足**——两格的**三态 N/A 判定本身独立成立**（modlist lzc Line 为 `--`、modinfo lzc 区无 Line 节；stream_register 区只有 Line/Toggle/Branch 三节），**但成因要件未满足**："被自己援引的数据源证伪的成因不是已核实的成因"。签核前必须各补一条**能被原始 urg 报告正向支持**的成因，两条硬警告：不得照抄脚注 4 的"无多项布尔组合式"规则去补 stream_register（`:34/:35` 有两条二元连续赋值，而 `spill_register_flushable.sv:78/79/84/85` 的连续赋值确实生成了 Cond bin，该规则解释不了本格）；不得只删脚注 3 的错句去补 lzc（其"generate 分支过程块"预设本身不成立——`lzc.sv` L39-103 全为 assign + generate、无 `always` 块）。另须对 8 条 N/A 成因脚注**每条至少一格**做"成因 vs modinfo 实有节"的正向抽样反证（成因维，非数字维）。closer ≠ fixer；非仿真类 ⇒ BUG-0029 guard 适用 | - | n/a — OPEN，待派 fixer 订正证据文件与其 §6.3 继承处（closer ≠ fixer） |
| BUG-0050 | OPEN | TB | **豁免/债务的"登记面"与下游"引用面"不一致——四处引用越界，与 BUG-0049（漏账）互为镜像**（M4 签核前 orch 四路独立复算 + 裁决者逐条核实发现）：(i) `doc/coverage-waivers.md:22` CW-010 的（模块,类型）列声称含"`fifo_v3` Branch/**Cond** … flush 位分量"，但 `vendor/common_cells/src/fifo_v3.sv:122` 的 `if (flush_i)` 是**单项**条件、VCS `-cm cond` 不为单项条件生成 bin（同报告 §2.3 脚注 4 自己确立的规则），实测 26 个 fifo 实例的 Condition 段源码行号集合恒为 {73, 88, 101}、**122 从不出现**——即**认领了一个物理上不存在的 bin**，并被 `doc/design-prompt/milestone_restructure.md:431` §6.3 照抄；(ii) CW-002（`:14`）登记列只写 `axi_xbar` Toggle，却被 §6.2/§6.3（`:424`/`:427`）列为 `axi_err_slv`/`axi_mux` Toggle 的承接者（`test_i` 端口在两模块确实存在，论证可平行成立，缺的是登记文本覆盖）；(iii) CW-007（`:19`）登记列为 `axi_mux`/`axi_demux_simple`/`axi_err_slv` Toggle，却被 §6.3（`:429`）列为 `rr_arb_tree` Toggle 的承接者；(iv) BUG-0044 承接 `axi_mux`/`axi_err_slv` Toggle 的 `atop[4]/[5]` 位这一归属**只**存在于 REV-030 与其下游转引，`doc/bugs.md` 行与 `doc/bugs/BUG-0044.md` 全文均未提及任何覆盖率格——单向链接，债务行改写或改状态时无机制提醒重裁。**四条均为"过度引用"而非"无人认领"，经逐格剥离有瑕疵引用后 UNOWNED 仍为 ∅（BUG-0049 的关闭不受影响）**，但削薄了 M4 出口第三条"具名归属"的诚实支撑。**枚举追加 (v)（arch fixer 完备性扫描发现，不在原四项内）**：`§6.3` 的 `fifo_v3` **Toggle** 格同样引 CW-010，而 CW-010 登记列从未含该格——同形第五处。fixer 按"不得扩大豁免 scope"取缩窄一侧（删 token），**是否应把 CW-010 扩到 `fifo_v3` Toggle 属 rev 裁决面**（其 `flush_i` 位物理实存 No/No/No，且 REV-031 §5 散文确曾把 fifo_v3 结构分量整体归给 CW-010），待裁 | n/a（非仿真；复现 = `grep -n flush_i vendor/common_cells/src/fifo_v3.sv` 只得 :25/:122 + 遍历 `sim/out/urgText6/modinfo.txt` 全部 26 个 fifo 实例 Condition 段无 LINE 122；另比对 `doc/coverage-waivers.md` :14/:19/:22 登记列与 `doc/design-prompt/milestone_restructure.md` :424/:427/:429/:431 引用列） | CW 行的"（模块,类型）"登记列与下游三处引用面（final-sweep §3、milestone_restructure §6.2/§6.3）之间**没有任何机器核对**，靠人工转抄；转抄方向恒为"引用面向登记面借名"，故错误恒表现为引用面 ⊋ 登记面。与 BUG-0049 同属 BUG-0047 coverage planning-gap 同伞、方向相反：BUG-0049 是格无人认领，本条是认领超出授权（含一个不存在的 bin）。**裁决回填（`doc/review/REV-035.md` §Q1；四子项现象全部经 rev 亲验成立）**：建议终态 **CLOSED**——四项处置均为低成本文本订正，无需仿真/RTL/spec 改动，cost-benefit 不支持 WONTFIX；它是 M4 出口第三条的诚实性支撑、到期即现在，故不得 ACCEPTED。逐项处置面：(i) **缩窄** `doc/coverage-waivers.md` CW-010 的（模块,类型）列，去掉 `fifo_v3` **Cond**（Branch 认领有效，IF-117 `0 1` bin 实存；Cond 侧实测 29 个 Cond 节的源码行号恒 ⊆ {73,88,101,105}、122 一次未现），§6.2/§6.3 同步，该格由 §6.1 重测-1 单独且完整承接（"非-flush 余量" ≡ 全格）；(ii) **扩** CW-002 的（模块,类型）列至 `axi_xbar`/`axi_err_slv`/`axi_mux`（删引用会使 `test_i` 位成孤儿——它是范围豁免而非结构不可达，定向 DV 卡不会去测 scan），**bin 范围列不得随之放宽**（BUG-0047 guard），并把 rev 记录列的占位"待 rev 卡填"填为 REV-024 §4 + REV-035 §Q1(ii) 双引；(iii) **删** §6.3 的 CW-007 token、**不扩** CW-007 登记列（REV-031 §5 对该格的裁决原文是"不登记、路由 DV-A + DV-F"，扩登记等于无新证据推翻在先裁决；该格已由点名了 `rr_arb_tree` 的 CW-010 + DV-A + DV-F 承接）；(iv) **成立为可审计性缺口、不成立为违反出口条件**——pin 住的 spec §0#4 把义务全挂在格这一侧，**不要求**债务行反向自述，故不改 spec/milestone、不给 BUG-0044 加新义务字段；处置面是**扩 BUG-0044 的 `## regression_guard`**（paths 纳入 coverage-waivers 与 milestone_restructure，note 补第二条消费路径"债务行状态变化使两格 Toggle 归属失效"），让 `make guards` 承担既有耦合。四项**均不属冻结记录回改**（waivers 是活登记本、design-prompt 是设计输入、`ACCEPTED@M5` 债务行的 guard 段本就随认识维护）。closer ≠ fixer；非仿真类 ⇒ BUG-0029 guard 要求实质复验位置须落 root_cause 列或详情页 `## rerun` 段 | - | n/a — OPEN，待 rev 裁决订正面（closer ≠ fixer） |
| BUG-0048 | CLOSED | TB | **`doc/lint-baseline.md` drifted stale again — recurrence of BUG-0040's exact class**: `cd sim && make clean && make lint-diff TEST=m1_01_smoke_test` fails with 62 new sites (NS 44/SVA-AECASR 3/SVA-UA 3/ULCO 8/WMIA-L 4, 0 new categories) on an **unmodified** `master`@`779ebf7` checkout (isolated via `git stash` before/after this DV card's own edit, same technique BUG-0040 used) — the six tb/ landings since BUG-0040's 2026-07-31 resync (M4-EB01/M4-BP02/M4-BP02 w_open[3] fix/M4-BP03) never re-ran the baseline regenerate-and-diff step. Discovered incidentally during an M1-01 enrichment DV card's own due-diligence lint-diff run (`tb/seq_lib.sv` is lint-scoped); not caused by nor blocking that card (`make evidence`/`make check`/`make selftest` don't invoke lint-diff). Detail: doc/bugs/BUG-0048.md | `cd sim && make clean && make lint-diff TEST=m1_01_smoke_test` (non-sim, no TEST/SEED failure signature — lint gate) | six tb/ feature landings after BUG-0040's 2026-07-31 resync each added/shifted lint-flagged constructs without re-running the resync step that row's own regression_guard calls for | 7f73451 | doc/evidence/v0.4.38/BUG-0048.log |
| BUG-0046 | ACCEPTED@M5 | upstream | **上游内部 doc-vs-RTL 约束松紧矛盾——`vendor/axi/doc/axi_xbar.md`（`<=`）与 common_cells `addr_decode_dync` 的 `check_start` 断言（`<`）对 `start_addr==end_addr`（`end≠0`）合法性判断相反；`doc/spec.md` §3.2 条 2 忠实跟随权威文档 `<=`，spec 侧无误**（REV-021 仲裁 BUG-0045 时逐行亲读 `addr_decode_dync.sv` 顺带发现，独立于 BUG-0045 本体；REV-023 补核许可来源 `axi_xbar.md` 后订正框架——原登记误框为"spec 蒸馏遗漏/spec 允许 RTL 会炸"，经 rev 独立核实为失实）：`vendor/axi/doc/axi_xbar.md`（L26）"The start address must be less than **or equal to** the end address"——spec §3.2 条 2 的 `<=` 是这条权威许可来源的忠实、正确蒸馏；`vendor/common_cells/src/addr_decode_dync.sv` 头注释 L26 用严格 `<`（且与同文件 L36-38"higher than"表述自相矛盾）、`check_start` ASSUME（L148，`start_addr < end_addr \|\| end_addr=='0`）用严格 `<`，对 `start==end`（`end≠0`）会触发断言报告。目前三张地址表（`ADDR_MAP`/`ADDR_MAP_V1`/`ADDR_MAP_OV1`）均 `start<end` 严格成立，无 `start==end` 构造，缺口潜伏无碍。详见 doc/bugs/BUG-0046.md | n/a（非失败触发，静态阅读发现；`workflow/bugs.md` "Dispatch: coverage hole" Q1 归类为 planning gap） | 根因是上游内部 doc-vs-RTL 不一致（`axi_xbar.md` L26 `<=` vs `addr_decode_dync.sv` L148 `<`，且 addr_decode 头注释内部 L26/L36-38 亦自相矛盾）——spec 站权威文档一侧，无蒸馏遗漏。与 BUG-0045 同源于同一次逐行核验但触发条件、解锁条件、到期锚点均独立，不合并 | - | `doc/review/REV-023.md`（rev 独立补核 `axi_xbar.md` L26 权威文档口径，推翻 REV-021 §6 与本行原登记的失实框架；taxonomy 维持 SPEC_ISSUE（重框为上游 doc-vs-RTL 矛盾）；处置 ACCEPTED@M5，独立否决"现在收紧 spec"——该前提经核实为假，收紧会令 spec 偏离权威文档反向对齐 RTL；到期动作与可证伪解锁条件见该记录 §Q3/§4） |
| BUG-0045 | ACCEPTED@M5 | spec | **`doc/spec.md` §3.2 未载 `end_addr == '0` ⇒ 视为地址空间末端的哨兵分支——RTL 及其自身文档均记录该行为，refmodel 未实现**（REV-020 仲裁 BUG-0041 时逐行亲读 `addr_decode_dync.sv` 顺带发现，非本条自身触发的失败）：`vendor/common_cells/src/addr_decode_dync.sv:112` 组合匹配条件为 `(addr_i < end_addr) \|\| (end_addr == '0)`，头注释 L60 明述"if `end_addr == '0` end of address space is assumed"；`doc/spec.md` §3.2（L117-119）只记半开区间 `addr>=start && addr<end` + `start<=end`，未提这个哨兵分支；`tb/xbar_types_pkg.sv` 参考模型 `decode_mst_port`（L354）同样未实现它。目前无任何测试场景构造 `end_addr=='0` 的地址表，缺口潜伏无碍；一旦有场景触及，spec（checker 期望值唯一来源）与 refmodel 双缺，会产生无源或错源期望值。详见 doc/bugs/BUG-0045.md | n/a（非失败触发，静态阅读发现；`workflow/bugs.md` "Dispatch: coverage hole" Q1 归类为 planning gap——无场景覆盖的情形先登记场景行，不先写 checker） | spec §3.2 蒸馏时只覆盖半开区间通用形态，未覆盖上游 RTL 记录的 `end_addr=='0` 末端哨兵分支——蒸馏遗漏，非上游文档矛盾（与 BUG-0016/0033 的 doc-vs-RTL 矛盾不同类，这里是 spec 对 RTL 已文档化行为的沉默） | - | `doc/review/REV-021.md`（rev 独立复算：算术自验 + tb 全域构造点扫描确认"无场景触及 `end_addr=='0`"为真；taxonomy 维持 SPEC_ISSUE——潜伏型 spec-completeness gap，处置 ACCEPTED@M5，排除"现在补 spec"（会造不可证伪 refmodel 死代码）与 WONTFIX（会埋没 RTL 全链路一等公民特性）；到期动作与可证伪解锁条件见该记录 §Q3/§4） |
| BUG-0044 | ACCEPTED@M5 | spec | **`doc/spec.md` §6 只规定 ATOP **atomic-load** 的应答义务（B 与 R 两通道都返回，SPEC-6.3），atomicstore（应仅 B）/atomicswap/atomiccompare（应 B+R，且 len/data 有约束）三个子类型的应答义务全节**未列**——M5 验证方法学拓展提案（`doc/design-prompt/verification_maturity.md` 决策点 2，Decision 2 约束随机架构）设计 `atop` 有界随机化时发现：DUT（`ATOPs=1`）按 AXI5 实际支持这些子类型，但 spec 相对 DUT 行为存在真实缺口，scoreboard 对非-load 子类型无 oracle 可判。REV-019 复核确认成立（`doc/review/REV-019.md` SP-1 段：核实 §6.3 全节确无 atomicstore/swap/compare 应答条款）。详见 doc/bugs/BUG-0044.md | n/a（非仿真类缺陷，`doc/design-prompt/verification_maturity.md` §6 SP-1 段 + `doc/review/REV-019.md` SP-1 段复现路径） | spec §6 蒸馏时只覆盖了 M2-AT01 场景用到的 atomic-load 编码（v0 初稿即如此，历次 spec 修订均未补齐非-load 子类型），全 §6（clause 1-5）无一条提及 atomicstore/atomicswap/atomiccompare 的响应通道数/响应码/len 约束 | - | `doc/review/REV-019.md` SP-1 段（"是真 SPEC_ISSUE"裁决 + 登记要求）——仲裁本身延迟批准（有界随机子集 `{'0} ∪ load 编码` 由现有 §6.3 完全支持、零 spec 改动，M5 不必现在放开），但登记按 CLAUDE.md §2 无条件规则现在补；containment：M5 决策点 2 的 `atop` 约束硬性限定在有界子集内，构造性挡住需要该 oracle 的组合，故本条不阻塞 M5 派卡 |
| BUG-0038 | SPEC_CHANGED | spec | **spec §0#4 把 `addr_decode`/`axi_demux` 列为强制覆盖范围命名模块，但二者均为纯透传 wrapper、自身无 Line/Cond/Branch/Assert 可覆盖内容**（M4 覆盖率基线测量卡实测发现，读 RTL 仅为测量范围诊断、未用于任何 checker 期望值）：`addr_decode.sv`（109 行）整体只例化 `addr_decode_dync`（真正的规则匹配逻辑都在后者）；`axi_demux.sv` 同理只例化 `axi_demux_simple`（真正的 ID 跟踪/路由逻辑都在后者）。二者在干净基线 `urg` 报告中 Line/Cond/Branch/Assert 四类均为空白（非 0%，是无 bin），只有 Toggle 有数（addr_decode 43.90~48.78%、axi_demux 62.01~63.19%）。spec 用"等"字兜底但未明确 `addr_decode_dync`/`axi_demux_simple` 是否算入 #4 范围——若不算入，则 M4 六类判据在这两个命名模块上要么恒定"4/6 类型 N/A"，要么真正需要监督的逻辑（addr_decode_dync/axi_demux_simple）反而不在明文范围内。详见 doc/bugs/BUG-0038.md | n/a（非功能缺陷，`doc/evidence/v0.4.0/M4-coverage-baseline.md` §3/§4 复现路径） | §0 item 4 用"命名枚举 + 等"表述覆盖率范围，与 item 5 原文已有的"间接例化即计入 #4"不对称，产生"闭合清单 vs 例化闭包"两可读法；且原文对"某类型无 bin 时如何判 ≥90%"完全沉默。rev 终判 SPEC_ISSUE 成立，裁定"等"字**本就是例化闭包**（依据 REV-001 §3.3 C2 的例化关系判据 + item 5 原文），`addr_decode_dync`/`axi_demux_simple`/`axi_atop_filter`/`axi_multicut` 及内部 common_cells 原语均在范围内；补三态判定规则（无 bin ⇒ N/A、不入分子分母、须写明已核实成因）。**实质复验记录：`doc/review/REV-016.md` §1（例化层次逐条亲验，并订正本行 summary 与详情页"axi_demux 是纯透传 wrapper"、证据 §3.7"axi_atop_filter 由 axi_xbar_unmuxed 例化"两处失实——`axi_demux.sv` 自身有 7 个 `spill_register` + 4 条 assign，其 Line/Cond/Branch 空白尚未被证明是结构性的；`axi_atop_filter` 实由 `axi_err_slv.sv:45-58` 例化）、§3（"等"字裁定）、§4（判定口径）** | - | `doc/review/REV-016.md` §1/§3/§4（rev 独立复验）+ spec §0 item 4/5 重 pin 后的 `doc/spec.sha256`（非仿真类缺陷，无 sim log；BUG-0029 guard 要求的实质复验记录位置见 root_cause 列所引 REV-016 章节） |
| BUG-0031 | CLOSED | TB | **`stall_sva` 用编译期地址表译码，spec §3.4 的运行时可变表对它不可见；误差双向（可假红）**（REV-011 §5.1 亲读发现，中，非阻塞）：`tb/sva/axi_xbar_stall_sva.sv:99-100` 调 `decode_mst_port(aw_addr, **ADDR_MAP**, 1'b0, '0, aw_tgt)`——地址表取编译期 localparam（基线 V0）；而 `tb/sva_bind.sv:33-35` 该模块只接 `slv_if[k]`、**结构上拿不到** `cfg_if`（同文件 `:41-47` 的 `axi_xbar_route_sva` 却接了 `cfg_if.addr_map`/`en_default_mst_port[r]`/`default_mst_port[r]`）。`doc/design-prompt/sva_bind.md` §3「译码复用」段**明文要求**把地址表当输入参数传入、使 SVA 与 scoreboard 对同一个「运行时活值」表译码——函数签名已改（`xbar_types_pkg.sv:148` 已收 `amap` 形参），**但 SVA 侧调用点仍传编译期常量，要求只落实了一半**。M2-CFG01 **确实**在运行时改表（`tb/seq_lib.sv:993-996` `do_reconfig()` 把 rule 0 的 idx 由 0 改为 5 并使能全部 default port）⇒ 重配后对命中 region0 的**译码命中**事务，`w_id_tgt` 记的是 **mst0**、实际目标是 **mst5**。**误差方向双向**：目标记错既可让「异目标」条件假成立（**假红**）、也可让它假不成立（漏检）——与 BUG-0023/0024/0025 的单向漏检**不同类**。**今天为何没炸**：M2-CFG01 每端口至多一笔在飞、兄弟集恒空（REV-010 §2.2 实测该运行四组 cover 全 0/0），断言恒真空过；而该掩蔽事实**只写在 `stall_sva.sv:93-98` 的代码注释里**——正是 CLAUDE.md §2 记载的 FB-7 / BUG-0007 形状（变通只落注释、未进台账），故本条无条件登记。**与 BUG-0025 不可合并**（一个是「表版本陈旧」影响**命中**事务的目标计算，一个是「未命中被丢弃」；REV-010 §3.4 只订正到 `en_default` 硬编码、未及同一行的 `ADDR_MAP` 实参），但**应同卡修**（REV-011 §4 G4：同一根管线的两个症状）。详见 doc/bugs/BUG-0031.md | `make run TEST=m2_cfg01_reconfig_test SEED=1`（重配后表版本陈旧确已发生；因兄弟集恒空未产生可观测差别，复现可观测差别须新场景：重配 + 同桶异目标兄弟并存） | TB_BUG（要求半落实）：`sva_bind.sv` 未把 `cfg_if` 接进 `axi_xbar_stall_sva`，调用点仍传编译期 `ADDR_MAP` **REV-011 §5.4 裁决 → `ACCEPTED@M3`（与 BUG-0025 同到期、同卡修）**。判"不必在 M2 内修完"的三条实测依据（本卡亲跑 grep，非采信任何摘要）：(1) 全仓只有两处写 `cfg_vif.addr_map`——`tb/tb_top.sv:59` 初始化与 `tb/seq_lib.sv:994` 的 CFG01 vseq ⇒ **除 m2_cfg01 外所有 M0/M1/M2 场景的编译期表恒等于活值表**，本条在那些运行中不可能起作用；(2) 唯一用到陈旧表的运行 `sim/out/m2_cfg01_reconfig_test_1.log` 中，`axi_xbar_stall_sva.sv` 的 **84 条 cover 行（14 cover × 6 实例）全部 `0 match`**（对照：`m2_or01` 同名 cover 有非零命中，故非日志假象）⇒ 兄弟候选集全程为空 ⇒ 该模块两条 assert 在该运行中**结构性空转、贡献零判决**；(3) `doc/testplan.md` M2-CFG01 行的判决锚点明写为 scoreboard 参考模型（SPEC-3.1/3.2/3.3，按 `accept_time` 生效表版本译码）+ **独立 SVA C3.1**（`axi_xbar_route_sva`，经 `sva_bind.sv:41-47` 拿到活值 cfg），**不含 C3.2** ⇒ 无任何 M2 ✅ 依赖本条所在的路径。另：假红方向若发生会让运行变红，而 M2 无一运行失败 ⇒ 两个方向都不存在被污染的 PASS。**同卡修的理由**：与 BUG-0025 是同一调用点（`stall_sva.sv:99-100`）的两个实参，单独在 M2 抢修等于对同两行改两次、跑两次全回归（discipline rule 3/5），且 BUG-0025 的修法依赖刚 pin 的 spec §5.2.6 ⇒ 本就是 M3 卡。**M2 签核交接条件**：签核卡不得把 stall_sva 的通过计为 M2-CFG01 的独立证据（84/84 零命中即其空转的机械证明）。到期验收五判据见 doc/review/REV-011.md §5.4 | 482a47e | doc/evidence/v0.3.10/BUG-0031.log |
