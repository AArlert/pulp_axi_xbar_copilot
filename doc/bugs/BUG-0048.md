# BUG-0048 — doc/lint-baseline.md drifted stale again after 0.4.16-0.4.23 landings (BUG-0040 recurrence)

## symptom

`cd sim && make clean && make lint-diff TEST=m1_01_smoke_test` fails on a
clean, **unmodified** checkout of `master`@`779ebf7` (0.4.23, before this
DV card's own edit ever touched anything):

```
[LINT-DIFF][FAIL] 62 个新站点须分诊；判为风格才可并入基线，判为真缺陷另开 bug 行
```

## first_anomaly

signal: n/a (build/documentation-currency defect, not a DUT/TB signal)
time: n/a
how_found: incidental — this DV card (M1-01 r.data toggle enrichment,
REV-026 A-1/A-2) ran `make lint-diff` on `tb/seq_lib.sv` as due-diligence
before delivery (the file it touched is in lint scope). The diff reported
77 new sites; to separate "caused by this card's edit" from "pre-existing",
the card's own changes were `git stash`ed and `make clean && make lint-diff
TEST=m1_01_smoke_test` re-run against the untouched tree — **still fails,
62 new sites**, proving the drift predates and is independent of this
card's edit (same isolation technique BUG-0040's own `how_found` used).

## taxonomy
TOOL_ENV — matches `workflow/bugs.md`'s own cue verbatim ("stale build
artifacts... evidence older than the code it certifies"); identical class
to BUG-0040 (CLOSED), which this row is a recurrence of.

## rca
```
observed:   make lint-diff reports 62 new sites on an untouched tree,
            0 new categories (NS 44, SVA-AECASR 3, SVA-UA 3, ULCO 8,
            WMIA-L 4 — every one of these five tags already present in
            doc/lint-baseline.md's own category table)
anomaly:    doc/lint-baseline.md's "2026-07-31 全量重同步" snapshot
            (BUG-0040's fix) certified 236 unique sites as of that date;
            seven tb/ landings since (0.4.16 逐位 toggle 分解代码,
            0.4.17 M4-EB01/M4-BP02 注册, M4-EB01 落地 d92bf6e, M4-BP02
            落地 fb35c18, 0.4.19 dispatch, 0.4.22 M4-BP02 w_open[3] fix
            6888870, 0.4.23 M4-BP03 779ebf7) added/shifted tb/ source
            without a matching lint-baseline resync
chain:      BUG-0040's fix (2026-07-31) certified the baseline against
            that day's tb/ tree -> six further feature landings (M4-EB01/
            M4-BP02/M4-BP03, each adding new sequences/SVA cover
            properties per their own regression_guard entries) touched
            tb/seq_lib.sv, tb/sva/axi_xbar_stall_sva.sv and neighbors,
            shifting or adding lint-flagged constructs -> none of those
            six landings re-ran the "regenerate-and-diff" step BUG-0040's
            own regression_guard calls for -> the baseline is stale again,
            exactly the failure mode BUG-0040 already diagnosed once
class:      TOOL_ENV (recurrence of BUG-0040's exact class and root-cause
            shape — a checked-in baseline artifact silently outliving the
            code it certifies)
guard:      BUG-0040's own regression_guard already named this risk
            ("any future milestone close should re-run this... a green
            result today would be a false negative") — this row is the
            proof that the guard's predicted recurrence happened, because
            the guard is advisory (a note to future readers) rather than
            a gate wired into any card's mandatory checklist. `make
            lint-diff` itself is still the correct detector; it just
            needs someone to actually run it and fold the result.
```

## fix
commit: <待 orch 派发 fixer 卡>
what: not attempted by this card — per-site style-vs-defect triage (the
BUG-0040 precedent took a dedicated fixer card reading all 153 sites) is
out of scope for a DV card whose own deliverable is an M1-01 stimulus
enrichment, and closer ≠ fixer applies regardless. Registering per
CLAUDE.md's unconditional-registration rule; this card's own PASS/evidence
does not depend on `make lint-diff` (`make evidence`/`make check`/`make
selftest` do not invoke it), so it is filed OPEN and handed to orch for
dispatch rather than worked around inline.

## rerun
n/a until a fixer card lands — `cd sim && make clean && make lint-diff
TEST=m1_01_smoke_test` (or any baseline TEST) is the re-verification
command; closer must be a different instance than the fixer (invariant 3).

## regression_guard
type: script
paths: doc/lint-baseline.md, sim/Makefile (lint-diff target), tb/**
ref: `cd sim && make clean && make lint-diff TEST=<any baseline TEST>`
note: same guard BUG-0040 already registered — repeating it here because
the prior instance evidently was not enough to prevent recurrence.
Consider (out of this card's scope to decide) whether a milestone-close
checklist item, rather than a passive doc note, is warranted so this stops
recurring silently between signoffs.

## similar
BUG-0040 (CLOSED) — this row's direct predecessor; same taxonomy, same
root-cause shape (checked-in lint-baseline snapshot outliving the tb/ tree
it certifies), same fix category (resync, no gate relaxation). Not merged
into BUG-0040 because that row is terminal (CLOSED) per the status
lifecycle in `workflow/bugs.md`.
