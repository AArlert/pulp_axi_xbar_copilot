# BUG-0032 — err_slv 对 ATOP（尤其要求读响应的原子操作）的应答形态，许可来源未定义

## symptom

M3 错误路径场景设计时浮出：一笔 `aw.atop != '0` 且**要求读响应**的原子操作
（atomic load），其地址若不匹配任何 rule 且该 slave 端口未使能 default master
port，将被路由到本端口的内部 decode error slave（spec §4.2）。此时期望的应答
形态在许可来源中**没有答案**：

- spec §4.3 只按"读事务 / 写事务"两分：**写**事务在收齐整个 W burst 后返回
  **单拍 B**（DECERR）。原子读是由 AW 发起的写侧事务，按该条字面只该得到一拍 B。
- spec §6.3 则要求原子读 **B 与 R 两个通道都返回响应**。
- 两条都成立时，err_slv 是否也要为这笔 AW 产出一串 R beat（几拍？`AxLEN+1`？
  数据是否同为 `32'hBADCAB1E`？响应码 DECERR？）——**无条款可推**。

若不作定义，checker 只能"现场解释"，而两种解释（只回 B / 回 B+R）都会让对方
形态判为失配。这正是 spec §0 行 6 禁止的情形。

## first_anomaly

signal: n/a（设计输入阶段的条款缺口，非仿真观测）
time: n/a
how_found: M3 arch 场景注册卡在为 M3-DE01 写 decode error 判据时，把 spec §4 与
§6 交叉核对后发现；随后亲读许可来源确认缺口真实存在（非 spec 蒸馏遗漏）：
`vendor/axi/doc/axi_xbar.md` §Decode Errors and Default Slave Port（L31-35）
全段无 "atop"/"atomic" 字样（`grep -in "atop\|atomic" axi_xbar.md` 仅命中标题
L1 与 L3 的总述句）；`axi_demux.md`/`axi_mux.md` 对 err_slv 一字未提（REV-011
§4.3 已复核该 grep 空集）。⇒ 上游文档确实没有把 ATOP 与 decode error 这两个
机制放在一起定义过。

## taxonomy

SPEC_ISSUE

## rca

1. spec §4 的应答形态按"读/写"二分蒸馏自 xbar.md 的 "proper number of beats"
   一句，该句写于只有 AXI4 读写两类事务的语境。
2. spec §6 的 ATOP 条款蒸馏自 demux.md §Atomic Transactions，该节只谈 demux 的
   路由/计数器注入，不涉及 err_slv。
3. 两节各自完备，交集（ATOP × 译码未命中）无任何一方覆盖 ⇒ 缺口不是蒸馏遗漏，
   而是**上游文档本身没写**。
4. 该缺口在 M0-M2 不可触达（全部场景为译码命中路径，且 ATOP 只在 M2-AT01 出现、
   地址命中）；M3 的主题是错误路径，一旦 env 允许 ATOP 落在未命中地址即触达。

## fix

未派发。**arch 侧不作现场解释**（spec §0 行 6 / discipline rule 1）。
处置按本仓库既有先例（spec §6.2 的 `ATOPs=0` 无-ATOP 约束、§8.3 的稀疏
`Connectivity` 地址表约束）：以 **env 构造性约束**使该情形不可触发，缺口本身
列为**上游确认项**，不阻塞 M3。

env 约束（已写入 `doc/testplan.md` M3-DE01 行与 `doc/design-prompt/uvm_env.md`
§6 C6.2）：**M3 全部场景不向译码未命中地址发起任何 ATOP**（送往未命中地址的
AW 恒 `aw.atop ≡ '0`）。

若将来需要覆盖该组合（例如 M4 覆盖率收敛发现 `axi_err_slv` 的 ATOP 相关分支
未被激励），须先走 rev 仲裁补 spec 条款，再解除本约束——**不得**由 DV 在
checker 里二选一。

## rerun

不适用（未修复；当前构造性不可达，无失败可复现）。

## regression_guard

type: checklist
paths: doc/testplan.md, doc/design-prompt/uvm_env.md, tb/seq_lib.sv
ref: `doc/testplan.md` M3-DE01 行的 env 约束句；`doc/design-prompt/uvm_env.md`
§6 C6.2
note: 守卫的对象是**约束不被悄悄解除**。任何后续卡若开始向译码未命中地址发起
`aw.atop != '0` 的事务（M4 覆盖率收敛最可能踩这一步），必须先取得 spec 条款
再动 env——否则 checker 将不得不现场解释，而两种解释互为失配。里程碑签核时的
机械抽查形态：`grep` 激励侧"未命中地址"构造点，确认其 atop 字段恒为 `'0`；
或由 SVA/covergroup 给出一条"未命中地址上 `aw.atop != '0`"的计数，该数非 0
即表示约束已被解除、本条须重新走仲裁。

## arbitration

裁决记录：**REV-012 §Item 1**（2026-07-29）。

- **SPEC_ISSUE 分类：确认。** 本卡复跑 grep 亲验：`axi_xbar.md` §Decode Errors
  全段无 atop/atomic；`axi_demux.md`/`axi_mux.md` 对 err_slv 零命中；五份许可
  来源皆不定义 err_slv 对要求读响应的原子操作的应答形态。spec §4 clause 3
  （写→单拍 B）与 §6 clause 3（原子读→B+R）两读互斥，符合 SPEC_ISSUE 判据。
- **处置：确认（同 BUG-0002/0003 先例）。** env 构造性约束（M3 全部场景不向
  译码未命中地址发起 ATOP）使该组合不可触发，列上游确认项，不阻塞 M3。
- **升级点：须补 spec 正文条款（approve P-REV012-1）。** 当前约束只活在
  testplan M3-DE01 + uvm_env C6.2 + 本页 guard——这是 Retention 缺口（§4 正文
  未标记该未定义洞）。BUG-0003 的约束写在 spec §6 clause 2、BUG-0002 的写在
  §8.4，§4 应得平行条款。据此行状态终判 `SPEC_CHANGED`（非 `ACCEPTED@M4`）。
  - 主条款置 §4（decode-error 章，四段模板照搬 §8.4），§6 clause 3 旁加一行
    交叉引用指回 §4；exact wording 由 orch/arch 拟，rev 不代写。
  - **应用者：orch**（应用 P-REV012-1 + change record + 重 pin sha256）。在
    应用前约束的活载体仍是 testplan M3-DE01 + uvm_env C6.2 + 本 guard，不失守。
- **可证伪 rationale（rubric #7/#8）：** 裁决立于"err_slv 原子读应答形态在
  许可来源不可推导"。证伪条件：五份许可来源任一处若定义该应答形态，则
  SPEC_ISSUE 消解、env 约束失依据、须重开。本卡 grep 已证五源皆空。

**REV-013 spec-review 门禁（2026-07-29，conditional pass）**：arch 起草的
P-REV012-1 文本经四项定向核查（无越裁决发明 / 四段模板匹配 / 上游静默复验 /
编号惯例无违）全部通过，唯一必改——提案原文两处 `M3/M4` 收窄为 `M3`：
REV-012 处置确认句与 BUG-0032 本页 fix 段均锚 **M3**，且本页第 4 部分已把
"M4 覆盖率收敛触发该组合" 列为须重开仲裁的场景，写 `M3/M4` 会让 spec 断言一个
当前无 M4 config-matrix 承载的约束（Retention 不一致）。reopening 路径不因
收窄受损，由本节 + guard 承接。详见 doc/review/REV-013.md。

**已应用（2026-07-29）**：orch 按 REV-013 订正后的逐字文本应用——`doc/spec.md`
§4 新增 clause 7（许可来源未定义 + env 构造性约束，引本裁决）、§6 clause 3
尾部加交叉引用指回 §4 clause 7；change record #7；重 pin sha256
`9347b4ac71f824a05581468502109d78160781fd1712710d0d783a2f03b3b806`。约束的
持久归宿自此是 spec 正文，非仅 testplan/design-prompt/guard。

## similar

- **BUG-0002**（稀疏 `Connectivity` 下译码到非连通 master 端口的应答未定义 →
  裁决为 env 构造性约束 + 上游确认项，spec §8.3/§8.4）——**同一处置形状**。
- **BUG-0003**（`ATOPs=1'b0` 时收到 ATOP 的行为未定义 → env 保证不发起，
  spec §6.2）——**同一处置形状**，且同为 ATOP 维度的许可来源空白。
- BUG-0025（err_slv 与保序次序的关系未定义，REV-011 §1 三层仲裁）——同为
  "err_slv 在上游文档里几乎没有被写过" 这一根源的另一个后果面。
- searched-on: atop / atomic / err_slv / decode error / RESP_DECERR / §4.3 / §6.3
