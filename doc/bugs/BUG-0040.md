# BUG-0040 — doc/lint-baseline.md snapshot predates a tb/ refactor commit, 153 undiagnosed new lint sites

## symptom
`cd sim && make clean && make lint-diff TEST=m1_01_smoke_test` (also reproduces
with the baseline's own documented repro TEST=m2_or03_guard_test) fails:

```
[LINT-DIFF][FAIL] 153 个新站点须分诊；判为风格才可并入基线，判为真缺陷另开 bug 行
```

153 "new site" lines, spread across 7 categories **all already present** in
`doc/lint-baseline.md`'s own category table (no new category introduced):
`Lint-[NS]` 46, `Lint-[SVA-AECASR]` 11, `Lint-[SVA-CE]` 32,
`Lint-[SVA-DIU]` 32, `Lint-[SVA-UA]` 28, `Lint-[SV-PIU]` 2,
`Lint-[WMIA-L]` 2.

## first_anomaly
signal: n/a (build/documentation-currency defect, not a DUT/TB signal)
time: n/a
how_found: incidental — while re-verifying BUG-0037's guard list (which
explicitly names BUG-0014/0019/0021/0022 as lint-target guards to check
before/after), this DV card ran `make lint`/`make lint-diff` on both the
pre-fix and post-fix trees to confirm BUG-0037's edits hadn't disturbed the
lint gate. `make lint-diff` failed identically on **both** trees (confirmed
via `git stash` toggling BUG-0037's two changed files off and back on,
re-running `make clean && make lint-diff TEST=m1_01_smoke_test` each time —
153/153, same category breakdown, same site list) — proof this is
independent of BUG-0037's fix.

## taxonomy
TOOL_ENV

## rca
```
observed:   make lint-diff reports 153 new sites, 0 new categories
anomaly:    doc/lint-baseline.md's snapshot header says "基线快照：
            2026-07-28" but `tb/` received a refactor commit
            (01e7976, "refactor(tb): 0.4.2 落地零风险重构 + 新增
            doc/uvm.md") dated 2026-07-30 -- i.e. two days AFTER the
            baseline was last regenerated
chain:      0.4.2's tb/ refactor touched/added source that Lint- flags in
            already-known-style categories -> doc/lint-baseline.md's own
            header instruction ("每里程碑跑一次强制重编 lint 与本文件做
            差分") implies a re-diff-and-fold step should have run as part
            of landing that refactor -> it did not (or the diff step ran
            and the new sites were never triaged/folded) -> the baseline
            is now stale relative to the code it's supposed to certify as
            "already assessed"
class:      TOOL_ENV -- matches workflow/bugs.md's own TOOL_ENV decision
            cue verbatim: "stale build artifacts... evidence older than
            the code it certifies"
guard:      `make lint-diff` (any TEST) already goes red on this; that IS
            the guard (BUG-0021's own regression_guard), just currently
            red for a reason unrelated to real new defects being
            introduced -- someone must triage the 153 sites (style ->
            fold into baseline; real defect -> separate bug row per
            BUG-0021's own dispatch rule) before `make lint-diff` can be
            trusted green again
```

## triage

Fixer card, 2026-07-31 (VCS present, real loop run — `command -v vcs` found
`/home/synopsys/vcs-mx/O-2018.09-SP2/bin/vcs`). Repro per the row's own
`min_repro` (forced full recompile first, per BUG-0022):

```
cd sim && make clean
make lint-diff TEST=m2_or03_guard_test    # exit 2, 153 new sites, 0 new categories
```

Full per-site breakdown (category | count | style | true defect | note —
every one of the 153 read directly, not sampled; methodology per
`doc/bugs/BUG-0021.md ## rca`, its own established per-category rubric):

| category | count | style | true defect | note |
|---|---|---|---|---|
| `Lint-[NS]` | 46 | 46 | 0 | all `wait fork;` / `@(posedge …clk_i);` / `do @(posedge …); while(…)` / `repeat(n) @(posedge …);` / multi-line `assert…else \`uvm_error(…);` closing `;` / multi-line `cover property(…);` closing line — exact analogs of BUG-0021's own accepted NS forms (`wait(…);`/`@(posedge clk);`/`do…while`/`repeat…`/`cover property(…);`); zero `if(…);`/`for(…);` empty bodies found |
| `Lint-[SVA-AECASR]` | 11 | 11 | 0 | 9× `axi_chan_sva.sv` (5 stability-assert + 2 reset-idle + 2 WLAST/RLAST immediate-assert action blocks, shifted +1..+21 lines by BUG-0034's fix `d7f5011` which added r_id-keyed associative-array tracking ahead of them) + 2× `axi_xbar_stall_sva.sv` (the 2 main §5.2.3 judgement asserts' action blocks, shifted +105 lines by the BUG-0024/0025/0031 cover-property additions `0e9d516`/`68b6cca`/`482a47e` landed ahead of them). All already use `$sampled(...)` in the action block (BUG-0021 F3 fix, still present) or are immediate assertions inside `always_ff` (Active-region procedural code, no preponed/observed race per IEEE 1800 §16.4 — same class BUG-0021 already accepted for this exact file's WLAST/RLAST checks). No judgement-bearing `assert property` in either file calls a function directly in its expression (verified: `axi_xbar_stall_sva.sv`'s only 2 `assert property` statements reference `w_reorder_now`/`r_reorder_now`, `always_comb`-folded signals per the BUG-0015 fix pattern already in place) |
| `Lint-[SVA-CE]` | 32 | 32 | 0 | 5× `axi_chan_sva.sv` (same 5 stability asserts, `disable iff (!rst_ni)`) + 22× `axi_xbar_stall_sva.sv` (2 main asserts + 20 `cover property(… disable iff (!rst_ni) …)` added by BUG-0024/0025/0031, all referencing plain `always_comb`/`wire`-folded signals, never a raw function call) + 4× `axi_xbar_txlimit_sva.sv` (pre-existing `MAXMST`/`MAXSLV` ceiling covers, shifted +8 lines) + 1× `axi_xbar_worder_sva.sv` (pre-existing `compete_start` cover, shifted +5). Every site is `disable iff` with a non-bare-identifier argument — the tool's stated trigger, per BUG-0021's own confirmed reading of the full warning text |
| `Lint-[SVA-DIU]` | 32 | 32 | 0 | identical file:line set to SVA-CE above (same construct triggers both tags together, per BUG-0021's established co-occurrence finding) |
| `Lint-[SVA-UA]` | 28 | 28 | 0 | 9× `axi_chan_sva.sv` + 14× `axi_xbar_stall_sva.sv` + 4× `axi_xbar_txlimit_sva.sv` + 1× `axi_xbar_worder_sva.sv` — all unlabeled `assert property`/`cover property`. Cross-check against BUG-0021's own judgement criterion ("缺标签"): every *labelled* cover in `axi_xbar_stall_sva.sv` (`c_sib_diff_aw`, `c_bug25_default_aw`, `c_bug31_livev1_aw`, etc. — 8 of the 22 SVA-CE sites) is correctly *absent* from this list, confirming the tool's labelling-based trigger still holds on the new sites (no drift in tool behavior between the 2026-07-28 and 2026-07-31 runs) |
| `Lint-[SV-PIU]` | 2 | 2 | 0 | `import uvm_pkg::*;` in `$unit` scope, `axi_chan_sva.sv`/`axi_xbar_stall_sva.sv` — same 2 already-baselined import lines, shifted down by header-comment growth (BUG-0034/0024/0025/0031 doc comments added above them) |
| `Lint-[WMIA-L]` | 2 | 2 | 0 | `tb/scoreboard_refmodel.sv:761,831`: `src_port = ro.id >> xbar_types_pkg::ID_W_SLV;` — rewritten from the baselined part-select form `ro.id[ID_W_MST-1:ID_W_SLV]` specifically to stay legal when `NoSlvPorts=1` (`ID_W_MST==ID_W_SLV` would make the part-select's range invalid, `[4:5]`-style, whereas the shift degrades gracefully to 0). `ro.id` is `bit [ID_W_MST-1:0]` (unsigned), assigned into `int unsigned src_port` — a widening (8-bit → 32-bit) zero-extension per IEEE 1800 §10.7, same class as the already-accepted baseline judgement. Verified with the mandated deterministic tool rather than eyeballing: `xbit eval "8'hFF >> 5"` → unsigned 7, `xbit eval "8'hFF[7:5]"` → unsigned 7 (same magnitude; only the signed interpretation differs, irrelevant since the target is `unsigned`), and `xbit eval "8'h00 >> 5"` → 0 (no spurious sign-extension on the boundary case) |

**153/153 style, 0 true defects.** No new bug row needed for a functional
defect. All 153 sites are traced to already-registered, already-landed
fixes/features (`BUG-0034` fix `d7f5011`, the `BUG-0024`/`BUG-0025`/`BUG-0031`
SVA cover instrumentation `0e9d516`/`68b6cca`/`482a47e`, the M3 multi-config
build `c105d2d`, the M3-TL01 guard `bff8a04`, and 0.4.2's `01e7976`) that
landed between the 2026-07-28 baseline snapshot and today, none of which
were themselves re-diffed against `doc/lint-baseline.md` when they landed —
this is the actual root cause (a repeated omission across five separate
landings, not a single-commit slip as `## rca` above, written before this
triage, initially suspected of `01e7976` alone).

`doc/lint-baseline.md` full resync (not a bare 153-row append — see the
file's own "2026-07-31 全量重同步说明" note for why): of the 225 old rows,
83 matched byte-for-byte against the current lint.log, 142 no longer matched
(their exact line coordinates drifted, mostly by the same intervening
commits above; 8 of the 142, all in `tb/seq_lib.sv`, are `Lint-[ULCO]` sites
that were genuinely *removed* — not shifted — by `01e7976`'s consolidation
of duplicate fill/fanout loop bodies, confirmed via `git show 01e7976 --
tb/seq_lib.sv`). Rather than leave those 142 stale coordinates in the table
alongside 153 freshly-appended rows (which would leave the baseline
internally inconsistent with the code it certifies — 236 real sites vs. 378
table rows), the table was regenerated wholesale from the current
`out/lint.log` (236 unique rows, 296 raw incidences), with **no re-judgement
of any pre-existing style verdict** — every row that still matches carries
its prior BUG-0021/REV-010 classification forward unchanged, and every row
that doesn't (i.e. the 153 handled in this triage) was independently
re-derived per the table above. Header stats (合计/唯一站点/类别/按类别/
按文件) recomputed to match.

## fix
commit: <待 orch 回填>
what: `doc/lint-baseline.md` regenerated per the "2026-07-31 全量重同步"
note in that file (236 unique sites, 296 raw incidences, 8 categories,
0 new categories, 0 true defects among the 153 sites that triggered this
row). `sim/lintdiff.py` untouched (no relaxation of its matching logic —
the fix is entirely in the baseline data, not the gate). `make check` and
`make selftest` both still pass (rerun this card, 2026-07-31). Status field
in `doc/bugs.md` intentionally left OPEN — closer ≠ fixer (CLAUDE.md
invariant 3); an independent card must re-run the repro below and flip it.

## rerun
`cd sim && make clean && make lint-diff TEST=m2_or03_guard_test` (forced
full recompile via `make clean`, sidesteps BUG-0022's incremental-hit false
green by construction): **PASS** — `[LINT-DIFF] 本次 236 站点 / 基线 236
站点` / `[LINT-DIFF] PASS：无新类别、无新站点`, exit 0.

## regression_guard
type: script
paths: doc/lint-baseline.md, sim/Makefile (lint-diff target), tb/**
ref: `cd sim && make lint-diff TEST=<any baseline TEST>`
note (**no `make clean`** — corrected 2026-08-02 per BUG-0056; the
original `make clean && make lint-diff` form written here was BUG-0056's
exact defect, this row's own copy of it): `clean:` is `rm -rf $(OUT)`,
which deletes the 7 `cov.vdb` coverage databases under `sim/out/` — they
are inside `.gitignore`, have no backup in version control, and are M4
signoff's coverage evidence base. The `make clean` bought nothing anyway:
forcing the full recompile is already built into `lint-run:` (it deletes
`.vcs.timestamp` itself and then proves per-file that every `../tb/`
source was analysed this run — the BUG-0022 fix), so the destructive step
was pure cost. Trust `[LINT-GATE] 执行证明 OK` in the output, not a wiped
tree.
note: any future milestone close should re-run this before trusting
`make lint-diff`'s green/red as reflecting the *current* tb/ tree; a green
result today would be a false negative (baseline stale, not clean).

## similar
BUG-0021 (this file's own origin — WONTFIX conditioned on the
regenerate-and-diff discipline this row shows lapsed once already).
BUG-0022 ("didn't execute" reads as "no new warnings" — same family of
"an artifact silently outliving the code that would invalidate it," this
time the artifact is the checked-in baseline doc rather than a log file).
