# BUG-0017 — `xcov` 在本 VM 不可用（Verdi O-2018 的 pynpi 无 coverage 模块）

## symptom

CLAUDE.md §4 要求「覆盖率核对优先用 xverif 取事实，而非纯模型推理/手算」，
其中覆盖率维度的入口是 `$XVERIF_ROOT/tools/xcov`。DV 卡 F-M2-08（功能覆盖
采集基建落地）首次真正需要按 bin 级核对 covergroup 命中时调用该工具，
`session.open` 直接失败：

```
NPI_INIT_FAILED: cannot import name 'cov' from 'pynpi'
(/home/synopsys/verdi/Verdi_O-2018.09-SP2/share/NPI/python/pynpi/__init__.py)
```

即本 VM 安装的 Verdi O-2018.09-SP2 所带的 `pynpi` 包中不含 `cov` 子模块，
`xcov` 无法初始化 NPI 会话。工具本身可执行（`test -x` 通过），失败发生在
打开 coverage database 的那一步。

## first_anomaly

signal: n/a（工具链初始化失败，非仿真信号）
time: n/a
how_found: F-M2-08 DV 卡在 7 个 M2 场景跑出 coverage database 后，按
CLAUDE.md §4 的"优先用 xverif 取事实"纪律调用 `xcov` 做 bin 级核对，
`session.open` 即报上述 `NPI_INIT_FAILED`。

## taxonomy

TOOL_ENV

## rca

1. `xcov` 通过 Synopsys NPI（Native Property Interface）的 Python 绑定
   `pynpi` 访问 coverage database。
2. 其 coverage 能力依赖 `pynpi.cov` 子模块。
3. 本 VM 的 Verdi 版本为 O-2018.09-SP2（CLAUDE.md §4 记载的既定环境），
   其 `share/NPI/python/pynpi/` 目录**实测只含三项**：空的 `__init__.py`、
   `wave.py`、`_wave.so`——即该版本的 NPI Python 绑定**只提供 wave（波形）
   一个模块**，`cov` 子模块根本不存在（非导出问题，是文件不存在）。
4. `xcov` README 记载的验证形态为 Verdi V-2023.12，与本 VM 版本存在代际差。
5. 故 `xcov` 在本 VM 上对**任何** vdb 都会以同一签名失败，与被查询的
   coverage database 内容无关。

## scope

**本条的范围仅限 `xcov`。** orch 在 2026-07-28 对 xverif 工具体系做了一轮
逐工具实测，结论如下（避免本条被误读为"整套 xverif 在本 VM 不可用"）：

| 工具 | 状态 | 实测依据 |
|---|---|---|
| `xbit` | 可用 | `xbit eval "16'hDEAD & 16'hFF00"` → `16'hde00` / unsigned 56832 / signed −8704 |
| `xsva` | 可用 | `xsva scan --file tb/sva/axi_xbar_stall_sva.sv` → 8 inline assertions + 操作符分布 |
| `xloc` | 可用 | `xloc stats sim/out/m2_tl01_txlimit_test_1.log` 跑通（0 命中是因本仓库日志无 `L_XXXXXXXX` 压缩位置 ID，非工具故障） |
| `xentry` | CLI 正常 | `decode`/`explain`/`validate` 子命令齐全 |
| `xeda-runner` | CLI 正常 | `init`/`env-info`/`list-actions`/`describe-action`/`run` 齐全 |
| `xcov` | **不可用（本条）** | `pynpi` 无 `cov` 模块 |
| `xdebug` | 不可用，但**不是版本问题** | 见 BUG-0020——引擎走 C API `libNPI.so`（非 pynpi）、`ldd` 全解析；同 VM 同 Verdi 于 2026-07-09 对姊妹仓库 FSDB 成功开过 session。本仓库不可用的根因是**从不产 FSDB** |

即：确定性计算/文本类（xbit / xsva / xloc / xentry）全部可用；受本条版本墙
阻断的**只有覆盖率维度**。

## fix

未派发（工具体系不在本仓库范围内，`$XVERIF_ROOT` 为外部安装）。本条为
**环境能力缺口登记**，目的是让后续每个需要覆盖率事实的卡不再重新发现：
在本 VM 上 `xcov` 路径不通，直接走下述绕法。

**已验证可用的绕法**（F-M2-08 卡实际采用，产出了第 3 节的 bin 级命中表）：

```
make cov                                    # urg HTML 报告
urg -full64 -dir out/cov.vdb -format text -report out/urgText -metric group
```

后者若直接在命令行调用，需自带 `VCS_HOME` 与 `LM_LICENSE_FILE`；走
`make cov` 则由 `scripts/make/vcs-2018.mk` 代为导出，无需手工设置。
bin 级明细落在 `sim/out/urgText/grpinfo.txt`。

注意边界不变：urg 与 xcov 一样只是**观测"实际发生了什么"**的手段，
checker 的期望值仍只准从 `doc/spec.md` 推导（CLAUDE.md §4 红线）。

## rerun

不适用（未修复；绕法已验证有效并产出了 F-M2-08 的覆盖命中证据）。

## regression_guard

type: checklist
ref: CLAUDE.md §4（xverif 工具体系入口段）
note: 后续任何卡若在本 VM 上需要覆盖率事实，先读本条再决定路径，不要重复
撞 `NPI_INIT_FAILED`。若将来 VM 升级 Verdi（≥ V-2023.12）后 `xcov` 可用，
本条应转 CLOSED 并把绕法降级为备用路径。

## min_repro

```
printf '{"api_version":"xcov.v1","action":"session.open","target":{"vdb":"out/cov.vdb"},"args":{"name":"cov0"}}' | $XVERIF_ROOT/tools/xcov --once
```

（任意 TEST，无需 SEED；纯工具初始化失败，与 vdb 内容无关。需先
`export VERDI_HOME`，探测用 `test -x $XVERIF_ROOT/tools/xcov`——绝不用
`command -v`，见 CLAUDE.md §4。）

## similar

BUG-0014（同为 TOOL_ENV，工具/环境侧的能力缺口，与被测改动无关、对任何
版本同签名失败）。
none searched-on: xcov, pynpi, NPI_INIT_FAILED, coverage database
