# BUG-0043 — `make regress` 一次运行里 `m1_02_id_prefix_test` 以
"sim process exited nonzero" 判 FAIL，日志内容干净，不可复现

## symptom

orch 独立复验 M4-FT01/BUG-0042 交付物（DV 自报 `make regress` 24/24 PASS）
时亲跑 `cd sim && make regress`（未手动先 `make clean`，但
`scripts/regress.py` 自身在整批测试前已内建一次 `make clean`），结果
`23/24`：

```
FAIL   m1_02_id_prefix_test SEED=1  [sim process exited nonzero]
```

其余 23 个场景（含刚落地的 `m4_ov01_overlap_test`/`m4_ft01_cfge_test`）全部
PASS。检查 `sim/out/m1_02_id_prefix_test_1.log` 本身：`UVM_ERROR : 0`、
`UVM_FATAL : 0`，无任何 `ASSERT FAILED`/`Error-`/`FATAL` 字样，`$finish`
正常在 1885000ps 结束，`V C S Simulation Report` 尾部横幅完整——日志**内容**
层面看不出任何失败迹象，只有 `scripts/regress.py` 记录的 `make -C sim run`
子进程本身的 shell 退出码非零，触发其 `rc != 0 → 强制 FAIL` 逻辑
（`scripts/make/vcs-2018.mk` 头注释原话："A nonzero `make run` exit should
force FAIL even if the log looks clean — an abnormal process exit is never
evidence of a clean run"，即本设计是有意为之，不是脚本 bug）。

## first_anomaly

signal: n/a（非仿真信号异常——是 shell/进程退出码层面的现象，日志内容本身
无任何 DUT/TB 判决异常）
time: n/a
how_found: orch 复验 BUG-0042/M4-FT01 交付物时，第一次亲跑 `make regress`
即撞见；随后两次独立尝试（(a) 单独隔离重跑
`make clean && make run TEST=m1_02_id_prefix_test SEED=1`；(b) 整批重跑
`make regress`，脚本内建 `make clean`）均转绿、无法复现。

## taxonomy

TOOL_ENV（候选，未做代码改动、未深挖到具体触发条件——命中
`workflow/bugs.md` 决策提示原句："Result changes with seed/machine/build
but not stimulus"：同一 TEST/SEED/代码树，三次独立运行两次干净、一次异常，
唯一变化的是"这一次 VCS `simv` 进程恰好非零退出"这个运行时环境变量）

## rca

```
observed:   `make regress` 单次运行里 m1_02_id_prefix_test 的 `make -C sim
            run ...` 子进程返回非零，但其产出的日志文件内容完全干净
            （UVM_ERROR:0/UVM_FATAL:0，无 assert 失败，$finish 正常）
anomaly:    同一 TEST/SEED/代码树的另两次独立运行（隔离单测 + 整批重跑）
            均返回 0、日志同样干净——退出码本身不可复现
chain:      未定位到具体触发条件。`sim/Makefile` `run:` 目标的最后一行是
            `$(OUT)/simv $(SIM_OPTS_2018) ... -l $(RUN_LOG)`——VCS `simv`
            二进制自身的退出码是 `make run` recipe 最后一条命令的退出码，
            也就是 `rc != 0` 的直接来源。日志内容层面找不到任何异常，
            排除：(a) 编译期问题（`comp.log` 未检查出异常，且该次运行确实
            产出了完整仿真——若编译失败不会有仿真报告尾部横幅）；(b) 判决
            逻辑问题（UVM_ERROR/UVM_FATAL 计数与 SVA summary 均干净，同一份
            checker 代码在另两次运行里对同一激励给出一致的干净判决）；
            (c) BUG-0042 修复引入的新逻辑（`m1_02_id_prefix_test` 用基线
            `FallThrough=1'b0`，BUG-0042 新增的孤儿队列路径在此配置下应
            恒空——已读 diff 确认修复是"空队列＝老行为"的严格超集，且另外
            两次干净复现本身就是这条假设成立的直接证据：若是代码逻辑回归，
            理应每次都复现，不会时有时无）。剩下的假设收窄到 VM/VCS
            运行时层面的一次性状况（license 瞬时争用、磁盘 I/O、内存/资源
            压力等）——本次未进一步深挖（orch 在此期间确实密集连续跑了多次
            `make run`/`make regress`，对同一台 VM 造成了比单次手动验证更
            重的负载，是这一猜测的间接支持，但未做定量确认，故仍列"未定位
            到具体触发条件"而非下结论）
```

## fix

commit: none — **REV-022 终判：WONTFIX（accepted-transient）**。`rc!=0→FAIL`
是 `vcs-2018.mk` 头注释明载的有意设计，门禁本身工作正常、TB/脚本侧无 fix
对象（不是"修不动"，是"没有可修缺陷"）；判别性数据（进程死亡信号号/
stderr）已随该次运行永久丢失、不可事后还原，命中频率极低且触发条件不可
定位，cost/benefit 判定停手。排除 `ACCEPTED@M<n>`——本条无可调度工作、无
可证伪到期条件，强设只会把不可复现现象伪装成日程债（`workflow/review.md`
rubric #8）。修复方向本就未知（触发条件未定位，无法对症下药；若强行"修"，
最可能的动作是给 `sim/Makefile` `run:` 目标加重试逻辑，但那会掩盖真实的
TOOL_ENV 根因而非解决它，本条不建议这么做——REV-022 亦确认此判断正确）。
详见 `doc/review/REV-022.md`。

## rerun

- 隔离重跑：`cd sim && make clean && make run TEST=m1_02_id_prefix_test
  SEED=1` → `EXIT=0`，`UVM_ERROR:0`/`UVM_FATAL:0`。
- 整批重跑：`cd sim && make clean && cd .. && make regress` →
  `passed=24/24`，`m1_02_id_prefix_test SEED=1` 转 `PASS`。
- 两次独立复跑均未复现异常退出码——本条暂不可机械复现，`## regression_guard`
  只能是"留意"性质，非可执行判据。

## regression_guard

见 `doc/guards.md` G-0043（BUG-0043）。

## similar

- BUG-0014/0019/0020/0022/0030 — 同族"VM/VCS-2018 环境怪癖"既有登记先例
  （lint flag 缺失、多顶层枚举中止、波形 dump 路径、强制重编陷阱、xdebug
  环境变量继承），性质相近但触发机制各不相同，本条未确认与其中任何一条
  同根，仅作同族参考。
- BUG-0037 — 另一个"看似日志/结果干净但底层机制有问题"的先例（COV=1 覆盖
  率数据库跨拓扑静默合并），但那条是**确定性可复现**的，与本条的间歇性
  不同类。
