# BUG-0022 — `make lint` 增量编译命中时假绿：门禁不区分"真干净"与"根本没 lint"

## symptom

同一份代码，连跑两次 `make lint TEST=m2_cfg01_reconfig_test`：

```
第 1 次（强制重编）：../tb/ 范围命中 259 条 Lint 告警 → exit 2
第 2 次（增量命中）：lint clean within ../tb/ scope      → exit 0
```

代码一个字节都没改，门禁结论却从"不合格"翻成"合格"。

第 2 次 VCS 打印 `The design hasn't changed and need not be recompiled.`
并把 `sim/out/lint.log` **覆写**成一份不含任何 `Lint-[...]` 行的短日志；
`sim/Makefile` 的过滤逻辑（`grep -A1 '^Lint-\[' $(OUT)/lint.log | grep -qE '^\.\./tb/'`）
在这份空日志上自然一无所获，于是输出 `lint clean within ../tb/ scope` 并
返回 0。

即：门禁把"这次根本没有执行 lint"**误判为**"lint 干净"。

## first_anomaly

signal: `sim/out/lint.log`（增量命中时被覆写为不含 Lint 行的短日志）
time: n/a（编译期）
how_found: DV 分诊卡（BUG-0021）执行 min_repro 时，第一次调用即命中——
增量编译使门禁给出假绿；删除 `sim/out/simv_lint.daidir/.vcs.timestamp`
强制重编后才复现出 259 条。三次复跑确认稳定。

## taxonomy

TB_BUG

（门禁自身的判据缺陷。归 TB_BUG 而非 TOOL_ENV：VCS 的增量编译行为本身
是正常且有意的，缺陷在于 `sim/Makefile` 的判据把"日志里没有 Lint 行"
无条件等同于"lint 通过"，未校验这次是否真的执行了 lint。）

## rca

1. `sim/Makefile` 的 `lint:` 目标以 `$(OUT)/lint.log` 中是否存在
   `^Lint-\[` 且紧邻行落在 `^\.\./tb/` 作为唯一判据。
2. VCS 在设计未变时走增量路径，不重新分析源码、**不产生任何 `Lint-[...]`
   行**，但仍以 `-l $(OUT)/lint.log` 覆写日志文件。
3. 于是"lint 未执行"与"lint 执行且干净"在日志上呈现为同一形态——
   两者都是"没有 Lint 行"。
4. 判据无法区分二者，取了乐观的一侧 ⇒ 假绿。

这与 `workflow/discipline.md` rule 2 的 gate 例外条款正面相关：门禁是产品
本身，而一个会假绿的门禁比没有门禁更危险——它给出未经验证的通过信号。

## fix

未派发（发现本条的 DV 分诊卡边界为"只判定不修复"，且 `sim/Makefile` 在
该卡的硬性边界之外）。

修复方向（不预设实现）：让判据能证明"本次确实执行了 lint"，而不是仅仅
"没看到告警"。可考虑的锚点（由后续卡自行推导取舍）——日志中是否出现本次
分析的实证标记、是否强制 `lint:` 走全量重编、或把增量命中显式识别为
"未验证"而非"通过"。

**硬性红线**（同 BUG-0021）：不得通过放宽过滤范围或降低判据来"修好"它。
本条的正确修法是让门禁**更严**，不是更松。

临时缓解：跑 lint 前删除 `sim/out/simv_lint.daidir/.vcs.timestamp`
（该文件已 gitignore）。DV 分诊卡结束时已删除它，以免下一位直接撞上假绿。

## rerun

不适用（未修复）。

## regression_guard

type: checklist
ref: `sim/Makefile` `lint:` 目标
note: 修复后必须**同时**验证两个方向，缺一不可——
(1) true-positive：在有已知 `../tb/` 告警的树上连跑两次，第二次仍须失败；
(2) true-negative：在确实干净的树上跑，须通过。
只验证其中一侧正是本条得以长期潜伏的方式。另：BUG-0021 的
`regression_guard` 设想"lint 成为可常态执行的门禁"，该设想以本条修复为
前提——在此之前，任何"lint 通过"的结论都必须附带说明它是否走了强制重编。

## min_repro

```
make lint TEST=m2_cfg01_reconfig_test    # 第 1 次：259 条，exit 2
make lint TEST=m2_cfg01_reconfig_test    # 第 2 次：clean，exit 0  ← 假绿
```

（若第 1 次也是增量命中，先 `rm -f sim/out/simv_lint.daidir/.vcs.timestamp`。
任意拉入 `sim/flist/tb.f` 的 UVM TEST，无需 SEED。）

## similar

BUG-0021（本条在其分诊过程中被撞见；两条都关乎 `make lint` 的可信度，
但性质相反——0021 是我们的代码过不了门禁，本条是门禁会放行未经检查的树）。
BUG-0014、BUG-0019（同一 `lint:` 目标的前两处缺陷，均已修复）。
none searched-on: 增量编译, incremental, 假绿, lint.log 覆写, vcs.timestamp
