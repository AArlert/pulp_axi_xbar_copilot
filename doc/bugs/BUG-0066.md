# BUG-0066 — `scripts/regress.py:50` unconditionally `make -C sim clean`s away the M4 coverage-evidence base

## symptom

`make regress` (`sim/Makefile`'s `regress:` target forwards to
`python3 ../scripts/regress.py`) called
`subprocess.run(["make", "-C", str(sim), "clean"], check=True)`
unconditionally as its very first action, before running a single test in
the regression list. `sim/Makefile:187`'s `clean:` target is
`rm -rf $(OUT)` (`$(OUT)` defaults to `sim/out`) — a bare `make regress`
therefore deleted the entire `sim/out/` tree in one shot, including all 7
`cov.vdb` coverage databases (baseline + `m0/` + `cfgA`..`cfgE`) and
`sim/out/urgText6/`, with no confirmation and no way to opt out.
`sim/out/` is `.gitignore`'d (line 3) — none of that has a
version-control backup, and it is M4 signoff's entire coverage-evidence
base.

## first_anomaly

signal: n/a (build/tooling defect, not a DUT/TB signal)
time: n/a
how_found: static reading during REV-037 §3-A triage (registered in
`doc/bugs.md` as BUG-0066, orch-authored row): `grep -n clean
scripts/regress.py` → `:50`; cross-checked against `sim/Makefile:187`'s
`clean: rm -rf $(OUT)`; `ls -d sim/out/*/cov.vdb sim/out/cov.vdb
sim/out/urgText6` confirmed 8 items present at the time of filing. No sim
run was needed or performed to find this — the destructive repro itself
is banned by this fixer card's own hard boundary (`make regress`/`make
clean` forbidden for the duration of the fix), consistent with the row's
own note ("**本卡未实跑回归**").

## taxonomy
TOOL_ENV

## rca
```
observed:   scripts/regress.py:50 called `make -C sim clean`
            unconditionally, before iterating the regression list
anomaly:    sim/Makefile's clean: target (`rm -rf $(OUT)`) removes every
            build product AND every long-lived coverage artifact under
            $(OUT) alike — it does not distinguish "stale incremental
            build cache" (the thing regress.py's own comment said it was
            guarding against) from "signoff evidence with no other copy"
chain:      regress.py wanted a clean slate to avoid VCS incremental-
            build cross-contamination (mixing option sets, e.g. after a
            prior `make lint`, corrupts the build database — VFS_SDB_
            ERROR class) -> the only clean primitive available was
            sim/Makefile's blunt whole-tree `clean:` -> BUG-0037 (M4)
            later carved 7 isolated cov.vdb databases out of that same
            directory and turned them into the coverage-evidence base ->
            regress.py's clean call was never revisited when that
            dependency was created -> any run of `make regress` (which
            M4's `make check MILESTONE=4` condition 2 and M5's N=5
            multi-seed regression both require) now destroys its own
            evidence base as a side effect
class:      TOOL_ENV — matches workflow/bugs.md's TOOL_ENV cue: an
            automation step whose blast radius changed silently once a
            new artifact class (the coverage DBs) started living inside
            its target directory, without the step itself being updated
guard:      scripts/tests/test_regress.py (this row's fix) exercises the
            real filesystem effect against a fake sim/Makefile shaped
            like the real run:/clean: targets: default invocation must
            leave pre-existing cov.vdb/urgText6 in place; `--wipe`/
            `COV_WIPE=1` must remove them and print the list first.
            `make selftest` runs it every time.
```

## fix
commit: <待 orch 回填>
what: `scripts/regress.py`'s unconditional `make -C sim clean` call is
now gated behind an explicit opt-in — `--wipe` CLI flag or `COV_WIPE=1`
environment variable. The default path skips the clean entirely and
prints why (`"regress.py: skipping \`make -C sim clean\` (default,
BUG-0066)..."`). When the wipe *is* requested, a new `_wipe_targets()`
helper globs `sim/out/**/cov.vdb` + checks `sim/out/urgText6` and prints
the exact list of paths about to be destroyed before `make -C sim clean`
actually runs. `python3 scripts/regress.py --help` documents both
branches and the rationale for each. `sim/Makefile`'s `clean:` target
itself is untouched — the defect was in the caller, not the target (this
card's exclusion zone). New permanent regression coverage:
`scripts/tests/test_regress.py` (6 test cases, wired into `make
selftest`), which builds a fake `sim/Makefile` with `run:`/`clean:`
targets shaped like the real one and asserts the *actual filesystem
effect* (files present/absent after the run), not just what argv
regress.py happens to build.

## rerun
Fixer card (2026-08-02, no destructive command executed — this card's own
hard boundary forbids `make regress`/`make clean`; verification instead
via `make selftest`/`make check` and a scratch-directory unit test that
fully stubs `subprocess.run`, run before the permanent test file was
added):

- `make selftest` — PASS, 67/67 (61 pre-existing + 6 new
  `test_regress.py` cases), including `test_default_run_preserves_
  coverage_artifacts`, `test_wipe_flag_destroys_and_announces_targets`,
  `test_cov_wipe_env_var_equivalent_to_flag`, `test_help_documents_wipe_
  default`, and two pure-function checks on `_wipe_targets()`.
- `make check` — PASS (docs-check + chain audit; no new gap introduced).
- `python3 -c "import ast; ast.parse(open('scripts/regress.py').read())"`
  — exit 0.

Non-sim closure (signature is the real `unittest` outcome of a suite that
asserts filesystem state, not a self-printed token — the kill-test is
built into the suite itself: reverting the `if wipe:` gate reddens
`test_default_run_preserves_coverage_artifacts` immediately):

```
make evidence BUG=0066 \
  CMD='cd scripts/tests && python3 -m unittest test_regress -v' \
  EXPECT='^OK$'
```

closer ≠ fixer: an independent instance must re-run `make selftest`,
`make check`, and the CMD above before flipping status.

Independent closer (different instance — invariant 3, card 0b, 2026-08-02,
no VCS on this machine — non-sim closure only, per this row's own carrier):
own-designed kill test (not the fixer's reasoning, re-derived from reading
`scripts/regress.py`'s `if wipe:` gate directly) — mutated a working copy
of `scripts/regress.py` back to the pre-fix shape (unconditional
`subprocess.run(["make", "-C", str(sim), "clean"], check=True)` with the
`if wipe:`/`else:` gate removed entirely) and reran
`cd scripts/tests && python3 -m unittest test_regress -v`:

- **red**: `Ran 6 tests in 0.171s` → `FAILED (failures=2)` —
  `test_default_run_preserves_coverage_artifacts` (`AssertionError: False
  is not true : default run deleted cov.vdb`) and
  `test_wipe_flag_destroys_and_announces_targets` (the destroy-list is no
  longer printed before an unconditional clean, so the announcement
  assertion also breaks) both fail; the other 4 stay green.
- restored `scripts/regress.py` via `git checkout --`, confirmed byte-
  identical to the pre-mutation copy, reran: **green**, `Ran 6 tests in
  0.169s` → `OK`, all 6 pass.

Machine closure (own signature, not copied from the fixer's `EXPECT='^OK$'`
verbatim — strengthened to pin a real measured count, not just a bare
token): `python3 -m unittest test_regress -v`'s own `Ran 6 tests` line is
parsed for the digit and re-printed alongside the exit code, so the
EXPECT below can only be satisfied by an actual 6-test all-green run, not
an unconditionally-echoed placeholder:

```
make evidence BUG=BUG-0066 \
  CMD='out=$(cd scripts/tests && python3 -m unittest test_regress -v 2>&1); rc=$?; n=$(printf "%s" "$out" | grep -oE "Ran [0-9]+ tests" | grep -oE "[0-9]+"); printf "%s\n" "$out"; echo "REGRESS_KILLTEST rc=$rc ran=$n"; exit $rc' \
  EXPECT='^REGRESS_KILLTEST rc=0 ran=6$'
```

`make -n evidence` was run first to confirm the `$`-escaped (`$$`) form
survives Make's recipe expansion intact before the real run (BUG-0069
lesson). Evidence: `doc/evidence/v0.4.40/BUG-0066.log`.

## regression_guard
type: script
paths: scripts/regress.py, scripts/tests/test_regress.py, sim/Makefile (clean target)
check: `cd scripts/tests && python3 -m unittest test_regress -v` (also
runs as part of `make selftest`, i.e. every card's own signoff gate)
note: if `sim/Makefile`'s `clean:` target scope ever changes (a new
artifact class becomes destructible-by-accident), `_wipe_targets()` in
`scripts/regress.py` and this guard's fixture Makefile both need a
matching update — the exact "a new artifact starts living inside an old
blast radius, unnoticed" shape that caused this bug in the first place.

## similar
BUG-0056 (this fixer card's sibling row, same REV-037 §3 triage batch) —
same destructive-`make clean`-inside-a-guard shape; different carrier
(checked-in documentation `regression_guard.ref` fields instead of a
script).
BUG-0037 — the change (7 isolated `cov.vdb` databases carved out of
`sim/out/`) that turned regress.py's pre-existing `make clean` call from
"safe cleanup" into "silent evidence destruction"; its own fix never
revisited `regress.py`'s clean call, which is exactly the omission this
row closes.
