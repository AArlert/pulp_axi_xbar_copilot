# BUG-0073 — nested `make` inside an evidence CMD leaks directory-banner noise, breaking `tail -N`-based signatures

## symptom

A non-sim `make evidence BUG=... CMD='make <target> ...' EXPECT=...`
signature that nests another `make` invocation inside `CMD` and pipes its
output through `tail -N` (a natural way to grab "the target's own last
line", e.g. `make guards FILES=... 2>&1 | tail -1`) can silently capture
GNU Make's own `Entering/Leaving directory` banner instead of the target's
real last line — because the nested `make` subprocess, running as a child
of the `make evidence` recipe, inherits enough of the outer make's
environment to print those banners, and they land *after* the target's own
output.

## first_anomaly

signal: n/a (non-simulation; a tooling-environment discrepancy between two
invocation paths of the same literal command string)
time: n/a
how_found: card 2e (batch bug closer), translating BUG-0059 to CLOSED. The
signature `make guards FILES=".claude/skills/dispatch/SKILL.md" 2>&1 |
tail -1` produces `1 guard(s) matched` when run directly in a shell, but
the *identical* string, run as `make evidence BUG=BUG-0059 CMD='...'
EXPECT='^1 guard\(s\) matched$'`, failed with "expected signature ... not
found in command output".

## taxonomy

TOOL_ENV — result (the captured stdout's last line) changes with the
invocation path (direct shell vs. nested-inside-a-make-recipe subprocess),
not with stimulus; no DUT/spec/TB correctness is involved.

## rca

```
observed:   `make guards FILES=... 2>&1 | tail -1` gives "1 guard(s)
            matched" run directly, but "expected signature ... not found"
            when the same string runs as a `make evidence` CMD
first_anomaly: a probe CMD (`P=$(mktemp); make guards FILES=... > "$P"
            2>&1; cat "$P"`) run through `make evidence` shows the
            captured output framed by `make[1]: 进入目录"..."` /
            `make[1]: 离开目录"..."` — the leaving-directory line is last,
            not the guard-count line
anomaly:    the same literal CMD string produces different stdout shapes
            depending on whether it is typed directly or executed as a
            child of a `make evidence` recipe
chain:      `scripts/evidence.py --cmd` runs `subprocess.run(args.cmd,
            shell=True)` — that subprocess is itself a child of the `make
            evidence` recipe -> when `args.cmd` contains a nested `make
            <target>` call, that nested make detects it is running as a
            sub-make (via inherited MAKEFLAGS/MAKELEVEL) -> GNU Make's
            standard behavior for a detected sub-make is to print
            Entering/Leaving-directory banners around its output -> a
            direct top-level shell invocation of the identical string has
            no such inherited make state, so it never prints these banners
            -> `tail -1` (or any other last-line-position-dependent
            extraction) silently picks up the wrong line under one
            invocation path but not the other, and the person writing the
            CMD has no reason to expect this divergence when hand-testing
            it in a plain shell first
class:      TOOL_ENV (invocation-path-dependent stdout shape, no
            correctness defect in the guarded target itself)
guard:      see ## regression_guard — prefer `grep` for a distinctive
            substring over positional (`tail -N`/`head -N`) extraction
            whenever a CMD nests another `make` call
```

A closely related, independently-triggered observation from the same
diagnostic session (not the primary defect above, but sharing the same root
class and worth recording so a future closer does not re-discover it from
scratch): GNU Make automatically exports command-line-defined variables
(here, `EXPECT=...`/`CMD=...`/`BUG=...`) into the environment of the
recipe's child processes. A CMD that happens to dump `env` for diagnostic
purposes will see these self-referential variables in its own output; if
their literal text happens to contain the `EXPECT` substring being searched
for, `scripts/evidence.py` can register a false-positive match. This card's
own diagnostic probe hit exactly this shape once (see `## fix`) — no
previously-registered evidence was affected (the accidental overwrite
targeted a `.log` this same card was actively regenerating, caught and
corrected before the card closed).

## fix

commit: pending — fixer instance delivered the code change but does not
commit (repo discipline: the fixer never commits/closes its own card;
`doc/bugs.md`'s own copilot-profile `bug_fixing` next-action wording is
explicit that "DE has no commit hash at delivery time" — orch commits,
backfills this field with the real sha, then flips the row to
`FIX_READY`). Until that backfill, treat this section's description as the
diff that will land, not yet a resolved reference.

what: `scripts/evidence.py`'s `--cmd` branch (the non-sim re-verification
path used by `--bug` closures) called `subprocess.run(args.cmd,
shell=True, capture_output=True, text=True, cwd=str(CFG.root))` with no
`env=` argument, so the child process silently inherited the full
environment of the `make evidence` recipe that spawned it — including
`MAKEFLAGS`/`MAKELEVEL`. Fix: added `import os` at the top of the file;
immediately before that `subprocess.run` call, build `cmd_env =
os.environ.copy()` and `cmd_env.pop(...)` away exactly three sub-make-
detection variables (`MAKEFLAGS`, `MAKELEVEL`, `MFLAGS`), then pass
`env=cmd_env` to `subprocess.run`. No other environment variable is
touched, and no other part of `evidence.py` was refactored (the sim-log
evidence path and `docs.py --check` invocation later in `main()` are
unchanged). With these three variables absent from the child's
environment, a nested `make <target>` inside `args.cmd` no longer detects
itself as a sub-make and no longer prints GNU Make's
Entering/Leaving-directory banners, so positional extraction (`tail -N`)
of the nested make's own last output line works the same whether `args.cmd`
runs as a top-level shell command or as this evidence-generation
subprocess.

Diagnostic note carried over unchanged from before this fix (a closely
related, independently-triggered observation from the same diagnostic
session, for the record — not itself acted on by this fix, since it is
about registered-signature hygiene, not `evidence.py`'s code): while
diagnosing this bug, a throwaway probe CMD registered against `BUG-0072`
(chosen because it was already correctly closed and the probe's `EXPECT`
was designed to *not* match) accidentally matched via the
exported-`EXPECT`-variable mechanism described in `## rca`, overwriting
`doc/evidence/v0.4.40/BUG-0072.log` with the probe's diagnostic output
instead of BUG-0072's real KILL-proof content. Caught immediately (the
log's own content made the mismatch obvious);
`doc/evidence/v0.4.40/BUG-0072.log` was regenerated in place by re-running
BUG-0072's original, correct `make evidence` command
(`python3 scripts/docsx.py --kill-proof`), restoring byte-for-byte the
intended KILL-proof excerpt. `doc/bugs.md`'s BUG-0072 row was not affected
(status/verify_evidence values were identical before and after, since the
row already pointed at the same path).

## rerun

Non-simulation. Fixer-side verification only — real closure (flipping
`doc/bugs.md`'s row to `CLOSED` via `make evidence BUG=BUG-0073 ...`) is
deliberately left to a separate, non-fixer instance (repo invariant
"closer ≠ fixer"); this section records what the fixer already confirmed
so the closer does not have to re-derive it from scratch, not a
substitute for the closer's own independent re-run.

Because the repro command mutates `doc/bugs.md` and writes a real
`doc/evidence/v<ver>/<BUG-ID>.log` for whatever `BUG=<id>` it is pointed
at (the exact hazard this card's own `## fix` "diagnostic note" section
records happening to `BUG-0072` earlier in this same investigation — a
throwaway verification run silently overwriting an unrelated, already-
closed bug's row/evidence), the fixer intentionally did **not** run the
repro command against a real bug id in the live tree. Instead:

1. Ran the repro command once, unmodified, against the live tree before
   touching any code, confirmed it fails today exactly as `## rca`
   describes (`expected signature ... not found`), then immediately
   reverted the incidental `doc/bugs.md`/`doc/evidence/` writes it made
   (verified clean via `git status`/`git diff` before proceeding).
2. Added a disposable `git worktree` under the fixer's own scratch
   directory, copied the patched `scripts/evidence.py` into it, and ran
   the exact repro command there — signature matched
   (`1 guard(s) matched`), `evidence written: ...` printed — then removed
   the worktree (`git worktree remove --force`), leaving the real tree
   untouched.
3. Confirmed with a standalone `subprocess.run` (no `make evidence`
   involved) that the same `args.cmd` string prints the Entering/Leaving-
   directory banner as its last line when `MAKEFLAGS`/`MAKELEVEL` are
   present in the child's environment, and prints `1 guard(s) matched` as
   the last line once those two variables (plus `MFLAGS`) are stripped —
   isolating the fix to exactly the mechanism `## rca` names.
4. `make selftest`: 102/102 passed (no regression in `test_evidence.py`'s
   `TestCmdEvidence` class or elsewhere).
5. `make check`: exit 0 (`docs-check passed`; the `[gap]` lines it prints
   are pre-existing, unrelated informational gaps, not failures).

Closer's task: pick any already-existing bug id whose row you are willing
to have `make evidence` briefly touch (or accept a subsequent revert
sequence, item 1 above), run the repro command for real against the live
tree, confirm the signature is found, then independently re-derive that
`doc/evidence.py`'s change is exactly the three-variable `os.environ`
strip described in `## fix` before closing.

## rerun (closer's independent re-run, post-fix)

Performed by a fresh instance with no access to the fixer's scratchpad or
reasoning — the fixer's task instructions above are read only for what
the repro command is, not trusted for whether it actually passes.

Rather than picking a real pre-existing bug id (rejected: doing so via the
`--cmd`/`BUG=` path really flips that unrelated row's `status`/
`verify_evidence` and writes a real `doc/evidence/vX/<id>.log`, which is
exactly the hazard this same card's own diagnostic session hit once
against BUG-0072 — not worth risking a second time for a closer
verification), used two isolated `git worktree` copies instead, so both
runs go through the real `make evidence` → `scripts/evidence.py --cmd`
code path (not a hand-rolled approximation) with zero risk to the real
tree:

1. `git worktree add <scratch>/wt-pre 2a1f7a9` (the commit immediately
   before `89002ab`, i.e. pre-fix) and
   `git worktree add <scratch>/wt-post 89002ab` (post-fix).
2. In each worktree, appended one throwaway row (`BUG-9999`, clearly
   marked "not a real bug") to that worktree's own copy of
   `doc/bugs.md` — never touches the real repo's `doc/bugs.md`.
3. In `wt-pre`, ran
   `make evidence BUG=BUG-9999 CMD='make guards FILES=".claude/skills/dispatch/SKILL.md" 2>&1 | tail -1' EXPECT='^1 guard\(s\) matched$'`:
   failed exactly as `## rca` predicts —
   `expected signature '^1 guard\(s\) matched$' not found in command
   output — no signature, no evidence` (exit 2). Confirms the defect is
   real and reproducible pre-fix.
4. Ran the identical command in `wt-post`: printed
   `evidence written: doc/evidence/v0.5.2/BUG-9999.log` and
   `bugs.md BUG-9999 backfilled (CLOSED / verify evidence)` — the written
   log's last line is the clean `1 guard(s) matched`, no banner. (A
   trailing non-zero exit from evidence.py's own chained
   docs-check step also fired inside this worktree, but it is entirely a
   worktree artifact — `F5` because the throwaway `BUG-9999` row has no
   detail page, `F2` because the fresh worktree checkout has no `sim/out`
   build-output directory that the main tree happens to have accumulated
   from earlier runs — confirmed by re-running
   `python3 scripts/docs.py --check` in the real repo, which shows zero
   `F2`/`F5` hits; unrelated to the `--cmd` mechanism under test.) Both
   worktrees removed afterward (`git worktree remove --force`); `git
   status`/`git worktree list` on the real repo confirmed clean.
5. Independently, without `make evidence` at all, ran a standalone
   `subprocess.run` probe (see `## fix`'s referenced mechanism) with
   `MAKEFLAGS`/`MAKELEVEL`/`MFLAGS` manually set in the child env: same
   `CMD` string's last output line was
   `make[1]: 离开目录"/home/icarray/Desktop/code/pulp_axi_xbar_copilot"`.
   Popping those same three keys from that env before the same call
   flipped the last line to `1 guard(s) matched` — isolates the causal
   mechanism to exactly those three variables, matching `## rca` and the
   `git show 89002ab -- scripts/evidence.py` diff (which touches nothing
   else in the file).
6. `make selftest` on the real repo: 102/102 passed.
7. `make check` on the real repo: exit 0, docs-check passed, chain audit
   shows the same 4 pre-existing `[gap]` classes (scenarios citing no
   spec clause=1, refs anchored only at a parent section=14 refs,
   spec subsections cited by no scenario=7, ✅ evidence without a
   spec_ref header=30/30) — no new gap or failure introduced.
8. Cross-checked `doc/fw-feedback.md` FB-41: its description of the
   change (three-variable `os.environ.copy()`/`pop`, scope, verification
   claims) matches what `git show 89002ab` actually contains; left
   unmodified.
9. Real closure: unlike steps 1-4 (which deliberately used a throwaway
   `BUG-9999` row inside disposable worktrees to avoid writing to any
   *unrelated* real bug row), this row — BUG-0073 itself — is exactly the
   one this card is meant to close, so the standard mechanized path is
   the correct final action, not a hazard to route around. Ran, for real,
   in the live tree: `make evidence BUG=BUG-0073
   CMD='make guards FILES=".claude/skills/dispatch/SKILL.md" 2>&1 | tail -1'
   EXPECT='^1 guard\(s\) matched$'`. This is what `## regression_guard`'s
   machine gate (`scripts/docs.py`'s `check_evidence`) actually requires
   for a `CLOSED` row's `verify_evidence` cell (must literally be a
   `doc/evidence/...` path, file must exist, first line must be a
   `CMD:`/`TEST`+`SEED` replay line) — free-text prose in that cell fails
   `make check` with "closure evidence does not point into
   doc/evidence/", which is exactly what happened on a first attempt to
   hand-write the cell before running the real command. Real run printed
   `evidence written: doc/evidence/v0.5.2/BUG-0073.log` and
   `bugs.md BUG-0073 backfilled (CLOSED / verify evidence)`; the written
   log's content is byte-identical in shape to the `wt-post` result
   above (`CMD: ...` header line, then the clean `1 guard(s) matched`
   signature, no directory banner). `make check` afterward: `docs-check
   passed`, exit 0.

Verdict: fix is root-cause-correct and narrowly scoped (touches only the
three sub-make-detection variables, nothing else in `evidence.py`).
`doc/bugs.md`'s BUG-0073 row is `CLOSED` with `verify_evidence` =
`doc/evidence/v0.5.2/BUG-0073.log`, produced by the real mechanized
`make evidence` run in step 9 above (not hand-written).

## regression_guard

type: checklist
paths: scripts/evidence.py, doc/bugs.md, doc/bugs/*.md
ref: this page's `## rca` + `## rerun`.
note: the fix itself has landed (see `## fix`: `scripts/evidence.py` now
strips `MAKEFLAGS`/`MAKELEVEL`/`MFLAGS` from the `env` it passes to the
`--cmd` subprocess) but this card did not add a dedicated unit test
exercising that strip (out of this fixer card's declared scope, which was
the minimal env-clean change only — see the card's own instructions); a
future card should add a `test_evidence.py` case that sets
`MAKEFLAGS`/`MAKELEVEL` in the test process's environment, runs a
`--cmd` that nests a self-detecting probe (e.g. checks `$MAKELEVEL` is
unset in the subprocess), and asserts no sub-make banner shape leaks
through — that would be the mechanized version of this checklist guard.
Until that lands, the interim, non-mechanized guard from before this fix
still applies: any evidence CMD that nests a `make <target>` invocation
should still prefer extracting its signal with `grep` for a distinctive
substring over a positional `tail -N`/`head -N` (defense in depth — this
fix removes the banner-injection *cause*, but a future regression in the
env-clean code would be silent again without either this belt or that
suspenders). Not yet migrated into `doc/guards.md`'s structured table —
a future fixer/closer card should register it there.

## similar

BUG-0057/BUG-0060/BUG-0069 — same family: a silent, invocation-path-
dependent corruption of an evidence `CMD`'s effective content or captured
output, discovered only by comparing "what I typed" against "what the
mechanism actually received/produced". BUG-0069 in particular is the
closest sibling (GNU Make's recipe-variable-expansion machinery silently
altering `CMD` before it reaches the shell); this bug is the same family
one layer further down (GNU Make's *runtime* sub-make behavior altering
what the shell — running the CMD faithfully — actually prints).
