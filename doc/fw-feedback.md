# 实践记录（原：iverif-workflow 框架反馈台账）

> **本文件的性质自 0.3.7 起改变，文件名与 `FB-` 编号一律不动。**
> 改名会打断 29 处引用，其中 **10 处在冻结记录里**（`signoff-M0/M2`、
> `REV-004/010`、三份 bug 详情页、三份归档）——按 FB-23 裁决那些不得回改。
> 所以变的是这个文件**是什么**，不是它在哪。
>
> **旧性质（FB-1 ~ FB-27）**：向上游提交的反馈台账。本仓库是 iverif-workflow
> 的首次实战应用，摩擦当场登记、blocking 当日回流、等上游修。
>
> **新性质（FB-28 起）**：**实践记录**。框架 0.8.0 删除了 fwsync/manifest/
> divergence 三态，「绝不本地修改 `scripts/`/`workflow/`」这条红线在机制上
> 已不存在，也不再有回流义务。本仓库的做法改为——**自己先做，做好了让上游
> 来 cherry-pick**。因此每一行回答的问题从「求上游修什么」变成
> 「**我们做了什么改动、为什么、上游要不要抄**」。
>
> **唯一的硬要求没变、而且比以前更重**：既然没有 manifest 记录我们改过哪些
> 上游文件，**本台账就是那份记录本身**。任何对 `workflow/` `scripts/`
> `.claude/` 的本地改动，必须在此留行并在代码旁注明 `见 doc/fw-feedback.md
> FB-xx`。漏了就退化成 FB-7 / BUG-0007 那个形状（变通只落注释、无人可 grep），
> 而这次连 `fw-check` 都不会再提醒你。
>
> 历史行提到的 `fw-check` / `fwsync` / `workflow/{schema,taxonomy,dispatch,
> signoff}/` 等均为**当时事实**，按 FB-23 裁决不回改。

<!-- docsx:skip .claude/agents/orch.md .claude/skills/doc-mechanization/SKILL.md scripts/docsx.py doc/docsx-baseline.md scripts/tests/test_docsx.py scripts/cov_loop.py -->
（FB-37 之后的行历史性提及了已随 FB-40 删除的 `scripts/docsx.py`/
`doc/docsx-baseline.md`/`scripts/tests/test_docsx.py`——那些行写下时这些
文件确实存在，按本文件顶部"旧行按当时事实、不回改"的同一裁决处理，上面
这行标记覆盖它们。）
severity：blocking（门禁/脚本拒绝正确工作）| annoyance（文档/体验问题）

status（FB-1~27，上游台账时代）：open | reported | fixed@\<ver\> | wontfix
status（FB-28 起，实践记录时代）：**local**（本仓库已改，上游可抄）|
**noted**（只记录，未动手）| **upstreamed**（上游已 cherry-pick）

**第一轮回流已闭环（2026-07-27）**：FB-1~FB-7 全部落入框架 0.2.1，本仓库已
`fwsync --pull`（`iverif.json` 从 0.2.0 → 0.2.1，`make fw-check` / `docs-check`
双绿）。FB-5 的绕行（orch 手动回退 FIX_READY→FIXING）自此作废，按 `.claude/agents/de.md`
新措辞走即可。

| id | date | symptom | framework file | severity | status |
|----|------|---------|----------------|----------|--------|
| （FB-1~FB-27，上游台账时代 29 行，整体搬移，逐字不改）见 `doc/archive/fw-feedback-archive.md` | | | | | |
| FB-28 | 2026-07-29 | **`.claude/agents/orch.md` 把 87 行主会话硬规则投递给了除主会话外的所有人——本仓库已删除该文件并把内容并入 `/dispatch` skill + `CLAUDE.md` §0** | **机制事实（本会话直接观测，非推断）**：`.claude/agents/*.md` 在 Claude Code 里只有一个作用——注册成**可派发的子代理类型**；文件正文仅在该类型被 `Agent` 工具派发时注入**新实例**的系统提示。0.8.0 新增 `orch.md` 后，主会话收到的系统提示只有一行 frontmatter `description`（"Orchestrator (orch) — the main session…"），**87 行正文一个字都没到达**——直到 orch 显式 `Read` 它才看见。⇒ 一份通篇写着 "You are orch — the main session" 的文件，唯一读不到它的就是主会话。**为什么它也不该被派发**：派一个 orch 子代理会同时违反它自己两条规则——(1) §"Handoffs are records, not conversation" 禁止「只传上下文」的旁路，而子代理**唯一**的回传形式就是最终那段文字，即口头摘要；(2) §"Closer ≠ fixer is your call" 要求 orch 自己追踪派发路由，嵌套实例的路由决策对持有台账的主会话不可见。再加上它写不了 `status.jsonl`/`log.md`（closeout 是主会话职责）。**唯一站得住的用法是反过来**：把它当独立的**组卡审查员**（我组好卡、派一个实例只审隔离与共模防火墙），那是审计不是编排。**放大风险**：0.8.0 同时废弃了 `.claude/skills/`（orch 的派卡手册原本住那儿、主会话会读）**又**把规则搬进 agent 卡（主会话读不到）⇒ 对严格照上游默认走的采纳者，orch 的隔离硬规则**没有任何人读得到**：主会话拿不到正文，而 orch 实例永远不会被派出来。本仓库侥幸没事，只因保留了四个 skill 作本地资产。这是 FB-21/25/27「机制无消费者」家族里目前最尖锐的一例——前几例是文档指向退休命令，本例是 87 行硬规则投错了收件人。**本仓库改动（供上游取用）**：① 删除 `.claude/agents/orch.md`；② 其中三块 `/dispatch` 原本没有的操作性内容并入该 skill——**closer ≠ fixer 的路由判断**（skill 此前完全没提）、**rev 卡只给范围不给结论**、**§3b「旁路即漂移」原理与补救**（两角色需要同一背景时指向同一份归档记录，而非互相转述；一方挖出的上下文另一方要重挖是**留存失败**而非效率损失）；③ `CLAUDE.md` §0 的指针从 orch.md 改指 `/dispatch`。**分层理由**：CLAUDE.md 是「常驻但字节受限」层（预算仅剩数百字节），skill 是「用时载入、不受预算约束」层，而 `/dispatch` 恰在每次派卡前被调用 ⇒ 消费时机比 CLAUDE.md 更准 | .claude/agents/orch.md（已删）→ .claude/skills/dispatch/SKILL.md §3/§3b + CLAUDE.md §0 | annoyance（不阻塞门禁；但受影响的是 orch 全部隔离硬规则的可达性） | local |
| FB-29 | 2026-07-30 | **`check_kill_coverage()` 只扫 live `bugs.md`、不扫 `bugs_archive.md`，导致已归档的 KILL 自证行不计入里程碑 kill coverage**（本仓库 M3 实测发现，当场压测）：`scripts/docs.py:928-948` 的 `check_kill_coverage(mnum)` 仅调用 `parse_table(CFG.bugs)`（第 939 行），漏掉 `parse_table(CFG.bugs_archive)`。本仓库真实现状：KILL-0001 与 KILL-0002 两条 M3 标记的 KILL 行早已在 `doc/archive/bugs-archive.md`（`make archive` 的自动搬迁结果），但 `make check MILESTONE=3` 条件 4（kill coverage）仍报 `[FAIL]` → "none — no checker proven able to fail this milestone"——尽管 `doc/archive/bugs-archive.md` 第 38-39 行明确存在、summary 明确含 M3 token。**这是纯扫描范围遗漏**，不是缺失的 kill 自证——同文件内其他函数（470-471 行、517-518 行、524 行、806-809 行）在处理"终态行会被归档"的场景时，都同时读了两张表或两份文本（`bug_rows + abug_rows` 模式），唯独 `check_kill_coverage()` 漏掉。**测试未捕获**因为既有夹具 `TestSignoffGate._green_with_regress_summary()`（第 131-141 行）只在 live bugs.md 里造 KILL 行，没有模拟"KILL 已被归档"这个场景。**本仓库改动**：① `check_kill_coverage()` 改为同时扫两张表（跟第 470-471 行既有模式）；② `test_docs.py` 补一条回归测试：创建空 live bugs.md + 归档表里有 KILL 行，断言 `--check --milestone 1` 条件 4 仍 PASS；③ 代码旁注明 `见 doc/fw-feedback.md FB-29` | scripts/docs.py:928-948（`check_kill_coverage()` 扫描范围） + scripts/tests/test_docs.py（回归测试） | blocking（`make check MILESTONE=3` 条件 4 红、里程碑无法签核） | local |
| FB-30 | 2026-07-30 | **`scripts/make/vcs-2018.mk` 的覆盖率变量 `CM` 用 `:=`（立即展开）绑定 `-cm_dir $(OUT)/cov.vdb`，早于 `include` 它的 `sim/Makefile` 里任何 per-config `override OUT`/per-TEST 分支执行——`$(OUT)` 被冻结成 include 时刻的默认值，后续覆盖改不动已展开的字符串**（BUG-0037 DV 自修卡实测发现并修复，见 `doc/bugs/BUG-0037.md`）：后果是 `make regress COV=1` 对全部 TEST（含拓扑迥异的 `upstream_sanity` 与四个 config 点 cfgA-D）一律把覆盖率写进同一个 `out/cov.vdb`，`make cov` 的 `urg` merge 报 825 行 `UCAPI-INSTANCEMISMATCH` + 3139 行 `CMR-VCINF`（VCS 对结构不匹配 instance 采取丢弃而非静默错误合并，安全网未失守，但"一次 `make regress COV=1` 即得干净基线数字"的假设被污染）。**本仓库改动**（两处，均已加内联注释指回本行）：① `scripts/make/vcs-2018.mk` 新增 `COV_DIR = $(OUT)/cov.vdb` 间接层并把 `CM` 自身也改 `=`（递归展开），使 `-cm_dir` 在 `compile:`/`run:` **recipe 执行时**才求值，此时 `sim/Makefile` 的 per-config `override OUT` 早已生效——cfgA-D 零改动即自动获得正确隔离；② `sim/Makefile` 给 `TEST=upstream_sanity` 分支单加一行 `COV_DIR := $(OUT)/m0/cov.vdb`，只挪覆盖率库路径、不动 `OUT` 本身（避免波及 `make clean` 默认作用域与 M0 构建产物路径）。修复后 `make regress COV=1` 一次运行自动产出 5 个隔离的 `cov.vdb`（baseline/m0/cfgA-D），逐一 `make cov TEST=<domain>` 核对 0 处 mismatch/CMR-VCINF；`make regress COV=1` 功能判定前后 22/22 PASS 不变（本条只影响覆盖率数据库，不影响任何场景的 PASS/FAIL）。**建议上游取用**：`CM`/`-cm_dir` 一类"依赖 including makefile 稍后才定的变量"的 pinned 片段，一律用 `=` 而非 `:=`——这是本仓库第二次撞上同型 GNU Make 陷阱（第一次是 BUG-0014/0019 的 flag 拼接顺序，但那两条是缺 flag 而非展开时机），值得在 `vcs-2018.mk` 自己的 workaround 清单里留一条通用提示，防止未来新增 per-`OUT`/per-`TEST` 覆盖率或波形路径时重踩 | scripts/make/vcs-2018.mk（`CM`/`COV_DIR` 绑定方式） + sim/Makefile（M0 分支新增 `COV_DIR` 覆盖 + `compile:`/`cov:` 目标随 `COV_DIR` 改写） | blocking（污染 M4 覆盖率基线报告的可信度——REV-016 条件 2 待重出的报告依赖干净的默认路径数字，本条修复前只能靠手工 `CM=`/`OUT=` 绕过） | local |
| FB-31 | 2026-08-02 | **`make next` 的里程碑完成判据是一份私有、比 `make check MILESTONE=<n>` 更窄的副本——从不检查 bug 终态/KILL 覆盖，只看 regress 证据 + 签核文件是否存在**（BUG-0054，REV-037 §BUG-0054 裁明"正确的收口不是解析散文里的 REJECTED 字样（脆弱且可绕），而是复用 `make check` 已经在算的那套条件求值器——判据只该有一份实现"）：`scripts/docs.py` 里 `cmd_signoff()`（`--check --milestone`）与 `cmd_handoff()`（`--next`）的里程碑判据各写一份，前者含四条（场景✅/regress 证据/bug 终态/KILL 覆盖，见 `doc/milestone.md:3`），后者只含两条——实测 `make next` 在 14 个 M4 bug 仍 OPEN 时仍打印"three hard conditions met → make bump-minor"。**本仓库改动**：① 把 `cmd_signoff()` 的 bug-终态判据抽成纯数据函数 `milestone_bugs_terminal(mnum)`（返回 `(ok, detail)`，不打印），`cmd_signoff()` 与 `cmd_handoff()` 共用；② 把 `check_kill_coverage()` 拆成纯数据 `kill_coverage_hits(mnum)` + 打印外壳，`cmd_handoff()` 同样复用；③ `cmd_handoff()` 的里程碑完成分支现同时要求 regress 证据 + bug 终态 + KILL 覆盖 + 签核文件存在，四条不齐则并入"still missing"清单；④ 即使四条机器条件全绿，成功文案也不再无条件说"go bump-minor"——改为"读 `signoff-M<n>*.md`，确认其记录的裁决不是 REJECTED"，因为**签核记录的实际裁决内容本就该是人读的事**（`doc/milestone.md:3`："机器核验…加 rev 签核记录…二者缺一不可"），机器解析 REJECTED 字样正是 REV-037 明确否决的路线；⑤ `scripts/tests/test_docs.py` 补一条红/绿回归（`test_open_bug_blocks_completion_message`，与 REV-037 给定的红/绿 KILL 用例同形）。**建议上游取用**：任何"两处各自实现同一判据"的形状都值得抽成共享函数——这是本仓库第二次撞上这类漂移（第一次是 FB-29 的扫描范围遗漏，性质不同但教训同源） | scripts/docs.py（`milestone_bugs_terminal`/`kill_coverage_hits` 抽取 + `cmd_handoff()` 里程碑分支）+ scripts/tests/test_docs.py（回归测试） | blocking（`make next` 是每次会话开头被信任的位置，CLAUDE.md §2） | local |
| FB-32 | 2026-08-02 | **`scripts/evidence.py` 对 CMD 型证据无仓内可重放约束（BUG-0057），且 `--bug` 分支先写证据文件、后校验行是否存在、且不校验 id 形式（BUG-0060）**：`doc/evidence/v0.4.6/BUG-0040.log` 首行 CMD 指向 `/tmp` 下的 scratchpad 脚本、签名是脚本自印口令，`scripts/evidence.py:186-217` 对 `--cmd`/`--expect` 内容本无任何约束（REV-037 §BUG-0057 已裁"仓内可重放"半面可机械化、"EXPECT 须来自门禁"半面不可机械化改走 checklist guard）；同一文件的 `--bug` 分支把 `ev_path.write_text(...)` 放在 `update_row()` 的行存在性校验之前，一次 id 笔误（如 `BUG=0048` 漏写前缀）会先落一份完整签名的孤儿 `.log`、再报错退出，`docs.py` 的孤儿检查只扫 `doc/bugs/*.md`、不扫 `doc/evidence/`（REV-037 §BUG-0060）。两条根因在同一文件同一 CMD 分支，REV-037 §BUG-0060 明判"应同卡"。**本仓库改动**：① 新增 `CMD_ABS_PATH_RE` 启发式扫描 `--cmd` 文本中的绝对路径 token，逐个 `Path(tok).resolve()` 核对是否落在 `CFG.root` 内，不在则在跑任何命令/写任何文件之前退出非零（不是沙箱化 shell 解析，只catch BUG-0057 的具体形状——嵌入的 `/tmp` 可执行路径）；② 新增 `row_exists()`（纯 peek，不写盘）+ `BUG_ID_RE`（`^BUG-\d+$`），在 `main()` 里紧跟 `rid = args.scen or args.bug` 之后、任何写盘/执行之前，先校验 `--bug` 的 id 形式与行存在性；③ `scripts/tests/test_evidence.py` 补 5 条回归（仓外路径拒绝+零落盘、仓内绝对路径不误伤、短形式拒绝、typo 先于写盘被拒）。**孤儿检查双向门本身**（`doc/evidence/` 有文件无行）留给 docsx F5（REV-038 §C 已改判），未在本条处置——本条只闭合 evidence.py 自身的两处判据缺口 | scripts/evidence.py（`CMD_ABS_PATH_RE`/`row_exists`/`BUG_ID_RE` + `main()` 校验前移）+ scripts/tests/test_evidence.py（5 条回归） | blocking（BUG-0057：关闭证据可被伪造且不可重放；BUG-0060：孤儿证据文件长期游离账目外） | local |
| FB-33 | 2026-08-02 | **`.claude/skills/dispatch/SKILL.md` 派卡前自检清单没有 scratchpad 隔离条款——同一 session 内所有子代理实例共用一个 scratchpad 根目录，closer 可直接读到 fixer 的中间产物**（BUG-0059，REV-037 §BUG-0059 裁明"真正的载体是一条 `type: checklist` guard"，因为该约定由 orch 手工执行、不是被脚本运行的动作，不可机械化）：`grep -rn scratchpad .claude/` 除 `settings.local.json` 外零命中；两份已入库证据的 CMD 各嵌一个不同的 session uuid，证明 scratchpad 的作用域是 session、不是 instance。CLAUDE.md §0 的隔离要求（"DV 绝不读 DE 的推理过程"）只在**卡片传参**层被落实，文件系统侧没有对应约束。**本仓库改动**：① `.claude/skills/dispatch/SKILL.md` §3 派卡前自检清单新增一条——每张卡分配 `<scratchpad>/<角色><卡号>/` 专属子目录（子代理自建），卡内明写该路径并禁止读取兄弟子目录；只加一行，未重构清单其余内容；② 新建 `doc/bugs/BUG-0059.md`，`## regression_guard` 登记 `type: checklist`（`paths: .claude/skills/dispatch/SKILL.md`），按 REV-038 §A A-c5 的例外通道形态写明不可机械化理由，供未来 docsx F10 baseline 落地时按 `rev_ref` 收录、不被"新增 checklist 一律红"误禁。**建议上游取用**：`.claude/skills/dispatch/SKILL.md` 的派卡自检清单本身就是"卡片传参防火墙"的执行点，scratchpad 隔离条款理应随框架发布，而不是每个采纳者各自撞上 | .claude/skills/dispatch/SKILL.md §3（新增一条自检项）+ doc/bugs/BUG-0059.md（新建，登记 checklist guard） | annoyance（不阻塞门禁；但受影响的是子代理隔离链的文件系统面，与 CLAUDE.md §0"共模误差是大敌"直接相关） | local |
| FB-34 | 2026-08-02 | **根 `Makefile` 的 `evidence:` recipe 用 `'$(CMD)'` 直接引用命令行变量，命令行变量是递归展开的，CMD 里若自带 `$(...)`/`$VAR`（如 `echo $(pwd)`）会在传给 `scripts/evidence.py` 之前被 make 自己再展开一次、把非 make-变量的部分静默吞成空串**（BUG-0069，`make -n evidence CMD='echo $(pwd)' EXPECT='.'` 实测打印 `--cmd 'echo '`）：本仓库亲测 `$(value CMD)`（取变量的原始未展开文本，跳过第二次求值）在 `make -n` 下逐字保留 `echo $(pwd)`；`export CMD` + `$$CMD`（环境变量间接传递的直觉方案）**实测无效**——export 时 make 仍对变量值做同一次递归展开，`$(pwd)` 照样被吞。**本仓库改动**：① `evidence:` recipe 的 `'$(CMD)'`/`'$(EXPECT)'` 与 `$(if $(CMD),...)` 条件判断均改用 `$(value CMD)`/`$(value EXPECT)`；② `help` 目标新增三行提示——CMD/EXPECT 现在可以直接写字面 `$(...)`/`$VAR`，不再需要 `BUG-0069` 行内记录的 `$$` 转义变通。**建议上游取用**：canon `Makefile` 的 `evidence:` recipe（若上游也走同一 `$(if $(CMD),--cmd '$(CMD)' ...)` 形状）需要同一处 `$(value ...)` 订正；这是 GNU Make"命令行变量默认递归展开、且导出/直接引用都不能跳过二次求值"的通用陷阱，与 FB-30 的 `CM :=` 陷阱同族（展开时机），但机理相反（FB-30 是展开太早，本条是展开太晚/太多次） | Makefile（`evidence:` recipe + `help` 目标） | blocking（跑不通报"跑通了却验证的不是原意图"两种失败模式都成立，见 BUG-0069 行内"更凶险的一支") | local |
| FB-35 | 2026-08-02 | **`.claude/agents/rev.md`/`.claude/agents/dv.md` 沿用上游 0.8.0 目录合并前的 `workflow/` 子路径，实际全部不存在**（BUG-0052，REV-037 §0 台账行 B 实测 7 处死引用/4 文件；本条覆盖其中属 `.claude/agents/` 的两处）：`rev.md:18-20` 引旧（合并前）子路径 `review/six_questions.md`、`review/rubric.md`、`fail/`（均在 `workflow/` 下）；`dv.md:58,63` 引旧 `evidence_record.md` row 6（REV-037 新增发现的第 5 处，未在原 4 处之列，按"一个根因一条 FL"并入同处置面，同样在 `workflow/` 下）/ 旧 `fail/*.md`。上游 0.8.0 把这些拆分文件合并进单文件 `workflow/review.md`/`workflow/bugs.md`/`workflow/records.md`，但两份 agent 模板的引用文本未同步；`docsx.py` 的 F2（仓内路径存在性）落地前，此类漂移无任何机器检查覆盖。**本仓库改动**：`rev.md` 的引用改指 `workflow/review.md` 的"The seven review questions"/"Milestone signoff rubric"两节 + `workflow/bugs.md`；`dv.md` 改指 `workflow/records.md` 的"Evidence record"字段表第 6 行 + `workflow/bugs.md`。均为纯文本订正，不改变两份文件的角色语义。**建议上游取用**：canon `agents/{rev,dv}.md`（或其 kernel 渲染源）需要同一处路径订正——这是 0.8.0 目录合并的收尾遗漏，`docsx.py` F2 落地后任何采纳 0.8.0 的项目都会立即在自己的 agent 模板上撞见同一红 | .claude/agents/rev.md（引用改指 `workflow/review.md` 两节 + `workflow/bugs.md`）+ .claude/agents/dv.md（引用改指 `workflow/records.md` 字段表第 6 行 + `workflow/bugs.md`） | annoyance（不拒绝任何操作，但 REV-034 的 rev 实例已实测因此差点降级执行——读不到七问与 rubric 时只能靠卡内订正绕过） | local |
| FB-36 | 2026-08-02 | **根 `Makefile` 的 `check:` target 与 `.githooks/pre-commit` 只接了 `scripts/docs.py --check`，新落地的 `scripts/docsx.py`（project-owned，F1/F2/F7/F10 批 1）不在任何自动门禁里**（`doc/design-prompt/doc_mechanization.md` §13 C13.1/C13.2 的接线要求）：不接线的话，`docsx.py --check` 只能靠人手动记得跑，等同没有门禁——与 M1-01 那次 VCS-2018 `bind` 变通"只落注释未进台账"同一形状的风险（事后补为 BUG-0007 的教训）。**本仓库改动**：① `Makefile` 的 `check:` target 在既有 `docs.py --check` 之后追加一行 `docsx.py --check`（docsx 尚无 `--scen`/`--milestone` 窄化视图，批 1 未实现，故不透传这两个参数）；② `.githooks/pre-commit` 在 `docs.py --check` 之后追加同一行 `docsx.py --check`，两者均 `|| exit 1`（保持 pre-commit "fail-closed、可 `--no-verify` 跳过"的既有语义不变）。**建议上游取用**：不适用——`docsx.py` 是本项目 project-owned 工具，不进 canon `Makefile`/`pre-commit`；仅在本仓库自己的接线点记录，供其他采纳 `doc_mechanization.md` 设计的项目参考同一接线形状 | Makefile（`check:` target 追加一行）+ .githooks/pre-commit（追加一行） | blocking（未接线的新检查等于不存在——CLAUDE.md §2 明文的"变通只落注释未进台账"教训） | local |
| FB-37 | 2026-08-02 | **新建 `scripts/docsx.py`（project-owned；批 1 = F1/F2/F7/F10）——上游 `scripts/docs.py` 全部检查作用于表格行/frontmatter 层，无一扫描正文自由文本**，留下三处已实证的空白：BUG-0067 行内计数过期无机制提示（FB-23 的复现命令写下当天就跑不出声称的数字）、BUG-0052 四处框架文档指向不存在路径无任何机器覆盖、BUG-0058 的单向差分（`gone` 只打印不进退出码）教训无处沉淀为通用底座。`doc/design-prompt/doc_mechanization.md`（`arch` 撰写，经 `doc/review/REV-038.md` §A 五条件放行）把三处收编为十族判据，本条实现其中 F1（数字断言，正文 `<!-- docsx:count check="..." -->` 标记 + 活体/冻结两形态 + 元检查）、F2（仓内路径存在性）、F7（快照短语启发式，REV-038 D-3 首版降 warning）、F10（baseline 双向）+ §12 执行器安全契约（allowlist/denylist/timeout/cwd 锁，A-c1 收窄 `python3` 只准调 `docs.py`/`svacheck.py`）。**本仓库改动**：① `scripts/docsx.py`（复用 `docs.py` 的 `parse_table`/`row_cells`/`check_table_structure`，不重写第二套表解析器，C0.2）；② `doc/docsx-baseline.md`（新建，F10 的存量豁免表，65 行，四条 B.1 边界全部落实——BUG-0052 的 7 处死引用直接订正、不洗入 baseline）；③ `scripts/tests/test_docsx.py`（39 条，覆盖四族 red_when + §12 两条 A-c3 KILL 自证，见 `doc/bugs.md`/`doc/archive/bugs-archive.md` 的 KILL-0006）；④ `Makefile`/`.githooks/pre-commit` 接线（FB-36）。F3/F4/F5/F6/F8/F9 与 `.claude/skills/doc-mechanization/SKILL.md`（§14 人读说明落点）留后续批次，`doc/design-prompt/doc_mechanization.md` §15 已预告"均由后续实现卡产出"。**建议上游取用**：不适用——`docsx.py` 是本项目 project-owned 工具（C0.1 自陈"不是 canon"），补的是正文自由文本扫描这一整类空白，若上游认为通用可自行评估是否收编，本仓库不主张回流 | scripts/docsx.py（新建）+ doc/docsx-baseline.md（新建）+ scripts/tests/test_docsx.py（新建） | blocking（不变量 5 的 §12 两条 KILL 自证是"任何 docsx 族关闭任何 bug 之前"的硬前置，REV-038 §A-c3——批 1 交付前必须到位，不是事后补） | local |
| FB-38 | 2026-08-02 | **`scripts/docsx.py` 批 2 落地 F3/F4/F5 + `doc/guards.md`（F4 的守卫单表）落地后，根 `Makefile` 的 `guards:` target 仍指 `docs.py --guards`（读 `doc/bugs/*.md` 的 `## regression_guard` 段）——迁移当天起该 target 会读到空**（`doc/design-prompt/doc_mechanization.md` §13 C13.3 已预告这一接线后果，"canon Makefile 改动、记 FB 行"）：49 份详情页的 guard 段已被本卡替换为一行指向 `doc/guards.md` 的指针（内容仍非空，满足 `fl_schema_enforce` 的 A-c2 接线——`check_fl_page` 只要求 `## regression_guard` 非空、不检查其内容形态，指针行天然满足，`scripts/docs.py` 零改动），但 `docs.py:1195` 的 `## regression_guard\s*\n(.*?)(?=^## \|\Z)` 正则再也读不到 `paths:` 行，`make guards FILES=...` 从此对任何文件恒零命中。**本仓库改动**：① 根 `Makefile` 的 `guards:` target 改调 `scripts/docsx.py --guards $(FILES)`（读 `doc/guards.md` 的结构化表，非文本标记，C1.1）；② `scripts/docsx.py` 新增 `--guards`/`cmd_guards`，输出契约与 `docs.py` 原版逐字保持——`"== <bugs 列> guard (hit: ...) =="` 头 + 末行 `"N guard(s) matched"`（用 `bugs` 列而非新 `id` 列作为标签，使既有 `grep '== BUG-XXXX guard'` 消费者，如 REV-037 §1 S1-S3 三条复验签名、`.claude/skills/dispatch/SKILL.md` §3 派卡自检，原样可用）；`scripts/docs.py:1138` 的 `cmd_guards` 按设计契约原样保留、不删（上游文件）。**复验**：stash 迁移前树重跑 S1/S2/S3 三条签名复现 REV-037 记载的失配（S1 只中 BUG-0066 漏 BUG-0043、S2 中 5 条漏 BUG-0045、S3 零命中）；pop 回迁移后树，`docsx.py --guards` 三条签名全部命中修复后的正确 guard，`sim/Makefile` 交叉核对命中数不降（12 vs stash 前 12）。**建议上游取用**：不适用——`doc/guards.md`/F4 是本项目对 `doc_mechanization.md`（project-owned 设计契约）的落地，不进 canon | Makefile（`guards:` target 改源）+ scripts/docsx.py（新增 `--guards`/`cmd_guards`） | blocking（不改则 `make guards` 静默失效——同 FB-36 点名的"新检查不接线等同不存在"形状，这次是"载体搬家后旧入口悬空"的姊妹形状） | local |
| FB-39 | 2026-08-03 | **M4 复盘裁决：文档/记账缺陷降出验证 bug 全重链——顺手修为主，「翻绿才登记」**。机械统计依据：M4 功能面 `31d514c`（08-02 02:27）封板后 21 小时/34 提交零源码改动，全耗在记账；M4 登记 37 条 bug 中 29 条（78%）是记账体系自身缺陷，每条走了台账行+详情页+fixer 卡+独立 closer 卡+评审的 RTL 级链条（16 条文档 bug = 2 轮评审 + 7 卡 × 2 派发）。**本仓库改动**：① `workflow/bugs.md`「The five classes」下新增 Scope 段——五分类与「登记无条件」适用域=**验证失败**（仿真红/checker 失配/工具拦仿真）；文档/记账缺陷默认**顺手修**（当前 commit 直接修，零登记，commit message 提一句），唯一例外=修复需**把已记录的绿翻红**（证据引用失实/✅或 CLOSED 站不住/门禁被绕），此时一行 `suspect=doc` 台账 + L0 卡，closer=机器（`make check`+`make selftest` 绿即 CLOSED，`min_repro`/`verify_evidence` 走 `CMD:` 形态），无详情页（除非 RCA 非显然），rev 收尾批量过目不逐条裁；② `workflow/records.md` 新增「Derived facts are never hand-copied」段——可推导的数字/计数/路径不手抄进散文（status/log/REV 正文），要么引用生成命令（`make handoff`/`make check MILESTONE=<n>`），要么落脚本独占回填区（evidence.py 三列是既有范式）——M4 有 6+ 条 bug 纯属抄写错（BUG-0049/0051/0062/0063/0064/0071）；③ `CLAUDE.md` 不变量 3 加限定（closer≠fixer 适用于验证判断类关闭；文档缺陷的 closer 是机器门禁）、§2 加适用域句、§5「上游关系」改「双向回流」表述（框架仓与本仓互相 cherry-pick 实践检验过的改动，无上下游之分）。`suspect=doc` 的 docs-check 放行（详情页/TEST=/SEED= 豁免）随同轮 docsx 溶解卡落地，见 FB-40。**建议框架仓取用**：①② 整段可直接 cherry-pick——任何采纳仓的元 bug 占比都会随记账机制增多而上升，两档规则是通用剃刀 | workflow/bugs.md（Scope 段）+ workflow/records.md（Derived facts 段）+ CLAUDE.md（不变量 3/§2/§5） | annoyance（无门禁失效，纯流程成本——但占了 M4 后期几乎全部 token） | local |
| FB-40 | 2026-08-03 | **`scripts/docsx.py`（project-owned，FB-37/38 分批落地的 F1-F5/F7/F10 + §12 执行器）整体溶解回 `scripts/docs.py`（canon），恢复单入口**（FB-39 已裁「已被 workflow/records.md 新契约 derived facts are never hand-copied 禁止」的检查对象消亡；F7 从未拦错一次）。**砍**：F1（数字断言，`docsx:count` 标记）、F3（双向集合断言，`docsx:bidiff` 标记）、F7（快照启发式，warning-only）三族及其共用的 §12 词法执行器（`exec_gate`/`_run`/`exec_check`/`_shell_tokens`/`_deny_hit_in_token`）全部删除，含 BUG-0072 的三条 KILL 自证代码——§12 不存在了，KILL-0006/0007 两行标记为不可再重放的历史证据（`doc/bugs.md` KILL-0007 行 min_repro 改 `n/a` 指回本行）。**并**：F2（仓内路径存在性）、F4（`doc/guards.md` 表结构 + fnmatch）、F5（bug 行↔详情页、证据↔引用双向）+ BUG-0053 tool-marker-leak 并入 `docs.py` 的 `cmd_check()`——F5 的 bug-page 方向**替换**了 `docs.py` 原有更弱的「文本显式提及 doc/bugs/X.md」扫描（同域两处合一，不并存）；`--guards` 改读 `doc/guards.md` 表，输出契约（`== <bugs> guard (hit: ...) ==` + `N guard(s) matched`）逐字保持，替换掉原闲置的 `## regression_guard` 页扫描实现。**baseline 处置**（`doc/docsx-baseline.md` 135 行，删除）：F1/F3 的 2 行随家族退役自动消失；F2 的 49 行中 31 行随 FB-1~FB27（29 条历史行，上游台账时代）整体搬移到新建 `doc/archive/fw-feedback-archive.md`（frozen prefix，F2 不扫）而消解；余下 18 行分四类落地——workflow/bugs.md 里旧误引用的 VENDOR.md 路径（缺 `vendor/` 前缀，2 处）与示例路径改写为占位符（1 处）系真实死引用，就地订正；workflow/records.md 的示例路径本已在 fenced block 内，F2 新增的 fenced-block 自动豁免免修；doc/milestone.md:25 的 flist 三层斜杠连写误伤改箭头分隔（真实修正，非豁免）；doc/design-prompt/verification_maturity.md:306 的 `scripts/cov_loop.py` 前瞻引用改用 fenced block（milestone_restructure.md 的两处同引用本就在 fenced block 内，天然豁免）；doc_mechanization.md 的 worked-example 假路径与 §14/15 前向引用、dispatch/SKILL.md 的 orch.md 历史注、doc/guards.md 的 G-0011 历史 note、doc/milestone.md 的 M6 cov_loop 提及、fw-feedback.md 的 FB-28/37 历史行——这 5 处用新增的 `<!-- docsx:skip <token...> -->` 逐文件豁免标记覆盖（每个标记按文件、非按行，可列多 token；全仓 5 处标记，逐处见交付报告）；F4 的 49 行（type: checklist 的 A-c5 授权豁免）与 F5 的 35 行（历史缺页/孤儿证据债务）**未落 a-d 四类**，按以下两条新设计处置：F4 的「checklist 需 baseline 授权」规则整体退役（该规则本身是 F10 派生物，无 F10 就无意义，废止而非另立授权通道，49 行 rev 批准不重新裁决）；F5 改用固定、不再追加的历史债务白名单（`F5_LEGACY_BUG_IDS`/`F5_LEGACY_EVIDENCE`/`F5_LEGACY_DANGLING_REF`，`scripts/docs.py` 内联常量，逐条对应原 REV-038 §B.1 35 行，2026-08-03 复核仍全部真实未关），使 `make check` 保持全绿而不复活 F10 通用豁免表机制。**suspect=doc**（FB-39 契约，本卡机械化落地）：F5 的 bug-page 检查对 `suspect=doc` 行豁免（无需详情页）；`suspect` 枚举 workflow/bugs.md 早已含 `doc`，代码侧无需改动（docs.py 从未对 suspect 做枚举校验）；min_repro 的 `CMD:` 形态本就不受 docs.py 任何格式检查约束（未新增该检查，因真实 doc/bugs.md 存量行——BUG-0044/45/46/73、KILL-0007——min_repro 既非 TEST=/SEED= 也非 CMD: 前缀，新增硬校验会当场撞红，超出本卡范围）。**测试**：`scripts/tests/test_docsx.py`（39 条）删除，幸存族（F2/F4/F5/tool-marker-leak/guards 契约）迁入 `scripts/tests/test_docs.py` 并新增 suspect=doc 三条 + F2 fenced-block 豁免一条 + F5/详情页合一红绿各一条；F4 的「checklist 新增即红」用例改写为「checklist 默认合法」（记录行为变更，非误删）。`make selftest` 迁移前 143 条全绿，迁移后 102 条全绿（`unittest discover` 机械统计，非估算） | scripts/docsx.py（删除）+ doc/docsx-baseline.md（删除）+ scripts/tests/test_docsx.py（删除）→ scripts/docs.py（F2/F4/F5/tool-marker-leak/cmd_guards）+ scripts/tests/test_docs.py（迁移+新增）+ doc/archive/fw-feedback-archive.md（新建）+ Makefile、.githooks/pre-commit（收敛单入口）+ workflow/bugs.md、workflow/records.md、doc/milestone.md、doc/design-prompt/verification_maturity.md、doc/design-prompt/doc_mechanization.md、doc/guards.md、doc/bugs.md（死引用订正 + 豁免标记） | blocking（`make check`/`make selftest` 是每次收卡的机器背书，双绿是本卡自身的验收判据） | local |
