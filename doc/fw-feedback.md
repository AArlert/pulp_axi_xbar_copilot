# iverif-workflow 框架反馈台账

> 本仓库是 iverif-workflow 的首次实战应用。任何框架摩擦（门禁误伤、脚本缺口、
> 文档失实）当场登记于此，**绝不本地修改 `scripts/` / `workflow/` /
> `.claude/skills/`**（fw-check 红线）。blocking 当日回流框架仓库，
> annoyance 里程碑边界批量回流。回流仪式见 CLAUDE.md §5 与
> `../iverif-workflow/docs/adoption.md`。

severity：blocking（门禁/脚本拒绝正确工作）| annoyance（文档/体验问题）
status：open | reported | fixed@<ver> | wontfix

**第一轮回流已闭环（2026-07-27）**：FB-1~FB-7 全部落入框架 0.2.1，本仓库已
`fwsync --pull`（`iverif.json` 从 0.2.0 → 0.2.1，`make fw-check` / `docs-check`
双绿）。FB-5 的绕行（orch 手动回退 FIX_READY→FIXING）自此作废，按 `.claude/agents/de.md`
新措辞走即可。

| id | date | symptom | framework file | severity | status |
|----|------|---------|----------------|----------|--------|
| FB-1 | 2026-07-27 | `--init` 拒绝仅含 LICENSE/README（及 .claude/settings.local.json）的新仓库，只能手工移开-恢复；建议加 `--allow-existing` 白名单或在 adoption.md 记录移开法 | kernel/fwsync.py (cmd_init) | annoyance | fixed@0.2.1（文档：adoption.md 记录移开法；`--allow-existing` 机制入 deferred 台账待第二次触发） |
| FB-2 | 2026-07-27 | `LM_LICENSE_FILE ?= 27000@localhost` 回退值与本 VM 实测值 `27000@icarray-virtual-machine` 不符，项目只能在 include 前 export 覆盖；建议回退值改为可探测或文档标注必须覆盖 | make/vcs-2018.mk | annoyance | fixed@0.2.1（文档：注释挑明是必须覆盖的占位值） |
| FB-3 | 2026-07-27 | 待验证：ucli `run; exit` 驱动的 `$stop` 型 tb 下，`-assert verbose` 的 `Summary:` 行是否出现（evidence.py SVA 腿 fail-closed，缺行即拒收）——首次 `make evidence` 时确认/证伪 | make/vcs-2018.mk / kernel/evidence.py | open question | fixed@0.2.1（经 FB-6 证实：`Summary:` 行确实出现，缺口在 evidence.py 窗口/正则，非工具选项） |
| FB-5 | 2026-07-27 | 角色文件与门禁自相矛盾：de.copilot.md 规定 DE"置 FIX_READY（不置 CLOSED）"，但 docs-check 要求 FIX_READY 行 fix_commit 非空，而 DE 交付时 orch 尚未提交、hash 不存在——按角色文件走必撞门禁（本次 BUG-0001 实撞）。docs.py NEXT_PHRASES 的本意是 FIXING→（commit 后）→FIX_READY；建议 de.copilot.md 改为"置 FIXING，FIX_READY 由 orch 在提交后连同 fix_commit 一并回填" | agents/de.copilot.md vs kernel/docs.py | blocking（有绕行：orch 回退状态） | fixed@0.2.1（de.copilot.md 改置 FIXING；docs.py 的 `make next` 短语同步改为 orch 提交后回填） |
| FB-4 | 2026-07-27 | Claude Code 会话中途 `fwsync --init` 渲染的 .claude/agents/ 注册有延迟（本次首批 arch/dv 派卡即报 "Agent type not found"，只能用 general-purpose 兜底承载角色文件；数分钟后类型才可用）；建议 adoption.md 在 --init 步骤后提示"重启会话（或等类型注册生效）再派卡" | docs/adoption.md | annoyance | fixed@0.2.1（文档：adoption.md 剧本 1 提示重启会话再派卡） |
| FB-6 | 2026-07-27 | 非 UVM tb（如上游 `tb_axi_xbar`，ucli `run;exit` 驱动 `$stop`）的记分板判决行（如 `Tests Failed: 0`）常落在 `PLAIN_MARK` 摘要窗口上沿之外，且不匹配 `KEY_LINE_RE`（pass\|match\|compare ok\|check ok\|running test），导致已登记证据的 `## Key check lines` 段为空——判定本身仍稳健（`svacheck.judge` 两腿制 fail-closed，SVA 腿独立零失败即拒收），但证据摘要未如实反映功能记分板结果，需人工复读源日志才能确认（signoff-M0 抽查 4 实测，R1）；建议上扩摘要窗口或为非 UVM tb 增补 `Tests Failed`/`ended`/`mismatch` 关键行模式 | kernel/evidence.py | annoyance | fixed@0.2.1（evidence.py 窗口 2→20 行 + 关键行正则，附保险丝测试；**存量 v0.0.1 三条记录仍空**，不追溯重写已签核产物） |
| FB-7 | 2026-07-27 | 四个角色模板（`agents/{arch,de,dv,rev}.copilot.md`）里 `doc/bugs.md` 登记指令都窄化到各自语境（DV 仅"on a mismatch"；DE/arch 仅"自己撞见的 spec 歧义/已挂号 bug 修复"）——`workflow/taxonomy/failure_taxonomy.md` 明确列出的五类判据（尤 `TOOL_ENV`）只要不是"scenario mismatch"触发（如实现期编译/工具报错、同卡内已绕过），没有任何角色文件的指令要求登记；taxonomy.md 正文本身也只描述判据，无一句"登记与是否阻塞 evidence/是否同卡内绕过无关"的显式声明。本仓库 M1-01 实测踩中：VCS-2018 拒绝 `bind <interface> <module>`，DV 同卡内改直接例化绕过，全程只留在代码注释 + rev 评审记录（REV-002/REV-003），未落 `doc/bugs.md`，taxonomy.md 自证的"下一个人能 grep 到"因此落空（orch 事后发现，已补登记 + 项目侧 CLAUDE.md §2 加了一条不豁免声明作本地缓解，但角色模板本身未修）；建议：① taxonomy.md 开头补"登记是无条件的，不因是否阻塞 evidence 或是否同卡内绕过而豁免"；② 四角色模板的固定交付报告格式里都加一条字段"本卡是否命中 taxonomy 任一类异常（含已绕过的）：是/否 + BUG-ID" | agents/*.copilot.md（kernel 渲染源）+ workflow/taxonomy/failure_taxonomy.md | annoyance（未被门禁拒绝，但是静默的可追溯性缺口，建议按核心不变量级别优先处理而非常规 annoyance 排期） | fixed@0.2.1（taxonomy.md 补"登记无条件"段 + 四角色模板增设强制字段） |
| FB-8 | 2026-07-27 | `kernel/docs.py` 的 `NEXT_PHRASES["copilot"]["undelivered"]`/`["prompt_missing"]` 硬编码"dispatch DE card"，隐含"copilot 项目里 feature-matrix 的 module 交付物永远是 DE 写的 RTL"；本仓库是 vendored-DUT 项目（无 `rtl/`，DE 卡按 CLAUDE.md §6 仅限 vendor 工具兼容补丁），feature-matrix 的 module 交付物其实全部是 DV 写的 tb 代码。`make next` 因此对**任何**尚未交付的 tb 模块（本次触发者：M2 新增的 `functional_coverage`，`doc/feature-matrix.md` F-M2-08）都会给出"dispatch DE card"的建议，人工需要凭 CLAUDE.md §6 的项目常识纠偏，而非照做——纯 advisory 文案不准确，未拒绝任何操作，`docs-check`/门禁均不受影响。且该短语无 per-project override 钩子（`columns_override` 有、`NEXT_PHRASES` 没有），项目侧无法本地纠正而不碰 `scripts/`。建议：`NEXT_PHRASES` 支持类似 `columns_override` 的 `iverif.json` 覆盖项（例如 `next_phrases_override`），或在 vendored-DUT 场景下把措辞改得角色中立（如"dispatch a card for the owning role（DE/DV，视本项目 CLAUDE.md 的 RTL/TB 归属而定）"） | kernel/docs.py (NEXT_PHRASES) | annoyance | fixed@0.3.3（root fix：`--next` 的交付物归属改由 `delivery.glob` 推导，tb/-rooted ⇒ DV-owned，零配置，不需要本仓库当初提议的 `next_phrases_override`；本仓库 2026-07-28 拉 0.4.1 后生效，措辞待下次触发 undelivered/prompt_missing 路径时目视复验） |
| FB-9 | 2026-07-28 | `scripts/evidence.py` 的 `KEY_LINE_RE` 不匹配 UVM 报告里的 `[FCOV_SUMMARY]` 明细行，功能覆盖率百分比因此进不了 evidence 摘要的 `## Key check lines` 段——只在"Report counts by id"里露出一个 id 计数（本仓库 F-M2-08 落地后：七条 evidence 各显示 `[FCOV_SUMMARY] 2`，而每个 covergroup 的 samples/inst_cov 数字全部落在摘要窗口之外）。后果：覆盖率证据的**数字**不在证据文件里，里程碑签核时要么回源 log、要么回跑 `make cov`，与"evidence 是自足的可复核制品"这一意图相悖；同类问题的既有先例是 FB-6（判决行落在摘要窗口之外）与 BUG-0008（M0 三条证据 `## Key check lines` 段为空）。建议：`KEY_LINE_RE` 增加对 `[FCOV_SUMMARY]`/`[COV_*]` 一类覆盖率摘要标签的识别，或提供项目侧可配的额外关键行模式（类似 `columns_override` 的 `iverif.json` 钩子），使各项目能把自己的覆盖率摘要标签纳入而不必碰 `scripts/` | scripts/evidence.py (KEY_LINE_RE) | annoyance | fixed@0.3.3（`[FCOV_SUMMARY]` 升为 canon 约定而非配置钩子——框架侧裁定「零配置正确性优于配置钩子」；本仓库 tb 早已按该格式打印 ⇒ 拉取后零改动。已复验 `scripts/evidence.py:49` 的 `KEY_LINE_RE` 含 `\[FCOV_SUMMARY\]`；证据文件里的覆盖率数字待下次 `make evidence` 重登记时落地） |
| FB-10 | 2026-07-28 | **已登记的 `regression_guard` 没有强制消费机制——跨卡一致性无载体**。判例（本仓库完整闭环）：BUG-0015 修复后登记 `type: checklist` guard，原文"今后 `tb/sva/` 下任何 SVA 模块，property/cover 表达式中**不得**直接调用读取跟踪状态的函数……评审 SVA 卡时按此项核对"，且其 `## similar` 段**精确点名**了下一个受害者（"`tb/sva/axi_xbar_stall_sva.sv` 同样在 property 内直接调用读数组的函数（`w_reorder(b_id)` 等）……后续动该文件时建议顺手折叠"）；今天 DV 分诊卡在 `stall_sva.sv:212/218` 判出真缺陷，**与点名位置一字不差**。中间隔着一张 rev 卡（REV-008，评审范围恰含 `tb/sva/`）也未看到该 guard——因为 rev 的判据来源清单由 orch 派卡时给定，而**没有任何机制提示 orch 该写进去**。数据：本仓库 14 条 guard 中 **9 条为 `type: checklist`**（纯自然语言祈使句、无自动化载体）；`ref: sim/Makefile lint: 目标` **重复出现 4 次**（BUG-0014/0019/0021/0022，同一目标反复出问题、每次留一条 checklist guard、无一条阻止了下一条）；`workflow/signoff/rubric.md` 第 5 条只抽查 1 条做证伪（14 条抽 1 = 7% 覆盖）。根因层：隔离规则切断**推理**传播（正确），但**一致性约束也需传播**且当前无载体；解法不能是设跨卡常驻角色（等于重建共模误差），只能让约束以**事实**形式自动传播（事实传播不产生共模误差），`doc/bugs.md` 已是载体、缺的是强制消费。**建议 1（价值最高）**：派卡时机械执行「本卡触及文件 ∩ 所有 guard 的 `ref` → 命中的 guard 全文进入卡的判据来源清单」——纯路径匹配，orch 无须理解内容 ⇒ 不违反"orch 不产技术制品"，注入已登记事实而非推理 ⇒ 不引入共模误差；此机制若早存在，上述判例不会发生。前提：`workflow/schema/failure_record.md` 的 `ref:` 字段须结构化（现为自由文本、混着路径与说明，如 ``ref: `tb/sva/axi_xbar_stall_sva.sv`（L205-216 完成序不乱序 assert = 长期判决门；…``），建议拆为可机械匹配的路径列表 + 说明。**建议 2**：`rubric.md` 第 5 条抽查改为"至少覆盖本里程碑触及文件所命中的全部 guard"，与注入共用同一反向索引。**建议 3（schema 写作指引）**：明示"很多 checklist guard 是**还没被机械化**的、不是无法机械化的"——BUG-0015 那条完全可静态检查（property/cover 表达式中不得直接调用函数），写成 checklist 只因当时没问"这条能不能变成脚本"。三条均可复用既有 `fl_schema_enforce`/`sva_enforce` 的 `iverif.json` 开关模式。本仓库将先做原型（`make guards` 查询目标；`Makefile`/`.githooks/`/`sim/Makefile` 均不在 26 个 pin 文件内、有自由度），跑通后连同实测反馈回流。已于 2026-07-28 通过 session 通道把判例+数据+设计直接送达 iverif-workflow 侧 | workflow/schema/failure_record.md (regression_guard.ref) · .claude/skills/dispatch/SKILL.md (卡组装) · workflow/signoff/rubric.md (第5条) | annoyance（不拒绝任何操作，但已实证导致真缺陷按预警原样复发） | fixed@0.4.1（框架侧当日采纳全部三条：`## regression_guard` 新增 `paths:` glob 字段、`docs.py --guards` / `make guards FILES=…` 纯 fnmatch 求交、**双消费挂点**＝dispatch 自检清单逐字贴卡 ＋ rubric #5 改为「里程碑触及文件命中的全部 guard 入围复核 + 至少证伪一条」取代 1/N 抽查；schema 并写入本仓库提议的写作规则「checklist guard 是尚未机械化的 TODO、不是终点」。框架侧刻意**不挂 evidence**——guard 是输入侧约束、evidence 是输出侧咽喉，语义不同（FB-11 的 stamp 挂 evidence 反被判为正确）。**本仓库侧动作已完成**：18 条存量 guard 全部回填 `paths:`（无 `paths:` 者永不被注入，该句已写进 schema），复验 `make guards FILES="tb/sva/axi_xbar_stall_sva.sv"` 命中 5 条、含 **BUG-0015**——被 F1 违反的正是它，现在它会自动出现在任何触及 `tb/sva/` 的卡里。缺口关闭得证） |
| FB-11 | 2026-07-28 | **门禁缺少"自证执行"的通用不变式**——框架有 `no sim log no ✅`（保证被测**场景**被验证），但没有任何不变式保证**验证工具本身**有效。本仓库实证两处：(a) `sim/Makefile` 的 `lint:` 目标自接入脚手架（`2fbca13`，M0 期）起就是坏的（缺 `-assert svaext` + 缺 `-top`，BUG-0014/0019），能坏这么久是因为 `lint` **不在 signoff-check、不在 docs-check、不在任何门禁清单里**，只是根 Makefile 的一个 passthrough；(b) BUG-0022——`make lint` 增量编译命中时**假绿**：同一份代码连跑两次，第 1 次 259 条告警 exit 2、第 2 次 `lint clean` exit 0，因 VCS 增量路径不重新分析源码、不产生任何 `Lint-` 行但仍覆写日志，门禁判据在空日志上一无所获 ⇒ 把"这次根本没执行 lint"误判为"lint 干净"。抽象为一句话：**"没看到错误" ≠ "检查通过"**。同族后果还有 BUG-0020（`verdi:` 目标引用一个从不被任何目标生成的 `wave.fsdb`，半成品从 M0 活到现在，因为从没人执行过它）。建议把 `no sim log no ✅` 推广为通用不变式 **`no proof-of-execution no gate-pass`**：门禁须产出与被检查内容绑定的执行证明。本仓库原型形态——`$(OUT)/lint.stamp: $(TB_SRCS)`，规则内先强制重编（消除增量假绿）、跑 gate、`grep -q "elaboration done"` 校验**确实执行过**、最后把源码 sha256 写进 stamp；三性质缺一不可：依赖源码（变了自动失效，make 天然提供）＋校验执行实证标记（杀假绿）＋stamp 为内容指纹（`touch` 骗不过）。配套强制挂点建议：**框架已有一个不可绕过的咽喉——`make evidence` 是 ✅ 的唯一入口**，把 gate 挂此处即自动获得强制性（想登记证据 → stamp 须有效且匹配当前源码；绕过 evidence 就没有 ✅、没有里程碑），无须新造强制点；`.githooks/pre-commit` 是 soft gate（其首行自陈 "bypassable with --no-verify"），只适合秒级检查。分层建议按代价匹配频率：秒级（docs-check/guard 提示）→ pre-commit；分钟级（lint）→ evidence 前置；十分钟级（regress）→ 里程碑。已于 2026-07-28 随 FB-10 一并送达 iverif-workflow 侧 | scripts/make/evidence.mk (evidence 前置) · workflow/signoff/rubric.md · workflow/schema/evidence_record.md | blocking（门禁可在未执行检查的情况下发出通过信号——会假绿的门禁比没有门禁更危险） | open（方向已被框架侧采纳并记入 deferred 台账，trigger 已 fire＝本仓库 BUG-0022；canon 机制**刻意等本仓库原型的实测数据**再定型，候选形态即本条提出的三性质。本仓库待办：在 `sim/Makefile` 做 lint stamp 原型，并回流两项实测——(1) stamp 对增量编译假绿的杀伤是否完备；(2)「elaboration done」这类执行实证标记在本仓库 tb 形态下是否稳定。canon 化时将走 `sva_enforce` 式开关、新仓库默认开） |
| FB-12 | 2026-07-28 | **跨大改版本拉取时，项目侧 pin 的旧 `fwsync` 用旧配对表去量新框架，静默产出残缺快照而门禁照样绿**。复现：项目 `iverif.json` 已声明 `"profile": "copilot"`，`python3 scripts/fwsync.py --pull <fw>` 从 0.3.0 拉 0.4.1，得到 manifest 25 条、`workflow/profile.md` 根本不被写出，而 `make fw-check` **照样输出 `fw-check passed (framework 0.4.1, 25 files pinned)`**。**根因（经框架侧指正 + 本仓库用 `git show 8228d37:scripts/fwsync.py` 读回当时实际执行的那份脚本核实）**：执行这次 pull 的是项目 `scripts/` 里 pin 的 **0.3.0 旧脚本**，其 `vendor_pairs()` 第 114 行按 `for name in ("profiles", "discipline")` 去框架仓库找 `docs/profiles.md`——该文件在 0.4.0 已被拆分删除（改为 `docs/profile.{learning,copilot}.md`）⇒ 找不到即**静默跳过**⇒ 少 pin 一个文件。即"**用旧尺子量新东西还报绿**"，与 `--profile` 参数**无关**：`--profile` 在所有版本里都只进 `--init`，`do_pull` 一直是从 `iverif.json` 读 profile（0.3.0 脚本第 187-194 行 `profile = cfg.get("profile")` 可证）。本仓库当初补带 `--profile copilot` 重拉后恢复 26 条，是因为**那时 `scripts/fwsync.py` 已被第一次 pull 覆盖成 0.4.1**、新配对逻辑本就正确，该参数纯属巧合同行——**此前本行把根因写成"pull 路径未传 profile"是错的，已按实证更正，以免后来人去查一个不存在的参数缺陷**。两个后果：(a) 项目静默失去自己的 profile 契约文件——0.4.0 起该文件正是"项目只收到自己那份契约"的载体，缺了它没有任何提示；(b) 上一版被移除的 `workflow/profiles.md` 不会被 pull 清理，留下一个内容停在旧版本、不在 manifest、fw-check 不管辖的**孤儿文件**，而项目 CLAUDE.md 仍在引用它（本仓库已手工删除并改引用）。**本条与 FB-11 是同一失效模式**：门禁把"这次根本没做这项检查/没同步这个文件"呈现为"通过"——差别只在 FB-11 是 lint、本条是 fwsync。建议：(1) ~~`--pull` 缺省从 `iverif.json` 读 profile~~——**本条作废**：该行为本来就存在，是本仓库最初的根因误判所致（见上）；根治方向应为「项目侧 pull 一律改由**框架侧**脚本执行，使 pin 的旧脚本永不决定一次 pull」；(2) 取不到 profile 时**拒绝 pull**（fail-closed）而非静默少写文件；(3) pull 后比对新旧 manifest 差集，对"上一版 pin、本版不再 pin"的残留文件至少告警（可选清理）；(4) `fw-check` 把"profile 契约文件是否在 manifest 内"本身纳入判据——manifest 自洽哈希看不见"写残的 manifest"，缺失因此不可见 | kernel/fwsync.py (`vendor_pairs`/`snapshot_pairs` 配对表随版本漂移 · `do_pull` 由哪一侧脚本执行) · scripts/make/core.mk (`fw-pull`) | blocking（快照可在缺少 profile 契约的状态下通过 fw-check——同 FB-11 的假绿模式） | fixed@0.4.2（框架侧四层修复：① **bootstrap hop**——项目侧 `--pull` 一律 re-exec 框架侧 fwsync，pin 的旧脚本的配对逻辑永不再决定一次 pull，此为根治；② profile 非法/缺失时 fail-closed 拒绝拉取（refusing a partial pull）；③ 孤儿清扫——不再 pin 的旧文件 pristine 时自动删除并打印、有本地改动则报告人工处理（本仓库手工删的 `workflow/profiles.md` 这类以后自动处理）；④ fw-check 完整性探针——profile 契约是否在 manifest 内本身成为判据。60 例自测含三条 FB-12 fuse。**本仓库已复验**：2026-07-28 从 0.4.1 拉 0.4.2、**故意不带 `--profile`**，得 `pulled framework 0.4.2: 26 files` + `fw-check passed (framework 0.4.2, 26 files pinned)` + `workflow/profile.md` 就位——同样的命令形式在 0.4.1 时只得 25 files，此次 pull 本身即 bootstrap hop 的首次真实运行） |
| FB-13 | 2026-07-28 | **`KEY_LINES_MAX=30` 让 SVA 逐条命中数进不了证据、且取的是任意前缀**：`scripts/evidence.py` 的 `KEY_LINE_RE` 含 `match`，于是 `-assert verbose` 打印的每一条 assertion/cover 行（`"file", line: inst, N attempts, M match`）都算关键行；本仓库每次运行有数百条这类行，30 条上限被**最先出现的那个模块**（`axi_xbar_txlimit_sva`）整段吃掉，其余模块的 cover 命中数一条不进证据。实测：0.3.3 起新增的 2 条 `[FCOV_SUMMARY]` 明细行（FB-9 的修复）把 3 条 txlimit 行挤出，而本卡真正需要的 `axi_xbar_stall_sva` 6 条 cover 命中数从来就不在证据里——DV 卡验收项要求报告「cover 命中数变化」，只能回源 `sim/out/*.log` 取数，与 FB-6/FB-9/BUG-0008 同一族（证据摘要不自足）。且截断按**文件出现顺序**而非重要性，哪些模块的数字被保留纯属偶然，评审无法从证据判断「没出现 = 没这条 cover」还是「没出现 = 被截断了」。建议：(1) 关键行做**分类配额**（SVA 逐条行单独限额，或每模块保留其汇总）而非一刀切前 N 条；(2) 或对 SVA 逐条行按文件归并 attempts/match，把「每模块 cover 命中」压成几行；(3) 至少在截断处写一行 `... (K lines truncated)`，使截断本身可见（当前静默丢弃）。 | scripts/evidence.py (`KEY_LINES_MAX` / `KEY_LINE_RE`) | annoyance | fixed@0.4.3（采纳本条建议 (2)+(3) 的组合：`-assert verbose` 逐条明细行按源文件**聚合为单行**（`x_sva.sv: N properties/covers, A attempts, M match`）⇒ 每模块 cover 命中数从此必然在证据里；判决行独享 30 行上限；截断处打印 `... (K more key lines truncated)` 使截断可见。框架侧以本仓库 stall_sva 六条 cover 的场景为测试 fixture 原型。**本仓库已复验**：拉 0.4.3 后重新登记全部七条 M2 证据，每条都含 5 个 SVA 模块的聚合行，`axi_xbar_stall_sva.sv: 60 properties/covers, 2640 attempts, 24 match` 赫然在列——正是 BUG-0026 里说"从来就不在证据文件里"的那个数字。**BUG-0026 据此转 CLOSED**（verify_evidence `doc/evidence/v0.2.0/BUG-0026.log`）。框架侧提示：若真实 log 有行形变体导致漏聚合，那些行会 fail-safe 退回原样保留、不丢数据；本仓库七条证据未见漏聚合形态） |
| FB-14 | 2026-07-28 | **`docs-check` 不校验 `doc/bugs.md` 的表格列数，一个未转义的管道符即可静默损坏整行结构**。实例：BUG-0016 的 summary 里引用 RTL 表达式 ``full=\|cnt_full=(&in_flight)``——SystemVerilog 的规约或运算符 `\|` 当时未转义，markdown 遂把它当列分隔符，该行从此处起**整体右移一位**（解析为 9 列）：`fix_commit` 列实际装着 root_cause 的正文、`verify_evidence` 装着本该在 fix_commit 的 `-`、末尾多出一个 `-`。**`docs-check` 全程 passed**，损坏自该行登记之日潜伏，直到 orch 手工做列数体检才发现。**真实门禁风险**：`CLOSED` 状态的判据之一是「verify_evidence 非空」，行错位后该检查读到的是**错列的内容**——本例 BUG-0016 恰好是 `SPEC_CHANGED`、不触发该检查，但同样的错位落在一条 CLOSED 行上就会让门禁读着错误的格子判定通过。**第二个独立样本（同批发现）**：orch 用 Edit 追加 summary 时 `new_string` 多带了两列，该行变成 10 列，`docs-check` 同样 passed。两个样本方向相反（一个少解析、一个多写），门禁对两者都无感。**与 FB-11/FB-12 同族，这是第三个独立样本**：门禁在数据结构已损坏 / 检查根本没执行的情况下照常发出通过信号（FB-11 = lint 增量假绿，FB-12 = fwsync 缺 profile 静默降级，本条 = bugs.md 行结构损坏）——三者共同指向同一条缺失的通用不变式。建议（零成本）：`docs.py --check` 增加结构校验——每个数据行按 `(?<!\\)\|` 切分后的列数必须等于表头列数，不等即 FAIL 并打印行号与实际列数；同一条校验可复用于 `doc/testplan.md` / `doc/feature-matrix.md` / `doc/fw-feedback.md`。附带建议：在 `failure_record.md` 提一句「表格单元内的 `\|` 必须转义」——本仓库 DUT 是 AXI crossbar，RTL 表达式里的按位或/规约或极常见，这个坑会反复踩 | scripts/docs.py (`--check` 表格结构校验) · workflow/schema/failure_record.md | blocking（门禁在行结构损坏时通过，且 CLOSED 判据可能读到错列内容） | fixed@0.4.3（`docs-check` 新增表格结构门禁：testplan / feature-matrix / bugs / lint-waivers 四张活表逐行校验「列数 == 表头列数」，未转义 `\|` 移列即 FAIL 并打印行号与实际列数，报错文案带 `escape literal \| in cells as \\\|`；schema 正文加入转义规则。**本仓库复验**：拉 0.4.3 后首次 `make docs-check` **直接绿**——本仓库此前手工修复的两处（BUG-0016 的规约或运算符移列、orch Edit 造成的 10 列行）经得住独立实现的校验，双向印证。框架侧提示"拉取后可能立刻红属预期（门禁开始管辖存量）"，本仓库因已先修而未触发） |
| FB-15 | 2026-07-28 | **`fix_commit` 列隐含"修复发生在本仓库"，外部修复的 bug 关闭时无 sha 可填**。触发：BUG-0026（TOOL_ENV，根因在 `scripts/evidence.py`）随框架 0.4.3 修复，`make evidence BUG=…` 正确翻转为 CLOSED，但 `docs-check` 随即 FAIL：`BUG-0026 is CLOSED but the fix-commit column is empty`——修复的 commit 在**框架仓库**（`iverif-workflow@6bb77d5`），本仓库只有一次 `fwsync --pull`。本次绕法：填跨仓库引用 `iverif-workflow@6bb77d5（框架 0.4.3）`，门禁接受（它只校验非空）。**但存在填不出任何引用的可预见情形**：BUG-0017 是 Verdi O-2018 的 `pynpi` 缺 `cov` 模块（版本墙），其详情页写明「VM 升级 Verdi ≥V-2023.12 后应转 CLOSED」——**升级 VM 不产生任何 commit**，届时该行无论如何填不出 sha，只能造一个假引用或让门禁挂着。这不是边缘情形：本仓库 TOOL_ENV/外部修复类 bug 占比很高（BUG-0014/0017/0019/0020/0022/0026 六条），vendored-DUT + 外部工具链的项目普遍如此。建议：schema 明示 `fix_commit` 接受**任何可追溯引用**而非仅本仓库 sha——`<repo>@<sha>` / 框架版本号 / `env: <变更描述>`（如 `env: Verdi O-2018→V-2023.12`）；或对 `TOOL_ENV` 类把判据从"必须是 commit"放宽为"必须可追溯"，判据文字随之改为「fix-commit column is empty」→「fix reference is empty」 | scripts/docs.py (CLOSED 行的 fix_commit 判据) · workflow/schema/failure_record.md (`fix_commit` 字段定义) | annoyance（当前有绕法：跨仓库引用；但对纯环境变更类修复无解） | fixed@0.4.5（`fix_commit` 语义放宽为任意**可追溯修复引用**——`<sha>` / `<repo>@<sha>` / `env: <变更>`（如 `env: Verdi O-2018→V-2023.12`），schema 与门禁文案同步；判据保持非空，**列名保留**（改名会引发下游 churn，框架侧按 rca 裁决取舍）。本仓库 BUG-0026 填的 `iverif-workflow@6bb77d5` 即新语义范本；BUG-0017 将来随 VM 升级 Verdi 关闭时用 `env:` 形式即可，不必造假引用——本条提出时预见的那个死角就此消除） |
| FB-16 | 2026-07-28 | **`make evidence BUG=` 无法承载非仿真类复验**：`svacheck.judge` 两腿制 fail-closed（设计正确），对没有 UVM/VCS 运行横幅的日志直接 `NOSUMMARY` 退出 ⇒ 复验判据是**编译期或工具输出**的缺陷无法机械关闭。本仓库实例：BUG-0014/0019 是 `make lint` 的旗标缺失，REV-010 做了实质复验（`Error-[SE]` 归零、`Top Level Modules:` 由约 100 个孤儿顶层收敛为 2 个、过滤逻辑 true-positive 分支工作），但关闭时只能借一次**与缺陷无关**的通过运行当关闭件，`verify_evidence` 列指向的文件**不是**证明该 bug 被修好的那份材料，实质证据只在 `doc/review/REV-010.md` 正文里。这不是边缘情形——本仓库 7 条（0014/0017/0019/0020/0022/0026/0028）的复验判据都不是仿真结果，vendored-DUT + 外部工具链的项目普遍如此（同 FB-15 的成因家族：框架的形态假设是「缺陷由仿真复现」）。建议：`make evidence BUG=` 增加非仿真判据入口，例如 `CMD=<复验命令>` —— 把命令、退出码与输出一并记入证据文件，判据为「退出码 + 复验者声明的期望签名」；或至少在 `workflow/schema/failure_record.md` 承认「`verify_evidence` 可引用一份 rev 评审记录」这一形态，使追溯链不断 | scripts/evidence.py (`make evidence BUG=` 的日志判据) · workflow/schema/failure_record.md (`verify_evidence` 字段) | annoyance（关闭结论仍成立，受损的是证据可追溯性） | fixed@0.5.0（框架走了本条建议的 **CMD 路线**、未走「schema 承认 rev 记录引用」的弱路线：`make evidence BUG=<ID> CMD='<复验命令>' EXPECT='<签名正则>'` 当场执行命令，**双重 fail-closed**——非零退出拒收、输出无签名匹配拒收（本仓库 FB-11 的措辞「没看见错 ≠ 查过了」进了框架错误信息）；`EXPECT` 必填，因为关闭者必须说出「什么输出证明修好了」＝可证伪性（six questions Q2）在非仿真域的等价物；证据 line 1 = `CMD:` 即 replay 命令，四条核心不变量不破。**本仓库已复验**：`CMD='make -C sim lint-diff TEST=m2_or03_guard_test' EXPECT='LINT-DIFF.*PASS'` 关闭 BUG-0029——用一条真正的非仿真复验关掉「非仿真复验无法机械关闭」这条 bug，该形态的首次真实运行；BUG-0029 的临时守卫据此退役） |
| FB-17 | 2026-07-28 | **`signoff-check` 条件 3 没有「已接受、排期到下一里程碑」的表达方式**：该条要求 all bugs terminal，而终态只有 `CLOSED / TB_BUG / SPEC_CHANGED / WONTFIX` 四种。本仓库 M2 收尾时有 5 条 bug（0018/0020/0022/0024/0025）属于「已充分分析、有书面接受理由、明确排期到 M3/M4、但现在既没修完也不是不修」——REV-010 明确裁决**不得用 WONTFIX 表达「以后再做」**（那等于为让自己的卡过关而放宽门禁，且与 REV-004 立下的「不得用 OPEN 表达决定不做」互为逆命题）。于是这 5 条只能保持 OPEN，`signoff-check` 条件 3 永远红，里程碑无法签核——**除非把它们硬塞进一个语义不符的终态，或者在里程碑内把它们全部做完**。后者有时是对的（本仓库这次就选择了做完），但框架不该只提供这一条路：跨里程碑的技术债是常态，把它表达成「未处理」会让门禁与现实脱节，进而诱发「为过门禁而误标终态」。建议：增设一个非终态但可签核的状态（如 `ACCEPTED@<milestone>`，语义=已分析、有书面 rationale、排期到指定里程碑），条件 3 改为「所有 bug 处于终态**或** ACCEPTED 且其 rationale 非空 **且** 目标里程碑未过期」；`rubric.md` 第 6 条（spec debt 的书面接受理由）已有同构先例，可直接推广 | scripts/make/evidence.mk (`signoff-check` 条件 3) · workflow/schema/failure_record.md (状态机) · workflow/signoff/rubric.md (第 6 条) | annoyance（有绕法：在里程碑内做完；但绕法成本随项目规模上升） | fixed@0.5.0（新增 `ACCEPTED@M<n>` 合法态，**比本条建议多一道过期红线**：行内必须引 REV 记录（rev 签名的 rationale，同 rubric #6 同构）；**目标里程碑已过（target < 当前）docs-check 直接红**；signoff 条件 3 未到期放行、到期/过期拦截（防 ACCEPTED 变成新地毯）；`make next` 让到期债自动浮出；永不终态、永不归档。⇒ REV-004「OPEN 不表达已决定」与 REV-010「WONTFIX 不表达以后再做」两条裁决自此有了合法载体——**M3 排债时不必再像本轮这样把 5 条全做完**。M2 已收完不受影响；M3 是 ACCEPTED 的首次真实运行） |
| FB-18 | 2026-07-28 | **0.5.0 的 `ACCEPTED@M<n>` 只落到了机器侧，`signoff/rubric.md` 两处未同步——签核的人工判据来源与机器判据自相矛盾，且 ACCEPTED 行在签核时完全不被人工复核**。两半：**(a) 字面矛盾**——`workflow/signoff/rubric.md:14` 机器条件 3 仍写 "All bug rows are in terminal states (`CLOSED / TB_BUG / SPEC_CHANGED / WONTFIX`)"，而 `scripts/docs.py:877` 已改为 "all bugs terminal **or ACCEPTED-unexpired**"。本仓库 M2 签核当口正好有四行 `ACCEPTED@M3`（BUG-0018/0024/0025/0031）：认真读判据来源的 rev 会按 rubric 字面判条件 3 不满足，与它自己跑出来的 `[PASS]` 直接冲突。两条出路都坏——按工具走则判据来源形同虚设（连带贬值 rubric #5 证伪要求那类**只存在于 rubric 的**条目），按 rubric 走则里程碑签不掉。绕法是 orch 在派卡时写明"以 docs.py:877 口径为准"，但那等于让项目侧口头覆盖框架文档，不是项目该有的权限。**(b) 更实质的缺口——rubric 没有任何条目要求签核人复核 ACCEPTED 的 rationale**。FB-17 的修复设计里，防"ACCEPTED 变成新地毯"的机制是"行内必须引 rev 签名的 rationale" + "到期即拦"；但机器只能验**两件形式**：行内含 `REV-` 字样（`docs.py:489`）、目标里程碑未过期（`docs.py:855`）。**那份 rationale 是否真的存在、是否可证伪、上一轮的到期判据是否兑现，没有任何机器或人工条目去查**——而 rubric #6 对 spec debt 恰恰有同构条目（"each entry has a written acceptance rationale"），FB-17 提案时也正是引它作先例，落地时却没把它推广到 bug debt。本仓库实例：BUG-0031 登记后条件 3 立刻转红，把它填成 `ACCEPTED@M3` 只需 orch 改一个单词、机器完全查不出；我们靠"退回 rev 补裁"这条**本地自觉**挡住了，rev 也确实交回三条可被 grep 推翻的依据（REV-011 §5.4），但框架不该把防地毯的最后一道防线放在项目自觉上——FB-17 自己写的 "防止 ACCEPTED 变成新地毯" 目前只兑现了一半。建议：① `rubric.md` 机器条件 3 同步为 "terminal **or ACCEPTED-unexpired**"；② 新增人工抽查第 7 条（与 #6 同构）："每条 ACCEPTED 行：其 REV 记录中的 rationale 存在且给出可证伪的判据；若是上一里程碑顺延而来，说明上次的到期判据为何未兑现"——顺延须重走仲裁这条在本仓库由 REV-011 §4 G3 自立，但那只是一份项目内的评审记录，换个项目就没有了 | workflow/signoff/rubric.md（机器条件 3 + 人工抽查缺第 7 条）· 对照 scripts/docs.py:489/855/877 与 workflow/schema/failure_record.md | blocking（人工判据来源与机器判据在里程碑签核这道门上互相矛盾；绕法要求项目侧覆盖框架文档） | open（2026-07-28 送达 iverif-workflow 侧；本仓库 M2 签核卡**待其闭环后再派**——拿一份已知会误导的判据来源去派签核卡，与 FB-11「没看见错 ≠ 查过了」同族） |
