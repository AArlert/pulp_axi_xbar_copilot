# 实践记录（原：iverif-workflow 框架反馈台账）

> **本文件的性质自 0.3.7 起改变，文件名与 `FB-` 编号一律不动。**
> 改名会打断 29 处引用，其中 **10 处在冻结记录里**（`signoff-M0/M2`、
> `REV-004/010`、三份 bug 详情页、三份归档）——按 FB-23 裁决那些不得回改。
> 所以变的是这个文件**是什么**，不是它在哪。
>
> **旧性质（FB-1 ~ FB-27）**：向上游提交的反馈台账。本仓库是 iverif-workflow
> 的首次实战应用，摩擦当场登记、blocking 当日回流、等上游修。
>
> **新性质（FB-28 起）**：**实践记录**。框架 0.8.0 删除了 fwsync/manifest/
> divergence 三态，「绝不本地修改 `scripts/`/`workflow/`」这条红线在机制上
> 已不存在，也不再有回流义务。本仓库的做法改为——**自己先做，做好了让上游
> 来 cherry-pick**。因此每一行回答的问题从「求上游修什么」变成
> 「**我们做了什么改动、为什么、上游要不要抄**」。
>
> **唯一的硬要求没变、而且比以前更重**：既然没有 manifest 记录我们改过哪些
> 上游文件，**本台账就是那份记录本身**。任何对 `workflow/` `scripts/`
> `.claude/` 的本地改动，必须在此留行并在代码旁注明 `见 doc/fw-feedback.md
> FB-xx`。漏了就退化成 FB-7 / BUG-0007 那个形状（变通只落注释、无人可 grep），
> 而这次连 `fw-check` 都不会再提醒你。
>
> 历史行提到的 `fw-check` / `fwsync` / `workflow/{schema,taxonomy,dispatch,
> signoff}/` 等均为**当时事实**，按 FB-23 裁决不回改。

severity：blocking（门禁/脚本拒绝正确工作）| annoyance（文档/体验问题）

status（FB-1~27，上游台账时代）：open | reported | fixed@\<ver\> | wontfix
status（FB-28 起，实践记录时代）：**local**（本仓库已改，上游可抄）|
**noted**（只记录，未动手）| **upstreamed**（上游已 cherry-pick）

**第一轮回流已闭环（2026-07-27）**：FB-1~FB-7 全部落入框架 0.2.1，本仓库已
`fwsync --pull`（`iverif.json` 从 0.2.0 → 0.2.1，`make fw-check` / `docs-check`
双绿）。FB-5 的绕行（orch 手动回退 FIX_READY→FIXING）自此作废，按 `.claude/agents/de.md`
新措辞走即可。

| id | date | symptom | framework file | severity | status |
|----|------|---------|----------------|----------|--------|
| FB-1 | 2026-07-27 | `--init` 拒绝仅含 LICENSE/README（及 .claude/settings.local.json）的新仓库，只能手工移开-恢复；建议加 `--allow-existing` 白名单或在 adoption.md 记录移开法 | kernel/fwsync.py (cmd_init) | annoyance | fixed@0.2.1（文档：adoption.md 记录移开法；`--allow-existing` 机制入 deferred 台账待第二次触发） |
| FB-2 | 2026-07-27 | `LM_LICENSE_FILE ?= 27000@localhost` 回退值与本 VM 实测值 `27000@icarray-virtual-machine` 不符，项目只能在 include 前 export 覆盖；建议回退值改为可探测或文档标注必须覆盖 | make/vcs-2018.mk | annoyance | fixed@0.2.1（文档：注释挑明是必须覆盖的占位值） |
| FB-3 | 2026-07-27 | 待验证：ucli `run; exit` 驱动的 `$stop` 型 tb 下，`-assert verbose` 的 `Summary:` 行是否出现（evidence.py SVA 腿 fail-closed，缺行即拒收）——首次 `make evidence` 时确认/证伪 | make/vcs-2018.mk / kernel/evidence.py | open question | fixed@0.2.1（经 FB-6 证实：`Summary:` 行确实出现，缺口在 evidence.py 窗口/正则，非工具选项） |
| FB-5 | 2026-07-27 | 角色文件与门禁自相矛盾：de.copilot.md 规定 DE"置 FIX_READY（不置 CLOSED）"，但 docs-check 要求 FIX_READY 行 fix_commit 非空，而 DE 交付时 orch 尚未提交、hash 不存在——按角色文件走必撞门禁（本次 BUG-0001 实撞）。docs.py NEXT_PHRASES 的本意是 FIXING→（commit 后）→FIX_READY；建议 de.copilot.md 改为"置 FIXING，FIX_READY 由 orch 在提交后连同 fix_commit 一并回填" | agents/de.copilot.md vs kernel/docs.py | blocking（有绕行：orch 回退状态） | fixed@0.2.1（de.copilot.md 改置 FIXING；docs.py 的 `make next` 短语同步改为 orch 提交后回填） |
| FB-4 | 2026-07-27 | Claude Code 会话中途 `fwsync --init` 渲染的 .claude/agents/ 注册有延迟（本次首批 arch/dv 派卡即报 "Agent type not found"，只能用 general-purpose 兜底承载角色文件；数分钟后类型才可用）；建议 adoption.md 在 --init 步骤后提示"重启会话（或等类型注册生效）再派卡" | docs/adoption.md | annoyance | fixed@0.2.1（文档：adoption.md 剧本 1 提示重启会话再派卡） |
| FB-6 | 2026-07-27 | 非 UVM tb（如上游 `tb_axi_xbar`，ucli `run;exit` 驱动 `$stop`）的记分板判决行（如 `Tests Failed: 0`）常落在 `PLAIN_MARK` 摘要窗口上沿之外，且不匹配 `KEY_LINE_RE`（pass\|match\|compare ok\|check ok\|running test），导致已登记证据的 `## Key check lines` 段为空——判定本身仍稳健（`svacheck.judge` 两腿制 fail-closed，SVA 腿独立零失败即拒收），但证据摘要未如实反映功能记分板结果，需人工复读源日志才能确认（signoff-M0 抽查 4 实测，R1）；建议上扩摘要窗口或为非 UVM tb 增补 `Tests Failed`/`ended`/`mismatch` 关键行模式 | kernel/evidence.py | annoyance | fixed@0.2.1（evidence.py 窗口 2→20 行 + 关键行正则，附保险丝测试；**存量 v0.0.1 三条记录仍空**，不追溯重写已签核产物） |
| FB-7 | 2026-07-27 | 四个角色模板（`agents/{arch,de,dv,rev}.copilot.md`）里 `doc/bugs.md` 登记指令都窄化到各自语境（DV 仅"on a mismatch"；DE/arch 仅"自己撞见的 spec 歧义/已挂号 bug 修复"）——`workflow/taxonomy/failure_taxonomy.md` 明确列出的五类判据（尤 `TOOL_ENV`）只要不是"scenario mismatch"触发（如实现期编译/工具报错、同卡内已绕过），没有任何角色文件的指令要求登记；taxonomy.md 正文本身也只描述判据，无一句"登记与是否阻塞 evidence/是否同卡内绕过无关"的显式声明。本仓库 M1-01 实测踩中：VCS-2018 拒绝 `bind <interface> <module>`，DV 同卡内改直接例化绕过，全程只留在代码注释 + rev 评审记录（REV-002/REV-003），未落 `doc/bugs.md`，taxonomy.md 自证的"下一个人能 grep 到"因此落空（orch 事后发现，已补登记 + 项目侧 CLAUDE.md §2 加了一条不豁免声明作本地缓解，但角色模板本身未修）；建议：① taxonomy.md 开头补"登记是无条件的，不因是否阻塞 evidence 或是否同卡内绕过而豁免"；② 四角色模板的固定交付报告格式里都加一条字段"本卡是否命中 taxonomy 任一类异常（含已绕过的）：是/否 + BUG-ID" | agents/*.copilot.md（kernel 渲染源）+ workflow/taxonomy/failure_taxonomy.md | annoyance（未被门禁拒绝，但是静默的可追溯性缺口，建议按核心不变量级别优先处理而非常规 annoyance 排期） | fixed@0.2.1（taxonomy.md 补"登记无条件"段 + 四角色模板增设强制字段） |
| FB-8 | 2026-07-27 | `kernel/docs.py` 的 `NEXT_PHRASES["copilot"]["undelivered"]`/`["prompt_missing"]` 硬编码"dispatch DE card"，隐含"copilot 项目里 feature-matrix 的 module 交付物永远是 DE 写的 RTL"；本仓库是 vendored-DUT 项目（无 `rtl/`，DE 卡按 CLAUDE.md §6 仅限 vendor 工具兼容补丁），feature-matrix 的 module 交付物其实全部是 DV 写的 tb 代码。`make next` 因此对**任何**尚未交付的 tb 模块（本次触发者：M2 新增的 `functional_coverage`，`doc/feature-matrix.md` F-M2-08）都会给出"dispatch DE card"的建议，人工需要凭 CLAUDE.md §6 的项目常识纠偏，而非照做——纯 advisory 文案不准确，未拒绝任何操作，`docs-check`/门禁均不受影响。且该短语无 per-project override 钩子（`columns_override` 有、`NEXT_PHRASES` 没有），项目侧无法本地纠正而不碰 `scripts/`。建议：`NEXT_PHRASES` 支持类似 `columns_override` 的 `iverif.json` 覆盖项（例如 `next_phrases_override`），或在 vendored-DUT 场景下把措辞改得角色中立（如"dispatch a card for the owning role（DE/DV，视本项目 CLAUDE.md 的 RTL/TB 归属而定）"） | kernel/docs.py (NEXT_PHRASES) | annoyance | fixed@0.3.3（root fix：`--next` 的交付物归属改由 `delivery.glob` 推导，tb/-rooted ⇒ DV-owned，零配置，不需要本仓库当初提议的 `next_phrases_override`；本仓库 2026-07-28 拉 0.4.1 后生效，措辞待下次触发 undelivered/prompt_missing 路径时目视复验） |
| FB-9 | 2026-07-28 | `scripts/evidence.py` 的 `KEY_LINE_RE` 不匹配 UVM 报告里的 `[FCOV_SUMMARY]` 明细行，功能覆盖率百分比因此进不了 evidence 摘要的 `## Key check lines` 段——只在"Report counts by id"里露出一个 id 计数（本仓库 F-M2-08 落地后：七条 evidence 各显示 `[FCOV_SUMMARY] 2`，而每个 covergroup 的 samples/inst_cov 数字全部落在摘要窗口之外）。后果：覆盖率证据的**数字**不在证据文件里，里程碑签核时要么回源 log、要么回跑 `make cov`，与"evidence 是自足的可复核制品"这一意图相悖；同类问题的既有先例是 FB-6（判决行落在摘要窗口之外）与 BUG-0008（M0 三条证据 `## Key check lines` 段为空）。建议：`KEY_LINE_RE` 增加对 `[FCOV_SUMMARY]`/`[COV_*]` 一类覆盖率摘要标签的识别，或提供项目侧可配的额外关键行模式（类似 `columns_override` 的 `iverif.json` 钩子），使各项目能把自己的覆盖率摘要标签纳入而不必碰 `scripts/` | scripts/evidence.py (KEY_LINE_RE) | annoyance | fixed@0.3.3（`[FCOV_SUMMARY]` 升为 canon 约定而非配置钩子——框架侧裁定「零配置正确性优于配置钩子」；本仓库 tb 早已按该格式打印 ⇒ 拉取后零改动。已复验 `scripts/evidence.py:49` 的 `KEY_LINE_RE` 含 `\[FCOV_SUMMARY\]`；证据文件里的覆盖率数字待下次 `make evidence` 重登记时落地） |
| FB-10 | 2026-07-28 | **已登记的 `regression_guard` 没有强制消费机制——跨卡一致性无载体**。判例（本仓库完整闭环）：BUG-0015 修复后登记 `type: checklist` guard，原文"今后 `tb/sva/` 下任何 SVA 模块，property/cover 表达式中**不得**直接调用读取跟踪状态的函数……评审 SVA 卡时按此项核对"，且其 `## similar` 段**精确点名**了下一个受害者（"`tb/sva/axi_xbar_stall_sva.sv` 同样在 property 内直接调用读数组的函数（`w_reorder(b_id)` 等）……后续动该文件时建议顺手折叠"）；今天 DV 分诊卡在 `stall_sva.sv:212/218` 判出真缺陷，**与点名位置一字不差**。中间隔着一张 rev 卡（REV-008，评审范围恰含 `tb/sva/`）也未看到该 guard——因为 rev 的判据来源清单由 orch 派卡时给定，而**没有任何机制提示 orch 该写进去**。数据：本仓库 14 条 guard 中 **9 条为 `type: checklist`**（纯自然语言祈使句、无自动化载体）；`ref: sim/Makefile lint: 目标` **重复出现 4 次**（BUG-0014/0019/0021/0022，同一目标反复出问题、每次留一条 checklist guard、无一条阻止了下一条）；`workflow/signoff/rubric.md` 第 5 条只抽查 1 条做证伪（14 条抽 1 = 7% 覆盖）。根因层：隔离规则切断**推理**传播（正确），但**一致性约束也需传播**且当前无载体；解法不能是设跨卡常驻角色（等于重建共模误差），只能让约束以**事实**形式自动传播（事实传播不产生共模误差），`doc/bugs.md` 已是载体、缺的是强制消费。**建议 1（价值最高）**：派卡时机械执行「本卡触及文件 ∩ 所有 guard 的 `ref` → 命中的 guard 全文进入卡的判据来源清单」——纯路径匹配，orch 无须理解内容 ⇒ 不违反"orch 不产技术制品"，注入已登记事实而非推理 ⇒ 不引入共模误差；此机制若早存在，上述判例不会发生。前提：`workflow/schema/failure_record.md` 的 `ref:` 字段须结构化（现为自由文本、混着路径与说明，如 ``ref: `tb/sva/axi_xbar_stall_sva.sv`（L205-216 完成序不乱序 assert = 长期判决门；…``），建议拆为可机械匹配的路径列表 + 说明。**建议 2**：`rubric.md` 第 5 条抽查改为"至少覆盖本里程碑触及文件所命中的全部 guard"，与注入共用同一反向索引。**建议 3（schema 写作指引）**：明示"很多 checklist guard 是**还没被机械化**的、不是无法机械化的"——BUG-0015 那条完全可静态检查（property/cover 表达式中不得直接调用函数），写成 checklist 只因当时没问"这条能不能变成脚本"。三条均可复用既有 `fl_schema_enforce`/`sva_enforce` 的 `iverif.json` 开关模式。本仓库将先做原型（`make guards` 查询目标；`Makefile`/`.githooks/`/`sim/Makefile` 均不在 26 个 pin 文件内、有自由度），跑通后连同实测反馈回流。已于 2026-07-28 通过 session 通道把判例+数据+设计直接送达 iverif-workflow 侧 | workflow/schema/failure_record.md (regression_guard.ref) · .claude/skills/dispatch/SKILL.md (卡组装) · workflow/signoff/rubric.md (第5条) | annoyance（不拒绝任何操作，但已实证导致真缺陷按预警原样复发） | fixed@0.4.1（框架侧当日采纳全部三条：`## regression_guard` 新增 `paths:` glob 字段、`docs.py --guards` / `make guards FILES=…` 纯 fnmatch 求交、**双消费挂点**＝dispatch 自检清单逐字贴卡 ＋ rubric #5 改为「里程碑触及文件命中的全部 guard 入围复核 + 至少证伪一条」取代 1/N 抽查；schema 并写入本仓库提议的写作规则「checklist guard 是尚未机械化的 TODO、不是终点」。框架侧刻意**不挂 evidence**——guard 是输入侧约束、evidence 是输出侧咽喉，语义不同（FB-11 的 stamp 挂 evidence 反被判为正确）。**本仓库侧动作已完成**：18 条存量 guard 全部回填 `paths:`（无 `paths:` 者永不被注入，该句已写进 schema），复验 `make guards FILES="tb/sva/axi_xbar_stall_sva.sv"` 命中 5 条、含 **BUG-0015**——被 F1 违反的正是它，现在它会自动出现在任何触及 `tb/sva/` 的卡里。缺口关闭得证） |
| FB-11 | 2026-07-28 | **门禁缺少"自证执行"的通用不变式**——框架有 `no sim log no ✅`（保证被测**场景**被验证），但没有任何不变式保证**验证工具本身**有效。本仓库实证两处：(a) `sim/Makefile` 的 `lint:` 目标自接入脚手架（`2fbca13`，M0 期）起就是坏的（缺 `-assert svaext` + 缺 `-top`，BUG-0014/0019），能坏这么久是因为 `lint` **不在 signoff-check、不在 docs-check、不在任何门禁清单里**，只是根 Makefile 的一个 passthrough；(b) BUG-0022——`make lint` 增量编译命中时**假绿**：同一份代码连跑两次，第 1 次 259 条告警 exit 2、第 2 次 `lint clean` exit 0，因 VCS 增量路径不重新分析源码、不产生任何 `Lint-` 行但仍覆写日志，门禁判据在空日志上一无所获 ⇒ 把"这次根本没执行 lint"误判为"lint 干净"。抽象为一句话：**"没看到错误" ≠ "检查通过"**。同族后果还有 BUG-0020（`verdi:` 目标引用一个从不被任何目标生成的 `wave.fsdb`，半成品从 M0 活到现在，因为从没人执行过它）。建议把 `no sim log no ✅` 推广为通用不变式 **`no proof-of-execution no gate-pass`**：门禁须产出与被检查内容绑定的执行证明。本仓库原型形态——`$(OUT)/lint.stamp: $(TB_SRCS)`，规则内先强制重编（消除增量假绿）、跑 gate、`grep -q "elaboration done"` 校验**确实执行过**、最后把源码 sha256 写进 stamp；三性质缺一不可：依赖源码（变了自动失效，make 天然提供）＋校验执行实证标记（杀假绿）＋stamp 为内容指纹（`touch` 骗不过）。配套强制挂点建议：**框架已有一个不可绕过的咽喉——`make evidence` 是 ✅ 的唯一入口**，把 gate 挂此处即自动获得强制性（想登记证据 → stamp 须有效且匹配当前源码；绕过 evidence 就没有 ✅、没有里程碑），无须新造强制点；`.githooks/pre-commit` 是 soft gate（其首行自陈 "bypassable with --no-verify"），只适合秒级检查。分层建议按代价匹配频率：秒级（docs-check/guard 提示）→ pre-commit；分钟级（lint）→ evidence 前置；十分钟级（regress）→ 里程碑。已于 2026-07-28 随 FB-10 一并送达 iverif-workflow 侧 | scripts/make/evidence.mk (evidence 前置) · workflow/signoff/rubric.md · workflow/schema/evidence_record.md | blocking（门禁可在未执行检查的情况下发出通过信号——会假绿的门禁比没有门禁更危险） | fixed@0.5.2（框架回执 2026-07-28：**gate 自证教义按本仓库的对抗原型全文重写**进 deferred 台账——本仓库送出的证伪结论（stamp 候选形态的两个洞 + 「elaboration done」在 VCS O-2018 根本不存在）推翻了原候选形态，重写后的教义即以此为据；canon 载体等模式的**第二个采纳者**出现再定型，本仓库 `sim/Makefile`（BUG-0022 的无条件重跑 + 逐文件执行证明）被记为参考实现。⇒ 本仓库无待办：原「待做 lint stamp 原型并回流实测」两项已随 BUG-0022 修复完成并回流） |
| FB-12 | 2026-07-28 | **跨大改版本拉取时，项目侧 pin 的旧 `fwsync` 用旧配对表去量新框架，静默产出残缺快照而门禁照样绿**。复现：项目 `iverif.json` 已声明 `"profile": "copilot"`，`python3 scripts/fwsync.py --pull <fw>` 从 0.3.0 拉 0.4.1，得到 manifest 25 条、`workflow/profile.md` 根本不被写出，而 `make fw-check` **照样输出 `fw-check passed (framework 0.4.1, 25 files pinned)`**。**根因（经框架侧指正 + 本仓库用 `git show 8228d37:scripts/fwsync.py` 读回当时实际执行的那份脚本核实）**：执行这次 pull 的是项目 `scripts/` 里 pin 的 **0.3.0 旧脚本**，其 `vendor_pairs()` 第 114 行按 `for name in ("profiles", "discipline")` 去框架仓库找 `docs/profiles.md`——该文件在 0.4.0 已被拆分删除（改为 `docs/profile.{learning,copilot}.md`）⇒ 找不到即**静默跳过**⇒ 少 pin 一个文件。即"**用旧尺子量新东西还报绿**"，与 `--profile` 参数**无关**：`--profile` 在所有版本里都只进 `--init`，`do_pull` 一直是从 `iverif.json` 读 profile（0.3.0 脚本第 187-194 行 `profile = cfg.get("profile")` 可证）。本仓库当初补带 `--profile copilot` 重拉后恢复 26 条，是因为**那时 `scripts/fwsync.py` 已被第一次 pull 覆盖成 0.4.1**、新配对逻辑本就正确，该参数纯属巧合同行——**此前本行把根因写成"pull 路径未传 profile"是错的，已按实证更正，以免后来人去查一个不存在的参数缺陷**。两个后果：(a) 项目静默失去自己的 profile 契约文件——0.4.0 起该文件正是"项目只收到自己那份契约"的载体，缺了它没有任何提示；(b) 上一版被移除的 `workflow/profiles.md` 不会被 pull 清理，留下一个内容停在旧版本、不在 manifest、fw-check 不管辖的**孤儿文件**，而项目 CLAUDE.md 仍在引用它（本仓库已手工删除并改引用）。**本条与 FB-11 是同一失效模式**：门禁把"这次根本没做这项检查/没同步这个文件"呈现为"通过"——差别只在 FB-11 是 lint、本条是 fwsync。建议：(1) ~~`--pull` 缺省从 `iverif.json` 读 profile~~——**本条作废**：该行为本来就存在，是本仓库最初的根因误判所致（见上）；根治方向应为「项目侧 pull 一律改由**框架侧**脚本执行，使 pin 的旧脚本永不决定一次 pull」；(2) 取不到 profile 时**拒绝 pull**（fail-closed）而非静默少写文件；(3) pull 后比对新旧 manifest 差集，对"上一版 pin、本版不再 pin"的残留文件至少告警（可选清理）；(4) `fw-check` 把"profile 契约文件是否在 manifest 内"本身纳入判据——manifest 自洽哈希看不见"写残的 manifest"，缺失因此不可见 | kernel/fwsync.py (`vendor_pairs`/`snapshot_pairs` 配对表随版本漂移 · `do_pull` 由哪一侧脚本执行) · scripts/make/core.mk (`fw-pull`) | blocking（快照可在缺少 profile 契约的状态下通过 fw-check——同 FB-11 的假绿模式） | fixed@0.4.2（框架侧四层修复：① **bootstrap hop**——项目侧 `--pull` 一律 re-exec 框架侧 fwsync，pin 的旧脚本的配对逻辑永不再决定一次 pull，此为根治；② profile 非法/缺失时 fail-closed 拒绝拉取（refusing a partial pull）；③ 孤儿清扫——不再 pin 的旧文件 pristine 时自动删除并打印、有本地改动则报告人工处理（本仓库手工删的 `workflow/profiles.md` 这类以后自动处理）；④ fw-check 完整性探针——profile 契约是否在 manifest 内本身成为判据。60 例自测含三条 FB-12 fuse。**本仓库已复验**：2026-07-28 从 0.4.1 拉 0.4.2、**故意不带 `--profile`**，得 `pulled framework 0.4.2: 26 files` + `fw-check passed (framework 0.4.2, 26 files pinned)` + `workflow/profile.md` 就位——同样的命令形式在 0.4.1 时只得 25 files，此次 pull 本身即 bootstrap hop 的首次真实运行） |
| FB-13 | 2026-07-28 | **`KEY_LINES_MAX=30` 让 SVA 逐条命中数进不了证据、且取的是任意前缀**：`scripts/evidence.py` 的 `KEY_LINE_RE` 含 `match`，于是 `-assert verbose` 打印的每一条 assertion/cover 行（`"file", line: inst, N attempts, M match`）都算关键行；本仓库每次运行有数百条这类行，30 条上限被**最先出现的那个模块**（`axi_xbar_txlimit_sva`）整段吃掉，其余模块的 cover 命中数一条不进证据。实测：0.3.3 起新增的 2 条 `[FCOV_SUMMARY]` 明细行（FB-9 的修复）把 3 条 txlimit 行挤出，而本卡真正需要的 `axi_xbar_stall_sva` 6 条 cover 命中数从来就不在证据里——DV 卡验收项要求报告「cover 命中数变化」，只能回源 `sim/out/*.log` 取数，与 FB-6/FB-9/BUG-0008 同一族（证据摘要不自足）。且截断按**文件出现顺序**而非重要性，哪些模块的数字被保留纯属偶然，评审无法从证据判断「没出现 = 没这条 cover」还是「没出现 = 被截断了」。建议：(1) 关键行做**分类配额**（SVA 逐条行单独限额，或每模块保留其汇总）而非一刀切前 N 条；(2) 或对 SVA 逐条行按文件归并 attempts/match，把「每模块 cover 命中」压成几行；(3) 至少在截断处写一行 `... (K lines truncated)`，使截断本身可见（当前静默丢弃）。 | scripts/evidence.py (`KEY_LINES_MAX` / `KEY_LINE_RE`) | annoyance | fixed@0.4.3（采纳本条建议 (2)+(3) 的组合：`-assert verbose` 逐条明细行按源文件**聚合为单行**（`x_sva.sv: N properties/covers, A attempts, M match`）⇒ 每模块 cover 命中数从此必然在证据里；判决行独享 30 行上限；截断处打印 `... (K more key lines truncated)` 使截断可见。框架侧以本仓库 stall_sva 六条 cover 的场景为测试 fixture 原型。**本仓库已复验**：拉 0.4.3 后重新登记全部七条 M2 证据，每条都含 5 个 SVA 模块的聚合行，`axi_xbar_stall_sva.sv: 60 properties/covers, 2640 attempts, 24 match` 赫然在列——正是 BUG-0026 里说"从来就不在证据文件里"的那个数字。**BUG-0026 据此转 CLOSED**（verify_evidence `doc/evidence/v0.2.0/BUG-0026.log`）。框架侧提示：若真实 log 有行形变体导致漏聚合，那些行会 fail-safe 退回原样保留、不丢数据；本仓库七条证据未见漏聚合形态） |
| FB-14 | 2026-07-28 | **`docs-check` 不校验 `doc/bugs.md` 的表格列数，一个未转义的管道符即可静默损坏整行结构**。实例：BUG-0016 的 summary 里引用 RTL 表达式 ``full=\|cnt_full=(&in_flight)``——SystemVerilog 的规约或运算符 `\|` 当时未转义，markdown 遂把它当列分隔符，该行从此处起**整体右移一位**（解析为 9 列）：`fix_commit` 列实际装着 root_cause 的正文、`verify_evidence` 装着本该在 fix_commit 的 `-`、末尾多出一个 `-`。**`docs-check` 全程 passed**，损坏自该行登记之日潜伏，直到 orch 手工做列数体检才发现。**真实门禁风险**：`CLOSED` 状态的判据之一是「verify_evidence 非空」，行错位后该检查读到的是**错列的内容**——本例 BUG-0016 恰好是 `SPEC_CHANGED`、不触发该检查，但同样的错位落在一条 CLOSED 行上就会让门禁读着错误的格子判定通过。**第二个独立样本（同批发现）**：orch 用 Edit 追加 summary 时 `new_string` 多带了两列，该行变成 10 列，`docs-check` 同样 passed。两个样本方向相反（一个少解析、一个多写），门禁对两者都无感。**与 FB-11/FB-12 同族，这是第三个独立样本**：门禁在数据结构已损坏 / 检查根本没执行的情况下照常发出通过信号（FB-11 = lint 增量假绿，FB-12 = fwsync 缺 profile 静默降级，本条 = bugs.md 行结构损坏）——三者共同指向同一条缺失的通用不变式。建议（零成本）：`docs.py --check` 增加结构校验——每个数据行按 `(?<!\\)\|` 切分后的列数必须等于表头列数，不等即 FAIL 并打印行号与实际列数；同一条校验可复用于 `doc/testplan.md` / `doc/feature-matrix.md` / `doc/fw-feedback.md`。附带建议：在 `failure_record.md` 提一句「表格单元内的 `\|` 必须转义」——本仓库 DUT 是 AXI crossbar，RTL 表达式里的按位或/规约或极常见，这个坑会反复踩 | scripts/docs.py (`--check` 表格结构校验) · workflow/schema/failure_record.md | blocking（门禁在行结构损坏时通过，且 CLOSED 判据可能读到错列内容） | fixed@0.4.3（`docs-check` 新增表格结构门禁：testplan / feature-matrix / bugs / lint-waivers 四张活表逐行校验「列数 == 表头列数」，未转义 `\|` 移列即 FAIL 并打印行号与实际列数，报错文案带 `escape literal \| in cells as \\\|`；schema 正文加入转义规则。**本仓库复验**：拉 0.4.3 后首次 `make docs-check` **直接绿**——本仓库此前手工修复的两处（BUG-0016 的规约或运算符移列、orch Edit 造成的 10 列行）经得住独立实现的校验，双向印证。框架侧提示"拉取后可能立刻红属预期（门禁开始管辖存量）"，本仓库因已先修而未触发） |
| FB-15 | 2026-07-28 | **`fix_commit` 列隐含"修复发生在本仓库"，外部修复的 bug 关闭时无 sha 可填**。触发：BUG-0026（TOOL_ENV，根因在 `scripts/evidence.py`）随框架 0.4.3 修复，`make evidence BUG=…` 正确翻转为 CLOSED，但 `docs-check` 随即 FAIL：`BUG-0026 is CLOSED but the fix-commit column is empty`——修复的 commit 在**框架仓库**（`iverif-workflow@6bb77d5`），本仓库只有一次 `fwsync --pull`。本次绕法：填跨仓库引用 `iverif-workflow@6bb77d5（框架 0.4.3）`，门禁接受（它只校验非空）。**但存在填不出任何引用的可预见情形**：BUG-0017 是 Verdi O-2018 的 `pynpi` 缺 `cov` 模块（版本墙），其详情页写明「VM 升级 Verdi ≥V-2023.12 后应转 CLOSED」——**升级 VM 不产生任何 commit**，届时该行无论如何填不出 sha，只能造一个假引用或让门禁挂着。这不是边缘情形：本仓库 TOOL_ENV/外部修复类 bug 占比很高（BUG-0014/0017/0019/0020/0022/0026 六条），vendored-DUT + 外部工具链的项目普遍如此。建议：schema 明示 `fix_commit` 接受**任何可追溯引用**而非仅本仓库 sha——`<repo>@<sha>` / 框架版本号 / `env: <变更描述>`（如 `env: Verdi O-2018→V-2023.12`）；或对 `TOOL_ENV` 类把判据从"必须是 commit"放宽为"必须可追溯"，判据文字随之改为「fix-commit column is empty」→「fix reference is empty」 | scripts/docs.py (CLOSED 行的 fix_commit 判据) · workflow/schema/failure_record.md (`fix_commit` 字段定义) | annoyance（当前有绕法：跨仓库引用；但对纯环境变更类修复无解） | fixed@0.4.5（`fix_commit` 语义放宽为任意**可追溯修复引用**——`<sha>` / `<repo>@<sha>` / `env: <变更>`（如 `env: Verdi O-2018→V-2023.12`），schema 与门禁文案同步；判据保持非空，**列名保留**（改名会引发下游 churn，框架侧按 rca 裁决取舍）。本仓库 BUG-0026 填的 `iverif-workflow@6bb77d5` 即新语义范本；BUG-0017 将来随 VM 升级 Verdi 关闭时用 `env:` 形式即可，不必造假引用——本条提出时预见的那个死角就此消除） |
| FB-16 | 2026-07-28 | **`make evidence BUG=` 无法承载非仿真类复验**：`svacheck.judge` 两腿制 fail-closed（设计正确），对没有 UVM/VCS 运行横幅的日志直接 `NOSUMMARY` 退出 ⇒ 复验判据是**编译期或工具输出**的缺陷无法机械关闭。本仓库实例：BUG-0014/0019 是 `make lint` 的旗标缺失，REV-010 做了实质复验（`Error-[SE]` 归零、`Top Level Modules:` 由约 100 个孤儿顶层收敛为 2 个、过滤逻辑 true-positive 分支工作），但关闭时只能借一次**与缺陷无关**的通过运行当关闭件，`verify_evidence` 列指向的文件**不是**证明该 bug 被修好的那份材料，实质证据只在 `doc/review/REV-010.md` 正文里。这不是边缘情形——本仓库 7 条（0014/0017/0019/0020/0022/0026/0028）的复验判据都不是仿真结果，vendored-DUT + 外部工具链的项目普遍如此（同 FB-15 的成因家族：框架的形态假设是「缺陷由仿真复现」）。建议：`make evidence BUG=` 增加非仿真判据入口，例如 `CMD=<复验命令>` —— 把命令、退出码与输出一并记入证据文件，判据为「退出码 + 复验者声明的期望签名」；或至少在 `workflow/schema/failure_record.md` 承认「`verify_evidence` 可引用一份 rev 评审记录」这一形态，使追溯链不断 | scripts/evidence.py (`make evidence BUG=` 的日志判据) · workflow/schema/failure_record.md (`verify_evidence` 字段) | annoyance（关闭结论仍成立，受损的是证据可追溯性） | fixed@0.5.0（框架走了本条建议的 **CMD 路线**、未走「schema 承认 rev 记录引用」的弱路线：`make evidence BUG=<ID> CMD='<复验命令>' EXPECT='<签名正则>'` 当场执行命令，**双重 fail-closed**——非零退出拒收、输出无签名匹配拒收（本仓库 FB-11 的措辞「没看见错 ≠ 查过了」进了框架错误信息）；`EXPECT` 必填，因为关闭者必须说出「什么输出证明修好了」＝可证伪性（six questions Q2）在非仿真域的等价物；证据 line 1 = `CMD:` 即 replay 命令，四条核心不变量不破。**本仓库已复验**：`CMD='make -C sim lint-diff TEST=m2_or03_guard_test' EXPECT='LINT-DIFF.*PASS'` 关闭 BUG-0029——用一条真正的非仿真复验关掉「非仿真复验无法机械关闭」这条 bug，该形态的首次真实运行；BUG-0029 的临时守卫据此退役） |
| FB-17 | 2026-07-28 | **`signoff-check` 条件 3 没有「已接受、排期到下一里程碑」的表达方式**：该条要求 all bugs terminal，而终态只有 `CLOSED / TB_BUG / SPEC_CHANGED / WONTFIX` 四种。本仓库 M2 收尾时有 5 条 bug（0018/0020/0022/0024/0025）属于「已充分分析、有书面接受理由、明确排期到 M3/M4、但现在既没修完也不是不修」——REV-010 明确裁决**不得用 WONTFIX 表达「以后再做」**（那等于为让自己的卡过关而放宽门禁，且与 REV-004 立下的「不得用 OPEN 表达决定不做」互为逆命题）。于是这 5 条只能保持 OPEN，`signoff-check` 条件 3 永远红，里程碑无法签核——**除非把它们硬塞进一个语义不符的终态，或者在里程碑内把它们全部做完**。后者有时是对的（本仓库这次就选择了做完），但框架不该只提供这一条路：跨里程碑的技术债是常态，把它表达成「未处理」会让门禁与现实脱节，进而诱发「为过门禁而误标终态」。建议：增设一个非终态但可签核的状态（如 `ACCEPTED@<milestone>`，语义=已分析、有书面 rationale、排期到指定里程碑），条件 3 改为「所有 bug 处于终态**或** ACCEPTED 且其 rationale 非空 **且** 目标里程碑未过期」；`rubric.md` 第 6 条（spec debt 的书面接受理由）已有同构先例，可直接推广 | scripts/make/evidence.mk (`signoff-check` 条件 3) · workflow/schema/failure_record.md (状态机) · workflow/signoff/rubric.md (第 6 条) | annoyance（有绕法：在里程碑内做完；但绕法成本随项目规模上升） | fixed@0.5.0（新增 `ACCEPTED@M<n>` 合法态，**比本条建议多一道过期红线**：行内必须引 REV 记录（rev 签名的 rationale，同 rubric #6 同构）；**目标里程碑已过（target < 当前）docs-check 直接红**；signoff 条件 3 未到期放行、到期/过期拦截（防 ACCEPTED 变成新地毯）；`make next` 让到期债自动浮出；永不终态、永不归档。⇒ REV-004「OPEN 不表达已决定」与 REV-010「WONTFIX 不表达以后再做」两条裁决自此有了合法载体——**M3 排债时不必再像本轮这样把 5 条全做完**。M2 已收完不受影响；M3 是 ACCEPTED 的首次真实运行） |
| FB-18 | 2026-07-28 | **0.5.0 的 `ACCEPTED@M<n>` 只落到了机器侧，`signoff/rubric.md` 两处未同步——签核的人工判据来源与机器判据自相矛盾，且 ACCEPTED 行在签核时完全不被人工复核**。两半：**(a) 字面矛盾**——`workflow/signoff/rubric.md:14` 机器条件 3 仍写 "All bug rows are in terminal states (`CLOSED / TB_BUG / SPEC_CHANGED / WONTFIX`)"，而 `scripts/docs.py:877` 已改为 "all bugs terminal **or ACCEPTED-unexpired**"。本仓库 M2 签核当口正好有四行 `ACCEPTED@M3`（BUG-0018/0024/0025/0031）：认真读判据来源的 rev 会按 rubric 字面判条件 3 不满足，与它自己跑出来的 `[PASS]` 直接冲突。两条出路都坏——按工具走则判据来源形同虚设（连带贬值 rubric #5 证伪要求那类**只存在于 rubric 的**条目），按 rubric 走则里程碑签不掉。绕法是 orch 在派卡时写明"以 docs.py:877 口径为准"，但那等于让项目侧口头覆盖框架文档，不是项目该有的权限。**(b) 更实质的缺口——rubric 没有任何条目要求签核人复核 ACCEPTED 的 rationale**。FB-17 的修复设计里，防"ACCEPTED 变成新地毯"的机制是"行内必须引 rev 签名的 rationale" + "到期即拦"；但机器只能验**两件形式**：行内含 `REV-` 字样（`docs.py:489`）、目标里程碑未过期（`docs.py:855`）。**那份 rationale 是否真的存在、是否可证伪、上一轮的到期判据是否兑现，没有任何机器或人工条目去查**——而 rubric #6 对 spec debt 恰恰有同构条目（"each entry has a written acceptance rationale"），FB-17 提案时也正是引它作先例，落地时却没把它推广到 bug debt。本仓库实例：BUG-0031 登记后条件 3 立刻转红，把它填成 `ACCEPTED@M3` 只需 orch 改一个单词、机器完全查不出；我们靠"退回 rev 补裁"这条**本地自觉**挡住了，rev 也确实交回三条可被 grep 推翻的依据（REV-011 §5.4），但框架不该把防地毯的最后一道防线放在项目自觉上——FB-17 自己写的 "防止 ACCEPTED 变成新地毯" 目前只兑现了一半。建议：① `rubric.md` 机器条件 3 同步为 "terminal **or ACCEPTED-unexpired**"；② 新增人工抽查第 7 条（与 #6 同构）："每条 ACCEPTED 行：其 REV 记录中的 rationale 存在且给出可证伪的判据；若是上一里程碑顺延而来，说明上次的到期判据为何未兑现"——顺延须重走仲裁这条在本仓库由 REV-011 §4 G3 自立，但那只是一份项目内的评审记录，换个项目就没有了 | workflow/signoff/rubric.md（机器条件 3 + 人工抽查缺第 7 条）· 对照 scripts/docs.py:489/855/877 与 workflow/schema/failure_record.md | blocking（人工判据来源与机器判据在里程碑签核这道门上互相矛盾；绕法要求项目侧覆盖框架文档） | fixed@0.5.3（框架当日闭环，commit `12b1548`，两半全采纳：**(a)** `rubric.md` 机器条件 3 同步为"terminal or unexpired `ACCEPTED@M<n>`"；**(b)** 新增人工抽查**第 7 条**（与 #6 同构，`docs.py --signoff` 同步打印）——每条 ACCEPTED 行引用的 REV 记录必须给出**可证伪**判据，且**顺延必须重仲裁、不得直接续期**并说明上次到期判据为何未兑现。⇒ 本仓库 REV-011 §4 G3 的项目自立规则就此入 canon，§5.4 作为参考形状记入框架 CHANGELOG；框架侧另加 fuse 钉住 rubric/工具在条件 3 与第 7 条上的永久一致。**本仓库已 pull 0.5.3**，`fw-check`/`docs-check` 双绿，`make signoff-check` 尾部确认打印第 7 条；M2 签核卡据此解除暂停） |
| FB-19 | 2026-07-28 | **guard 逐字注入规则在里程碑签核这个规模上与 rubric #5 撞车，且没有分层出口**：`.claude/skills/dispatch/SKILL.md` §3 末条要求 orch 把 `make guards` 的**每一条命中正文逐字粘进卡里**（0.5.1 追加了「超 ~6 条分 hard/context 两栏」的呈现规则，但**两栏仍都要求 verbatim**，即分层解决的是**可读性**、没有解决**体量**）。这在功能卡上合适（本仓库前几张卡命中 6-8 条，粘贴成本可接受），但**里程碑签核卡的 FILES 天然是「整个里程碑触及的全部文件」**——本仓库 M2 实测 22 个文件命中 **22 条 guard、共 413 行**，而签核卡本身还要装 testplan/evidence/bugs/reviews/spec 五类范围与七条 rubric 判据。更关键的是**同一份内容被两条机制各要求了一次**：`workflow/signoff/rubric.md` 第 5 条的原文就是命令 **rev 自己**跑 `make guards FILES="<files this milestone touched>"`——即签核场景下 guard 注入**本来就有一条活的、不会随卡片漂移的通路**，orch 再粘 413 行是纯冗余，且粘贴快照有随时间失配的风险（卡是静态的，台账是活的）。本仓库本次的处理：给出确定性的 `FILES` 清单（附计算它的 `git diff --name-only <M1 签核 commit>..HEAD -- tb/ sim/` 命令）+ **逐字粘贴 22 条命中的 header 索引行**（保证没有条目被静默丢弃）+ 一条硬性自检（「你跑出的条数/id 集合与此不符则先停下报告差异，不要继续签核」），并在卡里**明说这是对派卡规则的偏离及其理由**，同时请 rev 若认为该偏离损害了签核就写进记录。建议：① 在 dispatch SKILL §3 该条上加一个**签核卡例外**——当卡的判据来源本身命令被派发者跑 `make guards` 时，orch 改为提供**确定性的 FILES 清单 + 命中索引 + 计数自检**，不粘正文（理由与 FB-16 的 `CMD=` 同族：**让被派发者自己执行并证明执行过**，比让 orch 转述一份可能过期的快照更强）；② 或给出一个体量阈值（如 >12 条命中）触发同样的降级路径，使规则对「卡的 FILES 是整个里程碑」这一形态有定义 | .claude/skills/dispatch/SKILL.md §3 末条（guard 逐字注入）· 对照 workflow/signoff/rubric.md 第 5 条（命令被派发者自跑同一命令） | annoyance（不阻塞：偏离有可审计的补偿措施；但规则对签核卡这一形态**无定义**，逼 orch 每次自行裁量——而「orch 自行裁量要不要遵守派卡规则」正是这套机制最不该留的自由度） | fixed@0.5.4（框架当日闭环，commit `4e0e346`，采纳建议 ①，本仓库当场用的三件套**原样入 canon**：dispatch SKILL §3 逐字规则新增例外——**当卡的判据来源本身命令被派发者跑同一条 `make guards` 时（签核卡即 rubric #5），orch 不粘正文**，改为确定性 FILES 清单 + 计算它的命令 + 命中索引行 + 计数自检；功能卡照旧逐字。**建议 ② 的体量阈值被否**，理由值得记下来：魔法数字又是一个留给裁量的口子，而原则性触发条件（判据来源命令被派发者自跑）没有这个问题——即本条要治的病，阈值方案自己也会得。本仓库的元观察原文入框架 CHANGELOG：「凡是需要靠自觉的地方，就是规则该补的地方」（与 FB-18(b) 同形）。**本仓库尚未 pull 0.5.4**——M2 签核卡正在以 0.5.3 的 rubric 为判据来源运行，中途 pull 会在其脚下换掉 `workflow/`/`.claude/`，等于让一张卡跨两个框架版本执行；pull 排在 signoff-M2 落盘之后。框架侧确认无阻塞） |
| FB-20 | 2026-07-28 | **终态行携带的未兑现义务对 `signoff-check` 条件 3 完全不可见——终态即免检**（M2 签核卡的 rev 实例在执行 rubric #7 时发现并报告，本仓库判定它与 FB-18(b) 同形、只是出现在终态一侧）：0.5.0 起条件 3 是「terminal **or** ACCEPTED-unexpired」，`ACCEPTED@M<n>` 一侧现在有两道机器校验（行内含 `REV-`、里程碑未过期）加 rubric **#7** 的人工抽查；而 **`CLOSED`/`WONTFIX`/`TB_BUG`/`SPEC_CHANGED`四个终态一侧，除 `CLOSED` 要求 `verify_evidence` 引一份证据外，没有任何机器或人工条目**。问题在于**终态不等于无义务**。本仓库实例 **BUG-0030**（`WONTFIX`）：其 WONTFIX 的理由是「这是环境使用约束、不是可修缺陷」，但框架 0.5.2（`iverif-workflow@68a7e83`）已把其中的尾冒号半边认作**快照缺陷并修掉**，剩下「`LD_LIBRARY_PATH` 必须**恰为** `$VERDI_HOME/share/PLI/VCS/LINUX64`」这条判据是否属**过度归纳**尚未实测（当初的二分定位是逐项*加*变量做的，从没测过「带无关前缀但无尾冒号」这一形态；实测需一份 FSDB）。即：**一条终态行挂着一个明确的、有判据的、两个方向都有价值的未做实验，而里程碑签核对此一无所知**——它在条件 3 里与一条真正了结的 WONTFIX 完全同形。本仓库靠 `make guards` 的路径求交 + 详情页 `## regression_guard` 里手写的「触发点已到，待兑现」note 兜住了（FB-10 的 guard 注入机制在这里救了场），但那是**项目自觉**，不是机制——与 FB-18(b)「ACCEPTED 的 rationale 无人复核」是同一个缺口的两侧，也印证本仓库那句已入框架 CHANGELOG 的观察：**凡是需要靠自觉的地方，就是规则该补的地方**。建议（形态不预设）：① 承认「终态 + 未兑现义务」是一种合法且需要被看见的状态——或给终态行一个可选的 `## pending_obligation` 段（非空即进签核抽查清单），或让 rubric 增一条与 #7 同构的终态侧抽查；② 最轻量的替代：`docs.py --signoff` 把**本里程碑内状态发生过变化的终态行**列出来供人工过目——本条的 BUG-0030 正是在 M2 期内因上游修复而理由半失效的。**注意别把它做成新的 ACCEPTED**：终态侧若要求人人写 rationale 会淹没真正了结的条目，触发条件应当是「该行的终态理由在本里程碑内被外部事件动摇过」这类**原则性条件**，而非普遍义务（同 FB-19 否决体量阈值的理由：魔法数字与普遍义务都是留给裁量的口子） | scripts/docs.py（`--signoff` 条件 3）· workflow/signoff/rubric.md（人工抽查缺终态侧条目）· workflow/schema/failure_record.md（终态语义） | annoyance（不阻塞：本仓库已用 guard + 详情页 note 兜住，且该实验两个方向的结果都有价值、不影响任何 ✅） | open（2026-07-28 M2 签核当场由 rev 报出，orch 登记；实例：BUG-0030 的「恰为」判据二值实验待兑现） |
| FB-21 | 2026-07-28 | **0.6.0 新增的 `make chain-audit` 没有任何门禁或判据来源消费它——这是同一失效家族的第三次**（本仓库 pull 0.6.0 后逐项核对发现）：实测 grep `chain.audit` 与 `chain_audit` 两式于 `workflow/signoff/rubric.md`、`workflow/*.md`、`.claude/skills/*/SKILL.md` = **空集**；`make signoff-check` 的机器条件 1-3 与人工抽查 4-7 **均未提及**它；`make docs-check` 不调用它；`make next` 不提示它。它只是 `scripts/make/evidence.mk` 里一个孤立 target。**而 0.6.0 的 CHANGELOG 自己写明其触发器是「the first coverage-driven milestone signoff … fired today with pulp's signoff-M2」——即它是为签核而生的工具，签核却不调用它。**这与 FB-11 的判例 (a) 逐字同构：`make lint` 从 M0 起就是坏的（BUG-0014/0019/0021 三层），「能坏这么久是因为 lint **不在 signoff-check、不在 docs-check、不在任何门禁清单里**，只是根 Makefile 的一个 passthrough」——那条论证已被框架采纳（FB-11 fixed@0.5.2，gate 自证教义入 deferred）；0.6.0 随即以完全相同的接线方式发布了一个新 target。本仓库的三次同族：BUG-0014/0019/0021（lint 无人调用 ⇒ 坏了整个 M0/M1）、FB-18(b)（ACCEPTED 的 rationale 无人复核）、本条。**建议不要做成硬门禁**——`spec_ref` 采纳率 0/23、gap 类别大多是约定而非缺陷，硬失败会立刻制造豁免压力；正确的诉求是「签核时它必须**被看见**」而非「它必须绿」：① `rubric.md` 人工抽查加一条（与 #6/#7 同构，如「里程碑签核须粘贴一次 `make chain-audit` 输出，并对每类 gap 给出处置或书面接受理由」）；② 或让 `docs.py --signoff` 在签核时**顺带打印**该报告（同它已经打印人工抽查清单的做法），使签核人不可能没看见。**本仓库首次运行结果**（0.6.0 实测）：0 dangling / 1 sourceless(M0-01) / 0 orphans / 5 parent-anchored / 19 uncited / 11-of-11 evidence 缺 spec_ref header——其中 §5.2.6（REV-011 当天新增的条款）被正确标为「尚无场景引用」，正是该审计存在的意义（M3 缺口）。**工具本身有价值，问题只在没有消费点** | scripts/make/evidence.mk（`chain-audit` 孤立 target）· workflow/signoff/rubric.md（人工抽查未含）· scripts/docs.py（`--signoff` 未打印）· 对照 gate 自证教义（FB-11 fixed@0.5.2） | annoyance（今天不阻塞任何事——但这正是 lint 坏了两个里程碑而无人发现时的状态） | fixed@0.6.1（框架 commit `87e2eef`，按本仓库处方「被看见而非必须绿」落地 **②+①**：`docs.py --signoff` 现**内嵌打印 chain-audit 全文**，且 `rubric.md` 新增 **#8**「Chain audit answered」——签核记录须粘贴一次运行、逐 gap 类给出处置或书面接受，**悬空引用只许修、不许接受**；刻意不做硬门禁，本仓库「0/23 采纳率下硬红 = 豁免压力」的论证原文入框架 CHANGELOG。框架侧原样收下了「无消费者家族第三撞、且发生在采纳该教训当天」这一指控，并据此加 fuse 钉住 rubric #8 与 `--signoff` 输出的永久一致。**本仓库实测验收**（0.6.1）：`make signoff-check` 输出中人工抽查第 8 条存在，其后紧接 `== chain audit ==` 全文六行——**不是只改 rubric 没改工具**） |
| FB-22 | 2026-07-28 | **`chain-audit` 的 uncited 行静默截断，且截断方向系统性地偏向编号最大的章节**（本仓库首次运行即撞上：计数与列表对不上）：`scripts/docs.py:1023-1025` 打印「spec subsections cited by no scenario: N — §…」，列表部分是 **`uncited[:15]`**，**没有省略号、没有「+N more」、没有任何被截断的提示**。本仓库实测：**计数 19、列出 15、静默丢弃 4**。同一份报告的另外四行（dangling / sourceless / orphans / parent-anchored）都是全量打印 ⇒ **报告内部自相矛盾**，读者没有理由预期唯独这一行会截断。**更要紧的是截断方向**：排序用 `sorted()` 的**字符串序**，故被砍掉的恒为编号最大的一批——本仓库丢的是 **§7.4.3 / §7.4.4 / §8.3 / §8.4**，即延迟不敏感原则与 Connectivity 稀疏矩阵，恰好是 **M3（错误路径 + 多配置回归）的主题地盘**；而 §7.4.4 与 §8.4 正是 spec §5.2.6 第 2.b 条明文引作「上游确认项、不阻塞里程碑」处置先例的两节。即：**一个为发现里程碑缺口而生的审计工具，其静默截断恰好优先隐藏下一个里程碑的缺口。**本条违反框架自己的既有约定——0.4.3 为 FB-13/14 引入的正是「关键行分类聚合与**可见截断**」；亦属本仓库 BUG-0028「分母静默缩水」同族（`make regress` 报绿而分母缺 7 个场景）。建议：① 截断必须可见（如「…（+4 条未列出，共 19）」）；② 排序改**数值序**（`key=lambda s: [int(x) for x in s.split('.')]`），否则 §10.x 一出现就会排到 §2 之前，截断偏向更离谱；③ 或干脆全量打印——本行天然随 spec 增长，而它恰恰是最该被完整看到的一行。复现：本仓库 `make chain-audit`，对照 `len(uncited)=19` 与列出的 15 条 | scripts/docs.py:1023-1025（`uncited[:15]` + `sorted()` 字符串序） | annoyance（不阻塞：计数本身正确，hard-fail 只在 dangling；但报告在最需要完整的一行上骗了读者的眼睛） | fixed@0.6.1（三条建议**全部采纳**：数值序 + **全量打印**（该行天然随 spec 增长，恰恰最不该截）+ fuse 钉住 §2.1 排在 §10.1 之前。框架侧把本条最值钱的观察记为「审计工具的静默截断优先隐藏它本该发现的东西」——字符串序恒砍编号最大章节 = 下个里程碑的地盘，连 spec §5.2.6 引作先例的 §7.4.4/§8.4 都在被砍之列。**本仓库实测验收**（0.6.1）：同一条 `make chain-audit`，计数 19 **列出 19**，尾部 §7.4.3/§7.4.4/§8.3/§8.4 已出现且排序为数值序） |
| FB-23 | 2026-07-29 | **canon 路径重排在采纳者的「冻结记录」里留下永久死指针，而 0.7.0 升级须知没有覆盖这一类**（本仓库 pull 0.7.0 时逐处核对发现）：0.7.0 的 adopter 段点名要手工迁移的是 `CLAUDE.md` 三条路径、`scripts/iverif.divergence.json` re-key、`next_phrases_override`——**全部是活文件**；对 `doc/evidence/`、`doc/review/`、`doc/bugs/<ID>.md` 这类按框架自身规则**不得回改**的记录只字未提。本仓库实测规模：**16 份冻结文件、39 处旧路径引用**（`grep -rc 'workflow/{schema,dispatch,taxonomy,signoff}/' doc/{review,evidence,bugs}`），含三份里程碑签核书的「判据来源」抬头（`signoff-M0/M1/M2.md` 指向 `workflow/signoff/rubric.md`）——即**签核书声明自己依据的那份判据，其路径已不存在**。这里有一个框架内部的真张力：落盘公理要求记录即事实、而 evidence 不可回改要求别去动它 ⇒ 指针**必然**死，不是谁做错了。但正因如此它需要一条明写的约定，否则每个采纳者都要自己重新判一遍（本仓库为此花了一轮判断才决定「冻结记录保留旧路径」）。且这是**首次** canon 重排，今后每次重排都会复发、存量只增不减。建议：① 最低成本——CHANGELOG adopter 段明文声明「冻结记录保留旧路径是正确行为，不要回改」，把判断从每个采纳者手里收回框架；② 更彻底——加一份 `governance/path-map.md`（旧→新对照，只追加不删除），让冻结记录里的死指针可被反查，成本一次性而收益随时间累积，且正好落在**消费**公理上：这些指针的消费者是未来沿审计线索回溯的人，今天没有任何机制服务他；③ 若嫌文档易漂，可由 fwsync 从历次 manifest 差分机械生成该表。本仓库处置：活文件（`CLAUDE.md`、`doc/{testplan,bugs,fw-feedback}.md` 表头、三份 design-prompt）已迁移，冻结记录一律不动，判断依据记于 `doc/log.md` [0.3.4] 区块 | CHANGELOG.md 0.7.0 adopter 段 + governance/adoption.md（无 path-map 机制） | annoyance（无门禁拒绝：`docs-check`/`fw-check`/`chain-audit` 都不校验 workflow 路径引用，故它静默——与 FB-21「无消费者」同族，只是缺口在**时间**维度而非调用维度） | open |
| FB-24 | 2026-07-29 | **`§id` 解析器把「章节内的有序列表项」和「跨文档引用」都当成 spec 章节——0.7.1 的 `explore` 是第一个把这个数字变成派工指令的消费者，于是不精确变成了错工单**（本仓库 pull 0.7.1 当场压测发现，**在派 arch spec-gap 卡之前**）：`make explore` 列出 9 条「uncited spec subsections」交 arch 逐字建行，其中 **4 条根本不是 spec 章节**——`§1.3` 来自 `doc/spec.md:436` 的 **`REV-011 §1.3`**（评审记录的章节号，被吸进 spec 命名空间）；`§7.1.2`/`§7.4.3`/`§7.4.4` 是 **`§7.1`/`§7.4` 正文有序列表的第 2/3/4 条**（`spec.md:328-345`、`376-400` 实读确认，§7.4 body 是 1.–5. 的列表，无任何子标题）。**幻影率 4/9 = 44%**。全谱统计（脚本实算）：spec 有 **25 个真标题**、正文出现 **45 个 §id**，其中 **22 个无对应标题**——`§5.4.1` 被引 9 次、`§5.2.1`/`§5.2.3` 各 7 次。即**本仓库 spec 的主导引用惯例就是「§<标题>.<列表项>」**，这不是笔误而是文体。**为何 0.7.1 才致命**：0.6.0 引入 inline-token 解析本是为了让 testplan 的 `SPEC-5.2.1` 能解析（CHANGELOG 自述「literal-token match 会 100% 假阳」），代价是 uncited 集合混入列表项；在 chain-audit 阶段它只是个没人行动的计数（**FB-21 家族：无消费者**），而 0.7.1 的 spec-gap 卡契约要求 explore 列表**逐字进卡**且「must not include orch's scenario ideas」⇒ orch 被禁止过滤，arch 被指令为不存在的章节建 testplan 行。**比幻影更糟的一类**：`§7.4.3` 是真实条款但内容是**禁令**——「任何 latency checker **不得**断言固定周期数」；禁令在语义上**永远无法被场景覆盖**（它由 checker 的**缺席**满足），arch 只能逐条写 decline，而 decline 按卡契约是 narrowing、须 rev 门禁 ⇒ **解析器的不精确机械地制造 rev 工作量**。**同一根因还解释了 parent-anchored=15**：`§4` 只有 `## 4.` 一个标题、条款是列表项 4.1–4.5，testplan 引 SPEC-4.1~4.5，其中恰好在正文被 inline 提及的（§4.1/§4.5）解析成功、未被提及的（§4.2/4.3/4.4）跌回父级 §4。⇒ **arch 在 0.3.3 提的那条「§4/§5.3 补自引用以降 parent-anchored」提案，实质是往 spec 正文里写字去迁就解析器**——rev 在裁决该提案前必须知道这一点（本仓库已在 log [0.3.5] 记明，该提案建议**否决**：spec 文体不该为工具让步）。建议：① 区分两类 id——有标题的进 uncited 前沿，只在正文出现的单列为「clause-level refs（解析用，不进前沿）」，`explore` 已有现成信号：**幻影的 title 恒为空**，用它过滤即可，改动量约一行；② 剥离跨文档引用——`REV-\d+ §x.y` / `BUG-\d+ §x.y` 形态不得计入 spec 命名空间；③ 前沿列表增设「本节是禁令/约束型条款」的 arch 快速 decline 通道，避免禁令类逐条走 rev。**本仓库处置：0.7.1 已拉，但 arch spec-gap 卡在本条闭环前不派**（派了就是让 arch 按 44% 错的清单干活） | scripts/docs.py `chain_gaps()` 的 §id 解析（headings ∪ inline tokens）+ `--explore` 前沿呈现 + .claude/skills/dispatch/SKILL.md「arch spec-gap sweep」卡契约的逐字要求 | annoyance（不阻塞：门禁全绿、chain-audit dangling 仍 0；但它精确地污染 0.7.1 唯一的新派工入口，且 orch 被卡契约禁止过滤） | open |
| — | 2026-07-29 | **对 FB-22 的自我订正**（非新条目，附此存证）：FB-22 举证「静默截断丢掉的是 §7.4.3/§7.4.4/§8.3/§8.4，恰是 M3 主题地盘」——按 FB-24 的全谱统计，**这四个 id 全部是幻影**（§7.4.3/§7.4.4 为列表项，§8.3/§8.4 亦无对应标题）。FB-22 的**核心主张不受影响且已正确修复**（字符串序 + 无提示截断确实存在，数值序 + 全量打印是对的）；受影响的只是「被隐藏的是什么」这半段举证——当时被砍掉的四条里有几条本就不该在那张表上。按独立公理与落盘公理，此处不改 FB-22 原文（冻结举证同 FB-23 裁决），只在此并列存证 | —（对本仓库自身举证的订正，非框架缺陷） | — | recorded |
| FB-25 | 2026-07-29 | **`CLAUDE.md` 由框架渲染一次后永久失养：框架每次改自己的路径，它就静默腐烂，而没有任何门禁读得到**（0.7.0 迁移时发现，0.7.1 复核确认机制未变）：`scripts/fwsync.py:432` 只在 `--init` 写一次 `CLAUDE.md`，**此后 `--pull` 永不再碰它**（0.7.0 CHANGELOG 亦明说 "CLAUDE.md does not [regenerate] — hand-migrate its three framework-owned paths"）；而 `scripts/iverif_config.py:175` 虽把它列进 `required_files`，那只是**存在性检查**——查文件在不在，**不读其中一个字**。⇒ 本仓库 0.7.0 那三条路径（`workflow/schema/testplan_entry.md`、`workflow/dispatch/*.md`、`workflow/taxonomy/failure_taxonomy.md`）若当时漏迁，`fw-check`/`docs-check`/`chain-audit` **三绿如常**，而每次会话最先被读的那份文件会指向三个不存在的路径，且**指的正是「登记是无条件的」这条核心纪律的正文所在**（本仓库 §2 那段）。**这是 FB-11「门禁不保证验证工具本身有效」/ FB-21「机制无消费者」的又一次**：框架亲手渲染了这份文件、亲手改名了它引用的路径、亲手在 CHANGELOG 里叮嘱采纳者手工迁移——唯独没给一个观察者去看采纳者到底迁没迁。与 FB-23 不可合并：那条讲**冻结记录**（不该迁、缺的是约定），本条讲**活文件**（该迁、缺的是校验）。建议：① 最便宜——`fwsync --check` 顺带扫 `CLAUDE.md` 里形如 `workflow/…` / `scripts/…` / `.claude/…` 的路径，凡不在 manifest 内即报（manifest 本就是 fwsync 手里的现成数据，约十行，且天然随每次重排自动生效）；② `--pull` 时对同一扫描给出 warning 而非 fail（迁移窗口内允许暂时不一致）；③ 若嫌 ① 越界，退而在 `--pull` 结束语里打印「本次重命名的路径清单」，让采纳者至少有一份可 grep 的对照 | scripts/fwsync.py:432（init-only 渲染）+ scripts/iverif_config.py:175（required_files 仅查存在性） | annoyance（无门禁拒绝，故静默；但腐烂的是每次会话第一份被读的文件） | open |
| FB-26 | 2026-07-29 | **0.7.1 新发的「每卡分级 vs 实际」观察者，结构上看不见 orch-only 的 chunk——而那恰是本仓库时间真正花掉的地方**（本仓库连续两次 pull 后自检发现）：0.7.1 的裁决本身是对的且很锋利——「无人抱怨流程重 ≠ 流程轻，因为每个子代理只看见自己那张卡、链条重量只有 orch 看得见、而 orch 不疼」。它给出的观察者是 dispatch 手册里的**每卡**失配行。**但框架 pull 不是卡**：它没有分级、没有交付报告格式、没有失配行，是纯 orch chunk ⇒ 新观察者对它零可见度。本仓库实测：M3 窗口（`951bc66..HEAD`，0.3.0 收官转入 M3 起）共 **5 个 commit，其中 4 个是 `chore(fw)`**；全仓 46 个 commit 中 **12 个是框架 pull（26%）**。**两条必须同时说清的限定，否则这个数字会被误读**：(1) 今天两次 pull 是因为**框架今天发了两版**，属共同开发期非稳态，把 80% 当稳态抱怨是错的；(2) 这些 pull **买到了东西**——FB-24 正是 0.7.1 压测压出来的。故本条**不是「框架太重」的抱怨**，而是：**刚发布的那个观察者存在一类结构性盲区，框架除非采纳者主动报否则学不到**，这与它自己的**消费**公理（「a trigger nobody observes never fires」）撞在同一处。建议：① 把 pull/升级也纳入可记录单元——`chore(fw)` chunk 在 closeout 时记一行「本次 pull 的 orch 开销档位 + 是否与收益相称」，成本约一行；② 或在 `governance/` 侧统计采纳者 `chore(fw)` 与功能 commit 的比值（数据在每个采纳者的 git log 里，框架侧可机械取）；③ 至少在 0.7.1 的裁决记录里补一句「本观察者仅覆盖卡，orch-only chunk 的开销仍无观察者」，免得下次有人误以为盲区已闭合。**本条自身即证据**：登记它又是一次纯框架维护的开销，故本仓库选择**不为它单独开 chunk**，随下一张卡的 closeout 一并落地 | .claude/skills/dispatch/SKILL.md 末段（每卡失配观察者）+ 0.7.1 裁决记录（governance/） | annoyance（不阻塞任何门禁；缺口在框架的**自我度量**能力，不在采纳者的执行路径） | open |
| FB-25 订正 | 2026-07-29 | **FB-25 被 0.8.0 部分闭环，此处订正**（不改 FB-25 原文，同 FB-23 裁决）：FB-25 断言「没有任何门禁读 `CLAUDE.md` 的内容」。0.8.0 起**不再完全成立**——`scripts/tests/test_docs.py:477` 的 `test_claude_md_carries_risk_grades` 会读项目 `CLAUDE.md` 并断言 L0–L3 分级表在场（0.8.0 把该表从 `skills/dispatch` 移入 CLAUDE.md，同时加了这道保险）。本仓库迁移时**实测撞到**：移植完机械层后 `make selftest` 60 例挂 1 例，正是这条。⇒ FB-25 现只在「**路径有效性**」那半边成立：仍然没有任何检查会发现 CLAUDE.md 指向了不存在的 `workflow/` 路径。修法建议不变（用 manifest 反查），但成本论据变了——0.8.0 已无 manifest，替代方案是扫 `workflow/*.md` 与 `scripts/*` 的实际文件名 | scripts/tests/test_docs.py:477（部分闭环）+ CLAUDE.md 路径有效性（仍缺） | annoyance | partially-fixed@0.8.0 |
| FB-27 | 2026-07-29 | **0.8.0 删掉了四个 make 动词，但它自己发布的两张角色卡仍在指令角色去执行其中两个**（本仓库迁移 0.8.0 时逐文件核对发现）：0.8.0 的 Makefile 实有目标为 `handoff next run evidence regress check guards bump commit archive selftest`——`explore` / `signoff-check` / `chain-audit` / `replay` 均已retire（前三者折进 `check`/`next`，`replay` 因不变量 2 而多余）。但随同发布的 **`.claude/agents/arch.md:34`** 仍写「turn the card's **`make explore`** list into proposed testplan rows」，**`.claude/agents/rev.md:51`** 仍写「verify the machine conditions (**`make signoff-check`**)」。**命中面精确地落在最坏位置**：这两条不是背景说明，而是各自角色**主任务的操作指令**——arch 的 spec-gap sweep 与 rev 的里程碑签核；两个角色照做都会得到 `No rule to make target`。与 FB-25 同族（发布文档引用已退休机制、无门禁读它），但更尖锐：**退休这些动词的正是同一个 release**，且 0.8.0 的核心论点恰是「把表面缩到一个人脑子装得下」——表面缩了，两张卡没跟上。建议：① 在 `scripts/tests/` 加一条保险，把 `.claude/agents/*.md` 与 `CLAUDE.md` 里形如 `make <verb>` 的 token 与 Makefile 的实有目标求差，非空即红（与 test_claude_md_carries_risk_grades 同一思路，约十行，且天然覆盖今后每一次动词增删）；② 该保险同时能闭合 FB-25 的剩余半边（路径有效性可用同一模式扫 `workflow/*.md` 实有文件名）。**本仓库处置**：两处已就地修（arch.md → `make next` 的探索前沿；rev.md → `make check MILESTONE=<n>`），代码旁均注明 `见 doc/fw-feedback.md FB-27` | .claude/agents/arch.md:34 · .claude/agents/rev.md:51 · scripts/tests/（缺 make-verb 保险） | annoyance（不阻塞门禁，但角色照指令执行会直接报 No rule to make target——发生在 arch/rev 各自的主任务上） | open |
| FB-28 | 2026-07-29 | **`.claude/agents/orch.md` 把 87 行主会话硬规则投递给了除主会话外的所有人——本仓库已删除该文件并把内容并入 `/dispatch` skill + `CLAUDE.md` §0** | **机制事实（本会话直接观测，非推断）**：`.claude/agents/*.md` 在 Claude Code 里只有一个作用——注册成**可派发的子代理类型**；文件正文仅在该类型被 `Agent` 工具派发时注入**新实例**的系统提示。0.8.0 新增 `orch.md` 后，主会话收到的系统提示只有一行 frontmatter `description`（"Orchestrator (orch) — the main session…"），**87 行正文一个字都没到达**——直到 orch 显式 `Read` 它才看见。⇒ 一份通篇写着 "You are orch — the main session" 的文件，唯一读不到它的就是主会话。**为什么它也不该被派发**：派一个 orch 子代理会同时违反它自己两条规则——(1) §"Handoffs are records, not conversation" 禁止「只传上下文」的旁路，而子代理**唯一**的回传形式就是最终那段文字，即口头摘要；(2) §"Closer ≠ fixer is your call" 要求 orch 自己追踪派发路由，嵌套实例的路由决策对持有台账的主会话不可见。再加上它写不了 `status.jsonl`/`log.md`（closeout 是主会话职责）。**唯一站得住的用法是反过来**：把它当独立的**组卡审查员**（我组好卡、派一个实例只审隔离与共模防火墙），那是审计不是编排。**放大风险**：0.8.0 同时废弃了 `.claude/skills/`（orch 的派卡手册原本住那儿、主会话会读）**又**把规则搬进 agent 卡（主会话读不到）⇒ 对严格照上游默认走的采纳者，orch 的隔离硬规则**没有任何人读得到**：主会话拿不到正文，而 orch 实例永远不会被派出来。本仓库侥幸没事，只因保留了四个 skill 作本地资产。这是 FB-21/25/27「机制无消费者」家族里目前最尖锐的一例——前几例是文档指向退休命令，本例是 87 行硬规则投错了收件人。**本仓库改动（供上游取用）**：① 删除 `.claude/agents/orch.md`；② 其中三块 `/dispatch` 原本没有的操作性内容并入该 skill——**closer ≠ fixer 的路由判断**（skill 此前完全没提）、**rev 卡只给范围不给结论**、**§3b「旁路即漂移」原理与补救**（两角色需要同一背景时指向同一份归档记录，而非互相转述；一方挖出的上下文另一方要重挖是**留存失败**而非效率损失）；③ `CLAUDE.md` §0 的指针从 orch.md 改指 `/dispatch`。**分层理由**：CLAUDE.md 是「常驻但字节受限」层（预算仅剩数百字节），skill 是「用时载入、不受预算约束」层，而 `/dispatch` 恰在每次派卡前被调用 ⇒ 消费时机比 CLAUDE.md 更准 | .claude/agents/orch.md（已删）→ .claude/skills/dispatch/SKILL.md §3/§3b + CLAUDE.md §0 | annoyance（不阻塞门禁；但受影响的是 orch 全部隔离硬规则的可达性） | local |
| FB-29 | 2026-07-30 | **`check_kill_coverage()` 只扫 live `bugs.md`、不扫 `bugs_archive.md`，导致已归档的 KILL 自证行不计入里程碑 kill coverage**（本仓库 M3 实测发现，当场压测）：`scripts/docs.py:928-948` 的 `check_kill_coverage(mnum)` 仅调用 `parse_table(CFG.bugs)`（第 939 行），漏掉 `parse_table(CFG.bugs_archive)`。本仓库真实现状：KILL-0001 与 KILL-0002 两条 M3 标记的 KILL 行早已在 `doc/archive/bugs-archive.md`（`make archive` 的自动搬迁结果），但 `make check MILESTONE=3` 条件 4（kill coverage）仍报 `[FAIL]` → "none — no checker proven able to fail this milestone"——尽管 `doc/archive/bugs-archive.md` 第 38-39 行明确存在、summary 明确含 M3 token。**这是纯扫描范围遗漏**，不是缺失的 kill 自证——同文件内其他函数（470-471 行、517-518 行、524 行、806-809 行）在处理"终态行会被归档"的场景时，都同时读了两张表或两份文本（`bug_rows + abug_rows` 模式），唯独 `check_kill_coverage()` 漏掉。**测试未捕获**因为既有夹具 `TestSignoffGate._green_with_regress_summary()`（第 131-141 行）只在 live bugs.md 里造 KILL 行，没有模拟"KILL 已被归档"这个场景。**本仓库改动**：① `check_kill_coverage()` 改为同时扫两张表（跟第 470-471 行既有模式）；② `test_docs.py` 补一条回归测试：创建空 live bugs.md + 归档表里有 KILL 行，断言 `--check --milestone 1` 条件 4 仍 PASS；③ 代码旁注明 `见 doc/fw-feedback.md FB-29` | scripts/docs.py:928-948（`check_kill_coverage()` 扫描范围） + scripts/tests/test_docs.py（回归测试） | blocking（`make check MILESTONE=3` 条件 4 红、里程碑无法签核） | local |
| FB-30 | 2026-07-30 | **`scripts/make/vcs-2018.mk` 的覆盖率变量 `CM` 用 `:=`（立即展开）绑定 `-cm_dir $(OUT)/cov.vdb`，早于 `include` 它的 `sim/Makefile` 里任何 per-config `override OUT`/per-TEST 分支执行——`$(OUT)` 被冻结成 include 时刻的默认值，后续覆盖改不动已展开的字符串**（BUG-0037 DV 自修卡实测发现并修复，见 `doc/bugs/BUG-0037.md`）：后果是 `make regress COV=1` 对全部 TEST（含拓扑迥异的 `upstream_sanity` 与四个 config 点 cfgA-D）一律把覆盖率写进同一个 `out/cov.vdb`，`make cov` 的 `urg` merge 报 825 行 `UCAPI-INSTANCEMISMATCH` + 3139 行 `CMR-VCINF`（VCS 对结构不匹配 instance 采取丢弃而非静默错误合并，安全网未失守，但"一次 `make regress COV=1` 即得干净基线数字"的假设被污染）。**本仓库改动**（两处，均已加内联注释指回本行）：① `scripts/make/vcs-2018.mk` 新增 `COV_DIR = $(OUT)/cov.vdb` 间接层并把 `CM` 自身也改 `=`（递归展开），使 `-cm_dir` 在 `compile:`/`run:` **recipe 执行时**才求值，此时 `sim/Makefile` 的 per-config `override OUT` 早已生效——cfgA-D 零改动即自动获得正确隔离；② `sim/Makefile` 给 `TEST=upstream_sanity` 分支单加一行 `COV_DIR := $(OUT)/m0/cov.vdb`，只挪覆盖率库路径、不动 `OUT` 本身（避免波及 `make clean` 默认作用域与 M0 构建产物路径）。修复后 `make regress COV=1` 一次运行自动产出 5 个隔离的 `cov.vdb`（baseline/m0/cfgA-D），逐一 `make cov TEST=<domain>` 核对 0 处 mismatch/CMR-VCINF；`make regress COV=1` 功能判定前后 22/22 PASS 不变（本条只影响覆盖率数据库，不影响任何场景的 PASS/FAIL）。**建议上游取用**：`CM`/`-cm_dir` 一类"依赖 including makefile 稍后才定的变量"的 pinned 片段，一律用 `=` 而非 `:=`——这是本仓库第二次撞上同型 GNU Make 陷阱（第一次是 BUG-0014/0019 的 flag 拼接顺序，但那两条是缺 flag 而非展开时机），值得在 `vcs-2018.mk` 自己的 workaround 清单里留一条通用提示，防止未来新增 per-`OUT`/per-`TEST` 覆盖率或波形路径时重踩 | scripts/make/vcs-2018.mk（`CM`/`COV_DIR` 绑定方式） + sim/Makefile（M0 分支新增 `COV_DIR` 覆盖 + `compile:`/`cov:` 目标随 `COV_DIR` 改写） | blocking（污染 M4 覆盖率基线报告的可信度——REV-016 条件 2 待重出的报告依赖干净的默认路径数字，本条修复前只能靠手工 `CM=`/`OUT=` 绕过） | local |
