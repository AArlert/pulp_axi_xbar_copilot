# Bugs archive

| id | status | suspect | summary | min_repro | root_cause | fix_commit | verify_evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-0011 | SPEC_CHANGED | SPEC | spec 缺口（arch M2 蒸馏新发现，中）：`MaxSlvTrans`（每 master 端口每 ID 在飞上限）在许可来源中无任何机制级依据——`axi_mux.md` 全篇未提供与 `axi_demux.md` 那类"按 ID 桶计数、封顶即拒收"对应的段落，唯一相关参数 `MaxWTrans`（AW→W 选择 FIFO 深度）语义不同（FIFO 深度，而非按 ID 分桶的事务计数上限，作用对象也是"整个 mux 的未完成 AW 队列"而非"单一 ID")；spec.md §5.4.2/§5.4.3 现表述"达到上限对应端口不再接受新事务"缺乏 mux 侧机制级文档支撑。影响 M2-TL02（feature-matrix F-M2-04）：其 checker 的精确触发条件/分组维度（是否真按前缀后 ID 分桶、是否存在硬性反压）均无法从许可来源推导，checker 实现前需先决（scenario 行本身可先注册，见 doc/testplan.md M2-TL02）；详见 doc/bugs/BUG-0011.md | -（spec 歧义，非仿真失败） | **REV-005 裁决 = SPEC_CHANGED**（rev 独立核验 xbar.md L47 含"per ID" / axi_pkg L492-494 省略 / mux.md 全文无按 ID 计数机制）：三段式——(1) **保留 §5.4.2 可观测上界为规范**（每 master 端口×每前缀后 ID 在飞 ≤ MaxSlvTrans，"per ID"采 xbar.md 口径，同 BUG-0005 精神）；(2) §5.4.3 的 MaxSlvTrans **执行机制列上游确认项**（mux.md 无来源，同 BUG-0004/0002 先例，不阻塞）；(3) **解锁 M2-TL02 弱化可观测上界 checker**（每端口×每可观测 ID×每方向 ≤ MaxSlvTrans，保守不假红），机制级断言不派发。spec 编辑+重 pin+状态转 SPEC_CHANGED 已由 orch 应用（doc/spec.md §5.4.2/§5.4.3，§2.1 `MaxSlvTrans` 字段行未改动——REV-005 该条裁决未授权此行；Change record #3，sha `946ec000`）。详见 REV-005 §3 / BUG-0011.md ## fix | - | - |
| BUG-0012 | SPEC_CHANGED | SPEC | spec 缺口（arch M2 蒸馏新发现，低，非阻塞）：`axi_demux.md` §Atomic Transactions→Implementation 述"原子读事务的 ID 被注入到 AR 方向的计数器"以避免下溢，并明确该机制"可能在读通道上引发额外的假冲突 stall"——即一笔原子读可能使**同一 slave 端口**上另一笔低位 ID 相同、目标不同 master 端口的**普通读**被 §5.2.1 保序 stall 阻塞（一次由写方向事件引发的读方向 stall，不落在现 §5.2.1"仅同方向配对"的字面框架内）。spec.md §5.2/§6 均未收录此交互；M2-AT01（feature-matrix F-M2-06/07）场景若撞见此现象，在未登记该条款前可能被误判为 DUT_BUG。本条不阻塞 M2-AT01 的核心 B/R 成对判据（SPEC-6.3），仅提请补录该观察；functional_coverage.md 已登记一条非判决性 coverage bin 留痕，等待裁决前不得作为任何 checker 的判决依据；详见 doc/bugs/BUG-0012.md | -（spec 歧义，非仿真失败） | **REV-005 裁决 = SPEC_CHANGED**（rev 独立核验 demux.md L83-87 原文：AW 注入原子读 ID 到 AR 计数器、明述"可致读通道额外假冲突 stall"、为正常行为）：来源完整可溯，**蒸馏为派生条款**——落 **§6（ATOP 副作用）+ §5.2 交叉引用**（超出 §5.2.1"仅同方向"框架的写→读跨方向 stall）。非阻塞：M2-AT01 核心 B∧R/ID 唯一判据照常落地；仲裁前过渡处置（`cg_atop_read_interaction` 非判决 bin + sva_bind C3.2 显式划界）经 rev 核验充分，**准予放行 M2-AT01**。spec 编辑+重 pin+状态转 SPEC_CHANGED 已由 orch 应用（doc/spec.md §5.2.5/§6.5，Change record #3，sha `946ec000`）。详见 REV-005 §3 / BUG-0012.md ## fix | - | - |
| BUG-0013 | SPEC_CHANGED | SPEC | spec 缺口（DV M2-OR01/OR02 仿真新发现，中，非阻塞）：spec.md §5.2.1"该 slave 端口的 AW/AR 通道被 stall"这一"接受边界即时可观测"字面表述，与基线 `LatencyMode=CUT_ALL_AX`（§7.2，AW/AR 各 2 拍）落地的 spill register 弹性缓冲交互未被蒸馏——`axi_demux.sv` 把持有按 ID 分桶计数器/目标绑定寄存器判决逻辑的 `axi_demux_simple` 包在 `SpillAw`/`SpillAr` spill register 之内，第二笔同 bucket 不同目标请求可在核心判决逻辑真正比对第一笔之前，先被这层缓冲"提前接受"（`make run TEST=m2_or01_stall_test SEED=1` 写/读方向均确定性复现：第二笔 AW/AR 在第一笔 B/rlast 完成前即被接受）。**关键旁证**：完成序（B/rlast 到达顺序）与接受序一致、scoreboard M1 式路由/数据判据零 mismatch——spec §5.2.3 所述保序**功能目的**被满足，只是 §5.2.1 的"边界即时性"字面读法与该基线配置有出入。详见 doc/bugs/BUG-0013.md | `TEST=m2_or01_stall_test SEED=1` | **REV-006 裁决 = SPEC_CHANGED（采候选 (a)）**（rev 亲验 xbar.md L84-86 "accept"=forward 进交换网络/L86 保序为功能目的 + demux.md L31 "spill register before the demultiplexer"/L37 不损吞吐 + `axi_demux.sv` L89-209 结构：spill 夹在外部 slave 端口与 `axi_demux_simple` 之间、`slv_resp_o.aw_ready`=spill 自身空位非核心判决 ready）：**收窄 §5.2.1**"接受边界"字面为"逻辑上不早于完成、判决锚 §5.2.3 完成序"，**§7.4 增第 5 条**把接受/拒收边界即时性通用归入延迟不敏感表现，**§5.4.3 挂交叉指针**（三处精确编辑范围见 REV-006 §3）。**驳回候选 (b)**（会在功能正确 DUT 上假红 + 窗口深度约束无许可来源=behavior leak）。DV 过渡处置（完成序判决门 + 非判决"接受边界"cover）经核验已提前对齐裁决 (a)，"接受边界"cover 维持为 cover、不升格 assert。延伸：C5.3 MaxMstTrans 侧判同类（M2-TL01 锚点原则已由 §7.4.5 条款化）、MaxSlvTrans 侧证据不足不下结论、C1.5 判不同类（select 边界锁存无危害）。spec 编辑+重 pin+状态转 SPEC_CHANGED 已由 orch 应用（doc/spec.md §5.2.1/§7.4.5/§5.4.3，Change record #4，sha `107cc1a5`）。详见 REV-006 / BUG-0013.md ## fix | - | - |
| BUG-0009 | CLOSED | TB | mstport_monitor 写 burst 收集缺陷（M1-02 落地时发现）：单槽"aw_valid 时抓取 AW 元数据 + 收 W 直到 w_last"方案在 master 端口处 AW 与 W 解耦/多写 burst 汇聚时错配——第二个 AW 在首个 W burst 未完成前被接受即覆盖 w_id/w_len，w_last 事件被错误归因，导致 mst 侧写观测的 beat 数/元数据错乱、甚至早于对应 slv 侧观测发布（因果倒置）。表现为 scoreboard 请求侧配对 SB_NOPEND/SB_WDATA_LEN/SB_DANGLING 假失败；DUT 功能正确（96/96 响应全部路由正确，resp-route C3.2 零失配）；详见 doc/bugs/BUG-0009.md | `make run TEST=m1_02_id_prefix_test SEED=1`（修复前 4 route 失配 + 7 dangling） | TB_BUG（TB monitor 缺陷，非 DUT）：mstport_monitor 未按 AXI 写 burst 语义配对 AW→W。修复=以 AW FIFO 队列配对（mux 处 W burst 非交织且按 AW 接受序，spec §5.5.2），mirror mstport_responder.w_collect_loop。M1-01 用独立随机 W/R id 且写 burst 汇聚时序较松故未暴露；M1-02 同低位 id + 并发轮次在共享 mst 端口紧密汇聚才触发。同轮修复 scoreboard pending_by_id 键加方向位（读写独立通道，同 id 读写不再别名）。**未改 vendor/RTL** | 5dc53f9 | doc/evidence/v0.1.2/M1-02.log |
| BUG-0008 | WONTFIX | TB | 证据保真缺口（signoff-M0 抽检 R1 发现，事后补登）：`doc/evidence/v0.0.1/{M0-01,BUG-0001,BUG-0006}.log` 三条已登记证据的 `## Key check lines` 段为空——框架 ≤0.2.0 的 `evidence.py` 对非 UVM tb（ucli `run;exit` 驱动 `$stop`）摘要窗口过窄且关键行正则不匹配，判决本身稳健但摘要未反映记分板结论；详见 doc/bugs/BUG-0008.md | -（工具产物保真缺口，非仿真失败；源日志 `sim/out/upstream_sanity_1.log` 可复核） | TOOL_ENV：框架 `evidence.py` 窗口 2 行 + UVM 形状正则双重窄化，已由 iverif-workflow 0.2.1 修复（窗口 20 行 + `tests failed\|ended\|mismatch` + 保险丝测试），本仓库已 pull 至 0.3.0。存量三条**不追溯重写**（signoff-M0 R1 已具名登记该缺口且判决建立在独立复跑源日志之上，重生成反致 R1 文本失真）——REV-004 独立仲裁：赞同不重写，且终态须由 OPEN 转 WONTFIX（不可停在 OPEN 表达"决定不做"） | - | - |
| BUG-0010 | SPEC_CHANGED | SPEC | spec 缺口（arch M2 蒸馏新发现，中）：`MaxMstTrans` 在飞上限的粒度在 spec.md §2.1/§5.4 现表述为"每 slave 端口一个扁平上限"，但 `axi_demux.md` §Ordering and Stalls→Implementation 明确述其机制为"`2×2^AxiLookBits`（=`AxiIdUsedSlvPorts`）个计数器：每个（低位 ID 桶）×（方向）各一个独立计数器，每个计数器独立封顶 `MaxTrans`（=`MaxMstTrans`）"——与现表述不一致：若流量摊满全部 ID 桶与两个方向，单 slave 端口合计在飞数可达 `MaxMstTrans×2^AxiIdUsedSlvPorts×2` 而非扁平 `MaxMstTrans`。影响 M2/M3 事务数上限 checker 的期望值粒度；M2-TL01（feature-matrix F-M2-04）已刻意收窄到单一 ID 桶×方向×目标 master 端口构造、两种读法在该简并情形下重合而不受阻塞，但任何跨桶/汇总上限的后续场景（M3/M4）落地前需先决；详见 doc/bugs/BUG-0010.md | -（spec 歧义，非仿真失败） | **REV-005 裁决 = SPEC_CHANGED**（rev 独立核验 xbar.md L46 / axi_pkg L489-491 扁平 vs demux.md L70-74 分桶机制原文）：**采分桶口径**——按（约简 ID 桶 × 方向）独立计数、各封顶 MaxMstTrans、各带目标端口绑定寄存器；补 §2.1/§5.4.1。原则=取机制精确且不假红者（扁平口径会令 M3/M4 跨桶场景假红），来源=demux.md（可信下层文档）+axi_pkg L510"see axi_demux"指针+§1/§5.2，非 spec-from-RTL。M2-TL01 收窄三元组不受阻塞。spec 编辑+重 pin+状态转 SPEC_CHANGED 已由 orch 应用（doc/spec.md §2.1/§5.4.1，Change record #3，sha `946ec000`）。详见 REV-005 §3 / BUG-0010.md ## fix | - | - |
| BUG-0005 | SPEC_CHANGED | SPEC | spec 缺口（arch 蒸馏 P-d，低）：`NoAddrRules` 最小值口径不一致（xbar.md "地址表至少一条" vs axi_pkg 注释 "每 master 端口至少一条"），影响不可达 mst 端口配置合法性判定 | -（spec 歧义，非仿真失败） | SPEC_ISSUE（源间冲突）确认（REV-001 裁决）：采信主文档 xbar.md 口径——全表 ≥1 条即可，无"每 master 端口 ≥1"硬性要求（axi_pkg 的 should 为软性建议）；无 rule 指向的 master 端口为合法配置。v0 已统一 §2.1/§3.1 措辞 | - | - |
| BUG-0007 | CLOSED | TB | M1-01 SVA 挂接机制偏离：`doc/design-prompt/sva_bind.md` C1.1（rev-gated）原定经 `bind` 挂接（CLAUDE.md §6），VCS-2018.09-SP2 对 `bind <slvport_if\|mstport_if> axi_chan_sva (...)` 报 `Error-[IIM] ... Interface has a module instantiation which is not allowed`，改为 `tb_top.sv` generate 循环内直接例化（`tb/sva_bind.sv`、`tb/sva/axi_chan_sva.sv`） | `make run TEST=m1_01_smoke_test SEED=1` | TOOL_ENV（REV-003 独立复现）：VCS-2018.09-SP2 拒绝 module 直接 `bind` 进 interface 作用域，用与 `scripts/make/vcs-2018.mk` 同源编译旗标的独立最小探针（interface+module+bind）复现同一 `Error-[IIM]` 签名（非本项目树内 revert 复现）；结构核验确认直接例化与 `bind` 对同一 module body 是等价连接语法，断言内容（C2.1-C2.5）与观测信号范围（仅 `slvport_if`/`mstport_if` 外部可见通道信号，无 DUT-internal 穿透）未变 | 64d0780 | doc/evidence/v0.1.1/BUG-0007.log |
| BUG-0004 | SPEC_CHANGED | SPEC | spec 缺口（arch 蒸馏 P-c）：`PipelineStages` 外部延迟语义仅 axi_pkg.sv 注释可溯源；基线配置即 PipelineStages=1，M1 smoke 时序预期需此条款落地 | -（spec 歧义，非仿真失败） | SPEC_ISSUE 确认（REV-001 裁决）：采延迟不敏感原则。v0 已补 §2.1/§7.4：PipelineStages 为延迟不敏感插桩（改延迟不改功能/吞吐），精确周期数未定义、latency checker 不得断言固定拍数；M1 功能 checker 须延迟不敏感，基线 PipelineStages=1 不阻塞；cycle-accurate 需求另行上游确认（§7.4.4） | - | - |
| BUG-0001 | CLOSED | TB | VCS-2018 编译上游 tb_axi_xbar 报 2×Error-[NCE]：generate 环内 `$sformatf(...,i)` 作 axi_chan_logger 参数 LoggerName 覆盖被判非常量（vendor/axi/test/tb_axi_xbar.sv:301/337），M0-01 smoke 无法编译；详情 doc/bugs/BUG-0001.md | `cd sim && make smoke TEST=upstream_sanity SEED=1`（compile 阶段即失败，与 SEED 无关） | TOOL_ENV：VCS-MX O-2018 不支持含 genvar 的 $sformatf 作参数覆盖常量（工具能力缺口，非 DUT 行为） | 1a15627 | doc/evidence/v0.0.1/BUG-0001.log |
| BUG-0006 | CLOSED | TB | VCS-2018 编译报 6×Error-[NCE]：`(NoAddrRules - 1)` 作端口位宽（xbar_cfg_t struct 成员参数选择）无法常量折叠，vendor/axi/src/axi_xbar.sv:84（module axi_xbar）与 :190（module axi_xbar_intf），经 tb 的 axi_xbar_intf 实例弹性触发；原被 BUG-0001 的先序错误掩盖，P-001 后浮出 | `cd sim && make compile`（elaboration 阶段确定性错误） | TOOL_ENV：VCS-MX O-2018 无法折叠 `X-1` 作打包维度位宽（X 为 xbar_cfg_t struct 成员选择或同参数列表兄弟参数），非 DUT 行为。实证（P-002 修复）：axi_xbar.sv:84/190 的 `Cfg.NoAddrRules-1` 修掉后，同类 NCE 由 tb_axi_xbar_pkg.sv:31（class axi_xbar_monitor 参数 `AddrMap` 位宽 `NoAddrRules-1`）浮出（先序错误掩盖，仍 6×NCE 且 expr 由 `Cfg.NoAddrRules-1` 变 `NoAddrRules-1`），共 3 处宽度声明经 `localparam AddrMapMsb=X-1` 中转后 `make compile` 编译+elab+link 全过（out/simv 生成，comp.log 0×Error-[NCE]）。同类先例 floo P-001（TCF-ICTC） | 8062976 | doc/evidence/v0.0.1/BUG-0006.log |
| BUG-0002 | SPEC_CHANGED | SPEC | spec 缺口（arch 蒸馏 P-a，高优先）：`Connectivity[i][j]=0` 时命中该路由的事务响应行为上游文档未载，M3/M4 稀疏矩阵维度 checker 无法推导期望值 | -（spec 歧义，非仿真失败） | SPEC_ISSUE 确认（REV-001 裁决）：许可来源不定义、禁抄 RTL。v0 已补 §8 构造性环境约束：稀疏配置地址表须构造为不译码到非连通 master 端口（未定义情形构造性不可触发）；M3/M4 稀疏维度在此约束子集上验证不降级；"被禁触发时 DUT 响应"留待上游确认（§8.4，不阻塞） | - | - |
| BUG-0003 | SPEC_CHANGED | SPEC | spec 缺口（arch 蒸馏 P-b）：`ATOPs=0` 配置下收到 ATOP 事务的行为未定义，影响配置矩阵 ATOPs{0} 维度 checker | -（spec 歧义，非仿真失败） | SPEC_ISSUE 确认（REV-001 裁决）：许可来源未定义、禁抄 RTL。v0 已补 §6 环境约束：ATOPs=0 时环境保证不发起 ATOP（aw.atop≡'0），违反即未定义；ATOPs{0} 维度在无-ATOP 约束下验证，不阻塞 M4 | - | - |
