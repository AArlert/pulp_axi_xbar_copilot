# Bugs archive

| id | status | suspect | summary | min_repro | root_cause | fix_commit | verify_evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-0065 | CLOSED | TB | **`scripts/docs.py` 四处提示的补救命令 `make docs-archive` 在本仓库不存在——实有 target 是 `make archive`**（BUG-0050/0051 的独立 rev closer 在关闭时实测撞上：两条转 CLOSED 使终态行 3→5 触发节流，`make check` 打印 `run: make docs-archive`，照跑会得 `No rule to make target`）：`grep -c docs-archive scripts/docs.py` = **4**（`:412/:419/:477/:544`），而 `Makefile:85` 定义的是 `archive:`。危害面小但形状典型——**机器给出的补救指令本身不可执行**，读者要么自己猜、要么以为门禁坏了。`scripts/docs.py` 属上游文件，改动须登 `doc/fw-feedback.md` FB 行（或本仓库加一个 `docs-archive: archive` 别名，属处置面裁决） | n/a（非仿真；复现 = 令终态行超限使 `make check` 打印该提示，或直接 `grep -n docs-archive scripts/docs.py` 对照 `grep -n "^archive:" Makefile`） | 上游 0.8.0 蒸馏重构时把 `docs-archive` 目标改名为 `archive`，`docs.py` 里四处硬编码的提示字符串未同步——与 BUG-0052（`workflow/` 路径漂移）完全同族：**同一次重构留下的两处未同步引用，一处是文档路径、一处是命令名**。**实质复验位置（BUG-0029 guard 兑现，独立 closer 实例）**：处置面裁决见 `doc/review/REV-037.md:604-631`。签名改为纯输出 diff 形态独立复算：`diff <(make -n docs-archive) <(make -n archive)` 空 + `grep -c docs-archive scripts/docs.py`=4，均成立。**证伪 2 次**：(a) 仅删 `Makefile` 的 `docs-archive: archive` 规则体、保留 `.PHONY` 里的名字——`make -n docs-archive` 单看退出码得**假绿**（exit 0，"对 docs-archive 无需做任何事"），但与 `make -n archive` 的输出**diff 后判红**（内容不同），证实 `doc/bugs.md` 本行原 verify_evidence 列所记陷阱真实存在；(b) 连 `.PHONY` 名一并删除得真红（exit 2，"没有规则可制作目标"）。两次均 `git checkout -- Makefile` 后复绿，工作区无残留 | 5795a81 | doc/evidence/v0.4.39/BUG-0065.log |
| BUG-0055 | CLOSED | TB | 〔勘误¹：本行原 verify_evidence 列曾载此注，转 CLOSED 时被 `make evidence` 机械改写为证据路径——原文见 `doc/review/REV-037.md:204-215` §(5)：summary 称 `doc/milestone.md`/`doc/log.md`/closeout skill 三处均写明须 `git tag v0.5.0`，经逐处实测**只有 `doc/milestone.md` 一处不成立**（该文件从未规定此事），另两处不订正（`log.md` 按 FB-23 历史区块不回改且指令已因删 tag 自动恢复可执行；closeout skill 写的是通式无冲突），故 REV-037 裁定三处summary 表述均不订正〕**本项目与上游框架共用同一 tag 命名空间，M4 关门要打的 `v0.5.0` 已被上游 tag 占用——`git tag v0.5.0` 会直接失败**（orch 组卡期机核发现，尚未触发因为还没走到打 tag 那一步）：`for t in $(git tag); do git branch --contains $t; done` 实测 24 个本地 tag 中**只有 5 个在 master 上**（`v0.1.0` M0 签核 / `v0.2.0` / `v0.3.0` M2 签核 / `v0.4.0` M3→M4 转段 / `v0.4.2`），其余 19 个（`v0.2.1`、`v0.3.1~3.3`、`v0.4.1`、`v0.4.3~4.6`、**`v0.5.0`~`v0.5.4`**、`v0.6.0`、`v0.6.1`、`v0.7.0`、`v0.7.1`、`v0.8.0`）是经 `git fetch upstream`（CLAUDE.md §5 的跟进流程）进入本地 tag 命名空间的**上游框架 tag**。`v0.5.0` 现指向上游 commit `fb2c193`。而 `doc/milestone.md` / `doc/log.md` [0.4.37] / `.claude/skills/closeout/SKILL.md` 步骤 2 三处均写明 M4 关门须 `make bump minor=1` + `git tag v0.5.0`。**碰撞是必然而非偶然**：两条版本序列都从 v0.1.0 起步、共用一个命名空间、上游跑得更快，此前只是恰好没撞同名（上游 v0.4.1/v0.4.3~4.6 已穿插在本项目 v0.4.0/v0.4.2 之间） | n/a（非仿真；复现 = `git rev-parse v0.5.0` 得 `fb2c193…`；`git branch --contains v0.5.0` 输出为空即"不在 master 上"） | CLAUDE.md §5 的上游跟进流程 `git fetch upstream` **默认连 tag 一起拉**（git 的 `--tags` 隐式行为：fetch 会带回指向已获取对象的 tag），而两个项目的版本号序列互不知情地共用一个扁平命名空间。处置面需裁决：本项目 tag 加前缀 / `git fetch upstream --no-tags` + 清理已污染的 19 个 / 上游 tag 移入独立 refs 命名空间 / 其他。**实质复验位置（BUG-0029 guard 兑现，独立 closer 实例）**：处置面裁决见 `doc/review/REV-037.md:164-218`。S1–S4 独立复算：`git tag`=5、5 个 tag 逐个用 `merge-base --is-ancestor $(rev-parse "$t^{}")` 判定 ours=5/theirs=0（无 BAD 无 LEAK）、`remote.upstream.tagOpt`=`--no-tags`；`refs/remotes/upstream/master` 存在（fail-closed 前置条件满足）。**证伪 2 次**：(a) `git tag v0.5.0 fb2c193`（REV-037 指定的证伪对象）使 S1（6≠5）与 S3（LEAK v0.5.0）双红，`git tag -d v0.5.0` 后复绿；(b) `git config --unset remote.upstream.tagOpt` 使 S4 红，`git config remote.upstream.tagOpt --no-tags` 后复绿。两次均未在 `.git/config`/tag 命名空间留残留 | 5795a81 | doc/evidence/v0.4.39/BUG-0055.log |
| BUG-0051 | CLOSED | TB | **M4 覆盖证据 `doc/evidence/v0.4.35/M4-coverage-final-sweep.md` §2.3 三处事实性错误——实例数错抄 + 两处 N/A 成因与自身数据矛盾**（M4 签核前 orch 四路独立复算 + REV-034 收卡各自发现）：(i) `:128` `counter` 实例数列写 **108**，而该表表头自称"实例数据源：`hierarchy.txt` 精确计数"，实测 `i_counter_open_w` 6 + `i_r_counter` 6 = **12**；108 是下一行 `delta_counter` 的数（`i_counter` 12 + `i_in_flight_cnt` 96），系整行误抄，且被 `doc/design-prompt/milestone_restructure.md:422` §6.3 原样继承；其余 21 行实例数经逐行复算全部正确（DUT 实例合计 1094）。(ii) `:162-169` 脚注 3 标题写"`counter.sv`/`lzc.sv` 的 Line/**Cond**/**Branch** N/A"、正文写"亲验 `modinfo.txt` `lzc` 小节 Line/**Branch** 均为空白"，而同表 `:136` 的 lzc 实测 Cond=**97.73**、Branch=**97.73**（`modlist.txt:48` 复核无误，modinfo 中 lzc 实例的 Branch Coverage 段真实存在）——**lzc 只有 Line 一类是真 N/A**，该脚注对 lzc 的成因陈述整体不成立。(iii) 脚注 6 对 `stream_register` Cond N/A 的成因（"全文无 `if`/`case`/`?:`"）被同一份 `modinfo.txt` 的 Line/Branch 表证伪（其中列有 IF-37/IF-38 宏展开；Cond 确为 N/A，但成因写错）。`doc/milestone.md` M4 出口条件第二条明文要求"逐格 N/A（附**已核实**成因）"——**成因写错等同该格未满足该条**；实例数虽非判据，但它是签核底板的一部分且已被继承一次。**枚举追加 (iv)（arch fixer 发现）**：脚注 5 的括注"`stream_register.sv` 全文无 `if`"与脚注 6 同根、同样被 `modinfo` 证伪；该格（Assert）的判定与操作性成因不受影响（RTL `assert` 计数 = 0），已按同一形态就地标记并入勘误段 | n/a（非仿真；复现 = `grep -c i_counter_open_w sim/out/urgText6/hierarchy.txt` 得 6、`grep -c i_r_counter` 得 6，对照 final-sweep `:128` 的 108；`grep -n lzc sim/out/urgText6/modlist.txt` 得 Cond/Branch=97.73，对照脚注 3 正文） | §2.3 的实例数列与 N/A 成因脚注均为人工撰写，与其自称的数据源（`hierarchy.txt`/`modinfo.txt`）之间无机器核对；三处错误各自独立存活过 REV-031/REV-032/REV-033 三轮复核 + 本次四路独立复算中的三路，唯一抓到 (i) 的路径也曾把它合理化后放过。**裁决回填（`doc/review/REV-035.md` §Q2；三处现象全部经 rev 亲验成立）**：建议终态 **CLOSED**。**(1) 冻结的证据文件裁决 = 追加勘误段 + 就地加"只增不删"的勘误标记，一个字节都不覆写。** 理由：FB-23 的保护对象是"**写作时为真、因外部改名而失效**"的陈述（其原文自陈"不是谁做错了"）；本条三处是**写作时即为假**、且被它自己援引的同一份 `hierarchy.txt`/`modinfo.txt` 当场证伪的陈述，FB-23 不为错误提供庇护。但落盘公理仍管用 ⇒ 不得覆写，读者须能看见"当时相信什么"以及三轮复核为何放过——那才是本条的教训。**与 0.4.37 处理 BUG-0047 详情页（追加不回改）方向一致，一处更严**：那次被追加的是散文，读者必然读到文末；本条错误在**表格单元格与脚注正文**里，纯追加会留活陷阱，而该陷阱已现实咬过一次（§6.3 原样继承 108）。可引用边界：**"不回改" = 不覆写、不删除；不等于不许批注**（加标记是加法，覆写是减法）。**(2) `milestone_restructure.md` §6.3 是非冻结的设计输入 ⇒ 直接就地订正，不追加勘误**；且**订正优于删"例"列**——该列是 §2.3 与原始 hierarchy/modinfo 之间唯一的交叉核对锚点（本卡正靠重数它确认了"单格笔误、非整列失效"：其余 14 个模块实例数逐个吻合）。**(3) (ii)/(iii) 使 `(lzc, Line)` 与 `(stream_register, Cond)` 两格在 M4 出口第二条下判为未满足**——两格的**三态 N/A 判定本身独立成立**（modlist lzc Line 为 `--`、modinfo lzc 区无 Line 节；stream_register 区只有 Line/Toggle/Branch 三节），**但成因要件未满足**："被自己援引的数据源证伪的成因不是已核实的成因"。签核前必须各补一条**能被原始 urg 报告正向支持**的成因，两条硬警告：不得照抄脚注 4 的"无多项布尔组合式"规则去补 stream_register（`:34/:35` 有两条二元连续赋值，而 `spill_register_flushable.sv:78/79/84/85` 的连续赋值确实生成了 Cond bin，该规则解释不了本格）；不得只删脚注 3 的错句去补 lzc（其"generate 分支过程块"预设本身不成立——`lzc.sv` L39-103 全为 assign + generate、无 `always` 块）。另须对 8 条 N/A 成因脚注**每条至少一格**做"成因 vs modinfo 实有节"的正向抽样反证（成因维，非数字维）。closer ≠ fixer；非仿真类 ⇒ BUG-0029 guard 适用。**实质复验位置（BUG-0029 guard 兑现，独立 closer 实例）**：`doc/review/REV-036.md` §V3/§V4/§V5/§V7——FB-23 边界经**旧版 395 行逐行**核对确认零覆写零删除（唯一行内插入满足 `新行 − 勘误标记 == 旧行` 字符级相等）；两格重写成因由 closer 独立回源 `modinfo.txt`/`modlist.txt`/vendor RTL **正向**成立（lzc：唯一过程块 `lzc.sv:56` 在 `:54 if(MODE)` 内、两实例均 `.MODE(1'b0)`、判别式在全闭包 9 无/1 例外/1 反例上自洽；stream_register：`:34/:35` 按位运算 + `FFLARNC` 单信号条件，模块内对照 `spill_register_flushable:90` 生成/`:93` 不生成、`lzc:76/95` 生成/`:75/93-94` 不生成，11 模块 57 行 Cond 源码行逐行验运算符 57/57 成立）；8 条脚注独立重做 3 条（1/4/7）全部成立；D3 签名由 closer 亲手证伪 5 次（K4-K8）。**新发现 I1/I2 见 REV-036 §发现，不阻断本条关闭，须另行登记** | 69cf5a6 | doc/evidence/v0.4.38/BUG-0051.log |
| BUG-0050 | CLOSED | TB | **豁免/债务的"登记面"与下游"引用面"不一致——四处引用越界，与 BUG-0049（漏账）互为镜像**（M4 签核前 orch 四路独立复算 + 裁决者逐条核实发现）：(i) `doc/coverage-waivers.md:22` CW-010 的（模块,类型）列声称含"`fifo_v3` Branch/**Cond** … flush 位分量"，但 `vendor/common_cells/src/fifo_v3.sv:122` 的 `if (flush_i)` 是**单项**条件、VCS `-cm cond` 不为单项条件生成 bin（同报告 §2.3 脚注 4 自己确立的规则），实测 26 个 fifo 实例的 Condition 段源码行号集合恒为 {73, 88, 101}、**122 从不出现**——即**认领了一个物理上不存在的 bin**，并被 `doc/design-prompt/milestone_restructure.md:431` §6.3 照抄；(ii) CW-002（`:14`）登记列只写 `axi_xbar` Toggle，却被 §6.2/§6.3（`:424`/`:427`）列为 `axi_err_slv`/`axi_mux` Toggle 的承接者（`test_i` 端口在两模块确实存在，论证可平行成立，缺的是登记文本覆盖）；(iii) CW-007（`:19`）登记列为 `axi_mux`/`axi_demux_simple`/`axi_err_slv` Toggle，却被 §6.3（`:429`）列为 `rr_arb_tree` Toggle 的承接者；(iv) BUG-0044 承接 `axi_mux`/`axi_err_slv` Toggle 的 `atop[4]/[5]` 位这一归属**只**存在于 REV-030 与其下游转引，`doc/bugs.md` 行与 `doc/bugs/BUG-0044.md` 全文均未提及任何覆盖率格——单向链接，债务行改写或改状态时无机制提醒重裁。**四条均为"过度引用"而非"无人认领"，经逐格剥离有瑕疵引用后 UNOWNED 仍为 ∅（BUG-0049 的关闭不受影响）**，但削薄了 M4 出口第三条"具名归属"的诚实支撑。**枚举追加 (v)（arch fixer 完备性扫描发现，不在原四项内）**：`§6.3` 的 `fifo_v3` **Toggle** 格同样引 CW-010，而 CW-010 登记列从未含该格——同形第五处。fixer 按"不得扩大豁免 scope"取缩窄一侧（删 token），**是否应把 CW-010 扩到 `fifo_v3` Toggle 属 rev 裁决面**（其 `flush_i` 位物理实存 No/No/No，且 REV-031 §5 散文确曾把 fifo_v3 结构分量整体归给 CW-010），待裁 | n/a（非仿真；复现 = `grep -n flush_i vendor/common_cells/src/fifo_v3.sv` 只得 :25/:122 + 遍历 `sim/out/urgText6/modinfo.txt` 全部 26 个 fifo 实例 Condition 段无 LINE 122；另比对 `doc/coverage-waivers.md` :14/:19/:22 登记列与 `doc/design-prompt/milestone_restructure.md` :424/:427/:429/:431 引用列） | CW 行的"（模块,类型）"登记列与下游三处引用面（final-sweep §3、milestone_restructure §6.2/§6.3）之间**没有任何机器核对**，靠人工转抄；转抄方向恒为"引用面向登记面借名"，故错误恒表现为引用面 ⊋ 登记面。与 BUG-0049 同属 BUG-0047 coverage planning-gap 同伞、方向相反：BUG-0049 是格无人认领，本条是认领超出授权（含一个不存在的 bin）。**裁决回填（`doc/review/REV-035.md` §Q1；四子项现象全部经 rev 亲验成立）**：建议终态 **CLOSED**——四项处置均为低成本文本订正，无需仿真/RTL/spec 改动，cost-benefit 不支持 WONTFIX；它是 M4 出口第三条的诚实性支撑、到期即现在，故不得 ACCEPTED。逐项处置面：(i) **缩窄** `doc/coverage-waivers.md` CW-010 的（模块,类型）列，去掉 `fifo_v3` **Cond**（Branch 认领有效，IF-117 `0 1` bin 实存；Cond 侧实测 29 个 Cond 节的源码行号恒 ⊆ {73,88,101,105}、122 一次未现），§6.2/§6.3 同步，该格由 §6.1 重测-1 单独且完整承接（"非-flush 余量" ≡ 全格）；(ii) **扩** CW-002 的（模块,类型）列至 `axi_xbar`/`axi_err_slv`/`axi_mux`（删引用会使 `test_i` 位成孤儿——它是范围豁免而非结构不可达，定向 DV 卡不会去测 scan），**bin 范围列不得随之放宽**（BUG-0047 guard），并把 rev 记录列的占位"待 rev 卡填"填为 REV-024 §4 + REV-035 §Q1(ii) 双引；(iii) **删** §6.3 的 CW-007 token、**不扩** CW-007 登记列（REV-031 §5 对该格的裁决原文是"不登记、路由 DV-A + DV-F"，扩登记等于无新证据推翻在先裁决；该格已由点名了 `rr_arb_tree` 的 CW-010 + DV-A + DV-F 承接）；(iv) **成立为可审计性缺口、不成立为违反出口条件**——pin 住的 spec §0#4 把义务全挂在格这一侧，**不要求**债务行反向自述，故不改 spec/milestone、不给 BUG-0044 加新义务字段；处置面是**扩 BUG-0044 的 `## regression_guard`**（paths 纳入 coverage-waivers 与 milestone_restructure，note 补第二条消费路径"债务行状态变化使两格 Toggle 归属失效"），让 `make guards` 承担既有耦合。四项**均不属冻结记录回改**（waivers 是活登记本、design-prompt 是设计输入、`ACCEPTED@M5` 债务行的 guard 段本就随认识维护）。closer ≠ fixer；非仿真类 ⇒ BUG-0029 guard 要求实质复验位置须落 root_cause 列或详情页 `## rerun` 段。**实质复验位置（BUG-0029 guard 兑现，独立 closer 实例）**：`doc/review/REV-036.md` §V1/§V2/§V6/§V7——132 格三态从 `modlist.txt` 自行枚举（`CELLS=132 NA=59 GE90=43 LT90=30`），登记面手工逐行转录后算得 `引用 ∖ 登记 = ∅`、**无第六处**；**§6.2 由人手工补核**（D2 只解析 §6.3）、**UNOWNED=∅ 由自写脚本独立复算**（两条签名均未钉该项）；CW-002 扩权经回源 `axi_xbar.sv:74/114/151` + `tb_top.sv:137` + urg 三模块页 `test_i No/No/No` 确认论证平行成立且 bin 范围未放宽（BUG-0047 guard 边界）；D2 签名由 closer 亲手证伪 3 次（K1-K3） | 69cf5a6 | doc/evidence/v0.4.38/BUG-0050.log |
| BUG-0048 | CLOSED | TB | **`doc/lint-baseline.md` drifted stale again — recurrence of BUG-0040's exact class**: `cd sim && make clean && make lint-diff TEST=m1_01_smoke_test` fails with 62 new sites (NS 44/SVA-AECASR 3/SVA-UA 3/ULCO 8/WMIA-L 4, 0 new categories) on an **unmodified** `master`@`779ebf7` checkout (isolated via `git stash` before/after this DV card's own edit, same technique BUG-0040 used) — the six tb/ landings since BUG-0040's 2026-07-31 resync (M4-EB01/M4-BP02/M4-BP02 w_open[3] fix/M4-BP03) never re-ran the baseline regenerate-and-diff step. Discovered incidentally during an M1-01 enrichment DV card's own due-diligence lint-diff run (`tb/seq_lib.sv` is lint-scoped); not caused by nor blocking that card (`make evidence`/`make check`/`make selftest` don't invoke lint-diff). Detail: doc/bugs/BUG-0048.md | `cd sim && make clean && make lint-diff TEST=m1_01_smoke_test` (non-sim, no TEST/SEED failure signature — lint gate) | six tb/ feature landings after BUG-0040's 2026-07-31 resync each added/shifted lint-flagged constructs without re-running the resync step that row's own regression_guard calls for | 7f73451 | doc/evidence/v0.4.38/BUG-0048.log |
| BUG-0049 | CLOSED | TB | **`stream_register`（`axi_atop_filter` 内 `r_resp_cmd`，spec §0#4 例化闭包 22 成员之一）Line 75.00/Toggle 22.00/Branch 50.00 三格 <90% 无归属——`doc/evidence/v0.4.35/M4-coverage-final-sweep.md` §3 归属表与 §8 UNOWNED 清单漏账（§2.3 L132 有实测数），REV-031 承接 §8 清单为输入故同漏，log [0.4.36] "UNOWNED=∅" 宣告因此失实**（REV-032 门禁轴 4 发现）：按 spec §0#4 判定单位 =（模块,类型）二元组 + BUG-0038 裁决"不得以父模块 N/A/豁免代表子模块"，三格不得折入父模块 `axi_atop_filter` 的 CW-001，须独立具名归属（Kind-A 登记或 DV 路由，待 rev 裁决）。归因三层：卡A §3 标注遗漏该行、orch 复验只抽查数字未做 §2.3→§3 完备性交叉核对、REV-031 未对 §2.3 全表复核。属 BUG-0047 coverage planning-gap 同伞 | n/a（非仿真失败；复现 = 对照 final-sweep §2.3 全表与 §3/§8 的集合差，`grep stream_register` 前者有后者无） | 卡A 归属标注步骤按 §5 UNOWNED 详述清单收口、未回扫 §2.3 全表逐行核对完备性；下游两级复核（REV-031、orch 收卡）均以 §8 汇总清单为输入、未独立重做集合差。**归属裁决回填（REV-033）**：三格具名归属完成——Line/Branch 全 Kind-A、Toggle 混合拆分（31 bit Kind-A + `data_i.len[7:4]` 8 bit 定向可达），登记 CW-014（`doc/coverage-waivers.md`），Toggle 定向分量路由 DV（DV-A err_slv 宿主族，L1）；逐格论证见 REV-033 逐格裁决表。**关闭复验（REV-034，closer≠fixer）**：从原始 urg 报告（`sim/out/urgText6/modlist.txt`）独立重做 132 格三态（N/A 59 + PASS 43 + <90% 30）与 30 格四表集合差，UNOWNED=∅；例化闭包 22 成员经 RTL BFS 独立重建完备、无子树外实例；CW-014 逐 bin 账平（39=4+1+26+8）、引用 RTL 逐条回源；D1 已录 §6.1 DV-A。**orch 侧另有四路互不知情的独立复算**（正向枚举/反向枚举/直查覆盖库/RTL 重推闭包 + 裁决者）同得 `CELLS=132 LT90=30 UNOWNED=0`，与 REV-034 逐格吻合。实质复验记录：`doc/review/REV-034.md` §V1–V6 + 附表 | 861c7f8 | doc/evidence/v0.4.37/BUG-0049.log |
| KILL-0005 | KILL | TB | **注伤自证（M1 数据完整性 checker 类别——WRAP 读回卷 + 稀疏 strb 写完整性两条比较路径；REV-026 批准清单 C-1 硬条件 (i)，M4 覆盖收敛加固卡；CLAUDE.md 不变量 5）**：M1-01 enrichment C-1 新激励（`slvport_sideband_div_seq`，WRAP 突发/长突发/稀疏 wstrb/属性轮换/user=1）实际吃到的两条数据完整性判决路径的可证伪性自证——植入缺陷→红→复原→绿。**独立技术核实结论**：这两条路径并非新 checker 代码——(A) WRAP 逐拍地址由既有共享预测器 `xbar_types_pkg::predict_beat_data`（L374）委托 `axi_pkg::beat_addr`（`vendor/axi/src/axi_pkg.sv:166-196`，AXI4 A3-51 通用回卷语义，非 DUT 专用）推算，responder（`mstport_agent.sv:269`）与 scoreboard（`scoreboard_refmodel.sv:1053`）共用该函数，缺口本在"从未有激励把 `axburst` 设 WRAP"（纯激励）；(B) 写完整性是全字 data+strb 相等比较（`scoreboard_refmodel.sv:737`），xbar 无差别透传故对任意 strb 取值成立，缺口本在 `fill_wr_payload` 恒 '1（纯激励）。但按 C-1 条件 (i) 精神仍对**本卡新激励真实吃到的比较路径**逐一证伪。**KILL-A（WRAP 回卷）**：把 `scoreboard_refmodel.sv:1053` 的 `ro.burst` 临时改为常量 `axi_pkg::BURST_INCR`（即把 WRAP 回卷"算错"为不回卷），`make run TEST=m1_01_smoke_test SEED=1` → `resp mismatch=48`、`UVM_ERROR:48`，样本报文 `[SB_RDATA] slv port 4 id 'h1b R beat 1 mismatch: got data='h0 ... expected data='h1000000010`（responder 产出回卷后地址 0 的数据，被注伤的期望按 INCR 算成未回卷地址 0x10——证 TB 侧注伤、DUT 正确）；改回 `ro.burst` 后同 SEED 复跑 `mismatch=0`、`UVM_ERROR:0`。**KILL-B（稀疏 strb）**：把 `scoreboard_refmodel.sv:737` 的期望 strb 临时改为 `rec.wstrb[k] ^ 8'h01`（扰动比较的 strb 项），复跑 → `route mismatch=234`、`UVM_ERROR:235`，`[SB_WDATA] ... payload mismatch`（证 strb 项为真判决、非空转）；改回后复跑 `mismatch=0`、`UVM_ERROR:0`。两次注伤均已还原，`git diff tb/scoreboard_refmodel.sv` 为空（与 HEAD 逐字节一致，无 `KILL-0005 inject` 残留）；复原后绿由 `make evidence` 背书（`doc/evidence/v0.4.25/M1-01.log`）。checker 期望值锚 AXI4 协议语义（spec §1「完整 AXI4」+ A3-51 回卷），非抄 DUT RTL | `make run TEST=m1_01_smoke_test SEED=1`（`scoreboard_refmodel.sv:1053` `ro.burst`→`BURST_INCR` ⇒ `SB_RDATA` 48 红；`:737` 期望 strb `^8'h01` ⇒ `SB_WDATA` 234 红；分别改回均归零、`UVM_ERROR:0`） | 自证记录（非缺陷）：checker 期望值从 pinned spec §1（完整 AXI4，含 A3-51 WRAP 回卷/payload 透传）推导、非抄 RTL；击杀证明 M1-01 数据完整性 checker 的 WRAP 回卷读比较（`SB_RDATA`）与稀疏 strb 写比较（`SB_WDATA`）两路均能真实见红、非空转恒真 | - | doc/evidence/v0.4.25/M1-01.log |
| BUG-0047 | SPEC_CHANGED | spec | **M4 出口条件"六类含 Toggle ≥90%"与项目"M5 前仅定向激励、随机不得替代 M4 定向关闭"约束之间存在可行性张力——但张力面经 REV-024 独立划界后远比表面窄，仅限宽 AXI 数据"载荷"位翻转（当前仅 `axi_mux` W/R.data 子集有结构依据），其余"可达未测"缺口均维持需补定向场景**（M4 完整签核卡首次浮出，REJECTED verdict 的附带发现，非本条自身触发的仿真失败）：`doc/milestone.md` M4 出口条件字面要求六类判据（含 Toggle）≥90%，与 M5 节"随机只能加固/发现，不能替代 M4 的定向关闭"、M5 排 M4 之后叠加，对宽数据载荷位产生可行性冲突。详见 doc/bugs/BUG-0047.md | n/a（非失败触发，签核卡人工抽查发现；`workflow/bugs.md` "Dispatch: coverage hole" Q1 归类为 planning gap，但本条更接近判据本身的可行性冲突，非单一 bin 缺场景） | M4 出口"六类含 Toggle ≥90%"（milestone L56-62/spec §0 item4）与 M5"随机不得替代定向、M5 排 M4 后"（milestone L70-73/REV-019 C1.1）两条治理规则各自成立，组合时对**宽数据载荷位翻转**这一类 bin 互斥（纯定向不经济、又非结构不可达）。rev 终判 planning-gap 型 SPEC_ISSUE 成立。**划界（REV-024 §2）**：证据 §6 ~6 类"可达未测"中**仅宽 W/R.data 载荷位属方法论受限**（当前仅 `axi_mux` Toggle 子集有结构依据）；`addr_decode_dync` 地址/rule 多样性、各 Cond/Branch/Assert、握手背压、窄配置索引、实例级颗粒度**均不属，维持需补场景**。处置 = 引入 Kind-A（结构/环境不可达，永久）/Kind-B（方法论受限、延后 M5，临时、可证伪解锁）双类豁免框架，落 `doc/milestone.md` M4 追加子项 + 新建 `doc/coverage-waivers.md`（REV-024 §4），**不动 pinned spec、不重 pin**。原 rca"位翻转组合爆炸/指数增长"表述失实，订正为"定向仅用少数固定取值致多数载荷位未双向翻转；随机廉价、定向不经济"（REV-024 §2.1） | - | `doc/review/REV-024.md` §2（逐条划界表，rev 独立复核 + RTL 结构亲验 `axi_xbar.sv:92` 窄索引 / `axi_mux.sv:33/39` 宽载荷）、§3-§4（处置框架 + milestone/coverage-waivers 草案）、§5（两张清单：清单A可经Kind-B处置仅axi_mux载荷子集且须先出逐位分解，清单B依然阻塞需补定向场景）+ 应用后的 `doc/milestone.md` M4 追加子项与 `doc/coverage-waivers.md`（非仿真类缺陷，无 sim log；本条为治理文档订正，无 spec 重 pin） |
| KILL-0004 | KILL | TB | **注伤自证（M4 重叠 rule tie-break 参考模型类别——`decode_mst_port`，CLAUDE.md 不变量 5，本仓库 M3 起生效）**：DV 全新实例独立核验 M4-OV01（重叠 rule 优先级）的参考模型 `decode_mst_port`（`tb/xbar_types_pkg.sv`）tie-break 逻辑的可证伪性自证——植入缺陷→红→恢复→绿。注伤：在扫描全表取最高下标命中的 `for` 循环里加一行 `break;`，把 tie-break 从"扫描全表、后命中覆盖前命中（SPEC-3.1.3 高位置胜出）"改为"取第一个（最低下标）命中即停"。重跑 `make run TEST=m4_ov01_overlap_test SEED=1`：注伤后 `SB_SUMMARY route: match=12 mismatch=48`、`UVM_ERROR:49`，样本报文 `[SB_ROUTE] routing/attr mismatch id 'h0: got port=7 ... expected port=0 ...`（DUT 仍正确路由到 port=7，即 SPEC-3.1.3 高位置 `OV1_HIGH_RULE` 胜出；被注伤的参考模型错误地期望 port=0，即低位置 `OV1_LOW_RULE`——证明是 TB 侧故障、非 DUT 问题）；恢复后同 SEED 复跑 `route: match=60 mismatch=0`、`UVM_ERROR:0`。`git diff tb/xbar_types_pkg.sv` 核实恢复后与原文逐字节一致（无残留）。全量回归 `make regress` 26/26 PASS，与 0.4.10 周期基线一致。orch 独立复验：`git status`/`git diff` 确认无残留改动，`sim/result_summary.txt` 交叉核对 26 PASS/0 FAIL 与 `m4_ov01_overlap_test PASS` 一致，`make check`/`make selftest`（61/61）复跑绿 | `make run TEST=m4_ov01_overlap_test SEED=1`（`tb/xbar_types_pkg.sv` `decode_mst_port` 循环内临时加 `break;` → `route: match=12 mismatch=48`/`UVM_ERROR:49`；去掉该行恢复 → `route: match=60 mismatch=0`/`UVM_ERROR:0`） | 自证记录（非缺陷）：checker 期望值按 spec §3.1.3（重叠时更高位置 rule 胜出）+ §3.2.1（半开区间）推导，非抄 RTL；击杀证明 `decode_mst_port` 的 tie-break 判据能真实见红、非空转恒真——M4-OV01 场景若被静默改错 tie-break 方向，参考模型确实能捕捉到 | - | doc/evidence/v0.4.6/M4-OV01.log（场景本身证据）；注伤/恢复过程本身未生成独立 evidence 文件，数字见本行描述，orch 已独立复核 |
| BUG-0043 | WONTFIX | TB | **`make regress` 一次运行里 `m1_02_id_prefix_test SEED=1` 以"sim process exited nonzero"判 FAIL，日志内容本身干净（`UVM_ERROR:0`/`UVM_FATAL:0`，SVA summary 0 failures，`$finish` 正常结束）——orch 独立复验 M4-FT01/BUG-0042 交付物时实测发现，同一 TEST/SEED/代码树、前后两次干净 `make regress` 均 24/24 全绿，无法复现**：orch 亲跑 `make regress`（未先手动 `make clean`，但 `scripts/regress.py` 自身在整批测试前已跑一次 `make clean`）第一次得 23/24（仅 `m1_02_id_prefix_test` 标 `[sim process exited nonzero]`，日志本身无任何错误字样）；单独隔离重跑该 TEST（先 `make clean`）与随后整批重跑 `make regress`（同样先由脚本内建 `make clean`）均 24/24，`m1_02_id_prefix_test` 转绿，问题不再出现。命中 `TOOL_ENV` 判据「Result changes with seed/machine/build but not stimulus」（`workflow/bugs.md` 决策提示原句，TEST/SEED/代码树三者都未变，只有那一次运行的 shell 退出码异常）。与 BUG-0042（M4-FT01 的 `mstport_agent.sv`/`axi_chan_sva.sv` 修复）**大概率无因果关系**：(1) `m1_02_id_prefix_test` 用基线配置（`FallThrough=1'b0`），BUG-0042 新增的孤儿队列路径在此配置下应恒空、行为与修复前逐位相同（已读 diff 确认是"空队列 = 老行为"的超集设计）；(2) 同一份代码树的另两次独立 `make regress` 均干净通过（DV 卡自报 24/24 + orch 本条第二次复跑 24/24），不满足"每次都复现"的回归特征。详见 doc/bugs/BUG-0043.md | `cd sim && make clean && make regress`（**非确定性可复现**——同 TEST/SEED/代码树在其余两次独立运行中均未复现，如实记录为间歇性，不宣称确定性 repro） | 待查：`sim/Makefile` `run:` 目标最后一行 `$(OUT)/simv ... -l $(RUN_LOG)`（VCS `simv` 二进制自身退出码），日志内容层面无任何异常字样，怀疑是该拍 VM 上一次性的资源/license/磁盘 I/O 级瞬时状况（同族 BUG-0014/0019/0020/0022/0030 一类既有的 VCS-2018/VM 环境怪癖），未定位到具体触发条件，未做代码改动。REV-022 终判：`rc!=0→FAIL` 是 `vcs-2018.mk` 头注释明载的有意设计，门禁本身工作正常、TB/脚本侧无 fix 对象；判别性数据（进程死亡信号号/stderr）已随该次运行永久丢失、不可事后还原；排除 ACCEPTED@M\<n\>（无可调度工作、无可证伪到期条件，强设只会把不可复现现象伪装成日程债），终态取 WONTFIX（accepted-transient），与同族先例 BUG-0030 同构 | - | `doc/review/REV-022.md`（rev 独立复算退出码机制归因 + 三次两清一异事实链，taxonomy TOOL_ENV 确认，终态 WONTFIX；`fix_commit`/`verify_evidence` 均 `-` 是 WONTFIX 行既有先例写法，见 BUG-0021/BUG-0024） |
| BUG-0041 | WONTFIX | DUT | **`addr_decode_dync`（common_cells，`axi_xbar` 例化闭包内）的 end-of-sim 调试型 `ASSERT_FINAL more_than_1_bit_set`（onehot0 检查 `matched_rules`）在 SPEC-3.1.3 允许的重叠地址表构造下必然触发**——M4-OV01 首次落地实测发现：地址表构造一对合法重叠 rule（SPEC-3.1.3）后，若仿真结束时某 slave 端口的 AW/AR 地址总线仍停留在重叠地址上（`slvport_driver` 握手完成后不清零地址总线，本场景全程只发重叠区域地址故必然如此），`final` 块对该拍残留组合值判 `matched_rules` 非 onehot0，12 处（6 端口 × AW/AR）报 `[ASSERT FAILED]`——`svacheck.py` Layer1/1b 均命中该行格式，会把日志判 FAIL，本可阻断 `make evidence`。**功能路由本身零失配**（scoreboard `route: match=48 mismatch=0`，DUT 路由决策已正确遵循 SPEC-3.1.3"更高位置 rule 胜出"）——该断言检查的 `matched_rules` 是模块自身注释标注的调试专用信号，非功能输出。详见 doc/bugs/BUG-0041.md | `make run TEST=m4_ov01_overlap_test SEED=1`（需回退本卡对 `tb/seq_lib.sv` `slvport_ov01_seq::body()` 追加的"收尾腿"落地前的版本——即只保留重叠区域激励、不追加非重叠收尾事务——方可复现；本卡交付的版本已内建收尾腿工作绕过，`make run` 现状为 PASS/CLEAN） | `vendor/common_cells/src/addr_decode_dync.sv` 头注释文档明述"地址区间允许重叠、高位置 rule 胜出"为受支持特性，但同文件的 `ASSERT_FINAL more_than_1_bit_set`（末尾 `final` 块，仅看 `matched_rules` 组合逻辑在仿真终止那一拍的残留值）沿用"该调试信号理应恒 onehot0"的假设，与模块自身文档化的重叠支持能力不一致；模块自带 `$test$plusargs("disable_assert_final_checks")` 逃生阀提示该冲突为作者已知/预留豁免路径，非纯粹疏漏 | env: tb/seq_lib.sv slvport_ov01_seq 收尾腿（工作绕过，非 RTL/spec 修复，见 doc/bugs/BUG-0041.md ## fix） | doc/evidence/v0.4.6/M4-OV01.log（M4-OV01 本身已绿）+ `doc/review/REV-020.md`（rev 独立逐行核验：DUT_BUG bar 未达——功能路由零失配、失败的是内部调试断言非 DUT 输出——候选不予签核，终态 WONTFIX/accepted-vendor-quirk；P-xxx 补丁与 `disable_assert_final_checks` 逃生阀均排除，理由见该记录 §2 Q5） |
| BUG-0042 | TB_BUG | TB | **`mstport_agent.sv`（monitor + responder）与 `axi_chan_sva.sv` 的 AW/W FIFO 配对逻辑均假设"AW 恒不晚于其自身 W burst 到达 master 端口"——M4-FT01（`cfgE`, `FallThrough=1'b1`）首次落地实测证伪**：`FallThrough=1'b1` + 基线 `LatencyMode=CUT_ALL_AX`（`MuxAw` 有 spill、`MuxW` 无）下，一笔写事务的 W beat 可在其自身 AW 到达 master 端口**前一拍**先被接受（临时逐拍 trace 实测：mst port 5 首笔命中写，`w_valid&&w_ready@135000ps` 早于同一笔的 `aw_valid&&aw_ready@145000ps`）。三处组件均只在假设成立时正确：monitor 先报假 `MON_WNOAW`/`SB_NOPEND`；responder 用陈旧 `cur`（id 恒 0）生成错 ID 的 B 响应，driver 等不到自己 ID 的 B 永久挂起（`WATCHDOG` FATAL）；`axi_chan_sva` 的 `SVA_WLAST_LEN` 在 6 个 master 端口实例上误判（16 次，全部 `gen_mst_sva[*]`，无一 slave 端口实例）。AXI4 协议本不要求 AW/W 相对序，SPEC-5.5.1/5.5.2 只约束"不同 W burst 间的序 = 其 AW 接受序"，从不要求一笔的 AW 早于自己的 W——三处代码的假设比 spec 更强、从未被写下、M0-M3 因激励构造恰好从未撞见。详见 doc/bugs/BUG-0042.md | `make run TEST=m4_ft01_cfge_test SEED=1` | 三处组件把"AW 先于自己的 W"当隐含前提；`FallThrough=1'b1` 打破前提后，`mstport_agent.sv`/`axi_chan_sva.sv` 的简单单向 FIFO 弹出对空队列要么误报、要么静默用错数据 | fd4b5b6 | doc/evidence/v0.4.6/M4-FT01.log（TB-only，非 DUT/spec；DV 本人一次性直接落 TB_BUG 终态，workflow/bugs.md 明文 closer≠fixer 语句只绑定 CLOSED 状态，未覆盖 TB_BUG）。**orch 独立复核（非仅采信卡内自报）**：逐行读 `tb/mstport_agent.sv`/`tb/sva/axi_chan_sva.sv` diff 确认对称双队列设计对基线（空队列）是老行为的严格超集；亲跑 `make run TEST=m4_ft01_cfge_test SEED=1` 复现 PASS；亲跑 `make regress` 两次（各一次先 `make clean`），24/24 与 23/24（一次间歇性 `m1_02_id_prefix_test` 异常退出、日志内容本身干净、后续两次独立复现均转绿，判非本条回归、另登记 BUG-0043，taxonomy TOOL_ENV）——两次干净全绿 + 一次不可复现的环境级瞬时状况，不构成本条回归证据 |
| BUG-0040 | CLOSED | TB | **`doc/lint-baseline.md`（快照 2026-07-28）落后于 `tb/` 的 0.4.2 重构提交（`01e7976`，2026-07-30）**——`make lint-diff TEST=m1_01_smoke_test`（同样复现于基线自带的 `TEST=m2_or03_guard_test`）报 153 个「新站点」，跨 7 个既有类别（`NS`46/`SVA-AECASR`11/`SVA-CE`32/`SVA-DIU`32/`SVA-UA`28/`SV-PIU`2/`WMIA-L`2），无新类别。BUG-0037 卡的 lint/lint-diff 回归自检中意外发现（复现与本卡改动无关：`git stash` 掉 BUG-0037 的两处改动后同样 153/同样类别分布，证实是独立于本卡修复的既有缺口）。命中 `TOOL_ENV` 判据「evidence 早于它所证明的代码」（workflow/bugs.md 决策提示原句）。详见 doc/bugs/BUG-0040.md | `cd sim && make clean && make lint-diff TEST=m1_01_smoke_test` | **分诊卡 2026-07-31 修订**：真正根因不止 `01e7976` 一个提交——2026-07-28 快照之后落地的 `d7f5011`(BUG-0034)/`0e9d516`/`68b6cca`/`482a47e`(BUG-0024/0025/0031 cover 补齐)/`c105d2d`(M3 多配置)/`bff8a04`(M3-TL01)/`01e7976`(0.4.2 重构) 六次落地均未回跑基线生成命令。153 个站点逐条读码分诊（方法论沿用 BUG-0021）：**153/153 全部风格，0 条真缺陷**；详见 doc/bugs/BUG-0040.md `## triage`。`doc/lint-baseline.md` 已全量重同步（236 唯一站点/296 条/8 类别），`make lint-diff TEST=m2_or03_guard_test` 强制重编后 PASS（236/236，无新站点）。closer≠fixer：orch 独立复验（亲跑强制重编+lint-diff、`git blame`/`git show` 逐条核对 WMIA-L/ULCO 两个历史高风险类别的分诊结论、六次中间提交时间戳核对）后机械关闭，非采信卡内自报数字 | 212a6fc | doc/evidence/v0.4.6/BUG-0040.log |
| BUG-0037 | CLOSED | TB | **`make regress COV=1` 把结构不同的多个 DUT 设计静默合并进同一 `out/cov.vdb`**（M4 覆盖率基线测量卡实测发现）：(1) `TEST=upstream_sanity`（`TOP=tb_axi_xbar`）与全部 M1+ UVM 测试（`TOP=tb_top`）共享默认 `OUT=out` ⇒ 共享 `-cm_dir out/cov.vdb`，两者是完全不同的顶层设计，merge 产生 3139 行 `CMR-VCINF`（"in test file X is not found in base design"，本卡复测口径，`grep -c` 逐行计，前次 2971 为不同计数口径）；(2) `sim/Makefile` 的 M3-CF01~04 config 分支只 `override OUT` 到 `out/cfgA`（构建产物路径），但 `scripts/make/vcs-2018.mk` 里 `CM := ... -cm_dir $(OUT)/cov.vdb` 用 `:=` 在 include 时就已按当时的默认 `OUT=out` 展开，后续的 `override OUT` 改不动已展开的 `CM` 字符串 ⇒ 4 个不同拓扑（1×8/6×1/4×4×2）的 cfg 点仍全部写入同一个 `out/cov.vdb`，产生 825 行 `UCAPI-INSTANCEMISMATCH`（"Coverage data of this instance won't be merged"）。VCS 对结构不匹配的 instance 采取丢弃而非静默错误合并（安全网），但污染了"一次 `make regress COV=1` 即可拿到干净基线数字"这一假设。详见 doc/bugs/BUG-0037.md | `make regress COV=1`（22/22 功能 PASS，仅覆盖率数据库受影响）后 `make cov`，grep 出 `mismatch` | **确认 rca 无需订正**：根因正是 `CM` 用 `:=`（立即展开）在 include 时冻结 `$(OUT)` 默认值，晚于其展开的 per-config `override OUT` 改不动已展开字符串；VCS 自身合并丢弃行为是安全网、非本缺陷。修法：把 `-cm_dir` 的目标改成新增的 `COV_DIR` 间接层，`CM`/`COV_DIR` 均改 `=`（递归展开，在 recipe 执行时才求值），`sim/Makefile` 给 `TEST=upstream_sanity` 单独设一份 `COV_DIR :=$(OUT)/m0/cov.vdb`（只挪覆盖率库路径，不动 `OUT` 本身，避免波及 `make clean`/构建产物路径），cfgA-D 沿用默认公式自动跟随既有 `override OUT`，无需改动 | 13cdeda | doc/evidence/v0.4.4/BUG-0037.log |
| BUG-0039 | SPEC_CHANGED | spec | **M4 六类收敛对 `axi_atop_filter` FSM 的要求与 spec §4 clause 7 的 BUG-0032 环境约束直接冲突——须按 REV-013 预留路径重开仲裁**（REV-016 §6.2 发现，orch 已独立复核例化事实与 FSM 转移条件）：`axi_atop_filter` 在本 DUT 内**全部 6 例都在 `axi_err_slv` 内**（`axi_err_slv.sv:45-58` 例化；`axi_xbar_unmuxed.sv` 全文无 `atop_filter` 字样——orch grep 复核，证据 `doc/evidence/v0.4.0/M4-coverage-baseline.md` §3.7 的"由 axi_xbar_unmuxed 例化"为失实）。其 W FSM 离开 `W_FEEDTHROUGH` 的唯一条件是 `axi_atop_filter.sv:137` 的 `slv_req_i.aw_valid && slv_req_i.aw.atop[5:4] != axi_pkg::ATOP_NONE` ⇒ 必要条件是"`atop != 0` 的 AW 抵达某个 err_slv"，即**打到译码未命中地址**——而 spec §4 clause 7（BUG-0032 环境约束，REV-012/REV-013 终判）明令禁止向译码未命中地址发起任何 ATOP。故 FSM 恒定 7.14% 与场景数量无关，**且"补几条 AtomicCompare/AtomicSwap 编码序列"方向无效**——现有 `tb/seq_lib.sv:375-377` 的 `ATOP_LOAD_ADD` 已含 `ATOP_ATOMICLOAD=2'b10`（`axi_pkg.sv:415`）≠ `ATOP_NONE=2'b00`（:400），编码多样性**已满足**该转移条件，缺的是地址落点（orch 复核 `axi_pkg.sv` 与 `axi_atop_filter.sv` 原件确认）。本行**不是** BUG-0038（那条只裁范围口径）**也不是** BUG-0032（那条已由 REV-012/013 以 M3 环境约束终结；spec Change record #7 明写"M4 若需触发须重开仲裁"，重开是新事件）。`## similar`：BUG-0032、BUG-0038、REV-013。详见 doc/bugs/BUG-0039.md | n/a（非功能缺陷；FSM 数字复现路径见 `doc/evidence/v0.4.0/M4-coverage-baseline.md` §0①/§2/§4 第 1 条，注意其 §4 第 1 条的归因已被 REV-016 §6.2 证伪） | **REV-017 仲裁：CONDITIONAL PASS**。逐条核实 BUG-0039 陈述的三条事实为真（例化层次/FSM 必要条件/编码多样性已满足）。否决"重开以定义 err_slv×ATOP 应答、放行激励"路径（REV-016 §6.2 选项 a/c）——五份许可来源仍皆空，放行=让 checker 抄被测 RTL 期望值，违不变量 #4。裁定路径（选项 b）：**§4 clause 7 环境约束由 M3 延展至 M3+M4**（目的不变：令该无源组合构造性不可触发）+ `axi_atop_filter` FSM 中仅经被禁激励可达的状态/迁移弧按 §0 item 4"有 bin 但 <90%"分支走 **rev 签核书面豁免**（不适用"无 bin ⇒ N/A"）。BUG-0032 guard 被延展、非解除。**条件化未闭合**——REV-013 重开要件 (b)（M4 config-matrix testplan 行须同步承载延展后的约束）尚未兑现，M4 在此之前不得签核；条件 3（M4 签核时 rev 出具书面豁免 + 跑 guard 抽查）留待 M4 签核卡。底层 SPEC_ISSUE（err_slv×ATOP 应答无源）不消解，仍作 BUG-0032 上游确认项存续。**实质复验记录**：`doc/review/REV-017.md`（全文，尤其 Item 1-4 + 逐字订正文本） | - | `doc/review/REV-017.md`（rev 独立复验+逐字订正文本）+ spec §4 clause 7 延展 M3→M3+M4 重 pin 后的 `doc/spec.sha256`（非仿真类缺陷，无 sim log；BUG-0029 guard 要求的实质复验记录位置见 root_cause 列所引 REV-017 章节） |
| KILL-0003 | KILL | TB | **注伤自证（M3 R burst 重建 checker 类别——`MON_RNOAR`/`SVA_RLAST_LEN`/`SB_RBEATS`/`SB_ATOP_DANGLING` 四路见证，CLAUDE.md 不变量 5，本仓库 M3 起生效）**：转录 `doc/bugs/BUG-0034.md` `## rerun` 段已完整记录的两次独立红→绿注伤自证（fixer 一次 + closer 一次，手法不同——fixer 把 monitor `rid` 折叠为常量 `'0`、SVA 各 per-id key 折叠为常量 `'0`；closer 独立复现时同样折叠 monitor `rid` 与 SVA 三处 per-id key（含 fill 侧 `r_exp_len_q[ar_id]/[aw_id]`）为常量 `'0`，未采信 fixer 转述、亲跑独立验证），可证伪性自证——植入缺陷→红→恢复→绿。同 `TEST=m3_at02_atop_read_test SEED=1`（leg A `p.len` 恢复为多拍 `len_t'(3)` 的构造）下，去分流态四路 checker 均见红：`MON_RNOAR`=2、`SVA_RLAST_LEN`=3、`SB_RBEATS`=3、`SB_ATOP_DANGLING`=2、`UVM_ERROR`=8；恢复按 `r_id` 逐拍分流后同 SEED 复跑四路全归零、`UVM_ERROR`=0。closer 独立实例的数字与 fixer 实例逐位吻合（含样本报文 `RLAST beat index 2 != AxLEN 3`/`index 1 != AxLEN 0`/`R beat count 3 != (4)`/`count 2 != (1)` 逐字复现），证明该吻合非巧合、checker 类别真实能见红（非平凡恒绿）。M3 签核 C2 条件（`doc/evidence/v0.3.20/signoff-M3.md`）要求把该已发生的注伤自证正式落一行 KILL 台账，兑现不变量 5 字面要求；本行为纯转录，未重新仿真、未改动 TB 代码。详见 doc/bugs/BUG-0034.md `## rerun` 段（"KILL 自证"/"独立 KILL 自证"两个子节）。 | `make run TEST=m3_at02_atop_read_test SEED=1`（leg A 多拍 `len_t'(3)` + monitor/SVA r_id 分流折叠为常量 `'0` → `MON_RNOAR=2/SVA_RLAST_LEN=3/SB_RBEATS=3/SB_ATOP_DANGLING=2/UVM_ERROR=8`；恢复分流后四路归零、`UVM_ERROR=0`） | 自证记录（非缺陷）：checker 期望值按 spec §1（AXI4 no-interleaving，经 BUG-0034/REV-015 订正范围后按 §5.5.4 不得断言 round-robin 发生序/beat 级授权时点）与 §6.5（原子读跨方向假冲突）推导；击杀证明 `MON_RNOAR`/`SVA_RLAST_LEN`/`SB_RBEATS`/`SB_ATOP_DANGLING` 四路均能真实见红、非空转恒真 | - | doc/evidence/v0.3.15/M3-AT02.log |
| BUG-0036 | CLOSED | TB | **`regress.list` 缺 M3-CFG02（`m3_cfg02_reconfig_test`），全回归分母漏一条 ✅ 场景**（rev M3 签核 spot check 6 回归覆盖差集抽查发现，中，非功能破损但阻塞 M3 完整签核）：`sim/regress/regress.list` 21 行不含 `m3_cfg02_reconfig_test`，而 `doc/testplan.md` M3-CFG02 为 ✅ 场景（repro=`make run TEST=m3_cfg02_reconfig_test SEED=1`，承载 BUG-0031 `c_bug31_livev1` 活值表守卫）。后果与 BUG-0028 同族（分母静默缩水）：`make regress`/`doc/evidence/v0.3.20/result_summary.txt` 报 21/21 PASS 但覆盖范围不含 CFG02，且其登记证据 `doc/evidence/v0.3.9/M3-CFG02.log`（2026-07-29）**早于** BUG-0034 monitor/SVA 重写（d7f5011，0.3.18，改 `tb/slvport_agent.sv`/`tb/sva/axi_chan_sva.sv`＝CFG02 观测路径），且不在 BUG-0034 回归防线复跑集（m1_01/m1_02/m2_at01/m3_de01/m3_or04）内 ⇒ 自该重写起从未被复验。**rev 已独立复跑当前 HEAD 定性**：`make run TEST=m3_cfg02_reconfig_test SEED=1` → UVM_ERROR=0、SB 全 mismatch=0、2143 assertions 0 failures、`c_bug31_livev1_aw/ar` 各 1 match（crit3 守卫兑现）⇒ **latent PASS，非功能破损**，缺口在回归网/记账。BUG-0028 详情页早已把"回归清单须覆盖 testplan 每条 ✅、差集非空即失败"记为真正守卫，但该守卫从未落成机器门 ⇒ 本条是其复发。**closer≠fixer**：rev 只登记，L0 修复卡由 orch 另派——把 `m3_cfg02_reconfig_test 1` 补进 `sim/regress/regress.list`、`make regress` 转 22/22、刷新 M3-CFG02 证据对当前 HEAD、重登摘要、重跑 `make check MILESTONE=3`。见 doc/evidence/v0.3.20/signoff-M3.md 条件 C1。 | 差集：`grep m3_cfg02 sim/regress/regress.list`（空）对比 `doc/testplan.md` M3-CFG02 行 ✅；rev 复跑当前 HEAD `make run TEST=m3_cfg02_reconfig_test SEED=1` = PASS（UVM_ERROR=0） | TB_BUG（回归配置缺口，非 `make regress`/`regress.py` 缺陷，同 BUG-0028 家族）：DV/planner 落地 M3-CFG02 时收版清单未强制"本场景已入 regress.list"，且 BUG-0028 的完整性守卫（差集非空即失败）未落机器门 | 4d712f9 | doc/evidence/v0.3.20/BUG-0036.log |
| BUG-0032 | SPEC_CHANGED | spec | **err_slv 对 ATOP（尤其要求读响应的原子操作）的应答形态，许可来源未定义**（M3 arch 场景注册卡交叉核对 spec §4/§6 时发现，低，非阻塞）：一笔 `aw.atop != '0` 且要求读响应的原子操作，若地址不匹配任何 rule 且该 slave 端口未使能 default master port，按 spec §4.2 被路由到本端口的内部 decode error slave——此时 spec §4.3 按"读/写"二分只给出"**写**事务收齐 W burst 后返回**单拍 B**（DECERR）"，而 spec §6.3 要求原子读 **B 与 R 两通道都返回响应**；err_slv 是否也要为这笔 AW 产出 R beat、几拍、数据是否同为 `32'hBADCAB1E`、响应码为何，**无条款可推**。缺口源自上游文档本身而非蒸馏遗漏（`vendor/axi/doc/axi_xbar.md` §Decode Errors 全段无 atop/atomic 字样；demux.md/mux.md 对 err_slv 一字未提，REV-011 §4.3 已复核该 grep 空集）。**处置沿用 BUG-0002/BUG-0003 先例**：以 env 构造性约束使该情形不可触发（M3 全部场景不向译码未命中地址发起 ATOP，见 doc/testplan.md M3-DE01 行与 doc/design-prompt/uvm_env.md §6 C6.2），缺口列**上游确认项**，**不阻塞 M3**；若 M4 覆盖率收敛需要覆盖该组合，须先走 rev 仲裁补条款再解除约束，不得由 DV 在 checker 里二选一（spec §0 行 6 / discipline rule 1）。详见 doc/bugs/BUG-0032.md | 无当前可复现命令（构造性不可达）；解除约束后的复现形如 `make run TEST=m3_de01_decerr_test SEED=1` 并令某笔 ATOP AW 落在未命中地址 | SPEC_ISSUE：spec §4 的应答形态按 AXI4 读/写二分蒸馏自 xbar.md "proper number of beats"，spec §6 的 ATOP 条款蒸馏自 demux.md §Atomic Transactions，两节各自完备而交集（ATOP × 译码未命中）无一方覆盖 **REV-012 §Item 1 终判**：SPEC_ISSUE 确认（本卡复跑 grep 亲验五源皆空），处置成立（env 构造性约束 + 上游确认项 + 不阻塞 M3，同 BUG-0002/0003）；升级为 `SPEC_CHANGED`——**approve P-REV012-1**：补 spec §4 平行条款（四段模板同 §8.4/§6.2）+ §6 clause 3 交叉引用，**orch 应用 + change record + 重 pin**，rev 不触 spec 正文。应用前约束活载体 = testplan M3-DE01 + uvm_env C6.2 + 本 guard。可证伪 rationale：五源任一定义该应答形态即须重开 **REV-013 spec-review 门禁 conditional pass**（内容/结构/上游静默/编号惯例四项核查通过，唯一必改：提案原文两处 `M3/M4` 收窄为 `M3`——BUG-0032 处置锚 M3、M4 被明确留作 reopening 触发点，写 M4 会绑定一个无 M4 artifact 承载的约束）；**已应用**：spec.md §4 新增 clause 7 + §6 clause 3 交叉引用，change record #7，重 pin sha256 `9347b4ac71f824a05581468502109d78160781fd1712710d0d783a2f03b3b806` | P-REV012-1（经 REV-013 订正后已应用，change record #7） | doc/spec.md §4 clause 7 · doc/review/REV-012.md · doc/review/REV-013.md · doc/bugs/BUG-0032.md ## arbitration |
| BUG-0035 | WONTFIX | TB | **`TOOL_ENV`：混用增量编译产物触发 `VFS_ZLIB_ERROR`（daidir db zlib uncompress error），仿真 time 0 即崩、非逻辑缺陷**（M3 covergroup 落地卡回归防线期间，做 `git stash`（HEAD 基线）→ `make compile`（增量）→ `make run` 时命中，低，不阻塞）：`m3_de02_default_test SEED=1` 在 stash/增量重编周期后崩于 `out/simv.daidir/nullcheck_class__VCSClass_uvm_pkg...db`（`Summary: 0 assertions`, `Time: 0 ps`）。**判据（workflow/bugs.md 分诊表第 4/第 2 问）**：同 SEED **clean rebuild 后不复现**（`make clean` + 全量重编，de02 复跑 UVM_ERROR:0、SB_SUMMARY 与 HEAD 逐位一致）⇒ 定档 `TOOL_ENV`（stale build artifacts），非 TB/DUT。**既有缓解已存在**：`scripts/regress.py` 注释明载「VFS_SDB_ERROR class」并在回归前强制 `make clean`（本卡 `make regress` 20/20 PASS 即走该路径，全程无复现）。本条无条件登记以兑现 CLAUDE.md §2「变通不落台账 = FB-7/BUG-0007 形状」的教训，无需新代码修复。手工 stash/增量对照实验后须 `make clean` 再取数——本卡 de02 before 基线即因此先污染、clean 重取后 before==after | 复现（脆弱、依赖增量产物污染）：`git stash` 部分文件 → `make compile`（增量）→ `make run TEST=m3_de02_default_test SEED=1` 偶发 `Error-[VFS_ZLIB_ERROR]`；**根治**：`make clean` 后重编重跑即消失 | TOOL_ENV：VCS O-2018 增量 build database（daidir/csrc）在换源码集（stash pop/push + 增量编译）后被污染，与 `scripts/regress.py` 头注记载的 VFS_SDB_ERROR 同类；非本卡改动引入（HEAD 基线码在同污染下同样崩） | -（既有缓解：scripts/regress.py 回归前强制 make clean） | 本卡 `make regress` 20/20 PASS（clean slate）+ de02 clean-rebuild UVM_ERROR:0、SB_SUMMARY/既有 FCOV/既有 SVA cover 命中数逐位 == HEAD |
| KILL-0002 | KILL | TB | **注伤自证（M3 cfgC UniqueIds 兜底监视类别，CLAUDE.md 不变量 5，本仓库 M3 起生效）**：`SB_UNIQUEIDS` 见证（`tb/scoreboard_refmodel.sv`，cfgC `UniqueIds=1'b1` 配置点下 spec §5.3.1 前置条件的 env 侧兜底监视）的可证伪性自证——植入缺陷→红→恢复→绿。人为构造一组违反 §5.3.1 的事务（同一 slave 端口、同一完整 ID、同方向、目标 master 端口不同，在 §5.3.1 允许范围之外），重跑 `m3_cf03_cfgc_test SEED=1`，`SB_UNIQUEIDS` 见红（`violations=1`）；撤销注入后同 SEED 复跑归零（`violations=0`），UVM_ERROR:0。此前 M3-CF03 交付的合法激励全程未触发该监视（全程持有 §5.3.1 构造性保证），本条证明该监视器并非恒真空转。脚手架已由卡⑤实例移除（`grep` 确认无残留），实测数字见 doc/bugs/BUG-0034.md 同批交付上下文 / M3-CF03 场景交付报告 | `make run TEST=m3_cf03_cfgc_test SEED=1`（注入 §5.3.1 违例：同完整 ID、同方向、异目标 master 端口 → `SB_UNIQUEIDS violations=1`；撤销后归零） | 自证记录（非缺陷）：checker 期望值按 spec §5.3.1 推导；击杀证明 `SB_UNIQUEIDS` 能真实见红、非空转恒真 | - | doc/evidence/v0.3.15/M3-CF03.log |
| BUG-0034 | CLOSED | TB | **REV-015 终判：DUT_BUG candidate 被否决，taxonomy = TB_BUG。** DV 三工具交叉诊断（xdebug+xwave 独立解析器逐拍一致 + xtrace 证实 `rr_arb_tree` 无 burst 级锁）的 RTL/波形观测**为真**，但援引的 spec 条款是伪造的——诊断卡称"spec §1/§5.5.3 禁止读交织"，rev 亲读 §5.5.3 原文实为"R 响应经 round-robin 仲裁**合并**"（不禁止），禁止交织的条款只存在于 §5.5.1（**W 通道**），R 侧无对应条款；§5.1.4 + 上游 `vendor/axi/doc/axi_mux.md:18` **明文允许**不同完整 ID 的响应交织（框定为性能特性）；§5.5.4 明文**禁止** checker 断言任何 round-robin 发生序/beat 级授权时点——命中的三个 checker（`MON_RNOAR`/`SVA_RLAST_LEN`/`SB_RBEATS`+`SB_ATOP_DANGLING`）恰恰断言的就是这个。rev 逐行代入诊断卡自己的逐拍表，证明四路症状是 `tb/slvport_agent.sv:503-548`（monitor 只在 burst 起始按 r_id 配对、之后盲收到 r_last、不再按拍校验 r_id）与 `tb/sva/axi_chan_sva.sv:243-256`（单一 r_id 无关的连续 beat 计数器）**共模**同一"R 永不交织"重建假设在合法交织下的必然误报，非四个独立信源；物理层逐拍收发计数全对（id0 实收 4 拍、id8 实收 1 拍），无数据丢失。DUT 侧：`i_ar_id_counter` 的 `mst_select_q` 只随 `push_en` 加载（`axi_demux_simple.sv:618`），atop 走 `inject_en`（`:555`）从不写目标寄存器 ⇒ 影子读结构上不参与跨端口 stall——`vendor/axi/doc/axi_demux.md` §Atomic Transactions 原文明文承认此交互"可能导致读通道额外假冲突 stall"、**从未**框定为正确性问题，与 spec §6.5"只影响是否 stall、不影响功能正确性"一致 ⇒ 这是设计者明知且明文承认的取舍，非漏洞、非撞出设计假设之外。**结论：不发起上游 issue、不走 P-xxx**（DUT 行为与其自身上游文档一致）。**须派独立 TB 修复卡**：使 monitor/bind SVA 按逐拍 r_id 分流重建 R burst，修复后恢复 M3-AT02 多拍两腿复跑转绿（closer≠fixer，修复者不得同时是复验者）；regression_guard = 多拍 atop×并发同桶异目标读定向交织场景，由非修复者收 evidence。**M3 签核须披露**：R burst 重建 checker 类别当前处于假阳性缺陷态、多拍交织覆盖缺失，在守卫到位前须以 residual risk 或 `ACCEPTED@M<n>` 明账，不得静默。详见 doc/review/REV-015.md、doc/bugs/BUG-0034.md。M3-AT02 单拍绕过交付合法有效，不阻塞 M3。原症状描述：同一 slave 端口上 atop 原子读的 R burst 与同桶普通读的 R burst 在 slave 端口 R 通道上疑似 beat 交织，多拍时观测破损**（M3-AT02 落地卡构造 §6.5 跨方向假冲突时发现，中，本卡已用单拍构造绕过、不阻塞 M3-AT02 转绿）：M3-AT02 需在同一 slave 端口令一笔要求读响应的原子操作（`aw.atop`=ATOMICLOAD，目标 mstB）与一笔**低 `AxiIdUsedSlvPorts=3` 位相同、完整 ID 不同、目标 mstA≠mstB** 的普通读在飞重叠（spec §6.5 影子 AR 假冲突）。当两腿的读 burst 均为**多拍**（leg A read `AxLEN=3`、4 拍）时，slave 端口 R 通道观测破损：`slvport_agent.sv:521 MON_RNOAR`（R beat 找不到匹配的已接受 AR/atomic-load AW）、`axi_chan_sva.sv:252 SVA_RLAST_LEN`（RLAST 落在错误 beat index，独立于 UVM monitor 的 bind SVA 也报）、`scoreboard_refmodel.sv:931 SB_RBEATS`（普通读 id 的 R beat 数被截短、另有游离 beat）、atop 的 R 半永不返回（`SB_ATOP_DANGLING`，B seen R not）。`r_ready` 全程恒 1（`slvport_agent.sv:45`，无 TB 反压），故非 TB 驱动侧背压所致；`signal.changes` 亲测 `tb_top.slv_if[0].r_valid` 全程仅 2 次跳变（单一连续 valid 块）而 `r_id` 在块内跳变 3 次 ⇒ 两个不同 ID 的多拍 R burst 在**同一连续 valid 块内交织**——AXI4 禁止读数据交织（spec §1），若 DUT 真交织则属协议违反，若非则 UVM monitor 的「r_busy 到 r_last 不换 burst」重建假设有缺口。**DUT_BUG（真交织）vs TB_BUG（monitor/SVA 无交织重建缺口）未决**，须波形逐 beat decode 定性（value.at 在本 FSDB 上取值返回 unknown，未能逐 beat 落定，留待专卡）。**本卡处置（不改判决本体、不猜期望值）**：M3-AT02 改用**单拍**两腿（leg A read `AxLEN=0`、atop `AxLEN=0`），单拍 burst 每 beat 即 r_last、无交织窗口，§6.5 假冲突仍真实发生（`cg_atop_read_interaction.colliding_read_present` 命中、atop B+R 配对、零失配），完整满足 testplan M3-AT02 三条判据。多拍腿的破损不是 M3-AT02 的判据要求，故绕过合法、缺口另账。详见 doc/bugs/BUG-0034.md | 复现（需一行激励改动）：`tb/seq_lib.sv` `slvport_at02_seq` 把 leg A `p.len` 由 `axi_pkg::len_t'(0)` 改回 `axi_pkg::len_t'(3)`，再 `make run TEST=m3_at02_atop_read_test SEED=1` ⇒ UVM_ERROR≥8（MON_RNOAR/SVA_RLAST_LEN/SB_RBEATS/SB_ATOP_DANGLING），单拍则 0 | 待定：DUT slave 端口 R 通道对 atop 影子读 R 与同桶普通读 R 未按 burst 串行化（AXI4 §1 no-interleaving）⇒ DUT_BUG；或 slvport monitor（`:503-530`）/axi_chan_sva（`:252`）的 R burst 重建未处理交织 ⇒ TB_BUG。两说均须波形逐 beat 定性后方可定档（closer 独立复验：REV-015 终判 TB_BUG，修复 d7f5011 按逐拍 r_id 分流重建 R burst，四路 checker 归零、KILL 自证红→绿与基线逐位吻合、全回归 20/20，终态 CLOSED 同 BUG-0031 先例；row 在归档故手工回填，非经 make evidence 机械翻转） | d7f5011 | doc/evidence/v0.3.18/BUG-0034.log |
| BUG-0018 | CLOSED | TB | covergroup 接受时刻交叉 bin 成盲区（DV F-M2-08 落地后逐 covergroup 核对非空转时发现，低，非阻塞）：`slvport_monitor` 为携带完整 `wdata`/`wstrb` 而把写事务 request 观测发在 **W burst 的 `w_last`** 而非 AW 握手当拍，scoreboard 因此拿不到"AW 被接受"时刻的事件流、写记录迟到若干拍进 `or_open_q`/`worder_pend`，于是 `cg_stall.x_state_dir` 的 `[stalled][write]`、`[not_stalled_diff_direction][read]` 与 `cg_w_order.multi_source_contended` 在**各自对口场景**（M2-OR01/OR02、M2-WO01）恒空——但情形真的发生了：同批运行 `axi_xbar_stall_sva.sv:244` 写方向 cover 6/6 端口命中、`axi_xbar_worder_sva.sv:107` `compete_start` 命中 46 次，即 assert 维度已精确记录，空的只是 covergroup 侧重复交叉 bin。**判决路径不受影响**（判决锚点是完成序非接受时刻，见 BUG-0013/REV-006；同批 scoreboard+SVA 零失配、2143 assertions 0 failures）。代价在 M4：这几个 cross bin 会以"永远填不满"形式再现，建议 M3 前解决。修复方向（不预设实现）：给 scoreboard 增一路 AW/AR 接受事件与现有 `w_last` 观测并存；触及判决输入管线，须单独派卡 + 全回归。详见 doc/bugs/BUG-0018.md | `make run TEST=m2_or01_stall_test SEED=1`（对照：SVA 写方向 cover 6/6 命中 vs `cg_stall.x_state_dir[stalled][write]` 计数 0） | TB_BUG（观测相位，非 DUT）：写事务 request 观测发在 `w_last`，接受时刻事件流缺失致 §5.2/§5.5 分类偏相位 **REV-011 §3 裁决 → `ACCEPTED@M3`（M3 签核到期拦截；不阻塞 M2 签核、不阻塞 M3 派发）**：判决路径不受影响经本卡独立复核（`slvport_agent.sv:376/418` 的 `accept_time` 是真实 AW 握手时刻并原样转抄；`or_key` 含方向；同端口 W burst 按 AW 序 ⇒ 完成时更老同向记录必已入队），症状亦由已提交证据独立证实（M2-OR01 `x_state_dir=16.67%`/`cp_stall_state=33.33%`、M2-WO01 `cp_w_contention=50.00%`）。**定档 M3 而非 REV-010 §3.4 的 M4**：spec §0#4 的六类是 line+cond+fsm+tgl+branch+assert、**不含 covergroup**，M4 没有能接住它的机器 gate（discipline rule 4）；真正接住它的是 rubric 第 4 条的逐次人工抽查，而 M3 新场景共用同一条 monitor 事件流，拖过 M3 需在 M4 重采。scope 补：`cg_tx_limit` 写方向在飞计数同样在 `w_last` 采样（`scoreboard_refmodel.sv:421`）而偏低，同根因同守卫家族。到期验收形态（逐 test、不看 merged）见 doc/review/REV-011.md §3.3 **closer 独立复验订正（DV，非 fixer 实例；7a1c912 工作树独立重跑 + 独立推导，未采信 fixer 转述）**：REV-011 §3.3 到期判据「`cp_stall_state` 由 33.33% 上升」对 M2-OR01 **几何不可达**——`cp_stall_state` 仅 3 bin（`stalled`=SC_STALLED / `not_stalled_same_target`=SC_SAME_TGT / `not_stalled_diff_direction`=SC_DIFF_DIR，SC_NONE 有意无 bin，`functional_coverage.sv:87-98`），而 M2-OR01 构造（同 slave 端口、低 `AxiIdUsedSlvPorts` 位同、同方向对、译码命中**不同** master 端口）经 `scoreboard_refmodel.sv:502-510` 分类只能落 SC_STALLED（SC_SAME_TGT 需同目标、被"命中不同 master 端口"排除；SC_DIFF_DIR 需异方向兄弟并存、被"同方向对"排除）⇒ 该场景 `cp_stall_state` 结构天花板 = 1/3 = 33.33%，修复前已由**读腿**达标；写腿修复只体现在更细的 `x_state_dir[stalled][write]`（16.67%→33.33%）而非 `cp_stall_state`。**实质到期判据 = `x_state_dir[stalled][write]` 由空转非空**（读腿修复前既已把 SC_STALLED 记入 stall_state，故 stall_state 维度不动）。独立重跑 7a1c912 确认：M2-OR01 `x_state_dir=33.33%`/`cp_stall_state=33.33%`、M2-WO01 `cp_w_contention=100%`、两次运行 SB_SUMMARY mismatch=0 且 UVM_ERROR:0；15 场景全回归 PASS（UVM_ERROR/FATAL/assert-fail 全 0）、M2-OR03 守卫见证 192/192/264/264 + stack_diff 24/24 字节不变（当次 w_lost_now=456/r_lost_now=162，范围边界见证，非失配）。fixer 结论成立、closer 复核采信 | 7a1c912 | doc/evidence/v0.3.13/BUG-0018.log |
| BUG-0024 | WONTFIX | TB | `w_id_open` 是单 bit 而非计数器，同 ID 合法重叠时跟踪记录被提前清除（DV 修复卡落地 BUG-0023 同沿互斥保护时顺势发现，**非 lint 报出**、不在 BUG-0021 的 259 条内，中，非阻塞）：`tb/sva/axi_xbar_stall_sva.sv` 用单 bit `w_id_open[id]` 表示"该完整 ID 有事务在飞"，而 spec §5.2.4 明确允许同 ID 同方向**同目标** master 端口的多笔事务合法堆积——两笔共用一个 bit，第 1 笔的 B 返回即把 bit 清 0，第 2 笔虽仍在飞却已从跟踪表消失，针对它的乱序检查静默失效；读侧 `r_id_open` 同构。方向为**漏检（假绿侧）**。与 BUG-0023 的区别：那条是**同沿**写冲突（一进一出撞同一拍，已修），本条是**跨沿**容量不足（两笔同时在飞而只有一个槽位），同一数据结构、成因与修法均不同。**对已登记证据无影响**（只漏检不误判；七条证据的判决锚点是 scoreboard 完成序判据，独立于本 SVA 模块）——但须注意 **M2-TL02 的 SVA 侧保序检查在本条修复前是空转的**（该场景全程同一完整 ID 堆积 ⇒ 第二笔起都不在表内），其判决门本就锚 scoreboard + 达标 cover，结论不变，但"SVA 也过了"对 TL02 不构成独立证据。**REV-009 追加条件（rev 亲读 `stall_sva.sv` 全文而非采信本页摘要）：本条的守卫在当前 SVA 结构下不可观测**——`w_sibling_open()`/`w_reorder()`（L189-228）均以 `cand != id` / `cand != completing_id` **排除同一完整 ID**，模块只比较同低位桶的**不同**完整 ID（兄弟）；而本条的角落（单 bit 在 §5.2.4 同目标堆积下丢计数）只有当**兄弟 ID 查询受影响 ID 的状态**时才产生可观测差异，M2-OR03 的构造却是单一完整 ID、无兄弟。⇒ **仅把 `w_id_open` 改成计数器不会让该角落通过任何现有 assert/cover 变得可观测**，守卫落地须新增一个真正的自引用（per-full-ID）检测点，并沿用 BUG-0023 已用的定量前后证伪法。该条件**不阻塞** M2-OR03 的 DV 派发，但**阻塞本条转 CLOSED**。详见 doc/review/REV-009.md §2.3 / doc/bugs/BUG-0024.md **REV-010 §2.2 订正（实测 10 份日志）**：本条在 **M2-OR01/OR02 中一次也未发生**（`w_lost_now`/`r_lost_now` 恒 0/0），实际发生范围仅 TL01(2046/2040) / TL02(1302/1296) / OR03(456/162)——本行此前笼统写「多笔堆积下」易被读成 OR02 也受影响，特此收窄。且 **TL02 空转的归因被否证**：TL02 全程无任何兄弟完整 ID 处于 open（两组兄弟 cover 双双为 0），而 `w_reorder()` 只枚举 `cand != completing_id` ⇒ 候选集恒空 ⇒ 断言恒真空过，这是「场景只用单一完整 ID」的构造后果，**改成计数器不会使它变非空转**；「SVA 也过了对 TL02 不构成独立证据」的结论不变但原因不同。REV-010 §4 G4 给出两条可接受出路：(a) 让跟踪表表达「同完整 ID N 笔在飞」→ CLOSED；(b) 正式收窄该 SVA 模块判决范围至「每完整 ID 至多一笔」并把 N≥2 明文交给 scoreboard 队列判据 → WONTFIX。 | `make run TEST=m2_or03_guard_test SEED=1` → 新增自引用见证 cover `w_lost_now`/`r_lost_now` 命中 456/162（2026-07-28 DV M2-OR03 卡起本条首次具备仿真可观测复现；同见证在 m2_tl01/m2_tl02 SEED=1 下亦命中 2046/2040、1302/1296；修复后须归零） | TB_BUG（建模假设与 spec 冲突）：跟踪表隐含"每完整 ID 至多一笔在飞"，与 spec §5.2.4 允许的同目标堆积冲突，第二笔起不入候选集 **REV-011 §2 裁决 → 择定 REV-010 G4 路线 (b)（正式收窄判决范围），状态 `ACCEPTED@M3`（不落 WONTFIX）**。择 (b) 的依据：`doc/design-prompt/sva_bind.md` C3.2 原文规定的就是单槽模型（"若该 key 同方向已有记录则不重复登记"）⇒ 走 (a) 是**改设计**而非修 bug；且 §5.2.3 完成序判决在 `scoreboard_refmodel.sv` 已是每事务一条的队列（`:137/:413/:649-668`，REV-010 V7 已证伪其非空转），(a) 等于二次实现；REV-010 §2.2 实测 `w_lost_now` 在 OR01/OR02 恒 0 ⇒ (a) 在现有场景集上买到的额外检出为 0。**不落 WONTFIX 的依据：本卡推翻了"只漏检、不会假红"这一记载**——登记侧对同完整 ID 覆写 `w_id_seq`（`stall_sva.sv:129-134`）使表内留下**最新**一笔的序号，而实际完成的是**最老**那笔，`w_reorder()` 的"更老"门槛被抬高，构造 `AW X→A(seq0) / AW Y→B(seq1) / AW X→A(seq2) / B(X 的 seq0 笔)` 即报假红。⇒ (b) 必须让判决在其声明范围之外真正解除武装，非纯文档改动，仍有实质工作 ⇒ 按 REV-010 G1 红线不得用终态表达"以后再做"。假红不制造假 PASS，故"对已登记证据无影响"结论不变。到期验收 b-1~b-4 见 doc/review/REV-011.md §2.3。 **【closer 独立复验 2026-07-29 → WONTFIX 终态】** 独立于落地 (b) 修复的 fixer 实例（closer≠fixer 硬不变量）的复验卡，亲读代码 + 独立重跑（未采信 fixer 转述数字）确认 b-1~b-4 齐备，据 REV-011 §2.3 本条以 WONTFIX（范围声明为 rationale）落终态：**b-1** 范围声明——`tb/sva/axi_xbar_stall_sva.sv` 头注 L51-69 与 `doc/design-prompt/sva_bind.md` C3.2(L119-126)/C4.1(L207-215) 均写明判决范围「每完整 ID 至多一笔在飞」并指明 N≥2 交 `tb/scoreboard_refmodel.sv` C5.1/C5.2 承担；**b-2** 范围外真解除武装——`w_reorder`/`r_reorder`（L344/L364）新增**独立于** is_err（L329/L357）的早退分支 `w_n[]/r_n[] >= 2`（依模块自有在飞计数 L238-279，非"未登记/陈旧默认值恰好为假"），二排除并存互不覆盖；`make run TEST=m3_or05_range_test SEED=1` 下 `SVA_OR_W_REORDER`/`SVA_OR_R_REORDER` 命中 **0**、2143 assertions 0 failures、UVM_ERROR=0；**b-3** 范围边界见证——同 run `w_lost_now`=**144**(24×6)/`r_lost_now`=**138**(23×6) 非 0、据实报出（范围边界被真实触达），scoreboard `stall(C5.1/C5.2) violations=0`、route/resp/worder mismatch=0；**b-4** 全回归 **11/11 PASS**（m1_01/m1_02/m2_or01/m2_or02/m2_cfg01/m2_or03/m2_at01/m2_tl01/m2_tl02/m2_wo01/m3_or05，各 SEED=1，UVM_ERROR=0、0 断言失败）；**历史守卫未受损**——m2_or03 collide 192/192、264/264 + stack_diff 24/24 + w/r_lost 456/162 与文档一致，m3_cfg02 livev1 6/6、m3_or04 errbucket 6/6 且 reorder 判决恒 0。 | - | - |
| BUG-0025 | CLOSED | TB | 译码未命中的事务不入跟踪表，其完成不参与乱序检查（DV 分诊 BUG-0021 时读码发现，**非 lint 报出**，低—但 M3 前必须处理，非阻塞）：`tb/sva/axi_xbar_stall_sva.sv` 的登记条件带 `aw_hit`/`ar_hit`（地址译码命中），走 default port / error slave 的事务因此从不进在飞集合；而注销条件（B/R 握手）**不带**对称过滤，未登记事务完成时读到未初始化的 `w_id_seq`（X）⇒ 序号比较恒假 ⇒ 该完成不参与任何乱序检查。安全但漏检（不会假红）。**当前严重度低**（M0/M1/M2 全部场景均为译码命中路径，本条从未被触发），**但 M3 里程碑的主题正是"错误路径/decode error"**——届时它会从理论漏检变成真实盲区，且盲区恰好落在该里程碑要验的东西上，故须在 M3 场景派发**之前**处理。修法前置：须先确认 spec §5.2 的保序约束是否适用于译码未命中事务；若 spec 未明确规定，本条应先走 SPEC_ISSUE 仲裁再定修法，不得由 DV 自行取舍（discipline rule 1）。详见 doc/bugs/BUG-0025.md **REV-010 §3.4 订正：本条范围被低估**——`stall_sva.sv:99-100` 的 `decode_mst_port(..., 1'b0, ...)` 把 `en_default` **硬编码为 0**，故丢弃的不止 error slave，还含 spec §3.3.2 的 **default master port 路由流量**。二者 spec 地位不同须拆开：**default port 半边 = 纯 TB_BUG 无需仲裁**（它是真实 master 端口，§5.2.1 直接适用）；**error slave 半边 = 真 SPEC_ISSUE 须先仲裁**（§5.2.1 以 master 端口为单位，§4.1 的 per-port `axi_err_slv` 不是外部 master 端口，§4.5 只说遵循 AXI4 一般规则 ⇒ spec 对「error slave 响应能否越过同 ID 同向、目标为正常 master 端口的更老响应」**未作定义**）。**归属裁决：SPEC_ISSUE 半边必须在 M3 场景被设计之前（不是派发之前）完成仲裁**——M3 判据形态取决于仲裁结论。另：其修复卡的守卫**不能**复用 `w_lost_now`（REV-010 §2.2 已证其对本条不敏感），必须新建。 | `make run TEST=m2_cfg01_reconfig_test SEED=1`——batch 2 每个 slave 端口各发一笔译码未命中读（`0x9000_0000+port*0x100`，`seq_lib.sv:950-952`）经 default port 承接，stall_sva 确实丢弃了这 6 笔（**订正 REV-011 §5.2(2)(i)：此前记作「不可触发」是错的**；该运行兄弟集恒空故无可观测差别，非「没发生」） | TB_BUG（登记/注销条件不对称）：登记带 `aw_hit` 过滤而注销不带，未登记事务完成时 seq 为 X、比较恒假 **REV-011 §1：SPEC_ISSUE 半边已仲裁完毕（不再是 open 的 spec 债，M3 场景设计前置解除）；行状态 → `ACCEPTED@M3`**。裁决分三层：(1) **default port 半边**——它是真实 master 端口（xbar.md L35 "routed to the default master port instead of the decode error slave"、spec §3.3.2），§5.2.1~§5.2.4 原样适用，纯 TB_BUG；(2) **完整 ID 维度可断言**（**与 REV-010 §3.4 的"未定义"读法分歧**）——crossbar 的 slave 端口本身是 AXI4 slave 接口（spec §1、xbar.md §开篇），xbar.md L86 原文即写明 "AXI transactions with the same ID and direction must remain ordered"，§4.5 又把 err_slv 响应显式拉回 AXI4 一般规则 ⇒ **同一 slave 端口上完整 ID 相同、同方向的事务，其 B/rlast 完成序须与接受序一致，与它被路由到 master 端口 / default port / err_slv 无关**，checker 必须把未命中事务纳入该维度；(3) **低位桶维度（完整 ID 不同、其一走 err_slv）确属未定义**——§5.2.1 以"不同 master 端口"为条件而 §4.1/xbar.md L33 把 err_slv 定为内部模块，demux.md/mux.md 对 err_slv 一字未提 ⇒ 不得断言（无来源且会在 M3 假红，同 BUG-0013/REV-006 先例），须显式排除 + 非判决 cover + 列上游确认项。条款提案 P-REV011-1（新 §5.2.6）/ P-REV011-2（§4 第 6 条）交 orch 应用并重 pin。**两处记载订正**：(i) "M0/M1/M2 全场景均为译码命中、本条从未触发"**错**——M2-CFG01 batch 2 每端口各发一笔未命中读 `0x9000_0000+port*0x100`（`seq_lib.sv:950-952`）走 default port，scoreboard 正确路由（`route: match=30 mismatch=0`）而 stall_sva 确实丢弃了这 6 笔（该场景兄弟集恒空故未受损）；(ii) "seq 为 X ⇒ 比较恒假"**错**——`int unsigned` 是 2-state 缺省 0，且同完整 ID 曾登记过时读到的是**陈旧**非零 seq/tgt（reset 只清 `*_id_open`，`stall_sva.sv:120-127`）、`w_reorder()` 又不检查完成侧自身 open 位 ⇒ 可产生无来源假红，安全性论证须重写。到期验收三判据见 doc/review/REV-011.md §1.4 | 482a47e | doc/evidence/v0.3.10/BUG-0025.log |
| BUG-0030 | WONTFIX | TB | xdebug NPI 初始化对**四项**环境变量敏感，`LD_LIBRARY_PATH` 尾部空元素即失败（DV 修复 BUG-0020 后验证 xdebug 时逐项二分定位，低，非阻塞）：补上 FSDB 后 `session.open` 仍可能 `has_fsdb:true` 却 `npi_init.failed`。实测四项同时敏感——`VERDI_HOME` / `VCS_HOME` / `LM_LICENSE_FILE` 必须有值，且 `LD_LIBRARY_PATH` 必须**恰为** `$VERDI_HOME/share/PLI/VCS/LINUX64`：尾部空元素（`…LINUX64:`）即失败，**而这正是 `scripts/make/vcs-2018.mk:36` 的 `:=` 拼接写法在父变量为空时的展开形态** ⇒ 从 Makefile 继承环境去调 xdebug 时默认就踩中。**与 BUG-0020 并列而非重合**：那条断言「根因是项目侧不产波形」成立但**不完整**，补上波形后仍有本条这堵墙，两条叠着、先撞哪条取决于先补哪个。不是可修缺陷而是环境使用约束，登记目的是让后续每个用 xdebug 的卡不再重新二分定位。绕法（已写进 `sim/Makefile` `verdi:` 注释与 BUG-0020 `## rerun`）：用 `env -i` 起干净环境并显式给全四项。实测通过后以 `scope.list path=tb_top` 复核 session 可用（22059 信号，含 `tb_top.i_xbar_dut.*`）。详见 doc/bugs/BUG-0030.md | `export LD_LIBRARY_PATH="$VERDI_HOME/share/PLI/VCS/LINUX64:"` 后调 `$XVERIF_ROOT/tools/xdebug <request.json>` → `npi_init.failed`；去掉尾冒号且其余三项齐备 ⇒ `ensure_session.success`（lifecycle 在 `~/.xdebug/engine/sessions/<id>/logs/lifecycle.ndjson`） | TOOL_ENV：动态链接器把 `LD_LIBRARY_PATH` 的空元素解释为当前目录，NPI 初始化在该形态下拒绝继续；make 的 `:=` 拼接惯用法在父变量为空时天然产生尾部空元素 **终态 WONTFIX（同 BUG-0017 先例）**：本条不是可修缺陷而是**环境使用约束**——`$XVERIF_ROOT` 与 Verdi 均为外部安装，`scripts/make/vcs-2018.mk` 是 pinned snapshot 不可本地改，本仓库无可修对象；成本/收益判定停手，**不是「以后再做」**（REV-010 立下的红线）。绕法（`env -i` 显式给全四项）已实测有效并写入 `sim/Makefile` `verdi:` 注释 + BUG-0020 `## rerun`，守卫为详情页的 checklist。已随 FB 回流告知框架侧：pinned snapshot 里 `LD_LIBRARY_PATH := …:$(LD_LIBRARY_PATH)` 的惯用写法会让任何从 make 环境调 NPI 类工具的项目踩同一个坑。 | - | - |
| BUG-0033 | SPEC_CHANGED | spec | **err_slv 译码错误读响应数据值与 spec §4.4 矛盾（doc-vs-RTL）**（DV 落地本卡 M3-DE01/DE02/OR04 decode-error checker 首次仿真时实测发现，中，**阻塞 M3-DE01 crit2 / M3-DE02 未使能端口读 / M3-OR04 读镜像转绿**，与本卡主题 BUG-0025/0031 保序无关）：spec §4.4（蒸馏自 `axi_xbar.md` L33「read response beat data is `32'hBADCAB1E` (zero-extended ...)」）规定译码错误读每 beat 数据 = `32'hBADCAB1E` 零扩展至 `AxiDataWidth=64` = `64'h00000000_BADCAB1E`（上 32 位 0）；DUT 的 `axi_err_slv`（`axi_xbar_unmuxed.sv:195/238` **未覆写** `RespData`）用默认 `RespData=64'hCA11AB1EBADCAB1E`（`axi_err_slv.sv:25`，`RespWidth=64` 无零扩展），实测 `r.data=64'hca11ab1ebadcab1e`——上 32 位 `0xCA11AB1E≠0`。resp 码 DECERR、beat 数 `AxLEN+1`、`RLAST`、下 32 位 `0xBADCAB1E`、响应回送**全部合 spec**，唯上 32 位违反「零扩展」。checker 期望值按 spec §4.4 推导（`ERR_RDATA=data_t'(32'hBADCAB1E)`）故 `SB_DECERR_RDATA` 见红——**checker 按 spec 正确捕获 DUT/文档偏差，非 checker bug**；DV 不得抄 RTL 值入 checker（spec-from-RTL 红线）、不得单方裁决（rule 1）、不得降级为 warning 求绿（REV-011 §4 G1）。修复方向（须 rev 仲裁）：大概率 SPEC_CHANGED（同 BUG-0016，以 RTL 为准校正 spec §4.4 为 `64'hCA11AB1EBADCAB1E`），或取 DUT_BUG 走上游 issue。详见 doc/bugs/BUG-0033.md | `make run TEST=m3_de01_decerr_test SEED=1` → `[SB_DECERR_RDATA] got data='hca11ab1ebadcab1e expected data='hbadcab1e`（12 处，全部且仅为读数据上 32 位；resp/beat/routing 零失配） | SPEC_ISSUE（doc-vs-RTL）：xbar.md L33「zero-extended」把 `32'hBADCAB1E` 上扩为 0，与 `axi_err_slv` 默认 `RespData=64'hCA11AB1EBADCAB1E`（xbar 未覆写）矛盾；spec §4.4 忠实蒸馏了（与 RTL 不符的）文档 **REV-014 终判 → SPEC_CHANGED**：taxonomy = SPEC_ISSUE 成立（doc-vs-RTL contradiction，同 BUG-0016 家族），**不改判 DUT_BUG**——AXI4 不规定错误响应 `RDATA` 值（don't-care），DUT 吐 `64'hCA11AB1EBADCAB1E` 不违反任何显式协议条款，仅与上游自家文档 L33 矛盾；`RespData` 默认魔数是刻意标记常量（正面证据表明 RTL 常量即意图，散文陈旧）。**不走 P-xxx 补丁**（未签 DUT_BUG、行为协议合法）。处置：提案 P-REV014-1 校正 spec §4 clause 4 为 err_slv 默认 `RespData=64'hCA11AB1EBADCAB1E` 按 `AxiDataWidth` 零扩展/截断（基线=`64'hCA11AB1EBADCAB1E`，宽度参数化保留文档正确的适配机制、仅校正基数值），标注来源=RTL。下游：refmodel `ERR_RDATA=data_t'(32'hBADCAB1E)`→`data_t'(64'hCA11AB1EBADCAB1E)`（从 pinned spec 推导，非抄 RTL）；testplan M3-DE01 crit(2) 措辞同步。衔接：本条是 M3-DE01/DE02/OR04 读侧转绿的唯一障碍，修法应用后 BUG-0025 crit2/crit3 读侧、BUG-0031 crit6 全回归的"读侧转绿"门槛具备满足条件，绿由 `make evidence` 背书。与 BUG-0032（许可来源沉默→环境约束）**不同处置**：本条许可来源明确但与 RTL 矛盾→以 RTL 校正 spec。详见 doc/review/REV-014.md | - | - |
| KILL-0001 | KILL | TB | **注伤自证（M3 decerr-rdata checker 类别，CLAUDE.md 不变量 5，本仓库 M3 起生效）**：`SB_DECERR_RDATA` 见证（`tb/scoreboard_refmodel.sv` `ERR_RDATA` 常量）的可证伪性自证——植入缺陷→红→恢复→绿。把 `ERR_RDATA` 从校正值 `64'hCA11AB1EBADCAB1E`（pinned spec §4.4，REV-014/BUG-0033）人为改回高 32 位为 0 的错误期望值 `64'h00000000BADCAB1E`，重跑 M3-DE01/DE02/OR04 SEED=1，`SB_DECERR_RDATA` 分别见红 **12 / 3 / 18** 处（落 BUG-0033.md §scope 基线区间 12 / 3-4 / 18-19）；改回校正值后三条同 SEED 复跑均归零、UVM_ERROR=0。三条场景绿由 `make evidence` 背书（doc/evidence/v0.3.9/M3-{DE01,DE02,OR04}.log）。实测数字见 doc/bugs/BUG-0033.md `## rerun` 段 | `make run TEST=m3_de01_decerr_test SEED=1`（`ERR_RDATA` 高 32 位改 0 时 `[SB_DECERR_RDATA] got 'hbadcab1e expected 'hca11ab1ebadcab1e` 12 处；改回归零） | 自证记录（非缺陷）：checker 期望值从 pinned spec §4.4 推导、非抄 RTL；击杀证明 `SB_DECERR_RDATA` 能真实见红、非空转恒真 | - | doc/evidence/v0.3.9/M3-DE01.log |
| BUG-0029 | CLOSED | TB | `make evidence BUG=` 无法承载非仿真类复验，关闭件与实质证据脱节（REV-010 执行 12 条终态裁决时发现，低，非阻塞）：BUG-0014/0019 是**编译期**缺陷，复验判据是 `make lint` 输出（`Error-[SE]` 归零、`Top Level Modules:` 收敛、过滤逻辑 true-positive 分支工作），但机械关闭路径 `make evidence BUG=<ID> TEST=… SEED=…` **只能读仿真日志**——`svacheck.judge` 对无 UVM/VCS 横幅的日志直接 `NOSUMMARY` 退出。于是关闭件只能借一次**与缺陷无关**的通过运行，真正的复验证据只存在于 `doc/review/REV-010.md` 正文里，即 `verify_evidence` 列指向的文件**不是**证明该 bug 被修好的那份材料。本仓库此类占比很高（0014/0017/0019/0020/0022/0026/0028 七条的复验判据都不是仿真结果）。**已关闭条目的结论本身成立**（REV-010 做了实质复验并逐项记录），受损的只是可追溯性。`scripts/evidence.py` 是 pinned snapshot、本地不可改，已登记 FB-16 回流框架。守卫：FB-16 落地前，任何非仿真类缺陷转 CLOSED 时须在 root_cause 或详情页 `## rerun` 写明实质复验的记录位置，签核抽查这类条目不得只看 `verify_evidence` 指向的文件。详见 doc/bugs/BUG-0029.md | `make evidence BUG=BUG-0014 TEST=<任一编译期复验> SEED=1`（无仿真日志可喂；改喂 `sim/out/lint.log` 会被 `svacheck.judge` 以 `NOSUMMARY` 拒收） | TOOL_ENV：`make evidence BUG=` 的设计前提是「bug 由某 TEST+SEED 仿真复现、修复后同一 TEST+SEED 跑通」，不覆盖复验判据为编译期/工具输出的 TOOL_ENV 与门禁类缺陷。**已由框架 0.5.0 修复并据此关闭**：新增 `make evidence BUG=<ID> CMD='<复验命令>' EXPECT='<签名正则>'`，当场执行命令、双重 fail-closed（非零退出拒收 + 输出无签名匹配拒收），`EXPECT` 必填＝关闭者必须说出「什么输出证明修好了」。**本条的关闭本身就是该形态的首次真实运行**（用一条真正的非仿真复验，关掉「非仿真复验无法机械关闭」这条 bug）⇒ 详情页 `## regression_guard` 的临时守卫**自此退役**。详见 FB-16 | iverif-workflow@fb2c193（框架 0.5.0，FB-16 回流；修复不在本仓库——`scripts/evidence.py` 是 pinned snapshot，经 `make fw-pull` 落地 0.5.2） | doc/evidence/v0.2.1/BUG-0029.log |
| BUG-0028 | CLOSED | TB | **`regress.list` 长期缺 7 个 M2 场景，回归门禁不覆盖 M2**（DV M2-OR03 卡执行"全回归无伤"验收项时发现，中，非阻塞）：本卡之前 `sim/regress/regress.list` 只有 3 条（`upstream_sanity` + M1 两条），M2 的七个场景自 2026-07-28 落地起**从未进入回归清单**——`make regress` 报 3/3 PASS 看起来一切正常，实际整个 M2 判决链从未被回归门禁覆盖过，即**绿灯的覆盖范围远小于它给人的印象**。成因链：M0 期清单只有 1 条；M1 签核前补入 2 条是**人工想起来补的、非任何机制提示**；M2 七个场景各自落地时，DV 卡验收标准都是"本场景 PASS + `make evidence` 登记"，**没有任何一条要求同时入回归清单**⇒ 七个场景全部合规落地、全部有 PASS 证据，而清单一行未增，`make regress` 持续报"3/3 PASS"、**分母静默缩水**。与 BUG-0022 / FB-11 / FB-12 / FB-14 同族（通过信号与实际覆盖不符），差别在于前几条是"检查没执行却报通过"，本条是"检查执行了但只覆盖了一小部分"。已修（DV M2-OR03 卡同批补入 8 条，`make regress` 3/3 → **11/11 PASS**，摘要登记 `doc/evidence/v0.2.0/result_summary.txt`）；**但补行不是守卫**——真正的守卫是"回归清单必须覆盖 testplan 中每一条 ✅ 场景、差集非空即失败"的完整性检查，详见详情页 `## regression_guard`。**复验待非修复方执行**（closer ≠ fixer）。详见 doc/bugs/BUG-0028.md | `wc -l sim/regress/regress.list`（修复前 3 条）对比 `grep -c "✅" doc/testplan.md`（同期 10 条已 ✅ 场景）——差集即本条 | TB_BUG（回归配置缺口，非 `make regress`/`regress.py` 缺陷）：DV scenario 卡的收版清单从不要求"本场景已入 regress.list"，清单分母全靠人工想起来补 | cbc77f3 | doc/evidence/v0.2.0/BUG-0028.log |
| BUG-0020 | CLOSED | TB | 仿真从不产 FSDB，xdebug 波形因果能力在本仓库无法使用（orch 在 BUG-0017 之后逐工具实测 xverif 体系时发现，中，非阻塞）：对 `out/simv.daidir` 调 `xdebug session.open` 报 `SESSION_UNHEALTHY: child_exited`；engine lifecycle 日志（`~/.xdebug/engine/sessions/<id>/logs/lifecycle.ndjson`）定位到 `server.transport_config` 的 `has_fsdb:false` → `npi_init.begin` → **`npi_init.failed`**。**根因非 EDA 版本**（与 BUG-0017 不同类）：`xdebug-engine` 是链接 Verdi **C API** `libNPI.so`/`libnpiL1.so`/`libvfs.so` 的二进制、`ldd` 全解析，不经 `pynpi` 路径；决定性对照是**同一台 VM、同一个 Verdi** 于 2026-07-09 对姊妹仓库 `ppa-lite-copilot` 的 `sim/out/wave.fsdb` 成功 `ensure_session.success`——即 xdebug 在本机实证可用，其 NPI init 需要 FSDB，只给 daidir 起不来。本仓库**没有任何目标产生 FSDB**：`sim/Makefile` 的 `verdi:` 目标引用了 `$(OUT)/wave.fsdb` 但该文件从不被生成，`sim/out/run.ucli` 全部内容是 `run; exit`、无 `fsdbDumpvars`。后果：任何需要回到波形溯因的工作（BUG-0018 那类观测相位问题为典型）无波形可查，只能退回日志推理，与 CLAUDE.md §4「优先用 xverif 取事实、而非纯模型推理」冲突。修复方向（不预设实现）：给 `sim/Makefile` 增一条**可选**波形 dump 路径（如 `make run WAVE=1`），**不得**成为 `make run`/`make regress` 默认路径（证据主链路成本不因调试能力抬高）；`sim/Makefile` 不在框架快照内、可改。详见 doc/bugs/BUG-0020.md | `make run TEST=m2_or01_stall_test SEED=1 && ls sim/out/wave.fsdb`（波形文件不存在；随后对 daidir 调 xdebug `session.open` 必得 `SESSION_UNHEALTHY: child_exited`。任意 TEST，无需特定 SEED） | TOOL_ENV（项目侧缺口，非环境能力缺失）：`sim/Makefile` 无 FSDB dump 路径，xdebug NPI init 缺波形输入。**orch 独立复验通过（非修复方，2026-07-28）**，实质复验记录见详情页 `## rerun` 的「orch 独立复验」小节——按 BUG-0029 守卫，`verify_evidence` 指向的 `.log` 对编译期/工具类缺陷是形式件，实质证据在详情页。 | 56b0eeb | doc/evidence/v0.2.0/BUG-0020.log |
| BUG-0022 | CLOSED | TB | **`make lint` 增量编译命中时假绿**（DV 分诊 BUG-0021 时首条命令即撞上，**高**，非阻塞但威胁门禁可信度）：同一份代码连跑两次 `make lint TEST=m2_cfg01_reconfig_test`，第 1 次（强制重编）`../tb/` 命中 259 条 exit 2、第 2 次（增量命中）输出 `lint clean within ../tb/ scope` exit 0——代码一字节未改而门禁结论翻转。根因：VCS 设计未变时走增量路径、不重新分析源码、**不产生任何 `Lint-[...]` 行**，但仍以 `-l` 覆写 `sim/out/lint.log`；`sim/Makefile` 的判据（日志中是否有 `^Lint-\[` 且紧邻行落 `^\.\./tb/`）在空日志上一无所获 ⇒ 把"**这次根本没执行 lint**"误判为"lint 干净"。即门禁**不区分"真干净"与"根本没检查"**，取了乐观一侧。与 discipline rule 2 的 gate 例外条款正面相关：会假绿的门禁比没有门禁更危险——它给出未经验证的通过信号。修复方向（不预设实现）：让判据能证明"本次确实执行了 lint"，而非仅"没看到告警"。**硬性红线**：正确修法是让门禁更严、不是更松，不得靠放宽过滤范围"修好"它。临时缓解：跑 lint 前 `rm -f sim/out/simv_lint.daidir/.vcs.timestamp`（已 gitignore；DV 卡结束时已删除以免下一位撞上）。详见 doc/bugs/BUG-0022.md | 连跑两次 `make lint TEST=m2_cfg01_reconfig_test`（第 2 次即假绿；若第 1 次也是增量命中，先删 `sim/out/simv_lint.daidir/.vcs.timestamp`。无需 SEED） | TB_BUG（门禁自身判据缺陷，非 VCS 缺陷）：增量编译下"lint 未执行"与"lint 执行且干净"在日志上同形态，判据无法区分而取乐观侧。**orch 独立复验通过（非修复方，2026-07-28）**，实质复验记录见详情页 `## rerun` 的「orch 独立复验」小节——按 BUG-0029 守卫，`verify_evidence` 指向的 `.log` 对编译期/工具类缺陷是形式件，实质证据在详情页。 | 56b0eeb | doc/evidence/v0.2.0/BUG-0022.log |
| BUG-0027 | CLOSED | TB | scoreboard §5.2.3 完成序判据把同一完整 ID 的完成响应认领成**最新**那笔记录，§5.2.4 合法堆积被误判为乱序（DV 落地 M2-OR03 守卫场景首次运行发现，中，非阻塞）：`tb/scoreboard_refmodel.sv` 完成时刻遍历 `or_open_q[k]` 找同 id 记录时**不 break**，`this_idx` 停在最后一条匹配上；M2-OR03 的 `[X→A ×14, X→B ×4]` 同完整 ID 队列里，每一笔 X→A 的完成都被认领成异目标的 X→B 记录，再与"更老仍在飞的 X→A"比较 ⇒ 一次运行 **336 条 SB_OR_REORDER 假红**。同一运行 route 864/864、resp（读数据逐 beat 预测）864/864、SVA 2143 条 0 失败 ⇒ DUT 转发序/完成序正确，属 checker 归属逻辑缺陷。修法：取**最早匹配**（同 ID 同向响应按接受序返回，spec §1/§5.2.3；`tb/slvport_agent.sv` 的 R 配对早已用此规则）。同完整 ID **内部**先后本判据不判（B 只带 id+resp），该向由 `SB_ROUTE`（转发序）与 `SB_RDATA`（读数据钉回具体那笔）承担；跨不同完整 ID 的 §5.2.3 检测不变。详见 doc/bugs/BUG-0027.md | `make run TEST=m2_or03_guard_test SEED=1`（修复前 336 条 SB_OR_REORDER；route/resp/SVA 全绿） | TB_BUG：完成记录认领取最后一条匹配而非最早一条，同完整 ID 多笔在飞且目标不一致时必然误判 | cbc77f3 | doc/evidence/v0.2.0/BUG-0027.log |
| BUG-0014 | CLOSED | TB | 工具/环境缺口（DV M2-OR01/OR02 交付前 `make lint` 核对新发现，低，非阻塞）：`sim/Makefile` 的 `lint:` 目标自行拼接 VCS 旗标、未复用 `scripts/make/vcs-2018.mk` 的 `VCS_FLAGS_2018`，遗漏其中的 `-assert svaext`；`vendor/common_cells/src/clk_int_div.sv` 的 elaboration system task 用法因此在 vendor 解析阶段即报 `Error-[SE]`、编译整体中止，`lint:` 目标自身"仅 `../tb/` 范围内失败才判不合格"的过滤逻辑从未被执行到——`make lint` 对本仓库当前任何版本都会以同一签名失败，与被测改动内容无关（复现不需要本卡任何代码改动）。不阻塞 M2-OR01/OR02（`make run`/`make evidence` 主链路不受影响）；未派发修复（`sim/Makefile` 非框架快照、可修，但不在本 DV scenario 卡范围）。详见 doc/bugs/BUG-0014.md | `make lint`（任意 TEST，无需 SEED；纯编译期失败） | `sim/Makefile` `lint:` 目标手写的第二份旗标列表缺 `-assert svaext`，未复用 `VCS_FLAGS_2018` | 446eefe | doc/evidence/v0.2.0/BUG-0014.log |
| BUG-0016 | SPEC_CHANGED | SPEC | 事务数上限不被守（DV M2-TL01/TL02 落地首运行发现，中，非阻塞；**REV-007 改判 SPEC_ISSUE，DUT_BUG 不予签核**）：master 端口侧延迟不敏感在飞计数（AW/AR 接受 +1、B/R-last 离开 -1，`MuxAw` spill 在 mux 计数之后、`DemuxAw` spill 在 demux 核心之前，故 master 端口所见 ≤ demux 核心计数——两上限唯一可锁定的外部映像）**超出**文档承诺的 `MaxMstTrans=10` / `MaxSlvTrans=6`：TL01 单 slave→单 master 单桶（跨 4 兄弟 ID 铺开、每 ID ≤3）连发 12 笔，全部 12 同时在飞（越 10）；TL02 单 slave→单 master 连发 8 笔同全 ID，全部 8 在飞（越 6）。根因（读 vendor RTL 定位，未搬进 checker）：`axi_demux_simple.sv` L69 `IdCounterWidth=idx_width(MaxTrans)=clog2(10)=4`，`axi_demux_id_counters` L557/L603 `full=\|cnt_full=(&in_flight)` ⇒ 每桶真实封顶 `2^4-1=15` 非 10（xbar.md L46/demux.md L72 明述"at most/up to and including MaxTrans"，RTL 违反）；MaxSlvTrans 侧 mux 无 per-ID 计数器（BUG-0011），观测在飞受 demux 每桶上限（15）主导、非 ≤6，**证伪** REV-005 解锁的"≤6 绝不假红"监视器。**非 §7.4.5/BUG-0013 的接受边界 ±spill 效应**（那是边界时刻，本条是延迟不敏感计数本身越限）。同批运行 scoreboard 路由/数据/响应/完成全绿（144/96 笔零失配），功能无损、仅数值上限未守。过渡处置：txlimit SVA 的"≤ 上限"断言降级为非判决 cover + `SVA_TXLIMIT_OVER` uvm_info 见证（同 BUG-0013 先例），TL01/TL02 判决门改锚 scoreboard 正确性 + 达标 cover。详见 doc/bugs/BUG-0016.md | `make run TEST=m2_tl01_txlimit_test SEED=1`（+ `TEST=m2_tl02_slvtrans_test SEED=1`；降级前断言各见红 12 次，降级后 `SVA_TXLIMIT_OVER` 见证 + "exceeded ceiling" cover 复现前提） | **REV-007 裁决 = SPEC_ISSUE（改判 DUT_BUG，DUT_BUG 不予签核）**（rev 独立逐行亲读 vendored RTL+文档，成本阶梯逐级排除、"清空一切之后 DUT 仍未被归咎"）：DUT 未产生任何错误输出（同批 SB 全绿/UVM_ERROR 0，AXI 不规定在飞上限，计数器未真回绕），且 `MaxTrans` 在 RTL 中只进 `idx_width` 定宽、从不进值比较器、"full"=`&in_flight`（满量程 `2^⌈log₂MaxTrans⌉−1`，MaxTrans=10⇒15；2 的幂取值下反向低于 MaxTrans，如 8⇒7）——DUT 对 MaxTrans 语义持一个由结构完全支撑的另一读法（**计数器定宽参数**，非硬上限）；许可来源自身**三方矛盾**（demux.md L72/xbar.md L46-47/axi_pkg L489-494 无一与 RTL 吻合）⇒ SPEC_ISSUE 教科书签名。MaxSlvTrans 经 xbar.sv L141 连到 mux `MaxWTrans`（AW→W FIFO 深度）、非每 ID 在飞上限，mux 无 per-ID 计数器——本条**证伪** REV-005 的"≤6 绝不假红"监视器。三条替代解释全排除（无 MaxTrans 合法性检查⇒baseline 合法；无 10/6 处上层背压；`overflow` 仅计数达 16 置位、被 `&in_flight`=15 提前阻止）。处置：走 spec 修改记录+重 pin（**非** DUT 行为补丁，CLAUDE.md §6 在此加强）；原文/新文/理由+orch 应用范围见 REV-007 §5，上游若提为文档澄清（REV-007 §6）。**spec 编辑+重 pin+状态转 SPEC_CHANGED+suspect DUT→SPEC 已由 orch 应用**（同 REV-005/006 先例；doc/spec.md §2.1 `MaxMstTrans`/`MaxSlvTrans` 字段行 + §5.4.1/§5.4.2/§5.4.3/§7.4.5，Change record #5，sha `bfe8542b`）：§5.4.1 每桶上限由字面 `≤MaxMstTrans` 改为有效上限 `2^⌈log₂MaxMstTrans⌉−1`（基线 15）；§5.4.2 撤销"每 ID ≤ MaxSlvTrans"可断言上界并正式收回 REV-005 M2-TL02 监视器；§5.4.3 拒收门改锚有效上限、MaxSlvTrans 侧升级为"mux 无 per-ID 机制"已定结论；§7.4.5"上限最终被守"绑定有效上限公式。详见 doc/review/REV-007.md / BUG-0016.md ## fix | - | - |
| BUG-0015 | CLOSED | TB | 新增 ATOP SVA checker 采样语义缺陷（DV M2-AT01 bring-up 首运行发现，同卡内修复）：`tb/sva/axi_xbar_atop_sva.sv` C3.5 属性 2（spec §6.4 ID 唯一性）首版在 property 表达式里直接调用读取模块内跟踪数组的函数 `id_busy()`——SVA 中直接引用的信号按 preponed（沿前）采样，但 property 内调用的函数在 observed region 执行、读到的是**当沿 NBA 更新后**的现值，于是断言"看见"这笔 ATOP AW 自己在同沿的登记，每笔 ATOP 均假红（含复位后端口第一笔事务；18/18 假触发），同日志 scoreboard 全绿（pairs=18 零失配）指向 checker 自身。修复=谓词折叠为 `always_comb` 信号（`aw_id_busy_now`/`any_inflight_now`）供 property 直接引用以获 preponed 采样，assert 与"非空在飞"cover 同修。**未改 vendor/RTL**。详见 doc/bugs/BUG-0015.md | `make run TEST=m2_at01_atop_test SEED=1`（修复前版本：18 次 SVA_ATOP_ID_UNIQ 假红） | TB_BUG（SVA 采样语义误用，非 DUT）：property 内函数调用绕过 preponed 采样读到 observed region 现值 | 0e62eb8 | doc/evidence/v0.2.0/M2-AT01.log |
| BUG-0017 | WONTFIX | TB | 工具/环境能力缺口（DV F-M2-08 功能覆盖落地期发现，低，非阻塞）：`$XVERIF_ROOT/tools/xcov` 在本 VM 不可用——`session.open` 报 `NPI_INIT_FAILED: cannot import name 'cov' from 'pynpi'`，本 VM 的 Verdi O-2018.09-SP2 所带 `pynpi` 不含 `cov` 子模块（coverage NPI 在该版本尚无 Python 绑定；xcov README 的验证形态为 V-2023.12），与 CLAUDE.md §4「覆盖率核对优先用 xcov」的纪律冲突。对任何 vdb 同签名失败，与 coverage database 内容无关。**已验证绕法**（F-M2-08 卡据此产出全部 bin 级命中证据）：`make cov` + `urg -full64 -dir out/cov.vdb -format text -report out/urgText -metric group`，bin 级明细落 `sim/out/urgText/grpinfo.txt`；直接命令行调 urg 需自带 `VCS_HOME`/`LM_LICENSE_FILE`，走 `make cov` 则不必。边界不变：urg 同 xcov 只是观测手段，期望值仍只准从 `doc/spec.md` 推导。未派发修复（`$XVERIF_ROOT` 为外部安装、不在本仓库范围）；VM 升级 Verdi ≥V-2023.12 后应转 CLOSED。详见 doc/bugs/BUG-0017.md | `printf '{"api_version":"xcov.v1","action":"session.open","target":{"vdb":"out/cov.vdb"},"args":{"name":"cov0"}}' \| $XVERIF_ROOT/tools/xcov --once`（任意 TEST，无需 SEED；纯工具初始化失败） | TOOL_ENV：Verdi O-2018 的 `pynpi` 未导出 `cov` 子模块，xcov 无法初始化 NPI 会话 **REV-010 裁决 → WONTFIX**（rev 独立复核，成本/收益判定停手）：`$XVERIF_ROOT` 为外部安装，本仓库**无可修对象**；等效路径（`make cov` + urg）已产出全部 bin 级证据，**无验证能力损失**。守卫见详情页（后续任何需覆盖率事实的卡先读本条再决定路径）；VM 升级 Verdi ≥V-2023.12 后本条应重开为 CLOSED。 | - | - |
| BUG-0019 | CLOSED | TB | 工具/环境缺口（DV 自修卡修复 BUG-0014 时新发现，低，非阻塞）：`sim/Makefile` 的 `lint:` 目标从未传 `-top $(TOP)`（`compile:`/`run:` 均传），BUG-0014 的 `Error-[SE]` 中止点消失后，VCS 缺省对整份 flist 做多顶层枚举，撞上 `flist/dut.f` 里大量"设计上只应被父模块带参数覆盖例化、从不单独顶层展开"的 vendor 测试专用 wrapper（`axi_lite_regs_intf`、`axi_to_detailed_mem_intf`、`stream_watchdog` 等），报 `Error-[NOODV]`/`Error-[ZMMCM]`/`Error-[CEPO]`/`Error-[TCF-ICTC]`、命中 VCS 默认 10 条报错上限后编译整体以 exit 255 再次中止，`../tb/` 范围过滤逻辑仍未被执行到——此前一直被 BUG-0014 更早的中止点完全遮蔽，与 `-assert svaext` 无关，是独立成因。已随 BUG-0014 同一 diff 追加 `-top $(TOP)`（复现 compile:/run: 已有模式）：`make lint` 默认（`upstream_sanity`）0 error/0 warning 到达 grep 过滤；`make lint TEST=m2_cfg01_reconfig_test`（真含 `../tb/` 文件）同样 0 error 编译通过，过滤逻辑随即在真实 `../tb/` 范围内命中若干既有 `Lint-[SVA-UA]`/`Lint-[NS]`/`Lint-[WMIA-L]` 等告警（未在本条内处理，交由 orch 分诊是否另开卡）。详见 doc/bugs/BUG-0019.md | `make lint TEST=m2_cfg01_reconfig_test SEED=1`（任意会拉入 `flist/dut.f` 的 TEST，无需 SEED；纯编译期失败） | `sim/Makefile` `lint:` 目标缺 `-top $(TOP)`，VCS 默认对全量 flist 做多顶层枚举撞上 vendor 测试专用 wrapper 的未约束参数 | 446eefe | doc/evidence/v0.2.0/BUG-0019.log |
| BUG-0021 | WONTFIX | TB | `make lint` 门禁首次可用即恒红：`tb/` 范围 259 条既有 Lint 告警（orch 复验 BUG-0014/0019 修复时暴露，中，非阻塞）：两条修复清除了编译期中止点后，`../tb/` 范围过滤逻辑**第一次真正执行到**，随即在本仓库交付物内命中 259 条告警并 exit 1——门禁本身工作正常（true-positive 分支正确拦截，证明修复未放宽判据），但**本仓库当前代码过不了它自己的 lint 门**，`make lint` 对任何版本恒定失败（失败签名由"编译期中止"变为"tb/ 范围告警"，恒红未变）。按类：67 `Lint-[NS]` / 59 `Lint-[SVA-AECASR]` / 38 `Lint-[SVA-UA]` / 32 `Lint-[SVA-DIU]` / 32 `Lint-[SVA-CE]` / 13 `Lint-[ULCO]` / 9 `Lint-[SV-PIU]` / 6 `Lint-[WMIA-L]` / 3 `Lint-[SVA-NSVU]`；按文件集中于 `tb/sva/axi_chan_sva.sv`(81)、`axi_xbar_stall_sva.sv`(47)、`axi_xbar_txlimit_sva.sv`(33)、`slvport_agent.sv`(24)。**未分诊、未判严重度**——其中 `SVA-AECASR`（断言副作用影响仿真结果）/`ULCO`（位宽不等比较）/`WMIA-L`（赋值位宽失配）/`SVA-NSVU`（SVA 用非采样变量）共 81 条可能指向真实缺陷，须 DV 逐条读代码判定，orch 不做技术判断；尤其 `SVA-NSVU` 与已 CLOSED 的 BUG-0015（property 内函数调用绕过 preponed 采样）**同族失效模式**。修复前须先分诊；**硬性红线**：不得靠放宽 `lint:` 过滤范围/加豁免/摘 flist 文件来让 lint 通过（discipline rule 2 的 gate 例外条款）。分诊出的真缺陷若影响已登记 ✅ 场景判据，须重跑重登证据。**F1/F2/F3 共 11 条真缺陷已修（DV 自修卡 2026-07-28，见详情页 ## fix/## rerun；`SVA-NSVU` 3→0、`SVA-AECASR` 剩余站点全为 action block）；其余风格类未动、总数 259→267（4 条新增守卫 cover 的风格计数）⇒ 门禁仍恒红，本条保持 OPEN**。详见 doc/bugs/BUG-0021.md | `make lint TEST=m2_cfg01_reconfig_test`（任意拉入 `flist/tb.f` 的 UVM TEST，无需 SEED；**须先 `rm -f sim/out/simv_lint.daidir/.vcs.timestamp` 强制重编，否则撞 BUG-0022 假绿**；**默认 `make lint` 不复现**——`upstream_sanity` flist 走 `tb_upstream.f`、不含 `../tb/` 交付物，会给出误导性的 "lint clean within ../tb/ scope"） | TB_BUG（本仓库交付物代码卫生；工具链此刻正确，被报出的是我们自己的代码）：259 条告警此前被 BUG-0014/0019 两层编译中止点完全遮蔽。**已分诊（DV 分诊卡 2026-07-28，结论见详情页 ## rca 的 A–H 节）：81 条"需分诊"中真缺陷 11 条（F1 判决 property 内调函数 2 条 / F2 同问题落在 cover 6 条 / F3 action block 报错未 `$sampled` 3 条，全部集中于 `tb/sva/axi_xbar_stall_sva.sv` 与 `atop_sva.sv:154`）、风格 70 条、工具误报 0；178 条"大概率风格"全部核实为风格（`Lint-[NS]` 逐条 67/67 全查，其余抽样+反查）。真缺陷方向单侧（F1 只造假红、F2 只漏计），已登记 7 条 M2 证据与 testplan ✅ 全部仍成立、无需重跑**（论证：F1 的 post-NBA 与 preponed 差集只能新增 seq 更大的候选或清掉已被排除的自身表项 ⇒ 不能把真实乱序判成合规；7 条证据均 0 assertion failure ⇒ 无一条 PASS 被放过） **REV-010 裁决 → WONTFIX（附守卫改写条件，已履行）**：rev 实测残余 **285** 条（`Lint-[SVA-NSVU]` 已清零、真缺陷类未新增），清零需跨 13 份交付文件的装饰性改动（discipline rule 3 禁止）或本条自身红线禁止的类别豁免 ⇒ 成本/收益判定停手。**守卫已按 REV-010 §4 G3 改写并落地**：`doc/lint-baseline.md` 按 `类别 × 文件 × 行` 登记 285 条（225 唯一站点 / 8 类别）入版本控制，守卫改为「每里程碑强制重编 lint 与基线差分，新类别或新站点即须分诊」；**该守卫以 BUG-0022 修复为前提**（否则一次「没执行」会被读成「无新增」，同 BUG-0028 的分母缩水）。WONTFIX 的含义是「这 285 条装饰性告警不清」，**不是**「lint 这条线不管了」。 | - | - |
| BUG-0023 | CLOSED | TB | `axi_xbar_stall_sva.sv` 跟踪表同沿写冲突致新事务 open 记录被吞（DV 分诊 BUG-0021 时读码发现，**非 lint 报出**，中，非阻塞）：`always_ff` 内 AW 分支先写 `w_id_open[aw_id] <= 1'b1`、B 分支后写 `w_id_open[b_id] <= 1'b0`，同沿 `aw_id == b_id` 时按 IEEE 1800 §10.4.2"最后一次赋值生效"⇒ 新事务的登记被静默吞掉，此后该笔事务**永不进入在飞集合**、针对它的乱序检查永久失效；读侧 `r_id_open` 同构。**方向是漏检（假绿侧）**——不会造假红，只会让本该检出的乱序悄然通过，与 BUG-0021 分诊出的 F1（只造假红）方向相反，两者须分开处置。同族对照证明是疏漏而非有意：`tb/sva/axi_xbar_atop_sva.sv:113-120` 对同一模式写了显式互斥保护。**对已登记证据无影响**（M2-OR01/OR02 的 `drive_pair` 每对用两个不同完整 ID 且 `wait (b_seen>=2)` 才推进 ⇒ 同沿同 ID 不可构造；TL01/TL02/WO01 全为同目标场景 ⇒ 保序 property 恒真空过）。守卫须新建定向激励（现有 M2 场景恰好绕开这个角落，这正是它长期潜伏的原因）。详见 doc/bugs/BUG-0023.md | 无（当前激励集不可构造；本条由代码结构分析确立，守卫激励须随修复卡新建——见详情页 ## regression_guard） | TB_BUG（并发角落丢记录）：`always_ff` 内同一变量多次非阻塞赋值，同沿 `aw_id == b_id` 时后写的注销覆盖先写的登记 | c29bede | doc/evidence/v0.2.0/BUG-0023.log |
| BUG-0026 | CLOSED | TB | 已登记证据被**静默截断**：SVA 逐条命中数进不了 `## Key check lines`（DV 修复卡执行"报告 cover 命中数变化"验收项时发现，中，非阻塞）：`scripts/evidence.py` 的 `KEY_LINE_RE` 含 `match`，于是 `-assert verbose` 为每条 assertion/cover 打印的行全部算关键行（本仓库每次运行数百条），`KEY_LINES_MAX=30` 的一刀切上限被**最先出现的模块**（`axi_xbar_txlimit_sva`）整段吃掉，其余模块一条不进；`axi_xbar_stall_sva` 的 6 条 cover 命中数**从来就不在证据文件里**，只能回源 `sim/out/*.log`。且截断按文件出现顺序、**不留任何标记** ⇒ 评审无法区分"这条 cover 不存在"与"它被截断了"，里程碑签核读证据会被误导。**判定本身不受影响**（`svacheck.judge` 两腿制读源日志全文而非摘要段，七条证据 PASS 结论仍成立），受影响的是人工复核的可用性。`scripts/evidence.py` 是 pinned snapshot、本地不可改，已登记 `doc/fw-feedback.md` FB-13 回流框架（三条建议：关键行分类配额 / SVA 逐条行按文件归并 / 截断处写 `... (K lines truncated)` 使截断可见）。本仓库侧不追溯重写已登记证据（同 BUG-0008 先例）。详见 doc/bugs/BUG-0026.md | `grep -A40 "## Key check lines" doc/evidence/v0.2.0/M2-OR01.log \| grep -c "axi_xbar_stall_sva"` → 0（同一次运行的 `sim/out/m2_or01_stall_test_1.log` 里有 6 条 cover 命中） | TOOL_ENV（框架脚本局限）：`KEY_LINE_RE` 含 `match` 使数百条 SVA 逐条行全部入选，`KEY_LINES_MAX=30` 一刀切前 N 条、截断静默 | iverif-workflow@6bb77d5（框架 0.4.3；修复不在本仓库——`scripts/evidence.py` 是 pinned snapshot，本地经 `fwsync --pull` 落地。FB-13 回流闭环） | doc/evidence/v0.2.0/BUG-0026.log |
| BUG-0011 | SPEC_CHANGED | SPEC | spec 缺口（arch M2 蒸馏新发现，中）：`MaxSlvTrans`（每 master 端口每 ID 在飞上限）在许可来源中无任何机制级依据——`axi_mux.md` 全篇未提供与 `axi_demux.md` 那类"按 ID 桶计数、封顶即拒收"对应的段落，唯一相关参数 `MaxWTrans`（AW→W 选择 FIFO 深度）语义不同（FIFO 深度，而非按 ID 分桶的事务计数上限，作用对象也是"整个 mux 的未完成 AW 队列"而非"单一 ID")；spec.md §5.4.2/§5.4.3 现表述"达到上限对应端口不再接受新事务"缺乏 mux 侧机制级文档支撑。影响 M2-TL02（feature-matrix F-M2-04）：其 checker 的精确触发条件/分组维度（是否真按前缀后 ID 分桶、是否存在硬性反压）均无法从许可来源推导，checker 实现前需先决（scenario 行本身可先注册，见 doc/testplan.md M2-TL02）；详见 doc/bugs/BUG-0011.md | -（spec 歧义，非仿真失败） | **REV-005 裁决 = SPEC_CHANGED**（rev 独立核验 xbar.md L47 含"per ID" / axi_pkg L492-494 省略 / mux.md 全文无按 ID 计数机制）：三段式——(1) **保留 §5.4.2 可观测上界为规范**（每 master 端口×每前缀后 ID 在飞 ≤ MaxSlvTrans，"per ID"采 xbar.md 口径，同 BUG-0005 精神）；(2) §5.4.3 的 MaxSlvTrans **执行机制列上游确认项**（mux.md 无来源，同 BUG-0004/0002 先例，不阻塞）；(3) **解锁 M2-TL02 弱化可观测上界 checker**（每端口×每可观测 ID×每方向 ≤ MaxSlvTrans，保守不假红），机制级断言不派发。spec 编辑+重 pin+状态转 SPEC_CHANGED 已由 orch 应用（doc/spec.md §5.4.2/§5.4.3，§2.1 `MaxSlvTrans` 字段行未改动——REV-005 该条裁决未授权此行；Change record #3，sha `946ec000`）。详见 REV-005 §3 / BUG-0011.md ## fix | - | - |
| BUG-0012 | SPEC_CHANGED | SPEC | spec 缺口（arch M2 蒸馏新发现，低，非阻塞）：`axi_demux.md` §Atomic Transactions→Implementation 述"原子读事务的 ID 被注入到 AR 方向的计数器"以避免下溢，并明确该机制"可能在读通道上引发额外的假冲突 stall"——即一笔原子读可能使**同一 slave 端口**上另一笔低位 ID 相同、目标不同 master 端口的**普通读**被 §5.2.1 保序 stall 阻塞（一次由写方向事件引发的读方向 stall，不落在现 §5.2.1"仅同方向配对"的字面框架内）。spec.md §5.2/§6 均未收录此交互；M2-AT01（feature-matrix F-M2-06/07）场景若撞见此现象，在未登记该条款前可能被误判为 DUT_BUG。本条不阻塞 M2-AT01 的核心 B/R 成对判据（SPEC-6.3），仅提请补录该观察；functional_coverage.md 已登记一条非判决性 coverage bin 留痕，等待裁决前不得作为任何 checker 的判决依据；详见 doc/bugs/BUG-0012.md | -（spec 歧义，非仿真失败） | **REV-005 裁决 = SPEC_CHANGED**（rev 独立核验 demux.md L83-87 原文：AW 注入原子读 ID 到 AR 计数器、明述"可致读通道额外假冲突 stall"、为正常行为）：来源完整可溯，**蒸馏为派生条款**——落 **§6（ATOP 副作用）+ §5.2 交叉引用**（超出 §5.2.1"仅同方向"框架的写→读跨方向 stall）。非阻塞：M2-AT01 核心 B∧R/ID 唯一判据照常落地；仲裁前过渡处置（`cg_atop_read_interaction` 非判决 bin + sva_bind C3.2 显式划界）经 rev 核验充分，**准予放行 M2-AT01**。spec 编辑+重 pin+状态转 SPEC_CHANGED 已由 orch 应用（doc/spec.md §5.2.5/§6.5，Change record #3，sha `946ec000`）。详见 REV-005 §3 / BUG-0012.md ## fix | - | - |
| BUG-0013 | SPEC_CHANGED | SPEC | spec 缺口（DV M2-OR01/OR02 仿真新发现，中，非阻塞）：spec.md §5.2.1"该 slave 端口的 AW/AR 通道被 stall"这一"接受边界即时可观测"字面表述，与基线 `LatencyMode=CUT_ALL_AX`（§7.2，AW/AR 各 2 拍）落地的 spill register 弹性缓冲交互未被蒸馏——`axi_demux.sv` 把持有按 ID 分桶计数器/目标绑定寄存器判决逻辑的 `axi_demux_simple` 包在 `SpillAw`/`SpillAr` spill register 之内，第二笔同 bucket 不同目标请求可在核心判决逻辑真正比对第一笔之前，先被这层缓冲"提前接受"（`make run TEST=m2_or01_stall_test SEED=1` 写/读方向均确定性复现：第二笔 AW/AR 在第一笔 B/rlast 完成前即被接受）。**关键旁证**：完成序（B/rlast 到达顺序）与接受序一致、scoreboard M1 式路由/数据判据零 mismatch——spec §5.2.3 所述保序**功能目的**被满足，只是 §5.2.1 的"边界即时性"字面读法与该基线配置有出入。详见 doc/bugs/BUG-0013.md | `TEST=m2_or01_stall_test SEED=1` | **REV-006 裁决 = SPEC_CHANGED（采候选 (a)）**（rev 亲验 xbar.md L84-86 "accept"=forward 进交换网络/L86 保序为功能目的 + demux.md L31 "spill register before the demultiplexer"/L37 不损吞吐 + `axi_demux.sv` L89-209 结构：spill 夹在外部 slave 端口与 `axi_demux_simple` 之间、`slv_resp_o.aw_ready`=spill 自身空位非核心判决 ready）：**收窄 §5.2.1**"接受边界"字面为"逻辑上不早于完成、判决锚 §5.2.3 完成序"，**§7.4 增第 5 条**把接受/拒收边界即时性通用归入延迟不敏感表现，**§5.4.3 挂交叉指针**（三处精确编辑范围见 REV-006 §3）。**驳回候选 (b)**（会在功能正确 DUT 上假红 + 窗口深度约束无许可来源=behavior leak）。DV 过渡处置（完成序判决门 + 非判决"接受边界"cover）经核验已提前对齐裁决 (a)，"接受边界"cover 维持为 cover、不升格 assert。延伸：C5.3 MaxMstTrans 侧判同类（M2-TL01 锚点原则已由 §7.4.5 条款化）、MaxSlvTrans 侧证据不足不下结论、C1.5 判不同类（select 边界锁存无危害）。spec 编辑+重 pin+状态转 SPEC_CHANGED 已由 orch 应用（doc/spec.md §5.2.1/§7.4.5/§5.4.3，Change record #4，sha `107cc1a5`）。详见 REV-006 / BUG-0013.md ## fix | - | - |
| BUG-0009 | CLOSED | TB | mstport_monitor 写 burst 收集缺陷（M1-02 落地时发现）：单槽"aw_valid 时抓取 AW 元数据 + 收 W 直到 w_last"方案在 master 端口处 AW 与 W 解耦/多写 burst 汇聚时错配——第二个 AW 在首个 W burst 未完成前被接受即覆盖 w_id/w_len，w_last 事件被错误归因，导致 mst 侧写观测的 beat 数/元数据错乱、甚至早于对应 slv 侧观测发布（因果倒置）。表现为 scoreboard 请求侧配对 SB_NOPEND/SB_WDATA_LEN/SB_DANGLING 假失败；DUT 功能正确（96/96 响应全部路由正确，resp-route C3.2 零失配）；详见 doc/bugs/BUG-0009.md | `make run TEST=m1_02_id_prefix_test SEED=1`（修复前 4 route 失配 + 7 dangling） | TB_BUG（TB monitor 缺陷，非 DUT）：mstport_monitor 未按 AXI 写 burst 语义配对 AW→W。修复=以 AW FIFO 队列配对（mux 处 W burst 非交织且按 AW 接受序，spec §5.5.2），mirror mstport_responder.w_collect_loop。M1-01 用独立随机 W/R id 且写 burst 汇聚时序较松故未暴露；M1-02 同低位 id + 并发轮次在共享 mst 端口紧密汇聚才触发。同轮修复 scoreboard pending_by_id 键加方向位（读写独立通道，同 id 读写不再别名）。**未改 vendor/RTL** | 5dc53f9 | doc/evidence/v0.1.2/M1-02.log |
| BUG-0008 | WONTFIX | TB | 证据保真缺口（signoff-M0 抽检 R1 发现，事后补登）：`doc/evidence/v0.0.1/{M0-01,BUG-0001,BUG-0006}.log` 三条已登记证据的 `## Key check lines` 段为空——框架 ≤0.2.0 的 `evidence.py` 对非 UVM tb（ucli `run;exit` 驱动 `$stop`）摘要窗口过窄且关键行正则不匹配，判决本身稳健但摘要未反映记分板结论；详见 doc/bugs/BUG-0008.md | -（工具产物保真缺口，非仿真失败；源日志 `sim/out/upstream_sanity_1.log` 可复核） | TOOL_ENV：框架 `evidence.py` 窗口 2 行 + UVM 形状正则双重窄化，已由 iverif-workflow 0.2.1 修复（窗口 20 行 + `tests failed\|ended\|mismatch` + 保险丝测试），本仓库已 pull 至 0.3.0。存量三条**不追溯重写**（signoff-M0 R1 已具名登记该缺口且判决建立在独立复跑源日志之上，重生成反致 R1 文本失真）——REV-004 独立仲裁：赞同不重写，且终态须由 OPEN 转 WONTFIX（不可停在 OPEN 表达"决定不做"） | - | - |
| BUG-0010 | SPEC_CHANGED | SPEC | spec 缺口（arch M2 蒸馏新发现，中）：`MaxMstTrans` 在飞上限的粒度在 spec.md §2.1/§5.4 现表述为"每 slave 端口一个扁平上限"，但 `axi_demux.md` §Ordering and Stalls→Implementation 明确述其机制为"`2×2^AxiLookBits`（=`AxiIdUsedSlvPorts`）个计数器：每个（低位 ID 桶）×（方向）各一个独立计数器，每个计数器独立封顶 `MaxTrans`（=`MaxMstTrans`）"——与现表述不一致：若流量摊满全部 ID 桶与两个方向，单 slave 端口合计在飞数可达 `MaxMstTrans×2^AxiIdUsedSlvPorts×2` 而非扁平 `MaxMstTrans`。影响 M2/M3 事务数上限 checker 的期望值粒度；M2-TL01（feature-matrix F-M2-04）已刻意收窄到单一 ID 桶×方向×目标 master 端口构造、两种读法在该简并情形下重合而不受阻塞，但任何跨桶/汇总上限的后续场景（M3/M4）落地前需先决；详见 doc/bugs/BUG-0010.md | -（spec 歧义，非仿真失败） | **REV-005 裁决 = SPEC_CHANGED**（rev 独立核验 xbar.md L46 / axi_pkg L489-491 扁平 vs demux.md L70-74 分桶机制原文）：**采分桶口径**——按（约简 ID 桶 × 方向）独立计数、各封顶 MaxMstTrans、各带目标端口绑定寄存器；补 §2.1/§5.4.1。原则=取机制精确且不假红者（扁平口径会令 M3/M4 跨桶场景假红），来源=demux.md（可信下层文档）+axi_pkg L510"see axi_demux"指针+§1/§5.2，非 spec-from-RTL。M2-TL01 收窄三元组不受阻塞。spec 编辑+重 pin+状态转 SPEC_CHANGED 已由 orch 应用（doc/spec.md §2.1/§5.4.1，Change record #3，sha `946ec000`）。详见 REV-005 §3 / BUG-0010.md ## fix | - | - |
| BUG-0005 | SPEC_CHANGED | SPEC | spec 缺口（arch 蒸馏 P-d，低）：`NoAddrRules` 最小值口径不一致（xbar.md "地址表至少一条" vs axi_pkg 注释 "每 master 端口至少一条"），影响不可达 mst 端口配置合法性判定 | -（spec 歧义，非仿真失败） | SPEC_ISSUE（源间冲突）确认（REV-001 裁决）：采信主文档 xbar.md 口径——全表 ≥1 条即可，无"每 master 端口 ≥1"硬性要求（axi_pkg 的 should 为软性建议）；无 rule 指向的 master 端口为合法配置。v0 已统一 §2.1/§3.1 措辞 | - | - |
| BUG-0007 | CLOSED | TB | M1-01 SVA 挂接机制偏离：`doc/design-prompt/sva_bind.md` C1.1（rev-gated）原定经 `bind` 挂接（CLAUDE.md §6），VCS-2018.09-SP2 对 `bind <slvport_if\|mstport_if> axi_chan_sva (...)` 报 `Error-[IIM] ... Interface has a module instantiation which is not allowed`，改为 `tb_top.sv` generate 循环内直接例化（`tb/sva_bind.sv`、`tb/sva/axi_chan_sva.sv`） | `make run TEST=m1_01_smoke_test SEED=1` | TOOL_ENV（REV-003 独立复现）：VCS-2018.09-SP2 拒绝 module 直接 `bind` 进 interface 作用域，用与 `scripts/make/vcs-2018.mk` 同源编译旗标的独立最小探针（interface+module+bind）复现同一 `Error-[IIM]` 签名（非本项目树内 revert 复现）；结构核验确认直接例化与 `bind` 对同一 module body 是等价连接语法，断言内容（C2.1-C2.5）与观测信号范围（仅 `slvport_if`/`mstport_if` 外部可见通道信号，无 DUT-internal 穿透）未变 | 64d0780 | doc/evidence/v0.1.1/BUG-0007.log |
| BUG-0004 | SPEC_CHANGED | SPEC | spec 缺口（arch 蒸馏 P-c）：`PipelineStages` 外部延迟语义仅 axi_pkg.sv 注释可溯源；基线配置即 PipelineStages=1，M1 smoke 时序预期需此条款落地 | -（spec 歧义，非仿真失败） | SPEC_ISSUE 确认（REV-001 裁决）：采延迟不敏感原则。v0 已补 §2.1/§7.4：PipelineStages 为延迟不敏感插桩（改延迟不改功能/吞吐），精确周期数未定义、latency checker 不得断言固定拍数；M1 功能 checker 须延迟不敏感，基线 PipelineStages=1 不阻塞；cycle-accurate 需求另行上游确认（§7.4.4） | - | - |
| BUG-0001 | CLOSED | TB | VCS-2018 编译上游 tb_axi_xbar 报 2×Error-[NCE]：generate 环内 `$sformatf(...,i)` 作 axi_chan_logger 参数 LoggerName 覆盖被判非常量（vendor/axi/test/tb_axi_xbar.sv:301/337），M0-01 smoke 无法编译；详情 doc/bugs/BUG-0001.md | `cd sim && make smoke TEST=upstream_sanity SEED=1`（compile 阶段即失败，与 SEED 无关） | TOOL_ENV：VCS-MX O-2018 不支持含 genvar 的 $sformatf 作参数覆盖常量（工具能力缺口，非 DUT 行为） | 1a15627 | doc/evidence/v0.0.1/BUG-0001.log |
| BUG-0006 | CLOSED | TB | VCS-2018 编译报 6×Error-[NCE]：`(NoAddrRules - 1)` 作端口位宽（xbar_cfg_t struct 成员参数选择）无法常量折叠，vendor/axi/src/axi_xbar.sv:84（module axi_xbar）与 :190（module axi_xbar_intf），经 tb 的 axi_xbar_intf 实例弹性触发；原被 BUG-0001 的先序错误掩盖，P-001 后浮出 | `cd sim && make compile`（elaboration 阶段确定性错误） | TOOL_ENV：VCS-MX O-2018 无法折叠 `X-1` 作打包维度位宽（X 为 xbar_cfg_t struct 成员选择或同参数列表兄弟参数），非 DUT 行为。实证（P-002 修复）：axi_xbar.sv:84/190 的 `Cfg.NoAddrRules-1` 修掉后，同类 NCE 由 tb_axi_xbar_pkg.sv:31（class axi_xbar_monitor 参数 `AddrMap` 位宽 `NoAddrRules-1`）浮出（先序错误掩盖，仍 6×NCE 且 expr 由 `Cfg.NoAddrRules-1` 变 `NoAddrRules-1`），共 3 处宽度声明经 `localparam AddrMapMsb=X-1` 中转后 `make compile` 编译+elab+link 全过（out/simv 生成，comp.log 0×Error-[NCE]）。同类先例 floo P-001（TCF-ICTC） | 8062976 | doc/evidence/v0.0.1/BUG-0006.log |
| BUG-0002 | SPEC_CHANGED | SPEC | spec 缺口（arch 蒸馏 P-a，高优先）：`Connectivity[i][j]=0` 时命中该路由的事务响应行为上游文档未载，M3/M4 稀疏矩阵维度 checker 无法推导期望值 | -（spec 歧义，非仿真失败） | SPEC_ISSUE 确认（REV-001 裁决）：许可来源不定义、禁抄 RTL。v0 已补 §8 构造性环境约束：稀疏配置地址表须构造为不译码到非连通 master 端口（未定义情形构造性不可触发）；M3/M4 稀疏维度在此约束子集上验证不降级；"被禁触发时 DUT 响应"留待上游确认（§8.4，不阻塞） | - | - |
| BUG-0003 | SPEC_CHANGED | SPEC | spec 缺口（arch 蒸馏 P-b）：`ATOPs=0` 配置下收到 ATOP 事务的行为未定义，影响配置矩阵 ATOPs{0} 维度 checker | -（spec 歧义，非仿真失败） | SPEC_ISSUE 确认（REV-001 裁决）：许可来源未定义、禁抄 RTL。v0 已补 §6 环境约束：ATOPs=0 时环境保证不发起 ATOP（aw.atop≡'0），违反即未定义；ATOPs{0} 维度在无-ATOP 约束下验证，不阻塞 M4 | - | - |
